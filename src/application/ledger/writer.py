from __future__ import annotations

from .writer_common import (
    Any,
    ComboMembershipResolution,
    ContractKey,
    Decimal,
    InvalidOperation,
    LedgerWriteResult,
    LegacySettlementSemanticUnavailable,
    LifecycleAttemptAuditEnvelope,
    LotCloseResolutionError,
    LotCloseSelector,
    Mapping,
    ProjectionRefreshResult,
    SETTLEMENT_SEMANTIC_SCHEMA,
    Sequence,
    SettlementAdmissionStateIncoherent,
    SettlementSemanticUnavailable,
    TradeEvent,
    _ACTUAL_FEE_COMPONENT_KEYS,
    _ACTUAL_FEE_RAW_SOURCE_KEYS,
    _ACTUAL_FEE_TOTAL_KEYS,
    _APPEND_SAFE_EVENT_TYPES,
    advance_direct_lifecycle_anchor_resolution,
    advance_lifecycle_case_decision_fact,
    allocation_id_for,
    attach_trade_event_cash_conversions,
    broker_external_event_key,
    build_combo_identity_intent,
    build_initial_lifecycle_case_decision_fact,
    build_lifecycle_case,
    build_notification_intent,
    build_source_consumption_claim,
    canonical_contract_symbol,
    canonical_decimal_text,
    canonical_payload_hash,
    canonical_source_economic_payload,
    canonical_source_payload_hash,
    canonical_state_fingerprint,
    canonical_symbol,
    capture_current_decision_projection_fence,
    capture_trade_event_decision_projection_fence,
    compare_projection_lots,
    date,
    datetime,
    defer_current_decision_projection,
    effective_contracts_open,
    effective_expiration_ymd,
    effective_multiplier,
    effective_strike,
    encode_trade_event_for_storage,
    ensure_projection_publishable,
    estimate_futu_executed_option_fee,
    expiration_observation_start_ms,
    finalize_current_decision_projection,
    identity_from_intent,
    json,
    lifecycle_case_generation_token,
    lifecycle_evidence_facts,
    load_cash_fx_payload,
    normalize_broker,
    normalize_contract_expiration,
    normalize_currency,
    normalize_position_effect,
    normalize_trade_side,
    project_stored_trade_events_to_position_lots,
    projection_diagnostics_summary,
    quantize_money,
    read_current_assigned_stock_fact,
    read_lifecycle_case_decision_fact,
    replace,
    resolve_allocations,
    resolve_combo_group_membership,
    resolve_fifo_close_targets,
    resolve_lifecycle_account_rows,
    run_position_projection_in_transaction,
    safe_int_count,
    settlement_evidence_id,
    settlement_semantic_from_evidence,
    strip_retired_strategy_metadata,
    symbol_market,
    terminal_event_id_for,
    timezone,
    to_decimal,
    update_assigned_stock_fact,
    utc_now_ms,
    valid_void_target_event_id,
    validate_assigned_stock_fact,
    validate_combo_identity,
    with_sqlite_repo_transaction,
    write_lifecycle_case_decision_fact,
)

from .writer_decision import (
    _advance_settlement_admission_head,
    _append_lifecycle_observation_attempt,
    _begin_lifecycle_decision_projection,
    _defer_lifecycle_decision_projection,
    _event_position_record_id,
    _finish_lifecycle_attempt_cleanup,
    _finish_lifecycle_decision_projection,
    _finish_trade_event_decision_projection,
    _lifecycle_resolution_after_allocations,
    _match_lifecycle_attempt_replay,
    _persist_direct_stock_settlement_evidence,
    _persist_settlement_admission_evidence,
    _prepare_settlement_admission,
    _projection_mode_for_events,
    _require_lifecycle_generation,
    _trade_events_by_id,
)

from .writer_lifecycle_support import (
    _allocate_lifecycle_reservation,
    _assert_combo_membership_exact,
    _broker_settlement_case_identity_matches,
    _combo_contract_count,
    _combo_leg_from_projected_record,
    _combo_nonnegative_contract_count,
    _effective_void_target_ids,
    _existing_combo_adoption_leg,
    _lifecycle_evidence_business_fact,
    _lifecycle_notification_transition,
    _lifecycle_state_payload,
    _matching_lifecycle_lots,
    _positive_lifecycle_contracts,
    _projected_remaining_by_lot,
    _require_duplicate_settlement_allocation_state,
    _require_duplicate_settlement_issue_state,
    _require_duplicate_settlement_state_base,
    _require_settlement_foreign_keys_clean,
    _validate_broker_settlement_pair_for_write,
    _validate_existing_lifecycle_evidence,
    _validate_existing_zero_price_evidence,
    _validate_lifecycle_event_allocation_plan,
)

from .writer_trade_events import (
    _allocate_actual_fee_group,
    _canonical_close_events_for_storage,
    _canonical_rows,
    _canonical_storage_event,
    _close_position_side,
    _event_type_from_position_effect,
    _event_with_actual_fee,
    _event_with_estimated_fee,
    _event_with_existing_cash_conversions,
    _event_with_existing_fee_evidence,
    _event_with_missing_fee,
    _events_for_storage,
    _freeze_formula_fee_group,
    _freeze_new_event_fee,
    _freeze_source_deal_fee_group,
    _group_fee_candidate_diagnostics,
    _incoming_fee_resolution,
    _missing_fee_resolution,
    _normal_close_notification_intent,
    _optional_fee_amount,
    _position_side_from_trade,
    _prepare_fee_evidence_for_storage,
    _raw_actual_fee_candidates,
    _raw_fee_sources,
    _required_broker_trade_time_ms,
    _source_deal_group_problem,
    _trade_event_from_normalized_deal,
    _trusted_actual_fee_context,
    adopt_existing_combo_identity_atomically,
    persist_normalized_trade_events_atomically,
    persist_trade_event,
    persist_trade_event_object,
    persist_trade_event_objects_atomically,
    persist_trade_event_with_combo_identity,
    persist_trade_event_with_wheel_intent,
    rebuild_position_lots_from_trade_events,
)

from .writer_lifecycle_allocation import (
    apply_lifecycle_allocation_atomically,
)

from .writer_lifecycle_evidence import (
    accept_option_close_evidence_atomically,
    bind_lifecycle_timing_policy_atomically,
    discover_expired_lifecycle_cases_atomically,
    record_assigned_stock_event_atomically,
    record_lifecycle_attempt_audit_atomically,
    record_lifecycle_evidence_issue_atomically,
    record_lifecycle_observation_attempt_atomically,
)

from .writer_lifecycle_state import (
    advance_lifecycle_case_state_atomically,
)
