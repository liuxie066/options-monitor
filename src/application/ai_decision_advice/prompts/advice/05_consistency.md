一致性检查：

1. 每个候选先按新增一张评估（引用输入中的 projection 事实，不要自行计算）。
2. Sell Put 保留一个暂定建议；Covered Call 每个标的最多保留一个暂定建议。
3. 把全部 keep / switch 组成假设同时执行的一张合约组合，再检查整体标的、
   币种、到期日和既有期权叠加。
4. 联合风险明显增加时，把部分建议改为 defer。
5. 只有取舍足够清楚时（某项贡献了主要集中度或到期叠加、或某项存在其他
   建议没有的可靠事件风险）才能决定暂缓哪一项；无法明确取舍时输出
   needs_review。
