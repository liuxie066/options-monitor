# Gateflow Scope Amendment — S1 Generated Dependency Artifacts

- Work unit: `earnings-near-expiry-window`
- Slice: `S1`
- Gate: `implementation`
- Date: 2026-08-11
- Status: accepted as a mechanical repository-quality requirement
- Artifact path: `docs/gateflow/earnings-near-expiry-window/s1-generated-artifacts-scope-amendment.md`

## Trigger

The accepted plan allowed the production/test imports needed for the shared earnings policy but did not enumerate
the generated dependency-graph files. After implementation, the repository's mandatory
`scripts/generate_dependency_graph.py --check` test reported both generated artifacts as stale because this slice
adds imports and one test module.

## Exact scope addition

Allow only these generated files in S1:

- `docs/DEPENDENCY_GRAPH.md`
- `docs/dependency_graph.mmd`

They are regenerated by the repository-owned generator without manual semantic edits. The resulting graph reports
zero production module cycles.

## Related conditional production scope

The plan already conditionally allowed `src/application/combo_yield_steps.py` if inspection proved a missing handoff
at the existing Funding Put underwriting boundary. Inspection proved that the formal path wrote the typed candidate
DataFrame to `*_combo_yield_put_universe.csv`, labelled that file, and read it back before underwriting. The CSV
round-trip coerced v2 earnings lists and booleans into strings. The change to keep formal underwriting on the typed
in-memory DataFrame therefore activates the plan's existing conditional allowance; it is not an additional strategy
or policy scope.

## Non-goals and safety

- No dependency-graph rule, package boundary, runtime entry point, configuration, or public interface changes.
- No broad CSV retirement; audit reports, market-data caches, historical/shadow readers, and Close Advice formats
  remain outside this work unit.
- No OpenD call, notification, production write, release, deployment, or remote upgrade.

## Validation

- `scripts/generate_dependency_graph.py`: `production_modules=585 cycles=0`.
- `tests/test_dependency_graph_generator.py`: `2 passed`.
- `git diff --check`: pass.

## Residual risks

- Generated graph drift: **fixed in current slice** by regenerating both repository-owned artifacts.
- Complete retirement of non-authoritative CSV reports: **assigned to later work unit** if desired; this slice only
  prevents candidate eligibility/Advice from using them as authority.
