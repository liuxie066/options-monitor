# Gateflow Fix Artifact — S1

- Gate: `fix`
- Work unit: `ai-decision-advice-drift-remediation`
- Slice: `S1`
- Review: `docs/reviews/code-review-20260809-202132.md`
- Status: `fix complete; pending re-review`

## Finding decisions

### DR-S1-01 — accepted — fixed

PM mapped account 现在只做前后空白清理，不再强制 lowercase。`all` 禁止仍按大小写
无关判断，而 response `accounts` 必须与真正发出的 mapped account 精确相等。

回归测试使用 `Feishu EXT` 证明：

- query 保留大小写和内部空格，并由 URL encoder 正确转义；
- `LX` 不再被当作 `lx` 的合法回包；
- `all` 和 `ALL` 都在发请前拒绝。

### DR-S1-02 — accepted — fixed

共享 freshness validator 现在按 vendored OpenAPI 对 status/trust enum 做精确匹配，
不再用 lowercase 临时值宽松放行非法协议响应。Distribution 和已有 valuation
路径都增加 uppercase 枚举拒绝测试。

### Re-review hardening

Config validator 删除了第二份 provider enum 集合，直接复用
`ai_decision_advice.config.PORTFOLIO_DISTRIBUTION_PROVIDERS`，避免 resolver 和
validator 以后分叉。

## Validation after fixes

- Focused config/PM contract suite: `121 passed`.
- Changed Python sources compile: passed.
- US and HK example config validation: passed.
- `git diff --check`: passed.
- No real PM request, notification, production configuration write, or runtime mutation occurred.

## Residual risks

- PM `by_asset` row schema/CNY unit remains the accepted cross-repository contract risk.
- Real-service canary remains outside this local implementation slice.
