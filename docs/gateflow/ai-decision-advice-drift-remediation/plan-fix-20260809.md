# Gateflow Fix Artifact — Plan Review Findings

- Gate: `fix`（plan review）
- Work unit: `ai-decision-advice-drift-remediation`
- Review artifact: `docs/reviews/plan-review-20260809-195811.md`
- Plan artifact: `docs/gateflow/ai-decision-advice-drift-remediation/plan-20260809.md`
- Design artifact: `docs/AI_DECISION_ADVICE_DESIGN.md`
- Artifact path: `docs/gateflow/ai-decision-advice-drift-remediation/plan-fix-20260809.md`
- Status: `fix complete; pending re-review`

## Finding decisions and fixes

### 1 — observation multi-writer lost update — accepted — 已修复

- Observation snapshot 改为 canonical-market partitions；market-scoped Tick 只替换自己负责的
  partition。
- Publisher 必须在 private writer lock 内严格读取、更新目标 partition、原子替换整份
  snapshot；损坏 snapshot fail closed。
- S5/S6 测试明确覆盖 US→HK、HK→US、同 market replacement、并发发布和损坏工件。
- 设计 5.3、12.3 和验收矩阵同步更新。

Final status: `已修复`。

### 2 — historical evidence implicitly renewed — accepted — 已修复

- 每次成功 status 现在必须保存 search mode、完整有序 `active_evidence_refs` 和
  `semantic_snapshot_hash`。
- 增量成功显式从上一成功 snapshot 合并；零增量保持 snapshot/hash。24 小时 full success
  以本轮完整集合替换，full zero 明确清空。
- Freeze 只按 current identity 下最近成功 status 的成员清单重建；missing/duplicate/
  cross-symbol/cross-identity/hash mismatch 全部 `evidence_snapshot_invalid`，不返回 partial。
- 设计 6.2、6.6、12.3 和验收矩阵同步更新；S5 测试矩阵加入 A→empty、A→B、full
  replace/zero、failure retain 和 member tamper。

Final status: `已修复`。

### 3 — PM single-envelope hash domain ambiguous — accepted — 已修复

- Envelope 固定为 `authority + payload + integrity`。
- `integrity.payload_sha256` 只覆盖 canonical payload，不覆盖自身；完整文件 bytes 的
  `artifact_sha256` 由 Tick handoff 外部携带，不写回文件。
- Loader 同时验证 expected artifact hash（存在时）、payload hash 和所有 authority binding。
- S2 测试明确分别篡改 payload、authority、artifact hash、账户和 valuation currency。
- 设计 12.2 与 plan 5.2/S2 同步。

Final status: `已修复`。

### 4 — `prefetch_done` recovery missing PM authority — accepted — 已修复

- S2 明确 initial path 唯一调用 PM 并发布 envelope/path/hash/status maps。
- Recovery path 只加载 exact run/account 工件，调用 PM 次数固定为 0；缺失、损坏、binding
  不符只让 Advice PM 输入 unavailable，不移除 scan account。
- Tick outcome/notification handoff 显式传 verified PM object 和 artifact authority；S6 不再
  二次读取 PM 文件。
- 测试加入 initial→recovery 零额外调用、missing/corrupt soft degradation 与多账户隔离。
- 设计验收矩阵同步加入 recovery 与 hash mismatch 行为。

Final status: `已修复`。

### 5 — TickNotificationRequest owner omitted from S6 — accepted — 已修复

- S6 allowed files 加入 `src/application/tick_notification_flow.py`。
- Plan 明确 request 新增 internal-only per-account verified PM object + artifact authority maps；
  notification flow 只按当前 account 传给 assembler，不写 metrics/audit/receipt。
- Delivery-only path 明确不消费这些字段；测试覆盖 account isolation、missing-map soft
  unavailable 和 delivery-only no-read/no-model。

Final status: `已修复`。

## Additional architecture clarification

Managed Collector 的业务 runner 明确归
`src/application/ai_decision_advice/managed_collector.py`；
`src/interfaces/cli/ai_evidence_collector.py` 只做 env/argv/exit-code service adaptation，且不再
由 `./om` parser/dispatch 暴露。这不是新增框架，只是保持 application/interface 单向依赖。

## Validation

- `git diff --check`（在 re-review 前执行）；
- plan/设计相关章节人工逐项交叉核对；
- 所有四项 finding 都有具体 state transition、allowed slice、failure behavior 和测试断言。

## Residual risks

- DeepSeek native citation live shape：`assigned to later work unit`（release/operator canary）；
- PM producer OpenAPI row-level CNY unit：`assigned to later work unit`（portfolio-management
  contract work unit）；
- 真实服务 integration：`assigned to later work unit`（release/upgrade verification）。

无未分类 residual risk；无 blocking open question。

## Current gate / next entry point

- Current gate: `re-review`
- Next entry point: planreview re-review；通过后创建 accepted plan commit。
