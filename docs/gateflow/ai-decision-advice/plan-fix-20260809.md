# Gateflow Fix Artifact — Plan Review Findings

- Gate: `fix`（plan review）
- Work unit: `ai-decision-advice`
- Review artifact: `docs/reviews/plan-review-20260809-114113.md`
- Status: `fix complete; pending re-review`

## Finding decisions and fixes

### 1 — 行业集中度无数据源 — accepted（用户裁决：去掉行业维度）— 已修复

- 设计文档 7.2：输入改为“标的和币种权重”，新增段落明确 v1 不包含行业维度
  （无可靠数据真源、不从搜索推断、留待后续扩展）；
- 设计文档 8：投影改为“标的和币种集中度”；
- 设计文档 9.3 / 9.5：去掉“行业”字样；
- Plan S4：明确 v1 只覆盖 symbol/currency/到期日集中度；测试断言无
  industry 键；风险表新增已裁决条目。

### 2 — 身份快照持久化未定义 — accepted — 已修复

- 设计文档新增 5.2：唯一真源
  `output_shared/state/ai_decision_advice/symbol_identity_snapshot.json`，
  原子写、schema（code/market/exchange/name/aliases/observed_at UTC + 内容
  hash）、确定性重建、证据记录携带身份快照 hash；
- Plan S2/S3：补充文件路径、schema、测试断言（快照 schema、重写幂等、
  证据-身份 hash 绑定）；
- Plan 第 5 节合同清单同步加入该产物。

### 3 — diff 扩展点未定位 — accepted — 已修复

- 设计文档新增 14.1：AI 动作进入
  `normalize_daily_decision_brief` 的独立 `ai_decision_advice` 段；
  `diff_daily_decision_briefs`（`domain/domain/daily_decision_brief.py`）
  扩展比较；动作迁移 material、`unavailable` 不 material；application 层
  不得平行实现；
- Plan S6 与第 5 节同步定位到 domain 函数，测试含 domain diff 扩展。

### 4 — collector 时区基准未定义 — accepted — 已修复

- 设计文档 6.1 新增：所有刷新/过期/全量核对一律 UTC，以证据记录持久化的
  `last_success_at` 为准；
- Plan S3 同步，测试覆盖 UTC 过期判断。

### 5 — 候选提醒路径 Advice 等待语义未说明 — accepted — 已修复

- 设计文档 13.1 修订：固定简报与候选提醒两条路径都当轮运行或复用 Advice，
  同一 30s 账户预算，超时/失败即 unavailable，不推迟提醒；复用条件一致；
- Plan S6 同步，编排测试覆盖两条路径的超时/复用/unavailable 分支。

### 6 — brief view 契约未先行冻结 — accepted — 已修复

- Plan 新增“S6 前置契约”小节：冻结 `brief["ai_decision_advice"]` 完整字段
  结构（status / unavailable_reason / evidence_as_of / per-strategy 动作 /
  zero_candidate / reused / advice_record_id）；S6 生产、S7 消费、domain diff
  均以该契约为准。

## Validation

- `grep -n "行业" docs/AI_DECISION_ADVICE_DESIGN.md` 剩余命中仅为搜索范围
  （6.3）与来源等级（6.4）中的合理语义，以及 v1 不含行业维度的说明段；
- Plan 中 slice 编号引用已校正（S6 前置契约）。
