# Gateflow Final Closeout — combo-yield-remove-staggered

- Gate: final closeout
- Work unit: `combo-yield-remove-staggered`
- Branch: `design/combo-yield-remove-staggered`（已 merge 并删除）
- Draft PR: https://github.com/liuxie066/options-monitor/pull/144
- Merge commit: `cd487de0`（Merge pull request #144，2026-08-11）
- Status: `final closeout pass` → **work unit completed**（用户已授权 merge，merge 后分支已删除）

## What Changed

从 Combo Yield 开仓策略路径完整移除错期 `staggered_expiry_pair` 支持，使同期
`same_expiry_pair` 成为唯一策略结构与单一排序路径：

- **domain 排序/指标（S1）**：删除 staggered rank keys 与 `_is_staggered_expiry_pair`；
  `yield_enhancement_rank_key` / `rank_yield_enhancement_calls_for_put` 收敛为同期
  单一路径；metrics / validate / funding decision 删除 `structure_mode` 参数与错期分支；
  `ComboYieldResearchPolicy` 仅接受 `same_expiry_pair`。
- **扫描/配对/数据需求（S1/S2）**：`sell_put_call_helper` 删除跨期 join、错期 call window、
  `resolve_staggered_expiry_gap_days` 与约 20 处 `None if is_staggered`；config 只允许
  `same_expiry_pair` 并显式拒绝 removed gap 字段；数据需求规划删除错期过滤与 gap 分支。
- **通知/展示/research/shadow（S3）**：删除错期文案分支与 alert high 分支；shadow replay /
  strategy-lab / performance attribution 删除错期变体与结算 horizon 分流；
  `combo_settlement` 收敛为 put_expiry 单 horizon（含 DR-2 死代码清理）。
- **文档与测试（S3 + DR-1/DR-3）**：runbook/架构文档/示例同步为同期语义；约 108 处错期
  测试引用清理；Daily Brief fixture 改为共享到期。

## What Was Verified

- 14 文件 S3 相关套件：**397 passed**（含聚合 deepreview 修复后的回归）。
- ruff 变更文件：`All checks passed!`。
- `rg -i "staggered|错期"` 开仓路径零命中（仅剩两处 removed-fields 报错文案）。
- PR review（`docs/reviews/pr-144-review-20260811-082548.md`）：未发现实质性问题；
  PR head 与本地已审查 HEAD 一致。
- CI/checks（审查时点）：CodeQL、agent-plugin、guardrails、Analyze(actions) `pass`；
  Analyze(python) `pending`——merge 前需确认最终结果。

## Docs Updates

- `docs/SHADOW_REPLAY_RUNBOOK.md`：错期研究变体 → 同期研究变体，共享到期结算语义。
- `docs/STRATEGY_ARCHITECTURE.md`、`docs/PRODUCT_ARCHITECTURE.md`：错期开仓路径移除。
- `docs/gateflow/combo-yield-remove-staggered/`：goal confirmation、plan、S1-S3
  implementation、本 closeout。
- `docs/reviews/`：plan review、S3 code review、聚合 deepreview、PR review。

## Finding Status

- 聚合 deepreview：DR-1（中）、DR-2（低）、DR-3（低）accepted → **已修复**；
  DR-4（低）deferred-with-owner（兼容层清理 work unit）。
- PR review：未发现实质性问题（accepted pass，无需 fix）。

## Remaining Risks / Owners

- 旧 shadow artifact / 旧 sealed combo snapshot 含错期行时按同期语义统一处理，strategy-lab
  fail-closed；owner = 研究兼容清理 work unit。
- deprecated 兼容层错期文案与双到期格式分支（DR-4）；owner = 兼容层清理 work unit。
- `derive_yield_enhancement_policy` 不自行校验 structure_mode（纵深防御缺口）；
  owner = 后续纵深防御 work unit。
- removed-gap 负向测试未单独锁定「仅 structure_mode=staggered、无 gap 字段」路径。
- CI `Analyze(python)` 审查时点 pending；merge 前需确认。
- ledger/生命周期错期兼容保留（Q2 决策），后续独立 work unit 可评估删除。

## Issue Link Status

- 非 issue 驱动 work unit：无关联 GitHub issue，PR body 不使用 closing keyword；
  无需 issue closeout comment。

## Next Entry Point

- 已完成：用户 review 后 mark ready → merge（`cd487de0`）→ 删除本地/远端分支 →
  关闭工作单元。
- 后续可选项：如需评估 ledger/生命周期错期兼容的删除，另开独立 work unit。
