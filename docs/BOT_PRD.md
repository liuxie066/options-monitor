# Bot 重建目标

当前目标已收敛：解释策略、分析报错、查看指定标的的历史过滤原因，支持记忆、上下文与只读工具调用。

验收必须使用实际源码和隔离的真实格式运行记录：回答应说明规则或失败原因、对应范围与出处；数据不足时说明具体缺口；连续追问保持问题和范围；明确保存的个人偏好能跨会话读取，其他用户不能读取；取消后的答案不得发送。

实现与边界以 [BOT_DESIGN.md](BOT_DESIGN.md) 为准。当前记忆采用明确请求后的维护，不包含后台自动整理；模型不负责交易、配置或服务写操作。真实模型效果、发布和部署分别验收。
