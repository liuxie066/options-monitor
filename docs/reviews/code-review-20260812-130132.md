# Code Review

## Scope

- Mode: current changes
- Work unit: `retire-ai-decision-advice`
- Gate: Slice 2 implementation review
- Branch: `refactor/retire-ai-decision-advice`
- Base: accepted Slice 1 commit `c37b2098`
- Output file: `docs/reviews/code-review-20260812-130132.md`
- Included scope: physical Advice subsystem deletion, Tick/config/service/credential/portfolio-client contract removal,
  shared-capability preservation, docs, generated dependency artifacts, and focused regressions
- Excluded scope: Slice 1 accepted compatibility implementation; release; deployment; runtime config/service/secret or
  notification mutation
- Parallel review coverage: none; reviewed as one source-to-runtime-boundary deletion

## Findings

### 1-未修复-低-删除 Advice 恢复逻辑后遗留的循环无副作用并遮蔽真实恢复流程

- **入口/函数**: `run_tick_account_execution()` -> `request.prefetch_done` recovery branch
- **文件(行号)**: `src/application/tick_account_execution.py:762-771`
- **输入场景**: Tick resumes an existing run with scanning accounts and `prefetch_done=True`.
- **实际分支**: the branch first loops over every scanning account only to assign local `account_key`, `config`, and
  `authority`; the immediately following real recovery loop assigns the same names again before any read.
- **预期行为**: recovery should enter the single validating loop directly; deleted Advice distribution recovery must
  not leave misleading inert control flow.
- **实际行为**: runtime output is currently unchanged, but every account is traversed twice and the first traversal
  falsely suggests a missing operation or precondition.
- **直接证据**: lines 763-766 contain assignments only, and none of the assigned values are consumed before lines
  769-771 overwrite them.
- **影响**: negligible runtime cost but reduced maintainability at a safety-sensitive resume boundary; future edits
  may be attached to the wrong loop under the assumption that it has recovery semantics.
- **建议改法和验证点**: remove the inert loop and rerun the recovery/barrier plus prepared option-position suites,
  Ruff, compile, and diff checks.
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

- 无。

## Residual Risk

- **requiring new issue or explicit user decision**: deployed config key removal and installed collector service/timer
  reconciliation remain separately authorized production operations.
- **requiring new issue or explicit user decision**: repository search cannot prove absence of external private imports
  of the deliberately removed Advice-only modules.
- **covered by aggregate validation**: full-suite and whole-branch integration evidence will be collected after the
  accepted Slice 2 commit.

## Conclusion

`fail`. One accepted low-severity finding requires a fix and re-review before Slice 2 can be committed.
