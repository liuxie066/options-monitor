# Code Review

## Scope

- Mode: current changes（Gateflow aggregate DeepReview，精确 commit range）
- Branch or PR: `feat/sell-put-top1-w1a`
- Base: `f5f9ea06`（accepted plan）；Head: `c3d73730`（accepted slice，单 commit `feat(strategy-lab): add Sell Put Top1 ranking core`）
- Output file: `docs/reviews/code-review-20260815-023941.md`
- Included scope: `f5f9ea06..c3d73730` 精确 range 内全部 16 个文件：Candidate Engine profile 扩展、Strategy Lab Top1 ranking 模块、4 个测试文件、2 个依赖图生成物、5 个 Gateflow/review 文档
- Excluded scope: 当前工作树全部未提交 dirty changes（已用 md5 逐一核对：range 内 W1A 文件的 committed blob 与工作树副本完全一致；`git diff c3d73730` 仅剩 assigned_stock/fee_calc/portfolio_assignment_scenario 三个越界文件，不纳入本审查）
- Parallel review coverage: 无；aggregate 以主 reviewer 身份复核前三轮走读证据链，并对 range 内 diff 做最终一致性核对

## Verdict

- 结论: **pass — 不存在任何 finding。**
- 验证证据（本轮重新运行）: 聚焦测试 136 passed；Ruff 全部 W1A 文件通过；`git diff --check f5f9ea06 c3d73730` 通过。

## Findings

未发现实质性问题。

逐项核对结果（证据见三轮前序 review 报告 `code-review-20260815-022023.md`、`code-review-20260815-022735.md`、`code-review-20260815-023230.md`，本轮已复核其证据链与 committed 代码一致）：

- **计划合同完整性**: `docs/gateflow/sell-put-top1-w1a/plan.md` 的全部公共合同（三 profiles、keyword-only 参数与默认值、projection 三函数签名与不变量、point_binding 精确 key 集、provenance 复用、reason codes）在 committed 代码中逐条兑现；scope amendment 仅授权依赖图两个生成物，diff 确认无其他越界文件。
- **Candidate Engine 单一排序权威**: 新排序语义全部落在 `domain/domain/engine/candidate_engine.py` 既有 `rank_candidate_rows()`/`_rank_return_bands()` 内；Strategy Lab 只调用公共 API，无平行排序器。`_sell_put_cross_symbol_tie_key` 重构为 concentration 前缀 + 共享尾部，字节级保持 default 顺序（`_sell_put_cross_symbol_tie_key_without_concentration` 即原尾部，逐字段一致）。
- **默认 parity**: 省略 profile 与显式 `current_tie_break` 完全一致（`test_candidate_engine_omitted_profile_matches_explicit_production_default`）；Covered Call 路径分支结构不变，仅接受默认 profile。
- **三 profiles**: `concentration_first` 按 canonical concentration 分组（unknown 最后），组内回落到既有 band + 去 concentration tie key；`without_concentration` 仅从 cross-symbol tie key 移除 concentration；within-symbol 排序对全部 profile 不变。三个精确 fixture + 相等 concentration 组内测试覆盖。
- **projection trust boundary / provenance**: point_binding 精确 key 集、safe relative ref、64/40 位小写 hash、UTC 时间、与 snapshot 的 market/account/run_id/content_sha256 交叉绑定；候选只复制 canonical normalized_input 字段、无 alias/fallback/重算；单一 `research_artifact_provenance.v1` content hash 绑定全部负载，篡改即 fail closed；validator 精确 key 集 + 有限数值 + rank 连续 + ID 唯一/顺序一致。
- **no_candidate vs partial/unavailable**: `ranking.py:235-242` 要求 put strategy status 严格等于 `candidates_found`（非空）或 `no_candidate`（空集），`partial_data`/`data_unavailable`/`not_applicable`/缺失/多行均 fail closed；合法空集仍可投影并 rerank 出 null Top1。
- **架构依赖**: domain 不依赖 strategy_lab；ranking.py 仅 import stdlib + `domain.domain.engine` + `opening_candidate_snapshot` + `shadow_replay.common` 纯函数；AST guard 测试锁定；无 I/O、时钟、网络、持久化。
- **测试充分性**: 覆盖三 profile 精确顺序、相等 concentration、within-symbol 不变、CC/未知 profile 拒绝、omitted/default parity、null-return 回归（DR-W1A-01）、源删除后 rerank、空集合法、两类不可用证据拒绝、point/candidate/provenance 逐 key 缺失/多余/类型/hash/path/NaN/重复/parity 篡改 fail-closed。断言为精确顺序与精确 payload 比较，非弱断言。
- **过度设计**: 无新增抽象层、注册表、配置键或推测性接口；一个 keyword-only 参数 + 一个纯函数模块；frozen set 常量、复用既有 provenance/hash/snapshot 校验，符合 parsimony 约束。

## Open Questions

- 无。

## Residual Risk

- 未运行 BasedPyright（仓库环境无此模块，按 accepted plan 不安装；已在前序报告记录）。
- 未执行 `scripts/generate_dependency_graph.py --check`（写操作，只读 review 不允许）；committed graph 生成物已在前序评审静态比对为仅含 W1A 授权增量。
- 历史真实 opening snapshot 语料尚不存在（plan 已声明由后续 readiness gate 覆盖）；当前行为由合成完整 snapshot fixture 证明。
