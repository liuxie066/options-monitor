# Code Review

## Scope

- Mode: current changes, post-fix deepreview
- Branch: `feat/futu-quote-broker-boundary` at `3ba1ffc2`
- Base: local `main` at `c0463267`; the branch is six commits behind `main` and has no branch-only commit
- Output file: `docs/reviews/code-review-20260804-232720.md`
- Included scope: the complete Futu quote/broker capability-boundary worktree, including route resolution and audit, gateway pooling, health/doctor/watchdog projections, calendar and market-data callers, trade lifecycle and settlement callers, position maintenance, CLI projections, dependency metadata, and their tests and verification evidence
- Excluded scope: unrelated Feishu latency plan/review artifacts; release, deployment, runtime service changes, production configuration changes, real notifications, broker writes, and live OpenD mutation
- Parallel review coverage: none; this round reviewed the end-to-end capability boundary as one integrated change

## Findings

未发现实质性问题。

上一轮发现的物理 endpoint 大小写归一化缺陷已经修复：账户 broker route 与 quote route 现在使用归一化 host 形成同一 endpoint key，重复 endpoint 会合并账户，并保留可用的 Telnet 元数据；回归测试同时覆盖了大小写不同的 broker/quote host、单次 quote doctor 调用、单个 legacy 物理 endpoint 投影以及账户和 Telnet 信息保留。

## Open Questions

- 当前分支比本地 `main` 落后六个提交，且 `main` 与本 worktree 在 `account_config`、tick、trade intake、external services 及相关测试/文档附近存在潜在集成面。交付前应先按受控方式集成最新 `main`，解决实际冲突后重新运行 focused tests、完整测试和最终 diff review；本轮结论不覆盖尚未发生的合并结果。

## Residual Risk

- 本轮验证覆盖了扩展 Futu 测试集（182 passed）、完整测试集（4290 passed, 10 skipped）、全仓 Ruff、`git diff --check`，以及依赖图校验（579 production modules, 0 cycles）。这些结果证明本地确定性路径，但不等同于真实 OpenD 多账户并发与异常网络条件下的生产行为。
- 本轮没有连接或重启真实 OpenD，没有改动 systemd/launchd、生产配置或运行时数据，也没有触发通知、交易或 broker 写入。因此连接抖动、跨进程资源竞争和真实 SDK 会话释放仍需在获授权的受控环境中验证。
- 多个逻辑账户共享同一 broker endpoint 的配置仍由既有 binding 校验拒绝；endpoint 聚合代码保留首个非空 Telnet 端口的行为只服务于合法的单物理 endpoint 投影，不应被视为允许冲突账户配置。
