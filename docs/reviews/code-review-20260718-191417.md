# Code Re-review — Python 3.12 Runtime Contract S1

## Scope

- Mode: current changes re-review
- Branch: `codex/python312-runtime-contract`
- Base: accepted-plan commit `d789a17b`
- Prior review: `docs/reviews/code-review-20260718-191253.md`
- Output file: `docs/reviews/code-review-20260718-191417.md`
- Included scope: S1 selector, launcher, installer/bootstrap, release preflight, Makefile, package guards, and tests
- Excluded scope: S2 files
- Parallel review coverage: none

## Finding decisions

| Finding | Decision | Re-review evidence |
|---|---|---|
| `CR-S1-001` symlinked bootstrap target escapes self-venv rejection | accepted/fixed | existing target directories and directory symlinks are now physically canonicalized with `cd <target> && pwd -P`; candidate paths use the same physical basis; focused test covers `.venv -> shared-venv` and rejects the through-symlink interpreter |

## Findings

未发现新的实质性问题。

## Open Questions

None.

## Residual Risk

- Activated target venvs can require explicit `OM_PYTHON` if the first PATH candidate is inside the target. This is a safe, actionable failure rather than shared-env mutation.
- Arbitrary direct shebang execution remains outside the accepted public-entrypoint scope.

## Validation observed

- `tests/test_python_runtime_contract.py tests/test_install_script.py`: 19 passed.
- S1 focused suite including existing entrypoint tests: 28 passed.
- Bash parse checks passed.
- Focused Ruff checks passed.
- `git diff --check` passed.

## Conclusion

**pass-with-risks**

The accepted finding is fixed, the bootstrap mutation boundary now handles real and symlinked target venvs consistently, and S1 is ready for accepted-slice commit.
