# Code Review

## Scope

- Review timestamp: `2026-08-04 13:53:15 CST +0800`
- Mode: current changes
- Branch or PR: `main` workspace changes
- Base: `main@ed2531e956f7ff818b3198a6dc3d918bb5328b3f`
- Output file: `docs/reviews/code-review-20260804-135315.md`
- Included scope: Gate A recorder-account implementation and the repair delta after `code-review-20260804-131433.md`; CLI argument plumbing; canonical YAML/runtime-JSON account resolution; lossless Futu port validation; systemd and launchd sampler projection; schema-v1 profile receipt; fresh and legacy service-drift paths; confirmed drift zero-write boundary; focused and full tests; generated dependency graph; public deployment/Strategy Lab docs; plan and prior review artifacts.
- Excluded scope: release publication, production service/profile application, Incus configuration, OpenD/Futu HOME or login state, broker/business writes, Gates C-E live evidence, and unrelated repository code.
- Parallel review coverage: 无；本轮由主 reviewer 独立复核完整 diff，并沿 fresh render、legacy drift、confirmed apply 三条路径重新走读。

## Findings

未发现实质性问题。

## Prior Finding Closure

### DR-01 — 已修复

Legacy endpoint owner resolution is now independent of OpenD root/service rendering. `resolve_strategy_lab_recorder_endpoint_matches()` resolves every selected cross-market Futu endpoint first (`src/application/service_deploy.py:466-521`); `_expected_bundle_from_profile()` rejects zero or multiple matches before invoking the full renderer (`src/application/service_drift.py:423-460`). OpenD root and service-name validation occurs only after a unique account has been selected.

The regressions at `tests/test_service_deploy.py:1783-1862` cover both previously missed counterexamples: a second matching account with no OpenD root and a second matching account with a cross-market root mismatch. Dry and confirmed drift both return an unsupported structured binding error, expose no operations, and preserve every unit/profile byte.

### DR-02 — 已修复

Futu ports now use lossless integer semantics at the account-config owner (`src/application/account_config.py:78-95, 131-137`) and canonical validation also enforces the `1..65535` range (`src/application/config_validator.py:1243-1251`). Boolean, float, lossy/non-canonical string, and out-of-range inputs can no longer be truncated into a valid-looking endpoint.

Coverage includes the public YAML render path (`tests/test_service_deploy.py:1386-1423`), runtime JSON render/drift and zero-write behavior (`tests/test_service_deploy.py:1346-1383, 1865-1923`), account-plan preservation (`tests/test_account_config.py:182-199`), and canonical validator behavior (`tests/test_validate_config_notifications.py:495-515`).

## Open Questions

- 无。

## Residual Risk

- The second-pass counterexample set passed: `6 passed`.
- Focused Gate A coverage passed: `188 passed`.
- Full suite passed: `3988 passed, 10 skipped, 6 warnings`.
- `make lint` and `git diff --check` passed; the regenerated dependency graph reports the production boundary guard as `PASS` with no new production import edges.
- The repository's existing `make test` runner still stops during collection because `tests/run_tests.py` imports the absent `test_multi_tick_account_state_dir` module. This is present at the reviewed base and is not introduced by this work unit; full pytest was used as the executable suite.
- The review host is macOS, so Linux `systemd-analyze verify` remains deferred to Gate C's Linux pre-apply check.
- No production profile, installed unit, service, OpenD process, Incus limit, notification, trade state, or broker-facing state was read or mutated during this implementation/review. Source approval does not establish incident closure or authorize Gates C-E.

## Review Conclusion

`approve`

The two previously blocking fail-closed defects are closed with owning-boundary fixes and adversarial zero-write regressions. Within Gate A's source scope, the recorder now has one explicit canonical endpoint identity, only the matching systemd dependency, a re-resolved profile receipt, and deterministic legacy migration behavior.
