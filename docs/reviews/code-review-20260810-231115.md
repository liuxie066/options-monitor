# Code Review

## Scope

- Mode: current changes
- Branch or PR: `design/combo-yield-remove-staggered`
- Base: `main`（`b07b4aad`，本地 ref；review 期间沙箱无法 fetch 远端，`git fetch` 报 `.git/FETCH_HEAD: Operation not permitted`）
- Output file: `docs/reviews/code-review-20260810-231115.md`
- Included scope: `git diff main` 全部 99 个文件（-33127/+2020 行），含三个逻辑工作单元：Position Advice v2 全链路移除、Close Advice 严格单策略重写（`strict_profit_capture.v1`）、Combo Yield 错期开仓路径移除；另含未跟踪文件 `domain/domain/portfolio_scope.py`、`src/application/source_receipts.py`、`tests/test_strict_close_advice.py`、`docs/plans/controlled-production-upgrade-design-20260810.md`
- Excluded scope: `output*/` 运行时产物、`.git`、gateflow 历史 artifacts
- Parallel review coverage: 无 subagent。已直接走读：`domain/domain/close_advice.py`（全文件）、`close_advice_runner.py`（关键路径）、`daily_decision_brief_service.py` / `daily_decision_brief_renderer.py` / `tick_notification_flow.py` / `tick_account_execution.py` / `account_run.py` / `service_cleanup.py` / `service_deploy.py` / `service_drift.py` / `service_upgrade.py` / `opend_symbol_outputs.py` / `required_data_snapshot.py` / `pipeline_watchlist.py` / `pipeline_runtime.py` / `pipeline_context.py` / `symbol_monitoring.py` / `cash_headroom_query.py` / `config_validator.py` / `agent_tool_registry.py` / `interfaces/cli/main.py` / `external_services.py` / `source_receipts.py`（diff 对照 main 旧版逐行）/ `portfolio_scope.py` / `shadow_replay/{__init__,capture,analysis,close_policy_analysis,close_decision_policy}.py` / 关键测试文件 diff；并以 AST/运行时 import 探针验证符号绑定与可导入性。
- 重要说明：review 期间工作树被持续并行修改（98→99 文件，`close_advice_runner.py` 从 3298 行变为 2300 行，combo yield 死代码在两次读取之间被移除）。本报告所有 finding 以报告写入时刻重新验证的代码状态为准。

## Findings

### 1-未修复-严重-close_advice 域策略重写后 shadow replay 与测试套件未同步更新，import 即崩且 CLI 在惰性 import 路径上必炸

- **入口/函数**: `./om` CLI 启动 -> `src/interfaces/cli/main.py:81` 顶层 `from src.interfaces.cli.research import ...`；`src/application/shadow_replay/analysis.py:66` 惰性 `from src.application.shadow_replay.close_policy_analysis import analyze_close_policy_rows`；以及任何 `import src.application.shadow_replay`
- **文件(行号)**: `src/application/shadow_replay/close_policy_analysis.py:10`（import 已删除的 `DEFAULT_TIER_RULES`）、`src/application/shadow_replay/close_decision_policy.py:6-20`（import 已删除的 `DECISION_EVIDENCE_REVIEW_REQUIRED`、`POLICY_VARIANT_*`、`RECOMMENDATION_REVIEW`、`CloseDecisionFacts`、`ClosePolicyResult`、`evaluate_close_policy`）；未同步的测试：`tests/test_close_advice_contract.py:4,75`、`tests/test_close_advice_domain.py:16-18,46-88`、`tests/test_close_advice_action_policy.py`、`tests/test_close_advice_shadow_policy.py`、`tests/test_close_advice_shadow_{capture,outcomes,analysis,readiness}.py`；本 work unit 新增的 `tests/test_strict_close_advice.py:4,126` 也 import 并使用已不存在的 `CloseAdviceConfig`
- **输入场景**: 任何 `import src.application.shadow_replay`、`om research`/`strategy_lab` 命令、或 pytest 收集 `tests/test_close_advice_*.py` / `tests/test_strict_close_advice.py`
- **实际分支**: `domain/domain/close_advice.py` 重写后（当前 368 行）删除了 `CloseAdviceConfig`、`DEFAULT_TIER_RULES`、`evaluate_close_policy`、`POLICY_VARIANT_*` 等全部 legacy 导出；上述消费方文件均未出现在 `git diff main` 变更列表中，仍按旧契约 import
- **预期行为**: 删除 domain 公共符号时，所有 import 方同步迁移或删除；`tests/test_strict_close_advice.py` 作为本轮新增的唯一严格策略测试，其 fixture 必须与现行 `evaluate_close_advice(inp)` 单参签名匹配
- **实际行为**: 运行时探针实测：`import src.application.shadow_replay` -> `ImportError: cannot import name 'DEFAULT_TIER_RULES'`；`close_decision_policy` -> `ImportError: cannot import name 'DECISION_EVIDENCE_REVIEW_REQUIRED'`；`tests/test_strict_close_advice.py`、`tests/test_close_advice_contract.py`、`tests/test_close_advice_domain.py` import 即 `ImportError: cannot import name 'CloseAdviceConfig'`
- **直接证据**: `close_policy_analysis.py:10` 引用 `DEFAULT_TIER_RULES`（`rg "^class CloseAdviceConfig|DEFAULT_TIER_RULES" domain/domain/close_advice.py` 零命中）；`close_decision_policy.py:9,111` 引用 `DECISION_EVIDENCE_REVIEW_REQUIRED`；`tests/test_strict_close_advice.py:4,126` 使用 `CloseAdviceConfig(...)` 而 domain 中该类已删除、`evaluate_close_advice(inp)` 已不收 config 参数（`close_advice.py:82`）
- **影响**: pytest 收集失败导致整个测试套件无法运行（质量基线断裂）；shadow replay / strategy_lab / research 相关 CLI 路径在触发惰性 import 时必炸。这直接违反 AGENTS.md「任务完毕前应执行 analyze + test 确保不破坏已有的质量基线」。本环境 venv 缺 pytest（`No module named pytest`），无法运行测试套件确认全量破损面，属于额外 CI gap
- **建议改法和验证点**: 二选一并闭环：(a) 删除 `close_policy_analysis.py`、`close_decision_policy.py` 及全部 `tests/test_close_advice_{contract,domain,action_policy,shadow_*}.py` legacy 用例，同步修 `tests/test_strict_close_advice.py` 移除 `CloseAdviceConfig` 引用；(b) 若 shadow 分析仍要保留，将其改为消费 strict 输出的新契约。验证：`python -c "import src.application.shadow_replay"`、`pytest tests/ --co -q` 零收集错误
- **修复风险（低/中/高）**: 低（纯删除或测试修正，无生产链路行为变化）
- **严重程度（低/中/高/严重）**: 严重

### 2-未修复-高-close_advice 配置面与严格策略语义脱节：validator 仍校验并放行已失效的 legacy 键，生产 config.us.json/config.hk.json 当前就在携带这些死键

- **入口/函数**: `validate_config()` -> close_advice 分支；`run_close_advice()` -> `select_close_advice_notification_rows()`
- **文件(行号)**: `src/application/config_validator.py:1165-1190`（仍校验 `notify_levels` 枚举为 `{'strong','medium','optional','weak'}`、校验 `max_spread_ratio`/`strong_remaining_annualized_max`/`medium_remaining_annualized_max` >= 0）；`domain/domain/close_advice.py:348`（`del notify_levels`，legacy tier 配置被显式忽略）；`src/application/close_advice_runner.py:2012`（`notify_level_set = {"strong"}` 硬编码）；生产配置 `config.us.json:156`、`config.hk.json:155` 均含 `notify_levels: ["strong","medium"]` 与三个阈值键
- **输入场景**: 操作者按 config validator 的许可修改 `close_advice.notify_levels: ["medium"]` 或调 `strong_remaining_annualized_max: 0.10`，预期通知范围/策略阈值生效
- **实际分支**: validator 通过；运行时 `select_close_advice_notification_rows` 只认 `RECOMMENDATION_CLOSE` 且 `del notify_levels`；`evaluate_close_advice` 全部阈值走模块常量 `STRICT_*`（close_advice.py:185-193），配置值在链路上无任何消费点
- **预期行为**: 策略语义改为「严格、阈值版本化不可配」后，validator 应拒绝或警告这些无消费点的键（与已有的 `'close_advice.optimizer has been removed'` 先例一致），或同步清理生产 config
- **实际行为**: 配置面承诺的语义与执行面脱节；`medium` 这个 tier 在新 domain 里根本不存在（`TIER_LABELS` 只剩 strong/none/not_evaluable），但 validator 仍把它列为合法 notify level；这是典型 semantic ownership drift——通知级别裁决权已收归 domain，配置层却仍对外承诺可配
- **直接证据**: `config_validator.py:1170-1173` 合法值集合含 `medium/optional/weak`；`close_advice.py:340-348` 函数签名收 `notify_levels` 但首行 `del notify_levels`；`close_advice_runner.py:2012` 硬编码 `{"strong"}`；`config.us.json:156-163` 实测携带全部死键
- **影响**: 用户可见的静默失效——调了配置不产生任何效果；且 `notify_levels` 参数残留在 domain 公共函数签名中，持续诱导调用方以为它有效
- **建议改法和验证点**: 从 `select_close_advice_notification_rows` 签名删除 `notify_levels` 参数；validator 对 `notify_levels` 中非 `strong` 的 level 及三个 legacy 阈值键报 removed/ignored 警告或 die；同步清理 `config.us.json`/`config.hk.json`（生产配置变更需按项目安全红线单独授权）。验证：对含 legacy 键的 config 跑 `om config validate` 有明确诊断；`rg "notify_levels" domain/domain/close_advice.py` 仅剩文档或零命中
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 高

### 3-未修复-中-portfolio_scope 派生 schema 字符串变更导致 decision-state fingerprint 不可比，且新旧模块语义重复

- **入口/函数**: `prepare_option_positions_contexts()` -> `decision_state_snapshot_from_rows(portfolio_scope_id=portfolio_scope_id(account))`；`build_source_manifest()` -> `_sha256(portfolio_scope_id, ...)`
- **文件(行号)**: `domain/domain/portfolio_scope.py:8`（`PORTFOLIO_SCOPE_SCHEMA = "options-monitor.portfolio.scope.v1"`）；main 旧版 `domain/domain/position_advice_authority.py:11,46-53`（`SCOPE_DERIVATION_VERSION = "options-monitor.position-advice.scope.v2"`，`scope_for()` 与新 `portfolio_scope_id()` 实现逐字节相同，仅 schema 字符串不同）；消费点 `src/application/prepared_option_positions_context.py:216`、`src/application/source_receipts.py:445-451,528`
- **输入场景**: 跨版本比较同一账户的 decision-state snapshot fingerprint（例如 required-data manifest 复用、snapshot 一致性校验、审计对账），或新代码校验旧版本产出的 source manifest
- **实际分支**: 同一账户 `lx` 的 scope id 从 sha256(position-advice.scope.v2) 变为 sha256(portfolio.scope.v1)，所有以它为输入的 hash/fingerprint 全部改变
- **预期行为**: 若仅做语义重命名（position_advice -> 通用 portfolio scope），应保持派生输入不变以维持 fingerprint 连续性，或显式声明为 breaking change 并在 CHANGELOG/迁移说明中记录
- **实际行为**: fingerprint 静默漂移；`source_receipts.py` 的 receipt/manifest schema 版本字符串也从 `position_advice_source_receipt.v1`/`position_advice_source_manifest.v2` 改为 `source_receipt.v1`/`source_manifest.v2`，`validate_source_receipt()` 严格等值校验 schema_version（`source_receipts.py:209`），任何由旧版本写入、新版本读取的存量 manifest 都会校验失败
- **直接证据**: 两版派生函数实现一致（`canonical_sha256({"schema": ..., "normalized_account_label": ...})`），仅 schema 常量不同；`source_receipts.py:209-211,551-553` 对 schema_version 做严格相等校验；`git diff main` 中无迁移/兼容分支
- **影响**: 本机 `output_shared/` 实测无旧 schema 存量 receipt/manifest（`rg` 零命中），且 manifest 均为逐 run 生成，实际生产影响有限；但 fingerprint 可比性是 decision-state 体系的核心契约，跨版本审计/对账工具会观察到全量漂移；属真实但低概率命中
- **建议改法和验证点**: 若无 breaking 意图，把 `PORTFOLIO_SCOPE_SCHEMA` 保持为旧字符串 `"options-monitor.position-advice.scope.v2"`（或提供 `scope_for` 兼容别名并保留旧常量）；receipt schema 版本字符串同理保持或在 validate 侧接受双版本。验证：同一账户在新旧代码下 `portfolio_scope_id("lx")` 与旧 `scope_for("lx")` 输出一致
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

## Open Questions

- review 期间工作树被并行修改（疑似另一 agent/进程正在继续本 work unit 的实施）。若该修改仍在进行，finding 1 的破损测试/模块可能已在后续编辑中处理；合入前必须以最终状态重跑 import 探针与 pytest 收集确认。
- 远端运行环境中已安装的 `options-monitor-position-advice-promotion.timer` 等历史单元：代码侧已从 bundle/drift 期望集移除且 `service_drift` 具备 retire extra units 能力（`service_drift.py:106,1330-1332`），但需确认下次升级/对账时该 retire 路径确实会删除已安装的旧 unit 文件，而非仅报告 drift。

## Residual Risk

- 本机 venv 缺 pytest，未能运行任何测试；全部结论基于静态走读 + import 探针。合入前必须跑全量 pytest 与 `om config validate` US/HK。
- `close_advice_runner.py` 在 review 期间被大改（删除约 1000 行 combo yield/legacy 分支），我以最终状态做了符号绑定与调用链抽查（`evaluate_close_advice` 单参调用点 1086 行、CSV 列头、`_position_to_input`、渲染函数），但未对最终版逐行全量重读；runner 内部仍可能残留与新版 `OUTPUT_COLUMNS` 不一致的字段写入。
- Position Advice 移除对运行时存量数据的影响未验证：历史 run 目录中的 position_advice artifacts、Feishu 侧历史数据不在本次检查范围。
- `docs/AGENT_WIKI.md:444,483-491`、`docs/TOOL_REFERENCE.md:119,277-281`、`docs/INDEX.md` 仍引用已删除的 `position_advice_read` 工具与 `om position-advice` CLI；按 AGENTS.md「public command/tool 变化应更新 docs」，文档清理属于未完成的收尾（未单列为 finding，因不影响运行时正确性）。
- 未对 99 个变更文件做逐行全量走读（约 30% 为纯删除文件已核对删除闭环，机械重命名文件已抽样 diff 验证无行为变化）。
