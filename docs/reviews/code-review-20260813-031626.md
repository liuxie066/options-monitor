# Code Review

## Scope

- Mode: current changes, latest-main integration DeepReview
- Branch or PR: `refactor/candidate-csv-retirement`
- Base: `origin/main@6940a96cc6e5`
- Output file: `docs/reviews/code-review-20260813-031626.md`
- Included scope: the complete candidate compatibility CSV retirement branch against current `main`; terminal candidate manifest and opening/SP+LC/CC+LP owner contracts; current and historical consumers; Research and Shadow Replay projections; configuration and CLI contraction; the 14 files changed by both the branch and current `main`; merge conflict resolutions; tests and documentation
- Excluded scope: live OpenD requests, notification delivery, broker/runtime writes, release, deployment, remote upgrade, and mutation or deletion of historical runtime artifacts
- Parallel review coverage: none; the latest-main integration and branch diff were reviewed end to end in the isolated worktree

## Prior finding dispositions

- Slice S1-S3 findings and aggregate findings `AGG-CR-01` through `AGG-CR-03` are fixed and accepted in the committed Gateflow history.
- The latest-main review revalidated those fixes after integrating 30 newer `main` commits, including candidate-evidence integrity, fixed Daily Brief rendering, AI Decision Advice retirement, trade fixes, and releases through `v1.13.17`.

## Findings

未发现新的实质性问题。

## Integration evidence

- Candidate production remains JSON/JSONL-only: the removed scanner output/reject-log parameters, retired CLI flags, v1 status-index publication, candidate CSV writers, parsers, and CSV-only adapters were not restored by the merge.
- The static retirement guard confines legacy candidate filenames to metadata-only history/archive classifiers; those classifiers do not parse candidate CSV bytes.
- Current consumers enter through `candidate_snapshot_manifest.v1`: the loader validates the bound v2 status index, exact owner set, file/content/config/policy hashes, exact scopes, terminal statuses/reasons/counts, and the complete current opening contract.
- Publication order remains status index, owner snapshots, then write-once terminal manifest. An interrupted run before manifest publication remains unavailable and cannot be salvaged from a partial owner.
- Historical pre-manifest evidence remains explicitly classified. Valid bounded v1 snapshot bundles are limited evidence; CSV-only, missing, orphaned, or invalid evidence does not contribute candidate facts.
- Opening accepted and rejected decisions remain constrained to the exact canonical symbol/mode scan scopes. Combo Yield v2 remains bound to owner `sp_lc`.
- Daily Brief now derives applicable owners and modes from the manifest-bound v2 status index, requires the complete current opening contract for both direct handoff and family loading, and does not infer current candidate authority from runtime config or legacy files.
- Current `main`'s complete AI Decision Advice retirement is preserved; the merge did not restore Advice runtime code, services, configuration, or delivery paths.
- Main's `net_premium_non_positive` handling is preserved: it is a definitive policy rejection only when opening contract status is explicitly ready; otherwise the calculation remains input-invalid.
- `allow_stale_config` continues to bypass only source-freshness comparison and still validates the current runtime schema, so it cannot revive removed `output_mode` or retired candidate output contracts.

## Validation

```text
latest-main focused integration suite: 370 passed
sandbox-compatible complete repository suite: 4540 passed, 10 skipped, 5 existing deprecation warnings
localhost-only HTTP quality suite outside the network-restricted sandbox: 4 passed
ruff check src domain scripts tests: passed
compileall src domain scripts tests: passed
dependency graph: current; production_modules=568; cycles=0
US/HK example config validate and build --dry-run: passed
git diff --check and staged diff check: passed
unmerged paths: none
```

The first complete-suite invocation used the external virtualenv executable without the isolated worktree's `.venv` link and exported a relative `PYTHONPATH`. Eight entrypoint tests therefore could not find the worktree-local interpreter path, and one launcher test correctly observed the injected relative path. A temporary worktree-local virtualenv symlink plus an unset `PYTHONPATH` reproduced the nine tests as passing (`11 passed` including the surrounding entrypoint cases), after which the complete suite passed unchanged. This was an invocation-environment issue, not a product-code fix.

## Open Questions

- None.

## Residual Risk

- No live OpenD, scheduled notification, runtime artifact migration, release, deployment, or remote upgrade was performed; those are separate operational authorities.
- Existing historical candidate CSV files are deliberately left untouched. They can be named for compatibility classification but cannot become current or replay candidate facts.
- Actual production evidence for the first post-change scheduled run remains a later deployment-verification concern; local tests establish the source contract, not live market-data availability.

## Conclusion

`pass`. The branch is ready for the latest-main merge commit and Draft PR gate.
