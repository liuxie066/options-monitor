# Code Review

## Scope

- Mode: accepted work-unit commits
- Branch or PR: `feature/option-notification-experience`
- Base: `origin/main@e47a4aa1`
- Head: accepted S2 commit `5da4bfb9`
- Output file: `docs/reviews/code-review-20260722-235547.md`
- Included scope: accepted bug-only plan plus S1/S2 implementation commits; shared option-fee authority; fee evidence and domain safety gate; lifecycle-to-I/O routing; action/combo/public-read handoff; tests, generated dependency graph, and contract documentation
- Excluded scope: strategy/product changes deferred by the plan; release/deploy; all current unstaged Feishu Bot, Sell Put visibility, AGENT_WIKI, and unrelated review files
- Parallel review coverage: 无；the aggregate review followed the two owning call chains end-to-end and checked their interaction at the runner/action/combo boundary

## Findings

未发现实质性问题。

The aggregate adversarial pass checked:

- official dated fee constants, buy/sell regulatory-fee direction, currency/broker validation, and public estimate labeling;
- every direct shared-fee consumer retains its existing threshold, rank formula, filter order, and tie-breaking;
- fee evidence is projected before the pure domain postcondition gate and before existing close-action/combo mapping;
- already-hold decisions cannot become actionable, while otherwise-actionable short/take-profit/salvage paths fail closed on their approved economic measure;
- one run-level business date owns both lifecycle/DTE and event enrichment;
- only active lots reach required-data fetch, OpenD fallback, event enrichment, normal thesis evaluation, or fee actionability;
- expiry-day, expired-open, and unknown rows stay diagnostic `not_evaluable` rows through action mapping, combo synthesis, render selection, and public read/analysis surfaces;
- no candidate engine, notification formatter/selector, config, ledger, Feishu, or broker-facing module changed in `e47a4aa1..5da4bfb9`;
- the generated dependency graph reports `1937` production/script edges, boundary pass, and zero production cycles; only test-edge counts changed;
- documentation describes estimate quality, lifecycle states, and deferred reconciliation without promoting either into strategy policy.

## Open Questions

- 无。

## Residual Risk

- USD actual account package selection is unavailable, so the result remains a dated fixed-package `schedule_estimate`; account-package integration belongs to a later fee-authority work unit.
- HK instrument tariff classification is unavailable, so Tier 1 remains a dated `conservative_estimate`; instrument metadata integration belongs to a later work unit.
- Midpoint execution/slippage remains unchanged and belongs to replay/strategy calibration.
- Expired-open lots still require broker/ledger reconciliation; this work unit deliberately does not infer exercise, assignment, called-away, or settlement.
- The review did not run a live broker or notification canary; all validation remained offline/read-only as required.
