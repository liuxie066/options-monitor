# Code Review

## Scope

- Mode: current changes, final post-fix re-review
- Branch or PR: `feat/ai-decision-advice`
- Base: `9b6941be` (`feat(ai-advice): accept drift remediation S4`)
- Output file: `docs/reviews/code-review-20260810-000147.md`
- Included scope: complete Gateflow S5 diff after remediation of DR-S5-01 through
  DR-S5-05 and DR-S5-RR-01 through DR-S5-RR-04; managed collector, anonymous
  observation and identity snapshots, OpenD identity adapters, concurrent search,
  DeepSeek Responses validation, native citation binding, evidence persistence and
  freeze, public CLI removal, service rendering, documentation and focused tests
- Excluded scope: S6 authority handoff/orchestration failures declared by the
  approved plan; live DeepSeek/OpenD calls; release/deployment
- Parallel review coverage: none; the primary reviewer re-walked the systemd
  wrapper -> managed collector -> OpenD identity -> concurrent provider calls ->
  evidence persistence/freeze chain and its failure paths

## Findings

未发现实质性问题。

The complete S5 diff now matches the approved contract:

- historical attempts influence refresh ordering, provider exceptions remain
  batch-local, and all-identity-unavailable runs surface as failed;
- Responses must be completed, every result/search/source is exactly attributable,
  and malformed URLs fail closed without aborting the round;
- at most two batches run concurrently under one shared deadline;
- market snapshot names fall back through an explicit-code OpenD basic-info call;
- anonymous observation writes validate timestamps before replacing valid state;
- the collector remains an internal managed-service entry and is absent from the
  public `./om` command surface.

All five initial findings and all four first-rereview findings are fixed with
regression coverage.

## Open Questions

- Whether a live DeepSeek Responses call returns bindable native
  `web_search_call.action.sources` without the unsupported `include` parameter
  remains a release-readiness canary question. The implementation correctly
  fails closed and does not trust URLs self-reported in model JSON.

## Residual Risk

- No live DeepSeek or OpenD canary was executed, and no systemd unit was rendered
  or installed in a production environment.
- The expanded AI Decision Advice suite still has the three known S6
  orchestration-signature failures; they are explicitly owned by the next
  approved slice and are not an S5 regression.

## Verification

```text
python3 -m pytest -q \
  tests/test_ai_decision_advice_identity.py \
  tests/test_ai_decision_advice_collector.py \
  tests/test_ai_decision_advice_evidence_store.py \
  tests/test_ai_decision_advice_collector_cli.py \
  tests/test_deepseek_responses.py \
  tests/test_service_deploy.py \
  tests/test_futu_gateway_minimal.py
251 passed in 1.47s

ruff: passed
py_compile: passed
git diff --check: passed
```
