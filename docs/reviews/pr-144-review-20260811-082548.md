# Code Review — PR #144

## Scope

- Mode: PR
- PR: https://github.com/liuxie066/options-monitor/pull/144
- Title: `refactor(combo-yield): 移除错期 staggered_expiry_pair 开仓路径`
- Author: liuxie066
- Head branch: `design/combo-yield-remove-staggered`（HEAD `89358283`）
- Base branch: `main`
- Output file: `docs/reviews/pr-144-review-20260811-082548.md`
- Included scope: PR 相对 base 的完整 diff（43 文件，+1731/-1306）——plan + S1/S2/S3 +
  聚合 deepreview 修复（DR-1 runbook、DR-2 combo_settlement 死代码、DR-3 Daily Brief fixture）；
  对应 GitHub checks（CodeQL、agent-plugin、guardrails、Analyze(actions)、Analyze(python)）。
- Excluded scope: ledger/生命周期保留面（`combo_yield_lifecycle.py`、`trades/resolver.py`、
  `positions/combo_pairing.py`、`combo_reconciliation.py`，Q2 用户决策）；`docs/plans/` 与
  `docs/reviews/` 历史归档；`CHANGELOG.md`。
- Parallel review coverage: 聚合 deepreview 已由 4 个分区子代理按模块并行走读
  （`docs/reviews/code-review-20260811-082229.md`）；本 PR review 由主 reviewer 复核
  PR 元数据、diff 与本地已审查 HEAD 的一致性、关键排序/校验/结算路径、文档一致性、
  全量测试与 ruff，并检查 CI/checks。

## PR Facts

- 状态: draft（OPEN）；无关联 GitHub issue（非 issue 驱动 work unit，PR body 未使用 closing keyword）。
- 分支与本地审查 HEAD 一致：`origin/design/combo-yield-remove-staggered == 89358283`，
  `git diff --stat origin/main...origin/design/...` 与本地 `origin/main...HEAD` 完全一致
  （43 文件，+1731/-1306）。
- CI/checks：CodeQL `pass`、agent-plugin `pass`、guardrails `pass`、Analyze(actions) `pass`、
  Analyze(python) `pending`（审查时点）。

## Findings

### 1-未发现实质性问题-PR 改动面

- **入口/函数**: `yield_enhancement_rank_key` / `rank_yield_enhancement_calls_for_put`
  （domain/domain/engine/yield_enhancement.py:359、:655）；`_validate_yield_enhancement_cfg`
  （src/application/config_validator.py:624）；`combo_research_policy_from_dict`
  （src/application/shadow_replay/combo_variants.py:100）；`_settle_one`
  （src/application/shadow_replay/combo_settlement.py:238）
- **输入场景**: 任意 Combo Yield 开仓扫描、配置校验、shadow 研究变体、结算路径
- **实际分支**: 排序收敛为同期单一路径（主键 `funding_accepted` → `net_credit_retention` →
  call_delta → combo_spread_ratio → min OI → assignment margin → annualized yield → ...），
  无 staggered dispatch；`rank_yield_enhancement_calls_for_put` 直接委托
  `rank_yield_enhancement_rows`；`YIELD_ENHANCEMENT_STRUCTURE_MODES == {"same_expiry_pair"}`
  且 config_validator 对非 same 值 fail-closed；gap 字段显式 removed 检查；
  research policy 对 removed gap 字段 raise ValueError；settlement 恒走 put_expiry 单 horizon，
  `post_put_expiry_state` 恒 `"terminal"`，无死分支/冗余 mark 查询
- **预期行为**: 与 plan（`docs/gateflow/combo-yield-remove-staggered/plan-20260810.md`）一致；
  开仓路径只支持同期，排序口径单一，旧配置显式报错
- **实际行为**: 与预期一致；`rg -ni "staggered|错期"` 在开仓路径仅剩两处 removed-fields
  报错文案（config_validator.py:628、combo_variants.py:107），均属 fail-closed 错误信息，
  非策略分支
- **直接证据**: yield_enhancement.py:359-392 单 rank key 无 staggered dispatch；
  yield_enhancement.py:655-660 单排序入口；config_validator.py:625-630 显式 removed-gap 检查、
  :687-688 same-only 校验；yield_enhancement_config.py:15 结构模式集合收敛；
  combo_settlement.py diff 删除 `call_horizon` 分支、`_leg_mark`、`_executable_long_exit_value`
  与 `put_expiry_residual_call_bid_missing` 错误路径；docs/SHADOW_REPLAY_RUNBOOK.md 已改
  「同期研究变体」与共享到期结算语义
- **影响**: 无；PR body 与真实改动一致，未把 future work 写成已完成
- **建议改法和验证点**: 无需修改。验证：14 文件 `pytest` → **397 passed**（含
  test_combo_yield_research.py、test_daily_decision_brief_service.py 等）；ruff 变更文件 →
  `All checks passed!`；等待 CI Analyze(python) 完成
- **修复风险（低/中/高）**: 不适用
- **严重程度（低/中/高/严重）**: 无

## Open Questions

- 无。

## Residual Risk

- CI `Analyze(python)` 在审查时点仍 pending，最终结果需在 merge 前确认（PR draft 阶段允许）。
- deprecated 兼容层错期文案与双到期格式分支（`multi_tick/notify_format.py`、
  `report_summaries._format_combo_contract:212-218`）保留字面残留——DR-4 deferred-with-owner，
  owner = 兼容层清理 work unit。
- 旧 shadow artifact / 旧 sealed combo snapshot 含错期行时按同期语义统一处理（capture
  分组合并、`call_horizon` 恒 put_expiry）；strategy-lab 对旧错期 trace fail-closed；
  无生产路径生成该类数据，owner = 研究兼容清理 work unit。
- `derive_yield_enhancement_policy` 不自行校验 structure_mode，纵深防御依赖
  config_validator 与 research policy `__post_init__`，owner = 后续纵深防御 work unit。
- removed-gap 负向测试未单独锁定「仅 structure_mode=staggered、无 gap 字段」路径
  （当前负向用例同时携带 gap 字段）。
