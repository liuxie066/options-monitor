# Final Plan Re-Review — ai-decision-advice-drift-remediation

- Review type: `planreview final re-review`
- Reviewed target: revised `docs/gateflow/ai-decision-advice-drift-remediation/plan-20260809.md`
- Prior reviews:
  - `docs/reviews/plan-review-20260809-195811.md`
  - `docs/reviews/plan-review-20260809-200646.md`
- Fix artifact: `docs/gateflow/ai-decision-advice-drift-remediation/plan-fix-20260809.md`
- Code baseline: `feat/ai-decision-advice@437505a3`
- Artifact path: `docs/reviews/plan-review-20260809-200809.md`
- Timestamp source: local system clock (`20260809-200809`)

## Assumptions re-tested

- Multi-market and concurrent Tick writers preserve all observation partitions.
- Append-only evidence can deterministically reconstruct current incremental/full snapshots.
- PM envelope and handoff hashes have non-self-referential, testable domains.
- Initial and `prefetch_done` recovery paths share one PM authority without re-fetch.
- The verified PM object call path owns every required file from Tick outcome through Daily Brief.
- Failure/degraded states remain Advice-only and cannot remove Candidate Engine accounts.
- Slice boundaries are small enough for one implementation and one deepreview pass each.

## Finding status

| # | Finding | Final status | Evidence in revised plan |
|---|---|---|---|
| 1 | observation multi-writer lost update | 已修复 | 4.5, 5.5, S5/S6 market partitions, lock, atomic update, concurrency tests |
| 2 | historical evidence implicitly renewed | 已修复 | 5.5, S5 active refs/hash, incremental merge/full replace, tamper tests |
| 3 | PM envelope hash domain ambiguous | 已修复 | 5.2 authority/payload/integrity, external artifact hash, S2 tamper tests |
| 4 | `prefetch_done` PM recovery missing | 已修复 | S2 initial/recovery state machine, zero re-fetch, soft degradation |
| 5 | TickNotificationRequest owner omitted | 已修复 | S6 names `tick_notification_flow.py`, typed account handoff and delivery-only tests |

## Architecture boundary review

Pass. Futu operational capacity remains separate from optional PM strategic distribution; SQLite
prepared option context remains the only option source; Advice accepts typed frozen inputs; shared
evidence remains account-anonymous. The explicit Tick outcome -> notification request -> account
assembler path is single-directional and does not serialize PM private values into metrics or receipts.

## Best-practice review

Pass. The plan specifies strict empty/unavailable semantics, immutable run authority, re-entry behavior,
writer locking, append-only snapshot membership, failure retention, exact scopes, deterministic facts,
privacy allowlists and focused regression tests.

## Optimal-solution review

Pass. The plan reuses existing safe writers/loaders, PM client, prepared option pipeline, evidence JSONL
and Daily Brief surfaces. Market partitioning and status member refs solve the identified state problems
without introducing a database, event bus or generic LLM framework.

## Overengineering review

Pass. New persistent structures are limited to the already-required PM prepared envelope and anonymous
observation snapshot. The managed collector runner/thin wrapper separation follows existing layer
direction and does not create a plugin system or manual control surface.

## Overcoupling review

Pass. Each slice has an owning layer and focused tests. Cross-slice dependencies are directional:
config/wire -> prepared authority -> frozen facts -> Advice validation; evidence collection evolves
independently before the explicit orchestration slice joins them.

## New findings

无。

## Open questions

无 blocking open question。

## Residual risks

- DeepSeek live citation shape: `deferred-with-owner` to release/operator canary; runtime fail closed.
- PM producer OpenAPI CNY row schema: `deferred-with-owner` to portfolio-management contract work unit;
  current OM consumer is pinned by fixtures.
- Real service integration: `deferred-with-owner` to release/upgrade verification; local work does not
  claim production availability.

All residual risks are classified and none blocks implementation.

## Conclusion

`pass-with-risks`

The plan is code-generation-ready. All accepted findings are fixed, no new material finding remains,
and the only residual risks have explicit later owners and fail-closed behavior.
