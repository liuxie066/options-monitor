# Code Review

## Scope

- Mode: current changes re-review
- Branch or PR: `codex/diagonal-combo-yield-lifecycle`
- Base: `HEAD` (Slice S2 uncommitted implementation only)
- Output file: this artifact
- Included scope: DR-S2-001 fix plus full S2 intake/inventory/context diff
- Excluded scope: accepted S1; future S3/S4
- Parallel review coverage: 无；重新追踪 CLI/assistant structured open → preview → event payload → SQLite projection/restart → context inventory

## Findings

未发现实质性问题。

### DR-S2-001-已修复-高-structured/manual open metadata persistence

- Manual-open preview now promotes the existing strategy metadata keys from `strategy_snapshot` into preview fields.
- Manual-open preflight and persisted event payload retain both the nested snapshot and canonical top-level strategy metadata.
- Projection falls back to snapshot metadata when historical/manual payloads lack top-level copies, while explicit top-level values retain precedence.
- CLI `add` now exposes the minimal `--strategy-snapshot-json` handoff and README documents diagonal explicit-group usage.
- Integration coverage proves snapshot → preview → write → SQLite restart → `combo_yield_groups=active_combo`.

## Open Questions

- 无。

## Residual Risk

- Raw broker-only cross-expiry fills remain fail-closed by approved design.
- Assignment lineage and group advice remain intentionally deferred to S3/S4.

## Validation

```text
103 passed:
- resolver / context / lifecycle / linked-call / option-position CLI

70 passed:
- ledger publisher / service / SQLite workflows

git diff --check: passed
```
