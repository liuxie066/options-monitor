# Code Review

## Scope

- Mode: current changes re-review
- Branch: `refactor/candidate-csv-retirement`
- Base: `97a24479` (accepted S1 sealed candidate authority)
- Initial review: `docs/reviews/code-review-20260812-130109.md`
- Included scope: complete S2 historical classification, Research/archive migration, Shadow Replay and Strategy Lab
  evidence gates, Combo Funding Put JSONL projection, and the accepted fixes in `s2-review-fix.md`
- Excluded scope: S3 producer/config/v1-status removal; release, deployment, notification delivery, and production writes
- Parallel review coverage: none; re-reviewed classification -> scoped coverage -> dataset/run consumers -> strict gates,
  plus Combo source publication -> receipt/facet/integrity validation -> accepted-only evaluation

## Findings

No unresolved findings.

`S2-CR-01` is fixed by applying account/market scope to evidence classifications before aggregating coverage. Known
other-market evidence is excluded; unknown-market unsupported history remains fail-closed. Run-window and dataset
paths share the same rule and both have positive and negative regression coverage.

`S2-CR-02` is fixed by validating the current manifest inside publication locking and by validating canonical path,
dataset integrity, typed source receipt, exact manifest facet, row schema/count/hash, and post-read integrity under one
read lock. Evaluation consumes the rows returned by that validation instead of reopening the source.

Re-review also checked that trace remains supplementary, unsupported history cannot become an empty successful
dataset, limited legacy snapshots cannot satisfy strict replay/promotion, accepted Funding Put rows alone enter pair
evaluation, archive filename inspection does not parse candidate CSV bytes, and noncandidate CSV contracts remain
unchanged.

## Open Questions

- None for S2.

## Residual Risk

- Pre-manifest Combo/CC+LP count semantics remain intentionally limited and non-strict; S2 does not invent a count
  equivalence that the historical schemas did not guarantee.
- Candidate compatibility CSV writers, v1 status dual publication, `output_mode`, and dead adapters remain active by
  the S2 contract and are exclusively owned by S3.
- Archive metadata may list legacy candidate CSV names and copy untouched cold history, but automated candidate
  evidence never opens their bytes.
