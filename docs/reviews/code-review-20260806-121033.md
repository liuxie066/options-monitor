# Code Review

## Scope

- Mode: current changes, aggregate re-review after fixing `DR-SP-01`
- Branch or PR: `main` workspace changes
- Base: `main@46b37c8395f048185ed6bff6648da47449b945b7`
- Output file: `docs/reviews/code-review-20260806-121033.md`
- Included scope: Sell Put strategy documentation, OpenD recall and quote normalization, DTE-matched RV, quote freshness, earnings coverage, cash and FX capacity, candidate filtering and ranking, concentration projection, report/explain adapters, dependency graph, and all changed tests
- Excluded scope: live OpenD, event-provider, broker, and production-runtime canary; unrelated pre-existing workspace state
- Parallel review coverage: 无；the repaired change was re-reviewed end to end from required-data planning through candidate CSV, account aggregation, explanation, and final ranking

## Findings

未发现实质性问题。

首轮 `DR-SP-01` 已修复：Sell Put underwriting enrichment 现在从既有 portfolio/option context 构造风险事实，仅投影并持久化接货后集中度字段，不恢复集中度硬门槛。跨标的回归用例已证明，收益带内会选择接货后集中度较低的标的；持仓事实缺失时则显式记录 `concentration_evaluable=false` 和原因，候选仍不因此被硬拒绝。

## Open Questions

- 无。

## Residual Risk

- OpenD `update_time` 是当前价更新时间，不是 bid/ask 逐字段时间；实现只能将它作为供应商报价新鲜度代理，策略文档已明示这一数据边界。
- 本轮未连接实时 OpenD、事件 provider 或生产账户数据做 canary；结论基于源码、生成物和 deterministic tests。
- 本地全量测试为 `4553 passed, 10 skipped, 1 deselected`；唯一 deselected 用例需绑定回环端口，已在允许本地端口的环境中单独通过。生成依赖图已处于 current 状态，production dependency cycles 为 0；所有改动 Python 文件通过 Ruff，`git diff --check` 通过。
