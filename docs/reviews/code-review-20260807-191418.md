# Code Review

## Scope

- Mode: current changes（第三轮，最终复核）
- Branch or PR: `main`
- Base: `main`
- Output file: `docs/reviews/code-review-20260807-191418.md`
- Included scope: 修复后完整改动的决策时刻校验调用链、seal 路径是否绕过、US/HK 同步性、reject 三分类完整性
- Excluded scope: 现场 shadow replay、远端升级、二期 order-book shortlist 复核
- Parallel review coverage: 无

## Findings

未发现实质性问题。

第三轮 adversarial pass 复核的点（均未构成 defect）：

- **决策时刻校验无绕过路径**：`calculate_opening_candidate_metrics` 的 300 秒重验是所有候选的唯一正式入口（`scan_sell_put.compute_metrics`、`scan_sell_call.compute_metrics`）。`opening_candidate_snapshot._snapshot_decisions` 的 fallback `evaluate_opening_candidate_policy`（`opening_candidate_snapshot.py:554`）仅在 `candidate_evaluations is None` 时使用，且只处理已被接受的 `final_candidates`（已通过 scan 决策时刻校验）；生产 seal（`pipeline_watchlist.py:866`）传 `candidate_evaluations=captured_candidate_decisions` 走主路径，不经 fallback。
- **US/HK 同步性**：`normalize_option_observation`/`normalize_underlier_observation`/Candidate Engine 决策时刻校验对所有市场共用同一语义；市场差异仅保留在标的时区（`America/New_York`/`Asia/Hong_Kong`）与连续交易时段定义（US 单段、HK 午休两段），符合设计 §4。
- **reject 三分类完整**：`_opening_not_ready_reason` 对 `ineligible`/`data_unavailable`/`market_closed`/未知状态均能稳定归类，evidence 聚合（`evidence_summary_from_decisions`）正确区分 accepted/三分类，scan status 投影与 opening snapshot `_strategy_results` 一致。
- **scan-status JSON reason 保留**：`publish_strategy_scan_status` 对 `completed` 持久化受限 reason，`_validate_status` 与 success-empty 校验无回归，Daily Brief 对 `completed`+`partial_data` 补 warning gap。

## Open Questions

- 设计 §4.2“决策时刻”以 engine 决策入口为准；如后续要求逐合约时刻，可在该处收紧，属可演进点。
- 现场 shadow replay（15:00/15:30/15:50）需生产 OpenD 数据，仍为离线验证后续步骤。

## Residual Risk

- 全量测试 4541 passed（跳过仅网络沙箱的 `tests/quality/test_om_quality_gate_http.py`）。
- 现场 replay 未执行，恢复的合约数量与最终候选排序变化尚无生产数据佐证。
- `snapshot_received_at_utc` 成为候选决策必要证据，缺该列的旧 required-data CSV 在决策时 fail closed（`evidence_unavailable`）；部署后首轮 tick 会重建 required-data。
