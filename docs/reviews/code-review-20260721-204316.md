# Aggregate Deep Review — Re-review

## Scope

- Mode: current changes / aggregate Gateflow re-review
- Branch: `feature/option-notification-experience`
- Base: `main` at `9b1e200e`
- Source review: `docs/reviews/code-review-20260721-203532.md`
- Reviewed target: aggregate fixes for `AG-001` through `AG-004`
- Included scope: scheduler compatibility state, account target propagation, notification prepare/commit/send ordering, non-scheduled boundary, focused and full regression evidence
- Excluded scope: unrelated untracked files and separately approval-gated production operations
- Parallel review coverage: none; all four critical paths were re-traced locally

## Findings

未发现新的实质性问题。

## Re-review of accepted findings

### AG-001-已修复-高-Daily Brief 关闭时 scheduled scan 不再提交 processed-target 水位

- **修复状态**: 已修复
- **直接证据**: `run_tick_notification_flow` now calls `_commit_scan_targets_before_delivery` after either preparation path and before provider routing. The regression with `daily_brief.enabled=false` asserts the exact fixed target is committed.
- **结果**: scheduler authority is independent of the notification projection flag.

### AG-002-已修复-高-manual/force 扫描会写普通候选 delivery state 并可能发送候选通知

- **修复状态**: 已修复
- **直接证据**: `_prepare_daily_brief_notification` gates candidate-state recording, delivery decision and retry selection on `scheduled_trigger`. Manual and force parameterized tests prove successful current advances, delivery state remains absent, sender is not called, and the scheduler commit carries `None` only for completion observation.
- **结果**: non-scheduled scans are read/update-current operations without ordinary notification side effects.

### AG-003-已修复-中-多账户首次升级时 processed-target map 会让未迁移账户丢失 legacy seed

- **修复状态**: 已修复
- **直接证据**: `_last_processed_scan_target_for_account` falls back to `_last_run_for_account` when the requested account has no new-map entry. The account-isolation test proves `sy` uses its own legacy seed even after `lx` has migrated, then becomes due when that seed is removed.
- **结果**: compatibility switching is per account as designed.

### AG-004-已修复-高-account scheduler fallback 可在缺少 exact target 的情况下准备并发送 scheduled 报告

- **修复状态**: 已修复
- **直接证据**: `_validate_scheduled_scan_targets` runs at the flow entry, compares scheduled `ran_scan` accounts with non-empty exact mappings, audits mismatch and raises. The regression proves assembler, commit callback and sender are unreachable when `lx` lacks its target.
- **结果**: provider delivery is unreachable without an exact account target and the durable prepare -> target commit -> send invariant is restored.

## Adversarial failure pass

Rechecked:

- Daily Brief enabled/disabled target commits;
- fixed, half-hour, no-send and quiet paths;
- manual/force successful candidates and existing delivery-state isolation;
- successful and failed scheduled scans with exact/missing targets;
- per-account legacy/new watermark combinations;
- commit callback failure before provider;
- fixed/candidate retry envelope identity and confirmation behavior;
- multi-market delivery skip;
- query and renderer regressions;
- dependency boundaries and generated graph.

No additional material correctness, stability, maintainability, over-coupling or semantic-ownership finding remains.

## Validation

```text
44 focused scheduler/notification tests passed
136 Daily Brief tests passed
100 scheduler/multi-tick tests passed
149 agent/plugin/dependency/critical-flow tests passed
2947 full-repository tests passed, 10 skipped
ruff passed
compileall passed
dependency graph current; 477 production modules; 0 cycles
git diff --check passed
US/HK config validation returned ok=true
```

## Open Questions

- None.

## Residual Risk

- Production capacity, live-provider canary, confirmed pointer migration, release, remote upgrade, and next-normal-target observation remain separately approval-gated rollout work.
- The documented first-target legacy-seed ambiguity remains an accepted rollout observation risk.
- No unclassified residual risk remains.

## Conclusion

Pass. All accepted aggregate deepreview findings are fixed and re-reviewed.
