# Plan Review

## Scope

- Reviewed target: `docs/gateflow/om-shadow-replay-integrity-selection/plan.md`
- Work unit: `om-shadow-replay-integrity-selection`
- Base: `origin/main@8d467282`
- Output file: `docs/reviews/plan-review-20260801-225139.md`
- Included: integrity fact ownership, write eligibility, quota ordering, direct
  collection I/O boundary, dry-run compatibility, tests, release and canary
- Excluded: manifest migration/backfill, provider limiter redesign, unrelated
  Strategy Lab research semantics

## Assumptions tested

- Architecture boundary: confirmed status already owns integrity derivation via
  `validate_dataset_integrity(..., require_manifest=False)`; the plan only
  projects that fact to the existing application planner.
- Failure ordering: confirmed `_run_plan_rows()` owns quota/circuit ordering and
  `collect_shadow_replay_marks()` is the earliest direct write-mode I/O boundary.
- TOCTOU/retry: challenged reliance on the status projection; the plan explicitly
  requires direct validation again at collection time.
- Compatibility: confirmed dry-run can retain legacy rows because mutation is
  gated only when `write=True`.
- Optimality/overengineering: compared manifest backfill, planner-only filtering,
  and a new eligibility abstraction. Reusing the existing integrity payload plus
  boundary validation is the smallest path that covers both planned and direct
  calls.
- Validation: confirmed tests require both quota non-consumption and proof that
  the fetch adapter is never invoked for unverified direct writes.

## Findings

No material findings. The plan is code-generation-ready and preserves semantic
ownership without introducing a migration or a second integrity truth source.

## Open Questions

- None.

## Residual Risk

- Historical unverified datasets remain read-only and cannot participate in
  write-mode sampling. Tracking destination: normal trusted evidence
  regeneration; explicitly outside this work unit.
- Production verified datasets may still defer on the existing OpenD provider
  rate limit. Tracking destination: existing limiter/circuit behavior and the
  controlled canary.

## Conclusion

`pass`
