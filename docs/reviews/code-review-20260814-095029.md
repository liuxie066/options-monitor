# Code Review

## Scope

- Mode: current changes, final S4 aggregate re-review
- Branch or PR: `plan/data-storage-runtime-projection-p3a`
- Base: `5930e5ce`
- Output file: `docs/reviews/code-review-20260814-095029.md`
- Included scope: complete S4 cumulative diff, DR-S4-01 correction,
  full-suite resolver compatibility fixes, shared-runtime inventory update,
  generated dependency graph, and all S4 tests
- Excluded scope: accepted S1-S3 implementation, S5 migration/activation and
  formal benchmark work, live stores, release/deploy, and unrelated `.venv`
- Parallel review coverage: none; the reviewer traced the final cumulative
  preflight/resolver/writer/runtime transaction chain directly

## Findings

未发现实质性问题。

The final resolver keeps indexed active/primary-key reads on the canonical
SQLite production repository while preserving the existing public read
contract for adapters and subclasses. All event writes remain owned by the
single transactional projection runtime.

## Open Questions

- None.

## Residual Risk

- Formal 5-warmup/30-repetition latency, allocation, SQL row-count, WAL,
  rotation, and reference-host acceptance evidence remains S5 work.
- The pre-existing research-module public-API boundary failure is outside S4;
  none of its five offender files differ from base `5930e5ce`.
- The localhost HTTP quality-gate test cannot bind a socket in the filesystem
  sandbox; it requires the already established unsandboxed verification path.
- Special control and Combo identity/reconciliation paths intentionally remain
  force-full under the accepted Phase 3A boundary.

## Verification Evidence

- Final S4/runtime/publication/maintenance/manual-close set: `101 passed`.
- Expanded S4 and adjacent ledger/lifecycle set: `349 passed`.
- Final full suite: `4707 passed`, `10 skipped`; only the unchanged
  research-boundary failure and sandbox HTTP bind failure remain. The HTTP
  test passed outside the sandbox: `1 passed`.
- Dependency graph: current, 576 production modules, zero cycles.
- Ruff, compileall, and `git diff --check`: passed.
