# Plan Second Re-review — Copilot Option Performance MTD

- Reviewed target: `docs/gateflow/copilot-option-performance-mtd/plan.md`
- Initial review: `docs/reviews/plan-review-20260723-164351.md`
- First re-review: `docs/reviews/plan-review-20260723-164617.md`
- Fix artifact: `docs/gateflow/copilot-option-performance-mtd/plan-review-fix.md`
- Review timestamp: 2026-07-23 16:47:41
- Conclusion: `pass-with-risks`

## Finding disposition

- PR-01: fixed. Explicit invalid/empty model or static inputs are no longer deleted globally.
- PR-02: fixed. Account provenance is an explicit residual boundary rather than an
  unprovable host guarantee.
- PR-03: fixed. The Copilot adapter no longer pre-fills the period discriminator; only an
  explicitly supplied valid period can prune irrelevant period fields.

## Assumptions re-tested

1. A truly empty period request still defaults to MTD in canonical `PeriodRequest`.
2. `month` without `period` remains ambiguous and fails closed.
3. Explicit `period=mtd` can safely discard model-added month/year/range fields while preserving
   invalid relevant `as_of_date`.
4. The tool-owned normalizer stays adjacent to existing Copilot metadata and does not alter the
   public execution contract.
5. Option and assigned-stock PnL components come from exact generation-time fact collections,
   not renderer subtraction or source-ID inference.
6. Actual account scope is always rendered even though current-message provenance is not
   available to the host.

## Architecture boundary review

Pass. The plan keeps:

- strict period semantics in `domain/domain/performance/period.py`;
- model adaptation in the existing `AgentTool`/Copilot adapter boundary;
- financial aggregation in `domain/domain/performance/engine.py`;
- deterministic presentation in the assistant renderer;
- model preference and semantic reminders in prompt/eval only.

No reverse dependency, new persisted state, migration, or parallel report pipeline is proposed.

## Best-practice and optimal-solution review

Pass. The revised solution:

- removes the proven invalid defaults at their owner;
- preserves invalid explicit inputs for observable fail-closed validation;
- uses one optional normalizer instead of a tool-name router or discriminator framework;
- exposes additive metrics while preserving existing totals and fact kinds;
- proves the exact payload failure independently from model-answer synthesis.

## Overengineering and overcoupling review

No material finding. The optional callable is the smallest ownership-preserving hook that avoids
hard-coding option performance inside the Copilot engine. Each slice remains independently
reviewable and does not require future-slice state.

## Open Questions

- None.

## Residual Risks

- Account intent can still be inferred incorrectly from history because the scene has no
  account provenance. The visible actual scope limits silent ambiguity; a deterministic
  current-message-only guarantee belongs to a separate scene-scope work unit.
- The report retains a combined period-total PnL while only realized PnL is component-split.
  The renderer must label the period total as combined.
- Validation is deterministic and local; no live Feishu model canary or production mutation is
  authorized in this work unit.

## Final conclusion

`pass-with-risks`: the plan is code-generation-ready. All prior material findings are fixed,
there are no open blockers, and the residual risks have explicit boundaries and destinations.
