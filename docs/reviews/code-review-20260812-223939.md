# Code Review

## Scope

- Mode: aggregate current-branch review
- Work unit: `retire-ai-decision-advice`
- Gate: aggregate DeepReview
- Branch: `refactor/retire-ai-decision-advice`
- Base: `ded8f882242f5dc5eda9d564461e05a884ed64ba`
- Reviewed commits: `ea6eb4a9`, `c37b2098`, `377ea0d3`
- Output file: `docs/reviews/code-review-20260812-223939.md`
- Included scope: both accepted slices as one end-to-end current Brief, historical retry, Tick, config, service,
  credential, portfolio-client, documentation, and operational-retirement contract
- Excluded scope: release; deployment; runtime config/service/secret/data or notification mutation
- Parallel review coverage: none; reviewed the full branch after aggregate tests and dependency checks

## Findings

### 1-未修复-中-共享通知格式断言仍把已退役 AI 建议标题列为合法输出

- **入口/函数**: `assert_mobile_flat_markdown()` shared notification test helper
- **文件(行号)**: `tests/notification_format_assertions.py:9-22`
- **输入场景**: a current renderer accidentally reintroduces a `### AI建议` subsection in any notification covered by
  the shared mobile Markdown assertion.
- **实际分支**: the helper's `allowed_subheadings` explicitly contains `### AI建议`, and its comment points to the
  removed design document's former section 15.1.
- **预期行为**: after complete retirement, only current candidate subheadings are allowed; an AI Advice heading must
  fail the shared formatting contract.
- **实际行为**: such a regression can pass the common assertion even though product/runtime code is required to expose
  no AI Advice surface.
- **直接证据**: line 16 is an active allowlist entry, not a historical fixture or negative-test marker.
- **影响**: weakens repository-wide regression protection and conflicts with the work unit's “current product surface
  contains no AI Advice” success condition.
- **建议改法和验证点**: remove the retired heading and stale design-section reference; run all notification-format,
  Brief renderer, and full-suite tests.
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 2-未修复-低-生产清理文档声称旧 Collector 会成为 drift extra unit，但无精确回归锁定该名称对

- **入口/函数**: `service_drift()` -> `_installed_units()` -> extra profile/installed unit calculation
- **文件(行号)**: `src/application/service_drift.py:1326-1332, 1817-1824`;
  `tests/test_service_deploy.py:139-173, 1818-1920`
- **输入场景**: a later authorized production reconcile reads a persisted profile and systemd root that still contain
  `options-monitor-ai-evidence-collector.service` and `.timer` after upgrading to this source.
- **实际分支**: generic `options-monitor*` discovery appears to classify both names as extra, but Slice 2 tests prove
  only that new bundles omit them. Existing exact retirement coverage uses unrelated Feishu and position-promotion
  units.
- **预期行为**: the accepted plan's explicit operational invariant and live deployment documentation should be backed
  by a read-only exact regression for the two retired Collector unit names.
- **实际行为**: a future change to managed-unit discovery could strand precisely these legacy units while the stated
  retirement contract still appears covered by generic tests.
- **直接证据**: repository search finds the two exact names only in the render-omission test; no drift result assertion
  names either unit.
- **影响**: current implementation is likely correct, but the separately authorized production cutover lacks the exact
  evidence promised by the plan.
- **建议改法和验证点**: create a temp current profile plus installed legacy service/timer, call drift read-only, and
  assert both `extra_profile_units` and `extra_installed_units`; do not call confirm/apply.
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Aggregate validation evidence

```text
sandbox full suite: 4443 passed, 10 skipped, 1 sandbox-only localhost bind failure
isolated authorized localhost HTTP test: 1 passed
Slice 1 committed focused suite: 189 passed
Slice 2 committed focused suite: 450 passed, 1 existing deprecation warning
ruff check .: passed
compileall: passed
dependency graph: current; production_modules=568; cycles=0
US/HK validate and build --dry-run: passed
all accepted commit patch checks: passed
active production import/entry-point search: no retired generator/collector/adapter/service declaration
```

The interrupted broad-permission full-suite attempt is not acceptance evidence: after 87% it waited indefinitely in a
test environment that had more network authority than the suite needs. It was terminated without background residue.
The complete sandbox run plus the one isolated localhost-only test is the accepted full-suite evidence.

## Open Questions

- 无。

## Residual Risk

- **requiring new issue or explicit user decision**: actual deployed config key removal and installed-unit reconcile
  remain production mutations outside this work unit.
- **requiring new issue or explicit user decision**: external private importers of deleted Advice-only modules are not
  observable from this repository and receive no compatibility shim by accepted design.
- **fixed in current slice**: model generation, online-news collection, current rendering, config enablement, collector
  rendering, and Advice-only Tick/portfolio plumbing are absent from source.
- **covered by current fix loop**: shared heading rejection and exact read-only legacy Collector drift evidence.

## Conclusion

`fail`. Both accepted findings require test-contract fixes and aggregate re-review.
