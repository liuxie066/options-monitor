# Code Review

## Scope

- Mode: current changes, aggregate DeepReview after fixes
- Branch or PR: `main`
- Base: `HEAD@b07b4aad`
- Output file: `docs/reviews/code-review-20260811-012817.md`
- Included scope: Position Advice v2 removal, strict Close Advice policy and evidence path, account-run integration, Daily Brief projection, read tools, Shadow Replay close evidence, config/docs, and retirement of v2 promotion services
- Excluded scope: runtime output data, historical release records, `docs/plans/**`, `docs/gateflow/**`, release/deploy/remote upgrade, and the unrelated untracked `docs/plans/controlled-production-upgrade-design-20260810.md`
- Parallel review coverage: none; review performed directly against the final working tree

## Findings

### 1-已修复-严重-旧 Position Advice v2 策略和控制面仍可进入运行链路

- **入口/函数**: account run, Daily Brief, Agent tools, CLI, promotion timer/service
- **文件(行号)**: deleted `domain/domain/position_advice*.py`; deleted `src/application/position_advice*.py`; `src/application/account_run.py`; `src/application/tick_notification_flow.py`; `src/application/service_deploy.py`
- **问题**: 原实现仍拥有 roll/replace/reallocate、portfolio plan/current pointer、authority mode、promotion gate、专用 notification token 和 lifecycle reconciliation，超出重新确认后的 Close Advice 边界。
- **修复**: 删除 v2 domain/application/infrastructure/CLI 模块及其专属测试、文档和服务定义；Account Run 与 Daily Brief 只消费严格 Close Advice 报告。
- **验证**: 非历史路径残留扫描只剩迁移诊断、兼容 wire identifier 和退役服务测试；全量 import/pytest 收集通过。
- **修复风险**: 中
- **严重程度**: 严重

### 2-已修复-高-Close Advice 同时保留多套策略状态与可调阈值

- **入口/函数**: `evaluate_close_advice()`; `select_close_advice_notification_rows()`
- **文件(行号)**: `domain/domain/close_advice.py:10-340`; `src/application/config_validator.py:1158-1192`
- **问题**: legacy tier、review、risk exit、long-call、Combo、replacement 和可配置阈值会让一个持仓同时存在多套决策语义。
- **修复**: 收敛为固定 `strict_profit_capture.v1`，唯一状态为 `close / hold / not_evaluable`；只有当前策略、priced、complete-evidence 的 `close` 能进入提醒。旧策略键仅告警并被忽略，authority/optimizer 键直接拒绝。
- **验证**: `tests/test_strict_close_advice.py`、`tests/test_close_advice_runner.py` 及配置验证测试通过。
- **修复风险**: 中
- **严重程度**: 高

### 3-已修复-高-严格建议缺少稳定持仓身份、币种和手续费依据时仍可能被视为完整

- **入口/函数**: `evaluate_close_advice()`; `_strict_fee_estimates()`
- **文件(行号)**: `domain/domain/close_advice.py:70-219`; `src/application/close_advice_runner.py:973-1090`
- **问题**: 没有 `position_lot_id`、currency、Futu fee status/basis，或 multiplier 被静默截断时，经济性结果不可追溯且可能跨 lot 混淆。
- **修复**: 上述字段全部改为 fail-closed 必需证据；fractional multiplier 不再转换成整数；买回成本统一使用 ask 和全成本手续费。
- **验证**: 缺失证据、非 Futu、非法 currency、fractional multiplier 和多 lot 回归测试通过。
- **修复风险**: 低
- **严重程度**: 高

### 4-已修复-高-失败或禁用运行可能暴露上一轮成功报告

- **入口/函数**: `run_close_advice()`; `validate_close_advice_report_manifest()`; Agent materialization
- **文件(行号)**: `src/application/close_advice_runner.py:1589-1640`; `src/application/close_advice_report_manifest.py:127-166`; `src/application/agent_tools/materialization_impl.py`
- **问题**: 非封存运行在读取 context 前未先使旧 manifest 失效；禁用后生成的空文件可能与旧空成功报告字节相同；manifest 原先也未验证文本 hash。
- **修复**: 所有 enabled 运行先发布 `pending`，disabled 发布非成功 manifest；成功后才原子发布 `success`；读取同时校验 CSV 和文本 hash；Agent 工具在 runner 非成功时不再概述旧 CSV。
- **验证**: matching old empty report、legacy failure、tampered text 和 stale materialization 回归测试通过。
- **修复风险**: 低
- **严重程度**: 高

### 5-已修复-高-Daily Brief 的账户边界与展示价格口径不足

- **入口/函数**: `_load_close_advice()`; `_position_close_details()`; `_position_card_fields()`
- **文件(行号)**: `src/application/daily_decision_brief_service.py:915-1004`; `src/application/daily_decision_brief_renderer.py:1383-1441`
- **问题**: 一个 manifest 同时列出多个账户时，per-account Brief 可能读取其他账户行；严格触发按 ask 计算，但消息仍展示旧 mid 口径。
- **修复**: per-account Brief 对每个 Close Advice row 做精确账户校验，任一错配整份报告 fail closed；消息改为显示 `买回参考价 ...（ask）`。
- **验证**: mixed-account report 和 HK/US ask-rendering 回归测试通过。
- **修复风险**: 低
- **严重程度**: 高

### 6-已修复-中-通用 receipt/scope 重命名会破坏既有封存证据和 fingerprint 连续性

- **入口/函数**: quote receipt validation; portfolio scope fingerprint
- **文件(行号)**: `src/application/source_receipts.py:13-205`; `domain/domain/portfolio_scope.py:7-28`
- **问题**: 若在删除 v2 时一并改变 persisted schema 字符串，同一账户 fingerprint 会漂移，旧 sealed required-data snapshot 也会变成不可读。
- **修复**: 删除 v2 manifest/adoption 控制面，但保留两个旧 wire identifier，仅作为数据兼容标识；新 receipt 强制 market scope、无账户控制字段、无 dependencies。
- **验证**: required-data receipt/snapshot 测试与 fingerprint 回归测试通过。
- **修复风险**: 低
- **严重程度**: 中

### 7-已修复-中-Shadow Replay 和 Strategy Lab 仍消费旧 P0/P1/P2/P3 Close 契约

- **入口/函数**: close episode capture, readiness, settlement, strategy-lab fixtures
- **文件(行号)**: `src/application/shadow_replay/capture.py`; `src/application/shadow_replay/readiness.py`; `src/application/shadow_replay/settlement.py`; `tests/test_strategy_lab.py`
- **问题**: 删除正式旧策略后，研究路径仍可能把旧 policy projection 或 replacement 结果当成当前 Close 证据。
- **修复**: replay 只接受 `strict_profit_capture.v1` 的正式结果，保留单策略历史结果测量；删除 replacement 和 promotion 质量判断；更新 Strategy Lab fixture。
- **验证**: strict close replay capture/readiness/outcome 与 Strategy Lab 测试通过。
- **修复风险**: 中
- **严重程度**: 中

### 8-已修复-中-删除 promotion 服务定义后已安装旧 unit 可能长期残留

- **入口/函数**: service bundle/drift cleanup
- **文件(行号)**: `src/application/service_deploy.py`; `src/application/service_drift.py`; `tests/test_service_deploy.py:1834-1894`
- **问题**: 仅从期望 bundle 删除 unit 不足以证明旧 systemd/launchd unit 会在受控升级时退休。
- **修复**: 将旧 Position Advice promotion units 纳入通用 extra-unit drift retirement 测试；生成 profile 不再含 promotion section。
- **验证**: service deploy/drift 回归测试通过。未执行任何本机或远端服务变更。
- **修复风险**: 低
- **严重程度**: 中

## Open Questions

- 无。生产运行配置快照仍含部分旧 Close Advice 键；本次按安全边界未修改它们。验证器会明确告警并忽略，不能重新启用旧策略。

## Residual Risk

- 当前普通提醒仍随既有 Daily Brief 调度发送；本次没有新增 Close-only 即时通知状态机。
- 兼容 wire identifier 名称仍包含 `position_advice`，但不对应任何可执行 v2 策略、authority、promotion 或 adoption 控制面。
- 旧 promotion unit 只有在未来经用户明确授权的受控 service apply/upgrade 中才会实际退休；本次没有部署或服务写入。
- 本 artifact 写入时，最新 focused suite 为 `98 passed`；最终全量复审和测试结果记录在后续 re-review artifact。
