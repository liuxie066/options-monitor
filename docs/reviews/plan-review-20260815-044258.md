# Plan Re-review — Sell Put Top1 W1B

- Reviewed target: `docs/gateflow/sell-put-top1-w1b/plan.md`
- Prior review: `docs/reviews/plan-review-20260815-043833.md`
- Fix artifact: `docs/gateflow/sell-put-top1-w1b/plan-fix.md`
- Review scope: all prior findings plus architecture boundary, best-practice, optimal-solution, overengineering, overcoupling, failure-path, dependency, test, and prerequisite sequencing lenses
- Artifact path: `docs/reviews/plan-review-20260815-044258.md`

## Findings

未发现新的实质性问题。

## Prior finding status

### PR-W1B-01 — 已修复

Caller-supplied fee amounts are gone. W1B derives terminal kind and calls the canonical HK fee calculator with the same row's strike, integral multiplier, and one-contract count. Partial fee-plan evidence remains a structured fail-closed outcome, and the test plan proves the call binding.

### PR-W1B-02 — 已修复

All three W1A ranking profiles are accepted as unique experiment levels. The baseline-equivalent `current_tie_break` arm stays explicit and cannot pass the positive-mean gate.

### PR-W1B-03 — 已修复

Economics/statistics no longer depend on the full ExperimentSpec module for a shared exception. Their only non-stdlib owners are the fee calculator and lazy SciPy PPF respectively.

### PR-W1B-04 — 已修复

The plan chooses producer-compatible `market=HK` and includes the exact one-line product-contract correction in scope, eliminating the two-case public contract.

## Assumptions re-tested

- Fee correctness is bound at the owning calculator rather than inferred from an unbound output: supported.
- ExperimentSpec remains strict while representing missing fee-plan facts as evidence, not malformed schema: supported; allowed fee-plan keys may be partial, extra keys remain rejected.
- Research hashing works on either valid spec shape and excludes validation-only fields: specified and testable.
- Point/day output shape and decision precedence are implementation-ready: specified, including concentration gate position and deterministic reason ordering.
- One slice remains the smallest independently useful delivery: supported; splitting the three pure contracts would add unusable intermediate commits and more review state.
- SciPy exposure is minimal: one pinned runtime dependency, one lazy `ppf` call, no fallback or wrapper.

## Open Questions

None in the plan contract.

The external prerequisite is not a plan ambiguity: PR #156 and PR #157 must be explicitly authorized and merged before the accepted-plan implementation transition.

## Residual Risks

- Real HK/lx fee-plan/provider readiness remains assigned to W0R and later provider-dependent work units.
- SciPy installation compatibility is covered by the required clean Python 3.12 install check before slice acceptance.
- Research leader selection, hidden authorization, persistence, and runtime orchestration remain explicitly assigned to W3/W5/W6/W7.

## Conclusion

`pass` — all prior findings are `已修复`; the plan is code-generation-ready once the merged-base prerequisite is satisfied.
