# Code Review

## Scope

- Mode: aggregate branch review
- Branch: `feat/earnings-near-expiry-window`
- Base: `origin/main@8902f9fd`
- Head: `0da30a10`
- Accepted plan: `fa5076f2`
- Output file: `docs/reviews/code-review-20260811-223007.md`
- Included scope: the complete work-unit diff, including accepted plan, N=6 earnings-calendar policy and evidence
  contract, all three strategy paths, state aggregation, sealed JSON consumers, audit-CSV compatibility boundary,
  docs, generated dependency graph, Gateflow artifacts, and regression tests
- Excluded scope: live OpenD/provider behavior, production config/runtime/service state, release/deploy/notification,
  and unrelated formulas or historical lifecycle changes
- Parallel review coverage: 无；对 `origin/main..0da30a10` 按策略合同、OpenD 证据、应用状态、封存消费、
  兼容边界五个视角做 aggregate 复审

## Findings

无。

## Aggregate decision

- The committed implementation matches the accepted plan: one shared six-natural-day inclusive hard window,
  same-day pending for the whole market-local date, day 7+ contextual only, and no second Combo configuration.
- Hard-window provider gaps fail closed only outcome-unresolved contracts; definitive rejects retain diagnostic
  gaps without making the universe partial; accepted siblings survive with explicit non-exhaustive disclosure.
- Sell Put, Covered Call, and Combo Funding Put use Candidate Engine as the only formal filter. Combo cash enrichment
  and its legacy-named CSV are not independent eligibility authorities.
- `opening_candidate_snapshot.json` and `combo_yield_candidate_snapshot.json` remain the formal candidate truths.
  CSV artifacts retained in this diff are audit/report/compatibility surfaces and are not reread by Advice or Brief.
- All slice findings (`CR-S1-01`, `CR-S1-RR-01`, `CR-S1-RR-02`, `CR-S1-RR-03`) are fixed and covered; the final
  slice review also recorded no findings.

## Verification evidence

- Final affected chain: `265 passed`.
- Final snapshot/Advice/Brief focus: `106 passed`.
- Full repository: `4717 passed, 10 skipped`; the sandbox-only localhost bind failure passed outside the sandbox
  as the exact read-only test (`1 passed`).
- Ruff over `domain`, `src`, and `tests`: pass.
- compileall `domain src`: pass.
- Generated dependency graph: current, `585` modules, `0` cycles.
- `git diff --check`: pass.

## Open Questions

- 无。

## Residual Risk

- OpenD's lack of a per-symbol completeness guarantee is accepted and documented under the confirmed single-source
  policy; changing that policy requires a later work unit.
- Live provider/runtime behavior remains unverified because no release or upgrade was authorized.
- Complete retirement of audit/report/history/Close Advice/research/Shadow Replay CSVs is outside this work unit.
- PR/CI review remains the next Gateflow stage after the aggregate-review commit and draft PR creation.

No live OpenD request, notification, config/runtime write, broker/ledger write, release, deployment, or upgrade was
performed during this aggregate review.
