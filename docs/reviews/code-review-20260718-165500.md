# Code Review

## Scope

- Mode: current changes
- Branch: `codex/release-upgrade-slice1`
- Base: `origin/main` at `b5ba7693`
- Output file: `docs/reviews/code-review-20260718-165500.md`
- Included scope: uncommitted changes to `scripts/release_preflight.sh`, the two slow tests, and preflight command-plan tests; related cooldown/rate-limit call paths
- Excluded scope: accepted plan documentation already committed; Slice 2–8; production runtime/config/service behavior
- Parallel review coverage: none; scope is four small files and was reviewed end-to-end locally

## Change intent

Remove two deterministic 30-second wall-clock waits from tests and avoid running focused agent/plugin tests immediately before a full pytest run, while preserving cooldown values, error reporting, non-full preflight behavior, metadata validation, dependency graph checks, and complete test collection.

## Execution-path review

1. `test_prefetch_required_data_protections_minimal` still drives `prefetch_required_data` through multiple waves and OpenD rate-limit classification. Only `_sleep_after_rate_limit_wave` is replaced; the new assertion proves the requested cooldown remains 30 seconds.
2. `test_fetch_symbol_reports_snapshot_rate_limit_errors` still runs the real snapshot orchestration and gateway exception/error classification. The wrapper replaces only the rate gate wait for this test; the real limiter has dedicated tests elsewhere.
3. `release_preflight.sh` still runs metadata and dependency graph checks. With `FULL=1`, the complete suite subsumes focused files; with `FULL=0`, focused behavior is unchanged.
4. The fake-Python tests exercise the real shell argument parsing and branch condition without executing expensive commands.

## Findings

未发现实质性问题。

## Open Questions

无 blocking open question。

## Residual Risk

- Pre-existing local interpreter selection can choose a repository `.venv` without pytest. Classified as **assigned to later work unit**; validation for this slice explicitly uses `OM_PYTHON=python3`.
- Hosted/machine timing variance remains. Classified as **fixed in current slice acceptance method** by using three-run median rather than a single result.
- Timeout, dependency lock, shared venv lifecycle, GitHub workflow and uv rollout remain **covered by later approved slices**.

## Validation evidence

- Focused: 36 passed in 0.85s.
- Target calls: approximately 0.01s each.
- Full preflight: 40s / 38s / 38s, median 38s.
- Full pytest: 2682 passed, 10 skipped.
- `bash -n` and `git diff --check`: passed.

## Conclusion

**pass**
