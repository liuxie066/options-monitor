# Code Review

## Scope

- Mode: current changes, post-fix deepreview
- Branch or PR: `feat/futu-quote-broker-boundary` (`HEAD=3ba1ffc2`)
- Base: current local `main` (`c0463267`); the branch is six commits behind and has no branch-only commit, so the reviewed implementation is the complete tracked/untracked Futu worktree change plus its interaction risk with the newer base
- Output file: `docs/reviews/code-review-20260804-223606.md`
- Included scope: the corrected Futu quote/broker capability implementation; quote and broker routing/readiness; health compatibility projections; quote-only, broker-only, mixed position and lifecycle callers; config audit; tests, generated dependency graph, and operator documentation
- Excluded scope: unrelated Feishu latency plan/review artifacts; release, deployment, runtime config mutation, OpenD/service/Incus changes, broker/ledger/Feishu writes, notification delivery, and production resource-incident closure
- Parallel review coverage: 无；主 reviewer 复核上一轮六项 finding 的修复，并重新沿 health、watchdog/doctor、gateway、calendar routing、trade enrichment、lifecycle settlement、config audit 和 mixed position path 走读最终调用链

## Findings

### DR2-01-未修复-高-healthcheck 绕过 quote doctor，登录成功但行情字段不可用时仍会 false green，并丢失原有 Telnet 诊断

- **入口/函数**: `healthcheck` -> `run_healthcheck_tool()` -> canonical quote capability probe -> typed/legacy readiness projection
- **文件(行号)**: `src/application/agent_tools/healthcheck_impl.py:225-233, 572-595, 722-747`; `src/application/futu_doctor.py:175-227`
- **输入场景**: canonical quote OpenD 可连接、`program_status_type=READY` 且 `qot_logined=true`，但代表性期权的 option-chain/snapshot 必需字段缺失、quote entitlement 不可用，或 Telnet 端口未监听。
- **实际分支**: healthcheck 明确删除 `run_futu_doctor` 和 `healthcheck_symbols_for_futu` 依赖，只调用 `build_ready_futu_quote_gateway()`。该 builder 仅验证 global-state quote login；成功后 healthcheck 直接合成 `quote_ready=True`，并把所有 readiness payload 的 `telnet` 固定为空字典。虽然 `run_futu_doctor_checks(required_capability="quote")` 已保留代表性 symbol field probe 和 Telnet probe，但健康检查没有调用它。
- **预期行为**: quote inventory 应按计划用代表性 symbols 验证一次 quote capability，保留原有 SDK/option-field/Telnet 诊断；broker-only endpoint 不做 quote field probe，但 canonical quote endpoint 的必需字段失败必须使 typed quote、legacy endpoint aggregate 和 summary fail closed。
- **实际行为**: 只要 quote 登录成功，typed `opend_quote_readiness_*`、对应 `opend_readiness_<endpoint>`、aggregate `opend_readiness` 和 summary 都可为成功，即使实际 option chain/snapshot 已不可用；Telnet failure warning 和既有 `value.telnet` evidence 也不再出现。
- **直接证据**: healthcheck line 233 丢弃 doctor/symbol dependencies；lines 577-590 仅凭 ready quote builder 合成成功状态；lines 727-733 虽继续发布 compatibility `telnet` 字段，但上游 readiness 永远提供 `{}`。相对地 doctor lines 183-227 已实现 quote-only representative-symbol field validation，lines 196 和 229-238 仍拥有 Telnet/SDK evidence。计划 §7.3 要求 quote doctor 保留 required option fields，§8.5 要求 quote endpoint 使用 representative symbols，且 legacy `telnet` 字段保持。
- **影响**: 部署/运行健康门禁可能在 Futu quote 权限、字段或 option-chain 实际不可用时继续放行，随后 required-data/tick 才失败；同时 operator 失去原有 Telnet/2FA 可观测性。这是生产健康 false green，而非单纯诊断细节缺失。
- **建议改法和验证点**: 让 health quote inventory 复用 capability-aware doctor（`required_capability="quote"`）或等价的单次 typed probe，并传入 `healthcheck_symbols_for_futu(cfg)`；从真实 probe payload 投影 `ready/global_state/telnet` 和 legacy checks，不额外探测 broker capability。新增回归测试：READY+quote-login 但 required field failure => typed quote、legacy endpoint/aggregate、summary 均 error；Telnet failure evidence/warning 保留但不单独改变既有 readiness 语义；同物理 endpoint 双角色仍只计一次 compatibility failure。
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 高

## Open Questions

- 当前分支落后 `main` 六个提交，且新 base 与本 work unit 同时修改 `src/application/account_config.py`、`src/application/multi_account_tick.py`、`src/application/trades/auto_intake.py`、`src/infrastructure/external_services.py` 及其 tests/docs。交付前必须先集成 current `main`，重新检查这些 ownership/call-chain 交叉点，再重跑 dependency graph、focused suite 和 full suite；本轮在旧 base 上通过的测试不能替代该集成验证。

## Residual Risk

- 上一轮六项 finding 的代码路径均已修复：quote failure 会使 legacy aggregate/summary fail closed；settlement 可保留 broker receipts 并贯穿 `trd_env`；tick quote watchdog 不再依赖 trade login；broker readiness 对缺失状态字段严格失败；trading-calendar 使用 canonical route；detail enrichment readiness failure 被 containment。
- 修复后验证通过：focused suite `386 passed`，补充 focused subset `142 passed`，完整 suite `4287 passed, 10 skipped`，Ruff 通过，dependency graph 为 579 production modules/0 cycles，`git diff --check` 通过。
- 本轮未连接 live OpenD、未运行 notification-producing tick、未写 runtime config/broker/ledger/Feishu，也未执行 release、部署、服务重启或 matched resource window；source correctness 与原 IO incident closure 仍是独立结论。
