# Code Review — S10 Re-review

## Scope

- **Gate**: S10 re-review
- **Work unit**: `option-performance-refactor`
- **Mode**: current changes
- **Branch**: `excited-rhino`
- **Base**: `53ee3b5a`
- **Output file**: `docs/reviews/code-review-20260718-092348.md`
- **Source review**: `docs/reviews/code-review-20260718-091848.md`
- **Fix artifact**: `docs/gateflow/option-performance-refactor-S10-fix-20260718-092255.md`
- **Included scope**: F1-F5 fixes in the S10 reconciliation/service/test files plus their interaction with the public tool and domain report statuses.
- **Excluded scope**: unrelated release and Feishu dirty changes; no subagent coverage.

## Findings

未发现新的实质性问题。

### F1-已修复-[高]-proven-scope normalization no longer turns unreadable facts into zero

- Zero promotion is now gated by an explicit clean-warning check at the report boundary.
- The corrupt-event regression proves decode/projection warnings preserve `not_observed` monetary envelopes while report quality remains partial.
- Genuine empty configured scopes still become observed zero.

### F2-已修复-[高]-realized identity reconciliation fails closed

- Missing/non-list/non-mapping row payloads now fail with the missing side identified.
- One-sided identity maps produce an exact non-zero delta and fail.
- Explicit empty sets compare as an exact empty map and pass without using `not_applicable` as a loophole.

### F3-已修复-[中]-coverage traverses list-valued breakdowns

- Recursive traversal now includes list/tuple indices.
- Missing-as-zero under `breakdowns.monthly[0]` is detected with a precise indexed failure path.

### F4-已修复-[中]-missing fee evidence is classified as incomplete

- Native fee deltas are asserted only when gross/net currency coverage agrees and no non-FX evidence is missing.
- Missing-fee net values return `fee_coverage_incomplete` and no numeric delta.

### F5-已修复-[中]-observed non-CNY metrics require CNY/FX evidence

- Observed envelopes with null CNY now fail.
- Observed non-CNY native values without selected FX fact IDs now fail.
- Existing valid observed fixtures carry explicit FX evidence; partial FX-missing envelopes remain valid only with explicit missing reasons and null CNY.

## Adversarial Failure Pass

Re-ran the original counterexamples against the fixed implementation:

- legacy realized row + empty v1 rows → exact failure;
- corrupt configured-scope event → no observed-zero promotion;
- missing-as-zero in a breakdown list → coverage failure;
- missing fee → incomplete coverage classification;
- observed USD/null CNY/no FX IDs → coverage failure.

No new semantic ownership drift, dependency cycle, public contract bypass, or assigned-stock double-counting path was found in the S10 fixes.

## Validation

- Focused reconciliation/public-tool tests: **24 passed**.
- Performance engine/service/assignment/reconciliation/public-tool tests: **46 passed**.
- Expanded performance and CLI suite: **83 passed**.
- `python3 -m ruff check .`: **passed**.
- `python3 scripts/generate_dependency_graph.py --check`: **passed**, `production_modules=465 cycles=0`.
- `git diff --check`: **passed**.

## Open Questions

无 blocking open question.

## Residual Risk

- **fixed in current slice**: F1-F5.
- **fail-closed current policy**: UTC/Asia-Shanghai boundary mismatches still require source-fact investigation rather than automatic tolerance.
- **assigned to later work unit**: legacy removal after the deprecation window.
- **assigned to operational follow-up**: historical valuation/FX evidence backfill.
- **accepted v1 design limitation**: separate FX-attribution decomposition is not included.

## Completion Status

- **Gate result**: pass-with-classified-risks
- **Finding status**: 5 已修复, 0 未修复, 0 部分修复, 0 证据失效
- **Next Gateflow entry point**: S10 accepted slice validation and protected commit
