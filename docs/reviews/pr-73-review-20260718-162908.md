# Code Review

## Scope

- Mode: PR re-review
- Branch or PR: `liuxie066/options-monitor#73` — `feat: support true diagonal Combo Yield lifecycle`
- Base: remote `main` at `0af7adac` (v1.2.411)
- Reviewed candidate head: local in-progress merge result for `codex/diagonal-combo-yield-lifecycle`; remote PR head remains `ad6f4627` until the accepted PR-review commit is pushed.
- PR URL: `https://github.com/liuxie066/options-monitor/pull/73`
- Output file: `docs/reviews/pr-73-review-20260718-162908.md`
- Included scope: effective diff against current `origin/main`; staggered explicit-intent intake; single-cycle reuse protection; option/group lifecycle inventory; assignment metadata handoff; full-lifecycle reporting; leg-authoritative additive Close Advice; ledger metadata persistence; docs and tests.
- Excluded scope: unrelated Option Performance implementation already owned by current main, except integration boundaries touched by this feature.
- Parallel review coverage: 无。

## Findings

未发现新的实质性问题。

### PR-F1-已修复-严重-过期主干与重复 staggered Combo Yield architecture

- **原 finding**: `docs/reviews/code-review-20260718-160451.md` identified that PR #73 duplicated current-main staggered pairing contracts and was unmergeable.
- **修复证据**:
  - The branch is merged with current `origin/main` `0af7adac`; no unresolved merge paths remain.
  - Current-main vocabulary and owners are authoritative: `structure_mode=staggered_expiry_pair`, `pair_intent_id`, `funding_put`, `participation_call`, `src/application/positions/combo_pairing.py`, and `domain/domain/assigned_stock.py`.
  - Duplicate branch surfaces were removed/restored to current main; the effective feature diff adds lifecycle, assignment propagation, Close Advice synthesis, and current-owner intake validation rather than a second strategy architecture.
  - `src/application/trades/resolver.py:360-378,682-884` fails closed on explicit-intent conflicts, invalid expiry order/quantity, and `staggered_combo_yield_cycle_reuse`.
  - `src/application/positions/reporting.py:246-274,1112-1184,1329-1353,1439-1477` reconstructs snapshot metadata and reports the complete option/assigned-stock lifecycle.
  - `domain/domain/assigned_stock.py:67-74,922-947,1154-1171,1390-1397` propagates strategy identity to assigned-stock lots and sales without automating sale/exercise.
  - `domain/domain/combo_yield_lifecycle.py:41-331` owns quantity-aware option and full-group classification; unknown Call value at Put expiry is never coerced to zero.
- **验证**:
  - Focused feature/current-main suite: `321 passed`.
  - Option Performance and assigned-stock regression: `180 passed`.
  - Full repository: `2647 passed, 10 skipped`.
  - `python3 -m compileall -q domain src`: pass.
  - `git diff --check`: pass.
  - Dependency graph generation/check: pass, `production_modules=466 cycles=0`.
  - US/HK example config validate and dry-run build: pass; no config write applied.
- **最终状态**: 已修复。

## Adversarial Failure Pass

- Reusing a pair intent after any Put/Call capacity was closed or consumed returns unresolved `staggered_combo_yield_cycle_reuse` before persistence.
- Incoming quantities cannot exceed the already-open opposite leg; mismatches return unresolved `staggered_combo_yield_quantity_conflict`.
- Different account/symbol/role/structure/expiry evidence fails closed rather than silently retagging lots.
- Partial assignment reconciles assigned contracts against residual Call quantity; malformed settlement or quantity evidence becomes `review_required`.
- Close Advice retains each leg's tier/reason/action; group synthesis is additive and withholds group action when quotes, quantities, or identity evidence are incomplete.
- Residual Call decisions use only the Call's current real quote and long-Call policy; no Put-expiry terminal/residual value is predicted.

## Open Questions

- 无。

## Residual Risk

- Broker trade-intake validation is read-then-write and inherits the repository's existing concurrency model; no new transactional locking is introduced. Owner: existing ledger/intake architecture, assigned to later work unit only if concurrent duplicate fills are observed.
- Group Close Advice is option-inventory scoped and intentionally does not infer assigned-stock state; the reporting lifecycle is the authoritative assignment-aware view. Owner: documented product boundary, no current fix required.
- No live notifications, production config, option-position state, trade events, or broker-facing execution were mutated during validation.
