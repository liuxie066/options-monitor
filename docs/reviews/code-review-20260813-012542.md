# Code Review

## Scope

- Mode: current changes
- Branch or PR: `refactor/candidate-csv-retirement` S3 re-review
- Base: S2 checkpoint `7a84eb4d`
- Output file: `docs/reviews/code-review-20260813-012542.md`
- Included scope: the complete S3 candidate CSV producer/config/status/CLI retirement diff; fixes for `S3-CR-01` and `S3-CR-02`; real watchlist-to-symbol-to-status-to-owner-seal-to-manifest paths; calculation-focused SP, CC, SP+LC, and CC+LP tests; operator documentation and static guards
- Excluded scope: S1/S2 internals already accepted at their slice gates; live OpenD, notification writes, release, deployment, runtime configuration mutation, and historical artifact mutation
- Parallel review coverage: 无；主 reviewer 逐条复核两项 finding，并走读 scanner -> underwriting -> capture -> status -> snapshot/manifest、manual scan、runtime config freshness/validation、Research metadata classification 和 Shadow Replay evidence selection 路径

## Findings

未发现实质性问题。

### S3-CR-01-已修复-中-US/HK 全路径禁产回归矩阵

- 修复证据：`test_candidate_csv_retirement_account_run_matrix` 形成 US/HK × SP+LC/CC+LP × enabled/disabled/empty/failure/success-empty 共 20 个 case，实际进入 `run_watchlist_pipeline_default()`、`run_watchlist_pipeline()`、`process_symbol()`、`run_symbol_monitoring()`、per-scope status、owner sealer 和 manifest publisher；每个 case 递归检查账户运行目录无退役候选 CSV，并加载 v2 status index 与 sealed bundle。
- 复审判断：原 finding 的跨市场、异常、禁用与 success-empty 文件系统缺口已关闭。计算本身由确定性 adapter 隔离，生产 calculation owner 另由 focused tests 固定候选行、拒绝理由、顺序、摘要和手工 alert 文本；没有用 mock 伪造 status/seal 行为。
- 最终状态：`已修复`。

### S3-CR-02-已修复-低-Combo trace evidence_path 与实际证据所有者一致

- 修复证据：账户/run capture 同时具备 Combo evidence sink 和可解析 run/account scope 时，trace 指向 `state/combo_yield_candidate_snapshot.json`；manual/non-account-run 没有 terminal owner seal 时，trace 指向自身 `candidate_filter_trace.jsonl`。两条路径都有精确断言。
- 复审判断：trace 不再指向账户目录根部的不存在快照，也不会在手工扫描中虚构 sealed snapshot。snapshot 是账户/run 终态 authority，JSONL 是无 seal 场景的可解析审计证据，ownership 边界一致。
- 最终状态：`已修复`。

## Open Questions

- 无。

## Residual Risk

- 文件系统矩阵在策略 runner 边界使用确定性 adapter，因此它证明真实 orchestration/status/sealing 与禁产行为，不单独证明 OpenD 或各策略数学。后者由同一切片的 scanner、underwriting、Combo diagnostics/rank、CC+LP metrics/rank 和 renderer focused tests覆盖；分类为 `fixed in current slice`，将在 S3 checkpoint 前整体复跑。
- 没有执行 live OpenD、通知、生产 runtime 重写、发布、部署或历史 artifact 修改；这些属于独立授权的 operations work unit，不是本 source-only slice 的验收条件。分类为 `assigned to later work unit`。
