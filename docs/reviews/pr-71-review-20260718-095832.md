# Code Review — PR #71

## Scope

- **Gate**: PR review
- **Work unit**: `option-performance-refactor`
- **Mode**: PR
- **Repository**: `liuxie066/options-monitor`
- **PR**: #71 — Refactor option performance reporting and period semantics
- **Author**: `liuxie066`
- **URL**: https://github.com/liuxie066/options-monitor/pull/71
- **Head**: `excited-rhino` at `ad21538152ac94e77151d6e208f8df8086e61f33`
- **Base**: `main` at `669290106424e4d1785151d71ae9063545f1fdbc`
- **Output file**: `docs/reviews/pr-71-review-20260718-095832.md`
- **Included scope**: the complete PR diff from merge base `2d249ab6` through the accepted aggregate-deepreview head, current-main integration state, public option-performance/assistant contracts, generated dependency artifacts, tests, and GitHub checks.
- **Excluded scope**: unrelated local release/Feishu dirty working-tree changes, production config, runtime outputs, broker/Feishu writes, and external portfolio-management implementation.
- **Parallel review coverage**: none; subagents were not authorized.
- **Diff acquisition note**: GitHub's `.diff` endpoint rejected the patch because it exceeds 20,000 lines. The local `origin/main...HEAD` diff was reviewed instead. Its sorted 130-path inventory has the same SHA-256 as `2d249ab6..HEAD`, proving the PR scope is exactly the accepted work-unit range; GitHub's files metadata is capped at 100 entries.

## Intent and Contract Alignment

The PR intentionally replaces ambiguous monthly option-income semantics with Option Performance v1. The public read path now supports MTD, YTD, natural month, and natural year periods, keeps activity/cash/PnL separate, makes assigned-stock lifecycle and evidence quality explicit, and adds exact-account/timezone/report-quality gates to portfolio PnL and cash bridges.

The head SHA exactly matches the accepted aggregate-deepreview commit. The prior aggregate loop fixed all four accepted findings and passed the adjusted repository suite (`2588 passed, 10 skipped, 1 deselected`), Ruff, dependency-graph validation, the exact legacy-reference inventory, and `git diff --check`.

## Findings

### PR-01-未修复-[高]-PR cannot merge into current main

- **入口/函数**: GitHub mergeability calculation and the branch integration boundary.
- **文件(行号)**: `docs/AGENT_INTEGRATION.md`; `docs/DEPENDENCY_GRAPH.md`; `docs/dependency_graph.mmd`.
- **输入场景**: merge PR head `ad215381` into current `main` at `66929010` after releases v1.2.408/v1.2.409 landed.
- **实际分支**: GitHub reports `mergeable=CONFLICTING` and `mergeStateStatus=DIRTY`. A three-way `git merge-tree` reproduces content conflicts in the integration guide and both generated dependency-graph artifacts.
- **预期行为**: the draft PR head must integrate with current main and leave generated dependency artifacts derived from the combined production tree.
- **实际行为**: the PR is unmergeable; no merge commit can be produced without manual conflict resolution.
- **直接证据**: PR metadata returned base `66929010`, head `ad215381`, `CONFLICTING/DIRTY`; `git merge-tree 2d249ab6 HEAD origin/main` reports `changed in both` for the three files.
- **影响**: hard merge blocker; the accepted work unit cannot reach `draft-PR-pass`.
- **建议改法和验证点**: merge current `origin/main` into the feature branch; preserve both the option-performance and Feishu documentation changes in `docs/AGENT_INTEGRATION.md`; regenerate dependency artifacts from the combined tree; require `cycles=0`, full adjusted pytest, Ruff, and clean diff checks.
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 高

### PR-02-未修复-[高]-current-main Feishu integration test still asserts the deprecated income tool contract

- **入口/函数**: `handle_feishu_ws_event -> handle_assistant_request -> parse_assistant_command -> _parse_income -> _build_tool_payload`.
- **文件(行号)**: `src/application/assistant/command_parser.py:64-65,350-398`; `src/application/assistant/inbound_control.py:252-257`; current-main `tests/test_inbound_feishu_ws.py:111-155`.
- **输入场景**: an allowed Feishu sender sends `/income sy 2026-05` through the real WebSocket event handler after this PR is merged with current main.
- **实际分支**: the parser emits intent/tool `option_performance_report` and payload `{config_key: us, account: sy, period: month, month: 2026-05}`; the renderer emits the new `期权收益统计完成` prefix.
- **预期行为**: the current-main end-to-end Feishu test must assert the new primary public contract while retaining v1.2.409's mixed-case `Typing` reaction behavior.
- **实际行为**: current-main test line 150 still expects `monthly_income_report` with no `period`, and line 154 expects the old `收益统计完成` prefix. This is the one assertion previously deselected only because the Feishu work was still unrelated local dirt; it is now part of the PR's actual base integration surface.
- **直接证据**: `_parse_income` returns `_intent("option_performance_report", payload)`; `_build_tool_payload` includes `period`; focused inbound-control tests already assert the new exact tool/payload and prefix; current-main Feishu test retains the legacy expectation.
- **影响**: the merged repository test suite fails at a public transport-to-control integration boundary, masking whether Feishu still delegates and replies correctly under the new performance contract.
- **建议改法和验证点**: update only this end-to-end assertion/fixture to return a minimal valid Option Performance v1 response, expect the exact new tool/payload and response prefix, and preserve `Typing`. Run the test without deselection plus the full adjusted suite.
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 高

## Adversarial Failure Pass

- Cross-account aggregate bridge attribution, timezone mismatch, assigned-stock historical review debt, invalid assigned-stock sale, and partial-report bridge use remain covered by the accepted aggregate regressions.
- The PR path inventory matches the accepted work-unit range; no unrelated Feishu/release path is present in the PR diff itself.
- The two current findings are integration failures introduced by base advancement, not re-opened aggregate semantics.
- GitHub checks at review time: `Analyze (actions)` passed, `Analyze (python)` pending, CodeQL skipped.

## Open Questions

无 blocking open question. Both findings have deterministic fixes within the current Gateflow scope.

## Residual Risk

- External portfolio-management runtime contract conformance remains owned by that service; OM fails closed on mismatch.
- Historical assignment-lifecycle repairs remain an operational data follow-up.
- Legacy adapter removal remains a later versioned work unit.
- GitHub cannot render/download the entire PR diff through its 20,000-line endpoint; local git plus commit/path identity is the review source of truth.

## Completion Status

- **Gate result**: fail; fixes required
- **Finding status**: 2 accepted, 0 rejected, 0 deferred, 0 needs-more-evidence
- **Current gate / next entry point**: PR review fix
