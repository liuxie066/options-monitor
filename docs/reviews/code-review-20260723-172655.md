# Code Review

## Scope

- Mode: aggregate current-branch review
- Branch: `fix/copilot-option-performance-mtd`
- Base: `origin/main@0db40d50`
- Head: `b748bbea` plus generated dependency graph and aggregate validation artifact
- Work unit: `copilot-option-performance-mtd`
- Included scope: accepted Gateflow plan; Copilot payload construction; fixed UI scope precedence;
  canonical performance fact generation; option/assigned-stock PnL decomposition; public Agent
  contract; deterministic renderer; prompt guidance; exact Feishu regressions; P1 evaluation;
  generated dependency graph; full test evidence
- Excluded scope: live Feishu canary, production data/config mutation, release, deployment,
  historical backfill, accounting-policy changes

## Findings

- 无。

## Aggregate Invariant Verification

### First-call reliability

- Fake `None` defaults are absent from the Copilot schema and payload.
- Explicit invalid/null values remain fail closed.
- Tool-owned normalization runs before safe defaults.
- A valid explicit period prunes only irrelevant model-supplied period fields.
- Fixed UI scope overlays last and cannot be silently discarded.
- `period=mtd` with hidden month/year/range pollution executes on the first call.

### Financial ownership

- Combined realized gross/net remains the canonical total.
- Option and assigned-stock component metrics are built from exact generation-time fact sets.
- Assignment option realization stays in option PnL.
- Assignment settlement fee and assigned-stock sale fee affect stock net once.
- Assignment principal and stock-sale proceeds remain cash facts, not profit.
- Assigned-stock summaries no longer infer fact ownership from a shared assignment event ID.
- Missing fee/FX evidence remains partial/not-observed.

### User-visible answer

- Period kind/date/status and actual account scope are explicit.
- Combined period total, combined realized, pure-option realized, and assigned-stock realized
  gross/net are separate.
- Premium activity, option cash, fees, settlement principal, stock-sale proceeds, contract/share
  activity, assignment state, and evidence gaps are separate.
- Native-currency amounts retain two decimals, including sub-unit fees.
- MTD prompts use the canonical report first and keep all-account scope when account is omitted.

### Evaluation contract

- The exact reported question and correction are deterministic regression cases.
- P1 rejects generic analysis as the primary MTD evidence.
- P1 rejects natural-month or narrowed-account inputs.
- A correction can reuse prior canonical evidence; a retry must remain canonical.
- Prompt compaction preserves tool evidence and authoritative Control context.

## Verification

```text
ruff check .: passed
compileall domain src scripts: passed
pytest: 3065 passed, 10 skipped, 6 existing deprecation warnings
dependency graph: 481 production modules, 0 cycles, boundary PASS
git diff --check: passed
```

## Open Questions

- 无。

## Residual Risk

- A real hosted model can still drift; the P1 evaluator catches rather than mathematically
  prevents model behavior drift.
- No live Feishu canary was run, so production provider latency and model-specific wording remain
  unverified.
- Current-message-only account provenance is not available in the scene contract; the report
  exposes actual resulting scope instead of inventing provenance.
