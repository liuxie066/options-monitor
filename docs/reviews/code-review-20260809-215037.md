# Code Review

## Scope

- Mode: current changes aggregate re-review
- Branch or PR: `feat/ai-decision-advice`
- Base: `77f1bbd9` (`feat(ai-advice): accept drift remediation S2`)
- Prior review: `docs/reviews/code-review-20260809-213614.md`
- Output file: `docs/reviews/code-review-20260809-215037.md`
- Included scope: complete S3 deterministic candidate/PM/option/evidence freeze, option aggregation, one-contract projections, fact registry, semantic bindings, Combo identity/membership proof, FX helper and focused tests
- Excluded scope: S4 Advice schema/validator/reuse cutover, S5 evidence-store redesign, S6 production orchestration handoff, notification/release/deployment behavior, live PM/OpenD/DeepSeek calls
- Parallel review coverage: 无

## Findings

No material findings. The four findings from the prior review are fixed and
covered by deterministic regressions:

- semantic hashes ignore observation-only timestamps while retaining quality,
  coverage and fact changes;
- `SP+LC` requires a valid identity, exact ledger membership, matching member
  bindings and matching formal contract keys;
- complete PM distributions distinguish real zero/negative exposure from
  unavailable data;
- candidate option details retain multiplier identity.

## Open Questions

- 无。S3 boundaries and the later S4/S6 cutover are explicit in the accepted
  plan.

## Residual Risk

- The expanded AI Decision Advice suite reports `158 passed, 3 failed`. The
  three failures are the pre-existing, planned S6 handoff: current
  `orchestration.py` still calls `build_frozen_inputs()` through removed legacy
  `portfolio_context` / `position_lots` arguments. S3 correctly does not add a
  fallback, and the branch remains non-mergeable until S6 completes.
- S4 has not yet connected the new projections, fact registry and expanded
  bindings to the Advice schema, prompt, validator and reuse contract.
- The authoritative ledger currently exposes a formal Combo identity only for
  `SP+LC`; S3 therefore does not infer or label `CC+LP` heuristically.
