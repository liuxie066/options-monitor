# Plan Re-review — True Diagonal Combo Yield Lifecycle

- **Gate**: re-review
- **Work unit**: 真正错期 Combo Yield 的端到端处理
- **Reviewed target**: `docs/gateflow/diagonal-combo-yield-plan-20260718-130000.md`
- **Prior review**: `docs/reviews/plan-review-20260718-130142.md`
- **Artifact path**: docs/reviews/plan-review-20260718-130335.md
- **Current gate / next entry point**: `accepted plan commit`

## Fix Verification

### PR-1-已修复-高-Close Advice assigned-stock source-of-truth mismatch

- Plan now defines `option_group_inventory` from projected option lots and `full_group_lifecycle` from trade events + assigned-stock lots.
- Close Advice is explicitly limited to `evidence_scope=option_lots` and may classify only active/missing/residual/review/closed option states.
- `assigned_stock_with_residual_call` and `assigned_stock_only` are reporting-only classifications.
- The runner is forbidden from inferring assigned stock from last close type or querying reporting/ledger internals.
- **Status**: 已修复.

### PR-2-已修复-高-Explicit identity producer/handoff missing

- Candidate contract now emits stable `combo_pair_fingerprint` and account-scoped `strategy_group_id` when account is available.
- Structured/manual open is the explicit v1 producer/transport path and must persist the same ID on both legs.
- Candidate → open preview/write → projected lot → restart reconstruction is now an explicit contract test.
- Raw broker-only fills without diagonal intent are explicitly unresolved/fail-closed and no longer counted as automatic end-to-end support.
- **Status**: 已修复.

### PR-3-已修复-中-Inconsistent exclusive status sets

- Plan now keeps quantities, labels and issues as canonical facts.
- Both evidence adapters share one ordered `summary_classification` derivation.
- Any structural/quantity/mixed ambiguity wins as `review_required`, preventing a mutually-exclusive phase from hiding composition.
- **Status**: 已修复.

## Adversarial Lenses

### Architecture boundary

Pass. Domain owns pure classification; application adapters own evidence assembly; Close Advice consumes context rather than querying ledger/reporting internals; assigned-stock execution remains independent.

### Best-practice

Pass. Durable identity now has producer, transport, persisted consumer and restart reconstruction for the supported explicit-intent path. Unsupported broker-only transport fails closed with diagnostics.

### Optimal solution

Pass. The plan avoids adding an order-intent subsystem while still making the supported path usable and auditable. Splitting evidence scopes is smaller and safer than expanding Close Advice into lifecycle reporting.

### Overengineering

Pass. No schema migration, mutable group state, pricing model, stock evaluator or automation layer is introduced.

### Overcoupling

Pass. Shared semantics live in a pure classifier, while option context and full lifecycle reporting remain independently sourced.

## Open Questions

None blocking implementation.

## Residual Risks

1. Broker-only fills without OM intent metadata remain unresolved — **assigned to later work unit**.
2. Historical groups missing diagonal metadata remain best-effort/review-required — **assigned to later work unit**.
3. Future residual-value/scenario modeling is excluded — **assigned to later work unit**.
4. Notification promotion requires shadow evidence and CEO decision — **requiring explicit user decision after this work unit**.
5. Assigned-stock sale thesis remains manual/independent — **assigned to later work unit**.

All residual risks are classified and none blocks the approved implementation scope.

## Final Plan Review Conclusion

**Conclusion: `pass-with-risks`**

The repaired plan is code-generation-ready. All accepted findings from the prior review are `已修复`; no blocking open question or unclassified residual risk remains.
