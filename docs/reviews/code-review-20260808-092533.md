# Code Review — Combo Yield S2: 成本约束统一

## Scope

- Gate：implementation slice code review
- Work unit：combo-yield-policy-alignment
- Slice：S2（删除 `funding_mode` / `max_call_cost_to_put_credit` / `max_debit` / `max_debit_native`，统一 retention 约束）
- Base：`14e2afa2`（S1 accepted slice commit）
- Reviewed changes：
  - `src/application/yield_enhancement_config.py`
  - `src/application/config_validator.py`
  - `src/application/config_defaults.py`
  - `src/application/sell_put_call_helper.py`
  - `domain/domain/engine/yield_enhancement.py`
  - `configs/system.json` / `configs/examples/user.example.us.json`
  - `tests/test_config_yaml.py` / `tests/test_sell_put_linked_call_helper.py` / `tests/test_sell_put_yield_enhancement_validate_config.py`

## Change Intent

把 Combo Yield 的成本约束收敛为唯一的 `min_net_credit_retention`：删除 `funding_mode`（credit_or_even / max_debit）及配套 `max_debit` / `max_debit_native`，删除作为 retention 补数的 `max_call_cost_to_put_credit`；validator 对四个字段明确拒绝。

## Contracts / Sources of Truth Checked

- 确认文档 §4：删除 `funding_mode` 与 `max_call_cost_to_put_credit`，统一 retention；
- `retention >= 0.60` 隐含 `combo_net_credit >= 0.6 * put_net_credit > 0`，因此负净 credit 天然被拒；
- staggered 分支原有 `funding_ratio >= 1` / `call_cost_to_put_credit <= 1` 检查在 retention≥0.6 下恒成立（retention 0.6 隐含 funding_ratio≥2.5），为死代码，已删除；
- Shadow / research 层的 `ComboYieldResearchPolicy.max_call_cost_to_put_credit` 保留（确认文档 non-goal：不移除 research 兼容）。

## Findings

### 1-未修复-低-shadow 变体配置仍写入 `max_call_cost_to_put_credit` 显式字段
- **位置**: `src/application/shadow_replay/combo_capture.py:482-483, 510-511`
- **问题类型**: 契约一致性（deferred）
- **当前写法**: `_variant_combo_config` 在 `max_call_cost_to_put_credit` 非空时写入 cfg 并加入 `_explicit_fields`。
- **反例/失败场景**: shadow 变体配置只进入 research planning / `ComboYieldResearchPolicy`，不经过生产 validator；字段保留不影响生产路径，但若未来 shadow cfg 被复用进生产派生，会产生“字段存在但不生效”的静默状态。
- **为什么有问题**: 当前确认文档明确 research 兼容保留，因此不是本 slice 阻塞；但字段语义已从“生产成本门槛”变为“research-only”。
- **直接证据**: `combo_capture.py:169` 只把 `variant_cfg` 传给 `plan_builder`（research planning），不进入生产 `find_sell_put_yield_enhancement_pairs`。
- **影响**: 低；research 层保持兼容，无生产行为变化。
- **建议改法和验证点**: 作为独立后续 work unit 清理 research/shadow 层字段语义；本 slice 保留并记录 owner。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Adversarial Failure Pass

- 生产 config 四个旧字段均被 validator 拒绝（`config_validator.py:596-602`），不会静默忽略；
- `YIELD_ENHANCEMENT_ALLOWED_FIELDS` 已移除四个字段，`_reject_unknown_keys` 与显式 removed 检查双保险；
- `compute_yield_enhancement_funding_decision` 删除 `max_call_cost_to_put_credit` 参数后，生产调用点同步更新（`sell_put_call_helper.py`），无悬挂引用；
- staggered 死代码删除后，retention 仍由 `_candidate_pair_reject_reasons` 统一应用；
- 空结果与 pair diagnostics 列清理（`_PAIR_DIAGNOSTIC_COLUMNS`）不破坏消费端（Daily Brief / research 读取均通过测试）。

## Semantic Ownership Drift Pass

- 无所有权漂移：生产门槛统一留在 `_candidate_pair_reject_reasons` / domain funding decision，不新增平行逻辑；
- Shadow/research 字段保留有确认文档 non-goal 支撑，不是漂移。

## Residual Risks

- shadow `max_call_cost_to_put_credit` 语义清理：assigned to 后续 work unit（owner：research/shadow 维护者）；
- `min_net_credit_retention` 与 `min_net_credit_annualized` 并存语义（一个比例、一个年化）已在确认文档 §4 保留，S3 排序时复核。

## Conclusion

**pass**。S2 实现与确认文档一致；唯一 finding 为 deferred 的 research 层字段语义，无生产影响。
