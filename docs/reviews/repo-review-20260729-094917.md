# 产品域 DeepReview 复审：研究与复盘

## Findings

未发现实质性问题。

本复审逐项回查了 `repo-review-20260729-080930.md` 的 22 个 finding。所有原 finding 均已在对应语义 owner 处修复，并有负向回归测试或完整仓库测试覆盖；未把测试样本放宽、兼容 fallback 或下游二次修正当作修复。

## Scope

- **模式**: All Repository Mode follow-up，按 `docs/PRODUCT_ARCHITECTURE.md` 收窄到产品域 5“研究与复盘”
- **原始报告**: `docs/reviews/repo-review-20260729-080930.md`
- **仓库根目录**: `/Volumes/Workspace/workspace/options-monitor`
- **复审时间**: `2026-07-29 09:49:17 +0800`
- **包含范围**:
  - `src/application/research/`
  - `src/application/shadow_replay/`
  - `src/application/strategy_lab/`
  - `src/interfaces/cli/research.py`
  - archive cleanup 的受控 service cleanup CAS 边界
  - recorder service、CI/release checks、相关 tests 和 runbook
- **排除范围**:
  - 产品域 1-4 的完整业务正确性
  - 真实 SSH/rsync/prune、OpenD、systemd/launchd、通知、配置、broker、ledger、Feishu 或交易状态写入
  - 本工作区同时存在的产品域外配置/控制面改动

## Remediation Evidence

| # | 状态 | 修复归属与闭环 | 关键回归证据 |
|---:|---|---|---|
| 1 | 已修复 | Archive cleanup 严格校验 preview envelope/path，确认绑定 plan SHA-256；service cleanup 在 apply 前重算 CAS；未纳入同等级证据的 runtime logs 从删除路径移除 | `test_archive_prune_remote_rejects_malformed_cleanup_preview`; `test_archive_prune_remote_runs_confirm_after_guard_passes`; service cleanup CAS tests |
| 2 | 已修复 | Inventory 记录逐文件 hash、size、content digest、source host/target/runtime root；prune 前同时重读本地副本和当前远端内容 | `test_archive_verify_writes_latest_inventory`; `test_archive_prune_remote_rechecks_current_remote_content`; `test_archive_prune_remote_rechecks_current_local_copy` |
| 3 | 已修复 | `decision_instance_id` 绑定 run/account/stage/status/source occurrence；Combo 另用 run/account-scoped group occurrence；legacy evidence 仅在唯一匹配时绑定 | `test_shadow_replay_decision_evidence_is_account_scoped_and_terminal_is_monotonic`; `test_shadow_replay_group_occurrence_ignores_leg_status_but_scopes_account`; Strategy Lab decision-instance tests |
| 4 | 已修复 | Candidate variant 复用 owning eligibility path并保留现金、Covered Call 持股覆盖、非目标硬门槛和生产 Top-N 排序截断 | `test_candidate_impact_preserves_cash_coverage_and_rank_truncation_gates` |
| 5 | 已修复 | 只有 collection-time、当前 OpenD 成功采集且实际命中允许文件的 mark 才是 `verified_fresh_collection`；operator as-of/local cache 均不可结算 | `test_shadow_replay_marks_fail_closed_without_verified_collection_receipt`; `test_operator_asserted_historical_mark_cannot_settle_an_outcome` |
| 6 | 已修复 | OpenD collection 只把本次成功请求的 CSV 加入允许集合；失败符号不回退旧 cache，缺失行明确标成 `missing_quote` | `test_shadow_replay_collect_marks_does_not_reuse_stale_cache_after_partial_opend_failure` |
| 7 | 已修复 | Outcome 使用 revision/supersedes 单调演进；provisional mark-to-market 可被到期/强终态升级，终态不可倒退 | `test_shadow_replay_decision_evidence_is_account_scoped_and_terminal_is_monotonic` |
| 8 | 已修复 | `collect-marks --settle` 不再 blanket replace；candidate/Close outcome 采用证据等级单调 merge，既有可靠 lifecycle outcome 保留 | `test_later_settlement_without_lifecycle_does_not_downgrade_usable_terminal`; collect/data-plan settlement tests |
| 9 | 已修复 | Build 对既有 dataset 默认 fail closed；新 dataset 在 staging 中完整生成并原子发布，不再 truncate 长期证据 | `test_shadow_replay_build_refuses_existing_dataset_without_erasing_evidence`; archive/update preservation tests |
| 10 | 已修复 | JSONL parser 对坏行/非 object fail closed；写入原子化并有 dataset shared/exclusive lock；manifest 记录 generation/revision/hash/bytes/rows/schema，consumer 前后校验 | `test_shadow_replay_integrity_and_output_paths_fail_closed`; dependency/full-suite coverage |
| 11 | 已修复 | Candidate 与 Close 作为独立 facet 生成 data-plan action；相同动作可合并但不互相掩盖 | `test_shadow_replay_data_plan_tracks_close_facet_independently`; Close readiness/data-plan tests |
| 12 | 已修复 | Strategy readiness 只统计能绑定目标 decision/family 的 marks/outcomes；unmatched evidence 不再提升 data mode | `test_strategy_lab_readiness_excludes_unrelated_marks_and_outcomes` |
| 13 | 已修复 | Production eligibility 逐 variant 生成 receipt；family scorecard 和 proposal 对实际 best 独立验证 ready set、family、single-production-parameter 和 complete-closed gate | `test_strategy_lab_proposal_cannot_borrow_another_variants_gate` |
| 14 | 已修复 | Capture 冻结 decision-time parameter snapshot + digest；hypothesis 只接受单一权威 generation，缺失或 mixed generation 均阻断 | `test_strategy_lab_hypotheses_reject_mixed_parameter_snapshot_generations`; baseline snapshot tests |
| 15 | 已修复 | Dominance、best variant 和 advisory 按 strategy family 独立计算；跨 family 同时占优不会互相取消 | family scorecard/dominance tests in `test_strategy_lab.py` |
| 16 | 已修复 | Combo leg/group outcome 要求同一 group occurrence、complete-closed lifecycle、完整费用与可评估 PnL；identity-ready 与 outcome-ready 分离 | `test_combo_yield_outcomes_require_complete_fees_and_matching_occurrence`; Combo group evaluator tests |
| 17 | 已修复 | Research bundle 分开 producer 与 collector provenance；历史 commit/config 不匹配或缺失时禁用当前 configured ranker 归因 | `test_research_historical_attribution_disables_current_ranker_on_mismatch` |
| 18 | 已修复 | 统一结构化与自由文本 redaction，覆盖 private/signing key、cookie/session、credential、Authorization Basic/Bearer、query signature、JWT、PEM 和 webhook | `test_research_redaction_covers_private_keys_cookies_and_free_text_credentials` |
| 19 | 已修复 | 统一 artifact resolver，把输出限定在 managed research root 或系统临时目录；拒绝 config/state/ledger/SQLite/service unit 与 symlink escape；写入原子化 | `test_shadow_replay_integrity_and_output_paths_fail_closed`; research output-boundary tests |
| 20 | 已修复 | Data-plan 将 exception、partial fetch 和 operation-reported failure 汇总为 failed/partial_failed；CLI envelope `ok=false`，进程返回非零且保留 receipt | `test_research_data_plan_action_errors_produce_failed_envelope`; CLI/service tests |
| 21 | 已修复 | Experiment/proposal 具有 schema、artifact id、content hash、source generation；inline/不可信输入降级 display-only；任何可能产生 rollout patch 的 experiment 都基于冻结 dataset generation 只读重算语义，LLM context 也重算 linked proposal | `test_strategy_lab_inline_experiment_is_display_only`; `test_strategy_lab_self_attested_forged_file_fails_semantic_recompute`; `test_strategy_lab_llm_context_recomputes_linked_proposal` |
| 22 | 已修复 | PR Guardrails、Reusable Release、local preflight 与 verification matrix 均纳入五个研究域核心套件 | `test_required_pr_and_release_workflows_run_control_plane_suites`; release test-plan tests |

## Verification

- 产品域核心套件：

  ```text
  151 passed in 2.05s
  ```

- 相邻域扩展覆盖：
  - Combo Yield research
  - Close Advice shadow capture/outcomes/readiness
  - research CLI
  - service deploy
  - Agent plugin contract/smoke
  - release test plan

- 全仓：

  ```text
  3624 passed, 10 skipped, 6 warnings in 62.66s
  ```

- 静态与生成物：
  - `python3.12 -m py_compile`：通过
  - `python3.12 -m ruff check ...`：通过
  - `python3.12 scripts/generate_dependency_graph.py --check`：通过
  - `git diff --check`：通过

## Open Questions

无。

## Residual Risk

- 未执行真实 SSH/rsync/prune、OpenD、systemd/launchd 或任何生产写入；本轮使用 fake command、临时目录和本地 deterministic tests 验证边界。
- Runtime logs 尚无与 output runs 等价的 source-bound content manifest 和 apply-time CAS，因此 remote archive prune 现在明确不删除 logs，并在结果中报告该限制。
- 当前工作区同时存在产品域外的配置/控制面改动；本复审未把它们纳入研究域修复结论，也未重置或覆盖。
