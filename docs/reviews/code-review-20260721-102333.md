# Aggregate Deep Review: Daily Brief Close-Position Details

## Findings

### DR-001-未修复-中-分支修改后依赖图生成物未同步导致全量质量门失败
- **位置**: `docs/DEPENDENCY_GRAPH.md`; full-suite gate `tests/test_dependency_graph_generator.py::test_dependency_graph_generator_check_passes`
- **入口**: `python3 -m pytest` and `python3 scripts/generate_dependency_graph.py --check`
- **触发输入**: the complete branch diff against `origin/main`
- **实际分支**: the generator scans the changed Python tree, computes 4,194 internal edges, and compares it with the committed document that still records 4,192.
- **预期行为**: generated dependency documentation matches the checked-in Python graph and the generator check passes.
- **实际行为**: `[DEPENDENCY_GRAPH_ERROR] generated file is stale: docs/DEPENDENCY_GRAPH.md`.
- **直接证据**: full suite returned `2 failed, 2857 passed, 10 skipped`; one failure is the dependency graph check. Regenerating changes total internal edges `4192 -> 4194` and `tests -> application` edges `1572 -> 1574`; the two added test imports account for the delta. An isolated `origin/main` archive passes its generator check, proving the stale graph is introduced by this branch state rather than the base.
- **影响**: the branch cannot pass the repository's full quality gate or be considered draft-PR-ready.
- **修复方向**: run `python3 scripts/generate_dependency_graph.py`, include only the generated `docs/DEPENDENCY_GRAPH.md` delta, rerun `--check`, focused tests, and the full suite.
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中
- **Controller decision**: accepted

## Review Target and Scope

- Mode: Current Changes / aggregate branch review
- Selected base: `origin/main` at `502c0332`
- Head: `a3a76e70`
- Reviewed: all committed branch changes, plan/review artifacts, source, tests, docs, validation output, and project instructions.

## Intent and Real Path

The branch fixes a structured projection gap:

```text
close_advice.csv
-> daily_decision_brief_service._position_view
-> daily_decision_brief.v1 positions
-> daily_decision_brief_renderer._position_views
-> _position_close_details
-> compact Markdown
```

The main behavioral implementation remains correct:

- only existing close-advice facts are transported;
- close policy and action selection are unchanged;
- details require a usable status and explicit close action;
- partial/malformed metrics fail closed;
- signed P&L wording is correct;
- currencies are market-qualified;
- material diff and notification delivery state are unchanged;
- internal identifiers and arbitrary source columns remain excluded.

## Adversarial Failure Pass

Reviewed missing metrics, malformed numbers, unavailable quotes, stale values, hold actions, unknown actions, negative P&L, US/HK currency, old persisted briefs, long-call sell actions, message bounds, internal data leakage, action identity, materiality, and production side effects. No additional material finding was identified.

## Project Instruction Check

Pass except DR-001. The branch preserves dirty worktree isolation, does not mutate config/runtime/Feishu/ledger/broker state, updates public docs, and uses the owning service/renderer boundaries.

## Overcoupling and Semantic Ownership

- Overcoupling: pass. No renderer dependency on close-advice internals and no duplicate financial calculation.
- Semantic ownership drift: pass. Service transports allowlisted values; renderer only formats them.
- Overengineering: pass. No new entity, schema, config, dependency, or workflow.

## Validation Evidence

- Focused compile: pass.
- Focused tests: `68 passed`.
- Renderer/service tests: `37 passed`.
- Daily Brief suite excluding one expired baseline fixture: `107 passed, 1 deselected`.
- Ruff on changed Python files: pass.
- Full suite with a local interpreter shim: `2 failed, 2857 passed, 10 skipped`.
  - DR-001 dependency graph stale: accepted and must be fixed.
  - Existing date-sensitive fixture: on July 21, 2026, a brief valid only until July 20, 2026 correctly returns `planning_only`, while the test expects `live_actionable`; assigned to separate maintenance.

## Open Questions

无。

## Residual Risks

- Dependency graph mismatch: fixed in current aggregate review loop after accepted finding remediation.
- Expired date fixture: assigned to separate maintenance work unit.
- Production notification canary: assigned to later operator-authorized release/deployment work.

## Conclusion

`fail` until DR-001 is fixed and re-reviewed.

## Artifact

`docs/reviews/code-review-20260721-102333.md`
