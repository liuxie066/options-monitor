# Code Review

## Scope

- Mode: PR review（严格只读；未评论 PR、未改代码、未 stage/commit）
- Branch or PR: PR #157 `feat(fees): add fail-closed HK terminal fee contract` — https://github.com/liuxie066/options-monitor/pull/157
- Base: `main` @ `8528de6b59f89b815c9b481a69bfa6055333b93a`（与 plan Base 一致）；Head: `feat/sell-put-top1-hk-terminal-fee-contract` @ `c0f6e1dd3ed0b8a5b493ce99ef20df15815919fd`（与预期 OID 一致）；Draft: true；State: OPEN（`gh pr view 157` 实测）
- Output file: `docs/reviews/code-review-20260815-040649.md`
- Included scope: PR 完整 diff（merge-base `8528de6b...c0f6e1dd`，20 files, +1926/-80）；PR body 与代码/测试/边界一致性；3 个 commits（`8b879390` plan → `075f3659` slice → `c0f6e1dd` aggregate review artifact）；superseded artifact `034739` 影响复核；CI checks
- Excluded scope: release/deploy；生产写入；W0R runtime 项；全仓套件重跑（采信 implementation artifact + CI）
- Parallel review coverage: 无；主 reviewer 先以 `gh pr view/checks` + committed refs 核验元数据，再对 PR head tree 走读

## PR 元数据与 diff 一致性核验

- `gh pr view 157` 实测: baseRefName=main、headRefName=feat/sell-put-top1-hk-terminal-fee-contract、headRefOid=c0f6e1dd3ed0b8a5b493ce99ef20df15815919fd、isDraft=true、state=OPEN，全部与任务预期一致。
- 本地 `git fetch origin main pull/157/head` 后确认 `origin/main` 仍为 `8528de6b`（plan Base），无 base drift。
- PR 生产代码与测试 diff（`8b879390..c0f6e1dd` 的 `domain/` 与 `tests/`）与 aggregate review（`code-review-20260815-035941.md`）已走读的 `8b879390..075f3659` diff **逐字节一致**（`diff` 命令验证两路径输出完全相同）；`c0f6e1dd` 仅新增 aggregate artifact 本身。因此 aggregate review 的代码结论原样适用于 PR head。
- Committed tree 符号抽查（`git show c0f6e1dd:<file>`）: `FUTU_HK_TERMINAL_FEE_SCHEDULE_VERSION = "futu_hk_terminal_fee.v1"`（fee_calc.py:24）、`_finite_float` 拒 bool/str（fee_calc.py:295-297）、`calc_futu_hk_terminal_fee`（fee_calc.py:305）、`amount = round(sum(effective_components.values()), 6)`（fee_calc.py:424）、`_lifecycle_efficiency_summary`（assigned_stock.py:796）、行级 `fees_complete` 闸门（assigned_stock.py:1346）。
- 未跟踪临时文件检查: PR tree 无 `.venv`（`git ls-tree c0f6e1dd -- .venv` 为空）；本机 worktree 当前无 `.venv` symlink 与其他临时文件，`git status --short` 为空。

## PR body 与代码/测试一致性

- "versioned pure calculator for HK assignment, exercise, and expired-worthless" ↔ `calc_futu_hk_terminal_fee` 三 kind + 版本常量，属实。
- "reuse the existing HK stock fee arithmetic" ↔ `_standard_fixed_hk_stock_fee_components` 被 `calc_futu_hk_stock_fee` 与 terminal calculator 复用，属实。
- "actual broker fee evidence authoritative" ↔ `_actual_option_fee_fact` / `_stock_fee_fact` 先查 `fee_provenance` 与 `extract_actual_fees`，属实（测试 `test_hk_assignment_actual_fee_precedes_terminal_estimate`）。
- "fail closed when the account fee plan is unavailable" ↔ 两 consumer 不接受 payload 注入 plan，无 plan 时 `basis=missing` / `status=missing`、净额输出抑制，属实。
- "propagate missing net economics through lifecycle aggregates instead of treating them as zero" ↔ 行级 `fees_complete` 闸门 + `_lifecycle_efficiency_summary` None 传播，属实。
- Validation 数字: focused 45 passed 与相邻 329 passed 本轮在 PR head tree 复跑确认；full repository `4754 passed, 10 skipped, 1 sandbox-only failure（沙箱外 1 passed）` 与 implementation artifact 记录一致；Ruff 6 文件本轮复跑 `All checks passed!`。
- Boundaries 声明与 committed diff 一致: 无 provider/OpenD/config/service/notification/trade/ledger/release/deploy 改动（diff 文件清单仅 3 生产 + 3 测试 + docs/reviews/gateflow/dependency-graph）。

## Superseded artifact 影响复核

- `code-review-20260815-034739.md`（含不存在符号引用与 "1 skipped" 误报）仍在 PR diff 的 `docs/reviews/` 中，但已被 `code-review-20260815-035048.md` 头部显式标注 superseded；后续 `035941`（aggregate）与本 artifact 均未复用其引用。
- 结论: 034739 作为历史 review-loop 记录保留不影响当前结论；其错误引用未传播进任何仍有效的 artifact。若 controller 希望保持 review 目录只含有效结论，可在后续单独的 docs 清理中删除 034739（非本 PR 阻塞项）。

## CI checks（`gh pr checks 157` 实测）

- Analyze (actions) pass、Analyze (python) pass、CodeQL pass、agent-plugin pass、guardrails pass — 5/5 全绿。

## Findings

未发现实质性问题。

## Open Questions

- 无。

## Residual Risk

- **Runtime / W0R（PR body 明示 deferred）**: 真实 `lx` fee-plan receipt 与 validated intake 仍阻断 provider-dependent research 与试点；terminal calculator 在 receipt 接入前对生产路径 fail-closed，属合同设计行为。
- **Suite / CI**: full-suite 4754 passed / 10 skipped / 1 sandbox-only failure 证据来自 implementation artifact（本 PR 轮未重跑全仓）；focused 45 与 Ruff 已在 PR head tree 复跑；GitHub Actions 5 项 checks 全绿。
- **Docs hygiene（非阻塞）**: superseded 的 `034739` 仍留在 PR 的 review 目录中，虽有显式 supersede 标注，但存在被误读为有效结论的残余风险。
- **Fee schedule drift**: Futu HK 费率变更需新 schedule version + 新证据，不得原地改 v1（plan residual 明示）。
