# Code Review

## Scope

- Mode: PR
- Branch or PR: #161 `feat: add Sell Put Top1 experiment lifecycle store` (`feat/sell-put-top1-w3`), author liuxie066, state OPEN
- Base: `origin/main@d905fb9b16c4b1fe6f1dc8a2311d8fe8fcaab385`
- Head: `10474af1ec9e6126bb5f62487b6c652d254fb354`
- Output file: `docs/reviews/pr-161-review-20260815-131215.md`
- Review worktree: `/private/tmp/options-monitor-sell-put-top1-w3-20260815` (clean, `## feat/sell-put-top1-w3...origin/feat/sell-put-top1-w3`)
- Included scope: full PR diff base...head (21 files, 5137 insertions / 21 deletions), Gateflow plan/implementation/review docs, all implementation/test files, CI checks, and the #160 -> #161 replay equivalence claims
- Excluded scope: files outside the PR diff (only touched via reused contracts read for context)
- Parallel review coverage: 无；the PR was reviewed as one end-to-end range by the main reviewer

## PR #160 -> #161 replay equivalence verification

The user-supplied equivalence facts were re-verified live in this review:

- `git rev-parse baa3e363^{tree}` == `git rev-parse d905fb9b^{tree}` == `105af850a97cd2e4ed467e7d219a0d137faca7df`
- `git rev-parse ce8e645f^{tree}` == `git rev-parse 10474af1^{tree}` == `56c119f4063bc3d5eefa348b5e7099ee287cd38b`
- `git range-diff baa3e363..ce8e645f d905fb9b..10474af1` reports all four commits as `=` (plan, implementation, aggregate review, readiness)
- `git diff --stat d905fb9b...10474af1` matches the old range's file list and counts exactly (21 files)
- PR metadata confirms `baseRefOid=d905fb9b...`, `headRefOid=10474af1...`, matching the local branch head; no push drift detected (`HEAD == origin/feat/sell-put-top1-w3`)

Conclusion: the replay is byte-identical at the tree level; the #160 review evidence carries over, and this review re-examined the actual new-range diff rather than relying on that equivalence.

## Findings

未发现实质性问题。

## Open Questions

- 无。

## Residual Risk

- 全仓测试套件未在本次 PR 级 review 中重新运行（本 PR review 运行了 focused 214 项 + 邻接回归全量组合，全部通过；4881 项全仓结果来自 aggregate review 记录，PR CI 未含全仓 pytest job）。W3 模块不接入生产 tick/provider 路径，残留风险低。
- W3 故意不实现 normal completion（`complete_experiment` 留给 W5/W6）；day-20 后实验停留在 `awaiting_outcomes`，这是计划内的 deferred ownership，不是缺陷。
- 多主机/多进程并发写不在 W3 防护范围内（单 SQLite 写者 + `BEGIN IMMEDIATE` + busy_timeout 已覆盖单主机竞争）；若未来多主机写需单独设计。
- sandbox 内 `gh` 初始网络失败，已通过授权的外部网络调用获取 PR 元数据与 CI；`git fetch` 在 worktree 内写 FETCH_HEAD 被 sandbox 拒绝，但 `origin/feat/sell-put-top1-w3` 已存在且与 HEAD 一致，不影响 push drift 结论。

## Evidence notes

主要审查路径与结论要点（均来自直接代码走读，非推测）：

- SQLite 事务与幂等：所有写经 `_write()`（`BEGIN IMMEDIATE` + rollback on error），`_claim_event()` 同时强制 `(event_type, subject_key)` 自然事实唯一与 `(command_scope, idempotency_key)` 调用幂等，重放字节不同即 `idempotency_conflict`；alias event 机制保证不同 caller key 不会重复同一自然事实。测试覆盖了并发双写同点（ThreadPoolExecutor）与跨 key 冲突。
- 崩溃恢复：terminal request 先入 SQLite，再由 `recover_terminal_projection()` 按事件顺序 publish 并 `mark_projection_published()` CAS；注入 "publish 后崩溃" 的测试证明重放不产生重复 request/published event 且字节逐字恢复。`publish_exact_text` 走同目录 tmp 文件 + fsync + `os.link` 原子发布 + 父目录 fsync，拒绝 symlink 与字节冲突。
- 隐藏验证隔离：`read_public_status()` 只返回 phase/progress/authorization/hash 级别字段，不含 point ID、rank、daily delta、outcome；20 个 commitment date 行在验证开始的同一事务插入且永不删除，abort 保留全部日期占用。日期唯一性由 `strategy_lab_hidden_date_unique` 强制，active slot 由 partial unique index 强制。
- 账户/槽位隔离：slot index 与 date unique 均按 `(market, account, strategy_family)` 作用域；`_identity()` 强制 `market='HK'` 且 account 小写。
- 默认关闭与授权边界：maintainer env 非 `"1"` 或账户行缺失即 effective=false；enable 在 maintainer off 时不写任何行；disable 先落 `user_opt_in=0` 再 reconcile 终止；`_require_effective` 在所有正常写入口前执行，并先 reconcile 再 fail-closed。
- 模块边界：architecture import guard 测试证明 store 只依赖 stdlib/sqlite3/private_storage，production tick 模块不 import lifecycle/store，domain 未被反向依赖；`docs/DEPENDENCY_GRAPH.md` 重新生成且 check 通过（588 production modules, 0 cycles）。
- 过度设计检查：5 张当前态表 + 1 张 append-only event 表，无 ORM/queue/未来空表；错误码收敛到 plan §11 的封闭集合（store 侧的 `experiment_not_found`/`natural_fact_conflict`/`constraint_conflict` 等内部码在 `_raise_store` 统一映射到公开契约，未泄漏 SQL 文本）。
- `shadow_replay/common.py` 的改动是纯提取：`render_json_text()` 从既有 `write_json()` 函数体原样拆出，字节行为不变（既有排序/ensure_ascii/allow_nan 参数一致）。
- 等价性: 与 #160 被审查过的树逐字节相同（见上节），range-diff 四提交均为 `=`。
