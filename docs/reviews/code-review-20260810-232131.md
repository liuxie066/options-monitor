# Code Review

## Scope

- Mode: current changes（Gateflow S1 code review）
- Branch: `design/combo-yield-remove-staggered`
- Base: `5461d566`（accepted plan commit）
- Output file: `docs/reviews/code-review-20260810-232131.md`
- Included scope: S1 allowed files——`domain/domain/engine/yield_enhancement.py`、
  `src/application/sell_put_call_helper.py`、`tests/test_sell_put_linked_call_helper.py`；
  `domain/domain/engine/__init__.py` 已核对无需改动（无 staggered 导出、三个导出名不变）；
  S1 implementation artifact `docs/gateflow/combo-yield-remove-staggered/implementation-s1.md`
- Excluded scope: position advice 大量 dirty worktree 改动（与 S1 无关，物理重叠文件
  `config_validator.py` 等一律未触碰）；S2/S3 范围文件（`yield_enhancement_config.py`、
  `required_data_planning.py`、`required_data_prefetch_planning.py`、render/research/文档）
- Parallel review coverage: 无；单 reviewer 全量走读

## Findings

### 1-已修复-中-S1 阶段 staggered 配置仍会把行标记为 staggered，但内容已是同期语义
- **入口/函数**: `find_sell_put_yield_enhancement_pairs()` →
  `_build_yield_enhancement_pair_rows()` → `_build_pair_row()`
- **文件(行号)**: `src/application/sell_put_call_helper.py:257`（`structure_mode` 读取）、
  `src/application/sell_put_call_helper.py:277`（行字段写入）；`src/application/yield_enhancement_config.py:15`
  （S1 阶段 `YIELD_ENHANCEMENT_STRUCTURE_MODES` 仍含 `staggered_expiry_pair`）
- **输入场景**: S1 完成、S2（validator 收紧）尚未合入的窗口内，用户配置
  `yield_enhancement_cfg={"enabled": True, "structure_mode": "staggered_expiry_pair"}`（该配置在 S1 阶段
  仍能通过 `derive_yield_enhancement_policy`，证据：`yield_enhancement_config.py:15,64-65,196-218`）
- **实际分支**: 主函数已删除 staggered 解析块，`call_window = put_window`，join/metrics 全部走同期路径；
  但 `_build_pair_row` 仍从 `enhancement_cfg` 读取 `structure_mode` 并原样写入行字段
  （`sell_put_call_helper.py:257,277`），行被标记为 `"staggered_expiry_pair"`
- **预期行为**: 按 plan §6 "保留 `structure_mode` 读取与 row 字段透传（值恒为 `same_expiry_pair`）"，
  行字段语义应与实际内容一致（同期）
- **实际行为**: 行字段为 `staggered_expiry_pair`，但配对 join、call 筛选（strike>=spot 例外已删）、
  指标、funding 决策全部是同期语义；行字段与内容不一致，下游若按 `structure_mode` 分发展示
  （S3 清理前的 render/research）会得到错误语义
- **直接证据**: `sell_put_call_helper.py:257` 仍执行
  `structure_mode = str(enhancement_cfg.get("structure_mode") or "same_expiry_pair")`；
  `sell_put_call_helper.py:277` `"structure_mode": structure_mode`；
  主函数中 staggered 解析与 `resolve_staggered_expiry_gap_days` 调用已删除（diff 与当前文件均确认）；
  `yield_enhancement_config.py:15` S1 未动，staggered 仍为合法配置
- **影响**: 仅中间态行字段标签错误；不影响同期排序/指标正确性。S1 不单独发布、
  S2 收紧 validator 后窗口关闭，实际生产风险低，但 slice 边界语义漂移真实存在
- **建议改法和验证点**: 方案 A（防御，推荐）：`_build_pair_row` 不再读 cfg，
  直接 `structure_mode = "same_expiry_pair"`（row 字段契约不变，下游同值）；方案 B：
  接受窗口，S1 commit 与 slice artifact 明确"不得在 S2 合入前单独部署"。验证点：S2 合入后
  `config_validate` 对 staggered/gap 配置 fail closed；S3 后全仓无 staggered 生产引用
- **修复风险（低/中/高）**: 低（方案 A 是两行等价替换；方案 B 零代码改动）
- **严重程度（低/中/高/严重）**: 中

## Fix Record

- Finding 1 按方案 A 修复：`_build_pair_row` 中 `structure_mode` 固定为
  `"same_expiry_pair"`，不再读取 `enhancement_cfg`（`src/application/sell_put_call_helper.py:257`）。
- Re-review 结论：**pass**。修复后：
  - `pytest tests/test_sell_put_linked_call_helper.py -q` → `21 passed`；
  - `ruff check` 两个修改文件 → `All checks passed!`；
  - `rg "is_staggered|staggered_expiry_pair" src/application/sell_put_call_helper.py` 零命中，
    行字段无法再被配置成 staggered 标签。
  - Finding 1 的触发条件已消除：即使 S2 前的配置仍含 `structure_mode: staggered_expiry_pair`，
    扫描输出行恒为 `same_expiry_pair`，与同期 join/metrics 语义一致。

## Open Questions

- 无。

## Residual Risk

- S1 后 `resolve_staggered_expiry_gap_days` 仅剩 `yield_enhancement_config.py` 定义与
  `required_data_prefetch_planning.py:36,700` 生产调用（S2 allowed files 清理，计划内）。
- `ComboYieldResearchPolicy` 的 staggered/gap 支持（`yield_enhancement.py:401-537`）保留至 S3，计划内。
- S1 未新增"staggered 配置残留时不产出错期行"的回归测试；S2 validator 收紧后应由
  `test_sell_put_yield_enhancement_validate_config.py` 覆盖。
- 测试环境说明：`.venv` 未安装 pytest，验证使用 pyenv 3.12.13 `python -m pytest`（21 passed），
  与计划命令语义等价。
