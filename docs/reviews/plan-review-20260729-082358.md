# Plan Review

## Scope

- Mode: local implementation plan re-review
- Plan: `docs/plans/combo-yield-unified-selection-plan-20260729-080931.md`
- Prior review: `docs/reviews/plan-review-20260729-080931.md`
- Review focus: first-review finding closure, production/Research authority separation,
  cross-expiry data acquisition, counterfactual lifecycle evidence, ranking provenance,
  compatibility semantics, and promotion boundaries
- Repository state: plan and review artifacts are untracked; unrelated review artifacts were
  excluded

## Conclusion

- Result: `pass`
- Reason: PR-01 through PR-05 are closed by the normative Section 16 addendum. The repaired plan now
  supplies an isolated prospective capture path, a research-only pair/outcome model, exact rank
  provenance, deterministic dual-gate compatibility, and an explicit human promotion boundary.
  No unresolved high/medium issue makes S0-S3 unsafe or non-deterministic.
- Authorization boundary: this pass makes S0-S3 implementation-ready; it does not authorize S4,
  production ranking changes, config writes, notifications, ledger writes, release, or deployment.

## Prior Finding Closure

| Prior ID | Status | Closure evidence |
|---|---|---|
| PR-01 | fixed | Section 16.2 adds a dry-run-first Shadow Replay capture action, unions production and variant expirations, hashes exact captured bytes, and fails incomplete variants closed. |
| PR-02 | fixed | Section 16.3 adds research-only pair decisions, mark paths, outcomes, and `shadow_combo_pair_id`; it no longer requires canonical traded-group identity for counterfactual candidates. |
| PR-03 | fixed | Section 16.1 keeps the current production delegates unchanged in S0-S3 and defers removal of `premium_funding_score` from formal ranking to separately authorized S4. |
| PR-04 | fixed | Section 16.4 defines the Combo capture manifest as rank provenance authority and persists a typed rank key, scope hash, and source candidate count. |
| PR-05 | fixed | Section 16.5 makes retention and Call-cost ratio independent explicit hard gates, with both required when authored and deterministic equality behavior. |

## Assumption Audit

| Assumption | Review result |
|---|---|
| Historical production artifacts contain every cross-expiry contract needed by new variants. | Rejected. Section 16.2 requires prospective supplemental capture and marks missing history unavailable. |
| The current production rank can be reused to evaluate proposed variants without affecting live output. | Rejected. Section 16.1 defines separate immutable-policy research authorities. |
| Production filters can safely pre-filter a research universe. | Rejected. Section 16.2 requires a least-restrictive structural superset and per-variant gates. |
| Existing OpenD rate-limit settings are a total research-request cap. | Rejected. The plan uses `build_prefetch_budget_plan()` for estimation/waves and adds an explicit research-only total cap. |
| File identity proves executable quote simultaneity. | Rejected. Section 16.3 requires timestamped Put/Call/spot age and skew gates. |
| Counterfactual marks prove American-option early assignment. | Rejected. The plan labels expiry assignment as a model and emits an unweighted early-assignment stress envelope. |
| Same-expiry evidence is sufficient to promote staggered expiry. | Rejected. Section 16.6 makes promotion decisions independent and adds staggered lifecycle/exposure prerequisites. |

## Cross-Module Contract Review

- Candidate authority: the plan names the current domain delegates
  `rank_yield_enhancement_rows()`, `rank_yield_enhancement_calls_for_put()`, and
  `select_best_yield_enhancement_per_symbol()` correctly and keeps them untouched through S3.
- Required data: the research capture reuses the current planner's call estimates and waves while
  adding a separate authored total cap; production tick planning remains isolated.
- Research identity: Shadow pair IDs are explicitly distinct from `strategy_group_id`,
  `pair_intent_id`, and ledger identities.
- Performance semantics: research capital-days reuse the canonical Put-notional, Call-premium, and
  interval-boundary conventions while persisting component breakdowns.
- External surfaces: S0-S3 cannot write config, ledger, positions, production outputs, or send
  notifications; S4 requires a new decision record, planreview, and explicit authority.

## Evidence Plan Review

The plan has the required evidence layers:

1. schema and CLI safety tests before data writes;
2. immutable capture/hash/completeness tests, including disjoint expiry windows and stricter
   production filters;
3. baseline/proposed rank tests on the same captured universe;
4. quote age/skew and missing-data fail-closed tests;
5. same-expiry outcomes before the separate staggered/assignment sub-slice;
6. identical-complete-instance comparison with explicit coverage loss;
7. structure-specific promotion scorecards and a human decision record.

This ordering can falsify the data and ranking assumptions before production promotion work starts.

## Findings

无。

## Residual Risk

- Delta, retention, and expiry-gap values remain hypotheses; a planreview pass is not empirical
  validation.
- Prospective collection takes calendar time, and OpenD cannot reconstruct missing historical mark
  paths.
- The early-assignment envelope is a stress scenario, not an estimated probability distribution.
- Staggered expiry remains non-promotable until portfolio/symbol exposure controls and residual
  lifecycle policy receive separate review.
- S4 may still reject every proposed variant without changing production.
