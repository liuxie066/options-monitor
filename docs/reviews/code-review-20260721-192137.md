# Code Review

## Scope

- Mode: current changes re-review
- Branch or PR: `feature/option-notification-experience`
- Base: `HEAD` (`55551b10`), reviewing Slice 3 workspace changes and CR-S3-001..004 fixes
- Output file: `docs/reviews/code-review-20260721-192137.md`
- Included scope: success persistence date baseline; legacy key migration; candidate pending/envelope/retry state machine; exact payload/idempotency metadata; tests
- Excluded scope: accepted Slice 0-2 commits; Slice 4 transition/confirmation orchestration; unrelated untracked files; production state
- Parallel review coverage: 无

## Findings

未发现新的实质性问题。

## Re-review finding status

### CR-S3-001-已修复

Legacy full/delta keys now use strict version-stable prefix and SHA-256 component validation. The original delivery key is retained, while v2 `brief_digest` is recomputed from the immutable revision. A regression test proves a structurally valid historical full key need not match the current additive-schema semantic digest.

### CR-S3-002-已修复

Candidate comparison uses the previous successful candidate set only when its market trading date matches the new brief. The cross-day same-identity test now returns an empty previous baseline and reports the identity as newly detected.

### CR-S3-003-已修复

Candidate preparation is limited to current pending identities, and pending retry refuses an envelope whose identities were removed by a later successful snapshot. The exact ambiguous path remains unchanged and highest priority.

### CR-S3-004-已修复

Candidate replacement now requires a changed delivery key; same-key payload changes fail closed. New-key rotation uses fresh preparation/attempt metadata. Fixed failure -> report remains the only reviewed same-key content upgrade.

## Open Questions

- 无。

## Residual Risk

- `covered by later approved slice`: v2 provider attempt/ambiguous/confirmed transitions and their candidate/fixed side effects are Slice 4.
- `covered by later approved slice`: durable envelope -> scheduled target commit -> sender ordering is Slice 4.
- `requiring explicit user decision`: no production pointer was migrated and no real notification was sent.

## Conclusion

pass
