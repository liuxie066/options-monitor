# Code Review

## Scope

- Mode: PR re-review after CI fix
- Repository: `liuxie066/options-monitor`
- PR: `#81` — `Enforce Python 3.12 runtime contract`
- Base: `main@e128b412c3b1d1a9f8aeaa92bb9c9f37fe2d2f39`
- Reviewed head: `31973abeaf99de505a507fb60fff351696a8c375` plus local CI fix
- URL: `https://github.com/liuxie066/options-monitor/pull/81`
- Prior PR review: `docs/reviews/pr-81-review-20260718-194155.md`
- CI fix artifact: `docs/gateflow/python312-runtime-contract-pr-ci-fix-20260718-194949.md`
- Output file: `docs/reviews/pr-81-review-20260718-194949.md`
- Included scope: selector semantic-path vs physical-path ownership, venv symlink execution, bootstrap containment, CI failure paths, regression tests, and complete local validation
- Excluded scope: historical artifacts retained as snapshots
- Parallel review coverage: none

## Finding Decisions and Final Status

| Finding | Decision | Final status | Re-review evidence |
|---|---|---|---|
| `CI-001` repo selector resolves `.venv/bin/python` to base interpreter and loses installed dependencies | accepted | 已修复 | semantic execution path and physical containment path are now separate; repo selector preserves `.venv/bin/python`; bootstrap still resolves physical aliases; focused and full suites pass |

## Findings

未发现新的实质性问题。

## Open Questions

无。

## Residual Risk

- Arbitrary direct execution of internal files with generic `python3` shebangs remains outside supported public entrypoints; classification: accepted contract boundary.
- Exact Python 3.12 patch versions remain intentionally unpinned; classification: accepted minimum-version policy.
- Historical design/review/memory snapshots retain older commands; classification: intentional evidence preservation.

## Validation Evidence

- Focused runtime-contract regression: `11 passed`.
- `./om-agent spec` passes through a real venv symlink entrypoint.
- Full pytest: `2680 passed, 10 skipped in 95.57s`.
- Bash parse, `ruff check .`, Python 3.12 compileall, dependency graph check (468 modules, zero cycles), and `git diff --check`: pass.
- The two failing GitHub Actions jobs are fully explained by `CI-001`; a new push is required to obtain authoritative remote pass evidence.

## Conclusion

**pass-with-risks**

The CI regression is fixed and re-reviewed without new findings. Push the accepted CI-fix commit and require all checks to pass before `draft-PR-pass`.
