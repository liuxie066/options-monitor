# Code Review

## Findings

未发现实质性问题。

上一轮 `code-review-20260811-125832.md` 的中等级别 finding 已修复并验证：

- `opening_candidate_snapshot._strategy_results()` 现在优先保留扫描层明确发布的 `partial_data`，不会再被“存在任意 input-unavailable decision”的宽条件抢先覆盖。
- 当上游错误报告 clean `no_candidate` 时，防御性兜底按同策略 decision 的证据覆盖计数区分：全部不可评估为 `data_unavailable`，部分不可评估且其余 decision 可用为 `partial_data`。
- 原确定性复现现在返回 `strategy_status=partial_data`；新增集成测试同时覆盖显式 `partial_data`、误报 `no_candidate`、以及全部 `input_invalid` 三条边界。

## Scope

- Mode: current changes
- Branch or PR: `main`，13 个 unstaged changed files；无 staged 或尚未合入 `main` 的 committed diff
- Base: `main@c187382e`
- Output file: `docs/reviews/code-review-20260811-131257.md`
- Included scope: Candidate Engine term-matched RV canonical status；required-data RV payload validation；short-history term-matched RV；candidate decision evidence aggregation；Sell Put / Covered Call scope 状态投影；opening candidate snapshot seal 与状态聚合；AI Decision Advice fail-closed 消费；Daily Brief 状态/文案消费；相关 tests 与生成依赖图。
- Changed production files: `domain/domain/engine/candidate_engine.py`, `src/application/candidate_scanning.py`, `src/application/opend_symbol_outputs.py`, `src/application/opening_candidate_snapshot.py`。
- Changed tests reviewed: `tests/conftest.py`, `tests/test_candidate_engine_phase2_contract.py`, `tests/test_fee_calc.py`, `tests/test_opening_candidate_snapshot.py`, `tests/test_required_data_output_integrity.py`, `tests/test_scan_volume_gate_min_zero.py`, `tests/test_short_vol_metrics.py`。
- Changed generated docs reviewed: `docs/DEPENDENCY_GRAPH.md`, `docs/dependency_graph.mmd`。
- Excluded scope: 历史 review artifact；live OpenD / 生产 replay；真实通知发送；远端运行环境；发布与升级；无关仓库模块。
- Parallel review coverage: 无。

## Verification

- Focused snapshot/status regression: `22 passed`。
- Full suite excluding only the sandbox-blocked loopback case: `4682 passed, 10 skipped, 1 deselected`。
- The deselected loopback HTTP case was rerun outside the restricted sandbox: `1 passed`。
- Ruff over every changed Python file: passed。
- Dependency graph freshness: passed; `production_modules=585`, `cycles=0`。
- `git diff --check`: passed。
- Original mixed-evidence reproduction now returns `strategy_status=partial_data` with zero candidates.

## Open Questions

- 无。

## Residual Risk

- 未运行 live OpenD 或生产数据 replay；SPCX 类短历史标的通过 deterministic fixture、required-data validator、Candidate Engine integration 和完整本地测试覆盖。该剩余验证缺口不阻碍当前源码变更，但生产首次运行仍应通过正常只读运行产物确认 `term_matched_rv_status=ok`、strategy status 与 Daily Brief 一致。
- 本轮未发送通知、未写生产状态、未执行发布或远端升级。
