# Aggregate Deepreview — Python 3.12 Runtime Contract

## Scope

- Mode: current branch aggregate changes
- Branch: `codex/python312-runtime-contract`
- Base: `origin/main@e128b412`
- Output file: `docs/reviews/code-review-20260718-192227.md`
- Included scope: all three Gateflow commits, public launch/install/upgrade paths, current operator/agent documentation, generated command surfaces, CI, and focused tests
- Excluded scope: historical design documents whose commands are preserved as implementation history; unrelated business/runtime modules
- Parallel review coverage: none; reviewed locally along clean-worktree launcher, install/bootstrap, service-upgrade, release-validation, and operator-guidance paths

## Findings

### AGG-001-未修复-高-当前操作文档仍引导 bare python3，完整复现本 work unit 要消除的失败
- **入口/函数**: developer/operator follows repository setup, deployment, release, or agent handbook commands
- **文件(行号)**: `AGENTS.md:38-39,145-157`; `docs/AGENT_GETTING_STARTED.md:20`; `docs/DEPLOY_LINUX_MAC.md:37`; `docs/RELEASE_PROCESS.md:20,49-53`; `docs/AGENT_WIKI.md:161,454-459`; current README direct-module commands; dependency graph generator/output
- **输入场景**: clean worktree or macOS shell has no repo `.venv` selected and bare `python3` resolves to Apple Python 3.9.
- **实际分支**: the operator copies a still-current documented `python3 -m venv`, `python3 -m pytest`, or `python3 scripts/...` command, bypassing the new launcher selector and directly invoking the unsupported interpreter.
- **预期行为**: current repository guidance should use `python3.12` only for venv bootstrap and `./.venv/bin/python` for repository scripts/tests after bootstrap.
- **实际行为**: multiple authoritative/current docs preserve the ambiguous interpreter command that caused the reported configuration-command failures.
- **直接证据**: repository-wide `rg` finds current commands in the files above; S1 only guards `om`/`om-agent` and package imports, so direct `python3 -m venv` can create an unsupported venv before any import guard runs.
- **影响**: repeated false configuration failures / creation of incompatible `.venv` / runtime contract contradicted by repository guidance.
- **建议改法和验证点**: update current operational docs and generated dependency-graph command text to `python3.12 -m venv` for bootstrap and `./.venv/bin/python` thereafter. Keep explicit historical/design documents out of scope. Add a static test or guard assertion covering current operational runtime guidance.
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 高

### AGG-002-未修复-中-部署和工具参考仍声称 upgrade 使用 uv --python python3
- **入口/函数**: operator diagnoses or configures auto-upgrade from deployment/tool/release docs
- **文件(行号)**: `docs/DEPLOY_LINUX_MAC.md:224`; `docs/TOOL_REFERENCE.md:301`; `docs/RELEASE_PROCESS.md:158`; `src/application/platform_profile.py:56,76`
- **输入场景**: operator compares observed upgrade command/status with documentation or prepares a host where `python3` is old.
- **实际分支**: documentation says `uv venv --python python3`, while implementation now passes `sys.executable`; platform setup hints still say generic `python3`/Homebrew Python without the 3.12 requirement.
- **预期行为**: docs and setup diagnostics must state that upgrade uses the already-running supported Python 3.12+ executable and prerequisites must name Python 3.12.
- **实际行为**: operational truth and documented command diverge, suggesting PATH lookup still controls upgrade runtime.
- **直接证据**: `src/application/service_upgrade.py:_ensure_release_runtime` sets `bootstrap_python = sys.executable` and passes it to both uv and venv creation; the cited docs/hints retain the old contract.
- **影响**: misleading remediation / operator may install or rely on the wrong interpreter / review cannot verify runtime behavior from docs.
- **建议改法和验证点**: update the three current upgrade docs and platform profile hints; extend platform-profile tests to assert Python 3.12 wording.
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

## Open Questions

None.

## Residual Risk

- Historical design documents may retain old commands as snapshots; they should not be presented as current runbooks.
- Direct arbitrary shebang execution remains an explicit non-goal, but current docs must not recommend it as the normal path.

## Conclusion

**fail**

Implementation paths are coherent, but the aggregate repository contract is not complete while current operator and upgrade guidance still advertises ambiguous or factually obsolete Python selection.
