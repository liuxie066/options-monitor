# Code Review

## Scope

- Mode: aggregate current-branch re-review
- Work unit: `retire-ai-decision-advice`
- Gate: aggregate DeepReview re-review
- Branch: `refactor/retire-ai-decision-advice`
- Base: `ded8f882242f5dc5eda9d564461e05a884ed64ba`
- Initial review: `docs/reviews/code-review-20260812-223939.md`
- Fix artifact: `docs/gateflow/retire-ai-decision-advice/aggregate-review-fix.md`
- Output file: `docs/reviews/code-review-20260812-224551.md`
- Included scope: both accepted slices plus aggregate notification-contract and operational-drift fixes
- Excluded scope: release; deployment; runtime config/service/secret/data or notification mutation
- Parallel review coverage: none; re-reviewed the full source-to-historical-compatibility and future-cutover boundary

## Findings

No actionable findings.

## Verification of accepted findings

### 1-已修复-中-共享通知格式断言仍把已退役 AI 建议标题列为合法输出

- `### AI建议` is absent from the shared current-output allowlist.
- The stale reference to the former design section is removed.
- An exact negative test proves the heading now fails the shared mobile Markdown contract.
- Current renderer and notification-format suites remain green.
- Final status: `已修复`.

### 2-已修复-低-生产清理文档声称旧 Collector 会成为 drift extra unit，但无精确回归锁定该名称对

- A read-only regression uses the exact retired `.service` and `.timer` names in both persisted profile and temporary
  installed-unit facts.
- Both names are returned as `extra_profile_units` and `extra_installed_units`.
- `confirmed=False`, `changed=False`, and empty operations prove the test does not apply drift; files remain present.
- Final status: `已修复`.

## Validation

```text
sandbox full suite excluding localhost-only quality HTTP file: 4441 passed, 10 skipped, 5 warnings
localhost-only quality HTTP test file: 4 passed
Slice 1 focused suite from committed HEAD: 189 passed
Slice 2 focused suite from committed HEAD: 450 passed, 1 warning
targeted aggregate fixes: 78 passed, 4 warnings; exact final regressions: 6 passed
ruff check .: passed
compileall: passed
dependency graph: current; production_modules=568; cycles=0
US/HK validate and build --dry-run: passed
commit and diff checks: passed
```

The warnings are existing legacy renderer deprecations. One unrelated lifecycle lease timing test failed once during an
intermediate aggregate run and passed immediately in isolation; the accepted final full run also passed it.

## Docs decision

The retirement record and live operational docs match current behavior. Historical release/review artifacts remain
unchanged as history. No new public documentation is needed for the two aggregate test-contract fixes.

## Open Questions

- 无。

## Residual Risk

- **requiring new issue or explicit user decision**: removing the retired config key from deployed configuration and
  reconciling installed Collector units are separately authorized production actions.
- **requiring new issue or explicit user decision**: external private consumers of deleted Advice-only modules cannot
  be discovered from this repository and have no compatibility shim by design.
- **fixed in current slice**: current source, public rendering, config/service generation, credential consumers, Tick
  plumbing, and repository tests no longer enable or permit AI Decision Advice.
- **fixed in current slice**: exact historical retry guard and exact future service drift evidence preserve safe
  transition boundaries.

## Conclusion

`pass`. The work unit is ready for its accepted aggregate DeepReview commit and main-chain propagation.
