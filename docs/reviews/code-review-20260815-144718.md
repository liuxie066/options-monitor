# Code Review

## Scope

- Mode: current changes re-review
- Branch or PR: `feat/sell-put-top1-w4`, final W4 S2 workspace changes
- Base: `ce5f0759` (`feat(strategy-lab): capture top1 corpus evidence`)
- Output file: `docs/reviews/code-review-20260815-144718.md`
- Included scope: compact status; exact caller-bound window facts; feature gate; latest-mature fixed 40-day selection; cutoff chronology; expectation/projection/hash/schema/parity validation; multiple daily points; conflict priority; source-independent reference dataset; generated dependency graph; and S1 regressions
- Excluded scope: outcome maturity calculation, provider verification, W5 evaluation, W6 hidden validation, W7 timer/CLI/runtime wiring, release, deploy, and live Corpus writes
- Parallel review coverage: none; primary reviewer retraced the final facts -> indexes -> artifacts -> rerank -> dataset path after both finding fixes

## Findings

No substantive issues found.

The initial cutoff-leakage and old-schema classification findings are closed. The final path refuses evidence at or after the cutoff, preserves current-schema artifact conflict detection, classifies clean incompatible schema as missing compatible coverage, scans the exact latest 40 entries only, and validates every expected point in sealed order before publication.

## Open Questions

- None.

## Residual Risk

- Calendar and maturity truth are supplied by a separate authoritative caller and only hash-bound here; provider validation remains W0R/W5 ownership.
- This slice exposes application functions only. W7 still owns production scheduling and source-retention timing, and W5 owns experiment creation/evaluation.
