# Code Review

## Scope

- Mode: aggregate deepreview（完整 work unit，严格只读；不复用已 superseded 的 `code-review-20260815-034739.md` 任何引用）
- Branch or PR: `feat/sell-put-top1-hk-terminal-fee-contract`，隔离 worktree `/private/tmp/options-monitor-hk-terminal-fee-contract-20260815`
- Base: accepted plan `8b879390`；review 精确 committed range `8b879390..075f3659`（`075f3659e96148baff1f542cd00e4342bcd7a306 feat(fees): accept HK terminal fee contract`），read 路径用 `git show 075f3659:<file>` 取 committed source
- Output file: `docs/reviews/code-review-20260815-035941.md`
- Included scope: 端到端 HK terminal fee 合同（`calc_futu_hk_terminal_fee`）、两个 consumers（`assigned_stock.py::_option_fee_fact` / `_stock_fee_fact`、`portfolio_assignment_scenario.py::_fee_fact`）、aggregate fail-closed（`_lifecycle_efficiency_summary`）、全部新增测试、preflight/Gateflow docs、dependency graph、range 内 review artifacts、过度设计；plan alignment against committed `plan.md`
- Excluded scope: 临时 `.venv` symlink（仅为本机跑 pytest 重建，未跟踪，不入 artifact/commit，由主控移除）；release/deploy；生产写入；真实 HK broker fee / OpenD 等 W0R runtime 项
- Parallel review coverage: 无；主 reviewer 沿真实代码路径逐行走读 committed source

## 验证事实（rg / git show / 实跑）

- 符号（committed source 实测）: `fee_calc.py:295 _finite_float`、`fee_calc.py:305 calc_futu_hk_terminal_fee`、`fee_calc.py:262 _standard_fixed_hk_stock_fee_components`、`assigned_stock.py:796 _lifecycle_efficiency_summary`、consumers `assigned_stock.py::_option_fee_fact`（HK expired-worthless 分支，line ~378）与 `_stock_fee_fact`（HK assignment 分支，line ~477）、`portfolio_assignment_scenario.py::_fee_fact` HK 分支
- 测试（rg 实测存在）: `test_hk_terminal_assignment_standard_fixed_plan_hand_computed`、`test_hk_terminal_exercise_adds_two_hkd_per_contract`、`test_hk_terminal_expired_worthless_is_zero`、`test_hk_terminal_assignment_commission_free_plan_zeroes_commission`、`test_hk_terminal_missing_plan_facts_fail_closed_but_keep_estimate`、`test_hk_terminal_rejects_lossy_or_nonfinite_economic_inputs`、`test_hk_terminal_rejects_invalid_plan_fact_types`、`test_hk_terminal_rejects_unknown_kind`（tests/test_fee_calc.py:312-466）；consumer 侧 `test_hk_assignment_stock_fee_fails_closed_and_suppresses_net_outputs`、`test_hk_assignment_projection_rejects_unvalidated_fee_plan_payload`、`test_hk_assignment_fee_rejects_lossy_raw_numeric_inputs`、`test_hk_assignment_actual_fee_precedes_terminal_estimate`、`test_hk_expired_worthless_option_leg_has_explicit_zero_fee_policy`（tests/test_assigned_stock_projection.py）；`test_hk_assignment_stock_fee_estimate_fails_closed_without_account_plan`、`test_hk_assignment_rejects_unvalidated_account_fee_plan_payload`（tests/test_portfolio_assignment_scenario.py）
- 本轮在 committed tree 实跑: focused 45 passed；plan Validation 段相邻 17 个文件 329 passed；`scripts/generate_dependency_graph.py --check` → `[OK] dependency graph current; production_modules=577 cycles=0`；Ruff 6 文件 `All checks passed!`；`git diff --check 8b879390..075f3659` 通过
- Full-suite（implementation artifact 记录，本轮未重跑）: sandbox 内 `4754 passed, 10 skipped, 1 failed`；该失败为 sandbox-only HTTP 限制，沙箱外重跑 `1 passed`

## Plan alignment（against committed `plan.md`）

- 公共合同逐条对齐: `FUTU_HK_TERMINAL_FEE_SCHEDULE_VERSION = "futu_hk_terminal_fee.v1"`（fee_calc.py:24）；三种 kind + 未知 kind `ValueError`；严格 `commission_free` bool / `platform_fee` finite 非负（`_finite_float` 拒 bool/str/NaN/Inf）/ 非空 `fee_plan_ref`；结果固定 14 键且测试 `_assert_terminal_result_contract` 固化 key 集与两条求和不变量；assignment exercise_fee=0、exercise HK$2/张、expired-worthless 完整零费且 reason `hk_expired_worthless_no_fee`；估计值只落在 `estimated_*` 命名
- Non-goals 对齐: 未新增 provider adapter / registry / repository / 配置键 / 状态机 / CLI / 持久化；`_standard_fixed_hk_stock_fee_components` 单一私有 helper 被 `calc_futu_hk_stock_fee` 与 terminal calculator 复用，无第二算术路径
- Consumer 合同对齐: actual fee precedence 保留（`_actual_option_fee_fact` 先于 terminal；`_stock_fee_fact` 先查 `fee_provenance` / `extract_actual_fees`）；HK assignment 传原始未 coercion 的 price/shares/contracts（由 `_positive_integral` / `_finite_float` 在边界拒绝 bool/分数/非有限）；两个 consumer 均不从 payload 接受 `account_fee_plan`，无 plan 时 `basis=missing` 且净额输出被抑制；scenario HK 分支 `status=missing`、`fee_native/fee_cny=None`、估计仅 `estimated_stock_fee_*`
- 文件边界对齐: 生产/测试/docs 均在 plan `Affected files` 内；`docs/DEPENDENCY_GRAPH.md` 经 `scope-amendment-dependency-graph.md` 显式授权且由 generator 产生（check 通过）；`docs/dependency_graph.mmd` 未变化（committed range stat 无此文件），与 amendment 允许但不强制一致
- 退出门对齐: plan gate 4 的 Kimi DeepReview 闭环证据在 range 内——`code-review-20260815-034135.md`（2 findings）→ `code-review-fix.md`（决策与修复）→ `code-review-20260815-035048.md`（fix re-review pass、0 unresolved，并标注 034739 superseded）；本 artifact 为 plan gate 5 的 aggregate review

## Findings

未发现实质性问题。

补充说明（非 finding，证据已在 walkthrough 记录）：

- DR-HKF-01 原报告（034135 finding 1）的 `amount=2e16` 不可复现；committed 分支 `amount = round(sum(effective_components.values()), 6)`（fee_calc.py:424）在 `contracts=10**16` 复跑下不变量成立，fix doc 判不成立与本轮复跑一致。
- Aggregate fail-closed 双闸门核对: 行级 `fees_complete = not fee_summary["fee_missing_components"]` 门控 `lifecycle_pnl_net` / `annualized_capital_efficiency`（assigned_stock.py:1346-1351）；聚合级任一成员净额 None 则 bucket 净额与年化效率同置 None（assigned_stock.py:796-833）。`_summarize_fee_facts` 只把 `basis in {actual, estimated}` 计入 `fees_used`，missing 项不会静默入合计（assigned_stock.py:539-563）。
- 过度设计检查: diff 1473 行中约 950 行为测试与 docs；生产增量为一个纯函数、一个复用 helper、两个 consumer 分支替换；无新抽象/配置/兼容 shim。

## Open Questions

- 无。

## Residual Risk

- **Runtime / W0R（plan 明确 deferred）**: 真实 `lx` fee-plan receipt、OpenD FSM、quota/calendar/K-line 缺口继续阻断 provider-dependent research 与试点；terminal calculator 在真实 receipt 接入前对生产路径始终 fail-closed（`hk_account_fee_plan_missing`），属合同设计行为而非缺陷。
- **Test gap（已知、可接受）**: `exercise` close_type 的端到端 event ingestion 属后续 lifecycle/provider work（plan residual 明示）；`expired_worthless` 分支按合同不校验 contracts 输入，已有测试固化该语义。
- **CI / suite**: full-suite 4754 passed / 10 skipped / 1 sandbox-only failure（沙箱外 1 passed）证据来自 implementation artifact，本 aggregate 轮未重跑全仓套件；focused 45 与相邻 329 本轮已在 committed tree 复跑通过。
- **Fee schedule drift**: Futu HK 费率表变更需新 schedule version + 新证据，不得原地改 v1（plan residual 明示，归后续 owner）。
