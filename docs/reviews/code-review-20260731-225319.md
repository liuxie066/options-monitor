# Code Review

## Scope

- Mode: current changes
- Branch or PR: `main` workspace changes
- Base: `main@eb5d1e9674eef627a91a20cef7d02f5bb749d5b1`（与 `origin/main` 一致）
- Output file: `docs/reviews/code-review-20260731-225319.md`
- Included scope: 当前 workspace 中的只读 SQLite reconciliation evidence repository、真实 SQLite trade-intake reconciliation 回归、生成的依赖图，以及上一轮 finding artifact `docs/reviews/code-review-20260731-224207.md`；重点复核 account-coherent lifecycle snapshot、严格/宽松解析边界、异常传播、runtime preview unavailable、quality actionable-pending 消费与只读副作用边界。
- Excluded scope: `eb5d1e96` 中已提交的其余 post-trade Combo reconciliation 实现；真实 OpenD/Futu 流量；生产账本、配置或 runtime 写入；通知发送；release、deployment、remote upgrade。
- Parallel review coverage: 无；范围集中在一个 SQLite read boundary、一个消费链路及其回归测试，由主 reviewer 完整走读。

## Findings

未发现实质性问题。

## Open Questions

- 无。

## Residual Risk

- 损坏竞争 lifecycle case 已由真实 SQLite 回归覆盖；evidence、allocation、source claim、timing policy、position lot 与 trade event 的畸形 JSON 没有逐表参数化测试，但 coherent 路径分别复用同一 strict decoder，或在专用 strict reader 中显式重新抛出解析错误。
- 回归测试直接证明 preview 抛错且 state 文件不变；runtime summary 的既有异常捕获通过静态链路复核，未新增一条专门模拟该 JSON 错误的 runtime-status 用例。
- 本轮未连接真实 OpenD/Futu、未发送通知，也未读取或修改生产 runtime artifacts；生产账本实际完整性仍属于独立授权下的运行验证范围。

## Fixed Finding Verification

- `docs/reviews/code-review-20260731-224207.md` 的 `DR-01` 已修复。`read_lifecycle_account_rows()` 对 cases、evidence、allocations、source claims 与 timing policies 使用 strict object decoding；trade events 和 position lots 在 coherent 快照中使用 strict JSON decoding，同时普通诊断列表保留原有宽松行为。
- 合法账户闭包仍返回 canonical direct delegation；同一数据库插入损坏的竞争 case 后，预览不再删除坏行继续裁决，而是传播 `JSONDecodeError`。调用方 `_trade_intake_reconciliation_summary()` 将异常转为 `reconciliation_preview_available=False`、空 delegated ID 集合；quality 因此不会从 actionable pending 中扣除该 deal。
- SQLite 连接仍以 URI `mode=ro` 和 `PRAGMA query_only=ON` 打开；preview 固定使用 `apply_changes=False`，回归在成功与失败分支都验证 intake state 文件字节不变。

## Validation Evidence

- 聚焦 reconciliation/quality suite：`37 passed in 1.38s`。
- `PYTHONPYCACHEPREFIX=/tmp/om_strict_reader_full python3.12 -m pytest -q -p no:cacheprovider`：`3871 passed, 10 skipped, 6 warnings in 69.40s`。
- `./.venv/bin/python -m ruff check .`：passed。
- `python3.12 scripts/generate_dependency_graph.py --check`：passed，`production_modules=576 cycles=0`。
- `git diff --check`：passed；无 unmerged path。
- 验证期间实现、测试、依赖图与上一份 review artifact 的时间戳未发生并发变化；`HEAD == main == origin/main == eb5d1e9674eef627a91a20cef7d02f5bb749d5b1`。
