# Code Review

## Scope

- Mode: current changes
- Branch: feature/option-notification-experience
- Base: bb24f87b
- Output file: `docs/reviews/code-review-20260721-183355.md`
- Included scope: Slice 1 scheduler target generation、state watermark、DTO propagation、account outcome、runtime read surface 和测试
- Excluded scope: delivery repository、notification decision、renderer/query（后续 approved slices）
- Parallel review coverage: 无

## Findings

未发现实质性问题。

## Adversarial failure pass

1. **Late completion**：processed target 与 completion time 分离，`09:40 -> 10:01` 和 `15:30 -> 15:51` 不吞下一 target。
2. **Watermark regression**：迟到的旧 callback 不能覆盖较新的 processed target。
3. **Account isolation**：`lx` 已处理不会阻止 `sy` 执行同一 target。
4. **Mixed version**：旧 payload 缺少 scan target 时 normalize 为 `None`；旧 state 整体缺少 processed map 时使用 legacy completion seed。
5. **Half-hour validity**：排除 `09:30`，午休 target 被 break 过滤，next target 使用 fixed/half-hour 并集。
6. **Force/manual**：不生成 scheduled scan/fixed target。
7. **Ownership**：scheduler 只生成 occurrence facts；account execution 只返回 target map，不写 delivery 或 scheduler state。
8. **Semantic drift**：`scheduled_target_market` 继续只代表 fixed report；新增字段没有复用旧字段造成含义漂移。

## Project instruction check

- 未引入第二 timer/scheduler/config。
- domain 未反向 import `src/` 或 `scripts/`。
- 未修改生产配置、服务、通知或 broker state。
- 无关 dirty files 未暂存、未修改。

## Overengineering / overcoupling review

- 复用现有 scheduler state 和 DTO v1，仅增加一个 target field 和一个 account map。
- 没有新增服务、repository、database 或平行调度对象。
- account execution 与 future notification flow 通过现有 outcome 边界传值，无反向依赖。

## Open Questions

无。

## Residual Risk

- target commit 尚未接入 durable outcome：covered by later approved Slice 4。
- legacy state 首次切换：assigned to production rollout observation。

## Validation

- Focused: 144 passed。
- Broader: 203 passed。
- Ruff/compileall/diff check/import boundary: pass。

## Conclusion

pass

- Accepted findings: 0
- Unclassified residual risks: 0
- Current gate / next entry point: accepted Slice 1 commit
