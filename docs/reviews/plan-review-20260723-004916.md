# Plan Re-review — Close Advice Strategy Optimization

## Scope

- Reviewed target: `docs/gateflow/close-advice-strategy-optimization-plan-20260723.md`
- Fix artifact: `docs/gateflow/close-advice-strategy-optimization/plan-fix.md`
- Prior review: `docs/reviews/plan-review-20260723-004251.md`
- Reviewed baseline: `origin/main@64ccd3e0`
- Artifact path: `docs/reviews/plan-review-20260723-004916.md`

## Assumptions re-tested

- The P2 truth table uses only states current source can deterministically produce.
- No post-processing reallocation shadow can become hidden formal P2 authority.
- Episode identity represents duplicate runs, cross-day observations, and same-day state transitions.
- The close facet cannot pollute candidate snapshots, marking, settlement, readiness, or analysis.
- Counterfactual signs, fees, horizons, and lifecycle precedence are specified.
- Mechanical readiness and CEO policy judgment are separate gates.
- Old artifacts have a read-only compatibility path but cannot re-enter the production selector.

Architecture-boundary, best-practice, optimal-solution, overengineering, and overcoupling lenses were applied again.

## Finding status

### PR-01 — 已修复

The revised plan removes `weakened/lost`, limits P2 to `valid|observe|not_evaluable`, and makes medium underwriting hold/review only. Strong with incomplete thesis evidence is review. No observation-only P2 close remains.

### PR-02 — 已修复

P2 no longer consumes replacement evidence. P3 composes reallocation only offline after the formal close artifact exists; production P3 sequencing is assigned to a later work unit.

### PR-03 — 已修复

The plan now defines a material-fact fingerprint, timestamped episode ID, separate episode date, exact dedupe rule, and transition/account test cases.

### PR-04 — 已修复

Three optional close facet files have independent schemas and keys. Candidate files remain required/unchanged, facet processing dispatches separately, and S3 is split into S3a/S3b/S3c.

### PR-05 — 已修复

Decision-time incremental formulas now define close, hold, and switch comparisons; current-lot close cost cancels from switch-vs-close; lifecycle P&L precedence prevents intrinsic/ledger double counting; missing evidence remains inconclusive.

### PR-06 — 已修复

Only evidence coverage is a machine readiness gate. Policy-quality output is paired descriptive evidence with an exact close-precision denominator; trade-offs require the durable CEO decision artifact.

## Additional checks

- Decision time is anchored to the canonical UTC run ID; filesystem mtime is forbidden.
- Missing quote-native timestamps are accepted only for required-data artifacts inside the same run and are explicitly labeled `run_anchor`.
- `CloseDecisionFacts` excludes reallocation, preserving the domain/application boundary.
- S1 keeps formal P0 behavior; P1/P2/P3 remain unreachable from production until S6.
- `git diff --check`: pass.

## Open questions

None before implementation.

## Residual risks

- Evidence readiness may require forward collection across natural days: classified as requiring explicit user decision at S5.
- Covered Call stock/tax thesis is unavailable: classified as bounded by current called-away willingness and observation semantics.
- Production P3 two-stage orchestration is not implemented: classified as a later work unit; P3 remains offline.
- Missing historical marks cannot be reconstructed: classified as forward evidence lifecycle work.

## Conclusion

`pass-with-risks`

All prior findings are fixed. The revised plan is code-generation-ready for S1-S4. S5 remains the required evidence/CEO stop gate before any production policy, notification, or config promotion.
