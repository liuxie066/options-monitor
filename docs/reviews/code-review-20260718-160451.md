# Code Review

## Scope

- Mode: PR
- Branch or PR: `liuxie066/options-monitor#73` — `feat: support true diagonal Combo Yield lifecycle`
- Base: remote `main` at `0af7adac` (v1.2.411)
- Head: `ad6f4627`
- PR URL: `https://github.com/liuxie066/options-monitor/pull/73`
- Output file: this file
- Included scope: complete PR diff, latest remote base evolution, mergeability, current staggered Combo Yield architecture, candidate/intake identity, lifecycle/assignment ownership, Close Advice synthesis, tests and CI state.
- Excluded scope: unrelated Option Performance implementation except where it changed ownership boundaries used by this PR.
- Parallel review coverage: 无。

## Findings

### PR-F1-未修复-严重-PR 基于过期主干并重复实现已发布的 staggered Combo Yield contract，当前无法合并
- **入口/函数**: PR integration boundary / all public Combo Yield paths
- **文件(行号)**: PR overlap across `domain/domain/engine/yield_enhancement.py`, `src/application/sell_put_call_helper.py`, `src/application/trades/resolver.py`, `src/application/positions/reporting.py`, `src/application/close_advice_runner.py`, config and tests.
- **输入场景**: 将 PR #73 合入当前 `main` (`0af7adac`)。
- **实际分支**: GitHub reports `mergeable=false`, `mergeStateStatus=DIRTY`. Since this branch forked from `34e56cac`, main has released v1.2.403–v1.2.411, including `7f45b44e feat: add staggered Combo Yield pairing`, `ef81c5ad Combo Yield safety fixes`, and the assigned-stock projection extraction in PR #71.
- **预期行为**: PR must extend current owners/contracts: `structure_mode=staggered_expiry_pair`, explicit `pair_intent_id`, `src/application/positions/combo_pairing.py`, and `domain/domain/assigned_stock.py`; lifecycle/Close Advice additions must be layered on top without introducing a second incompatible vocabulary or restoring removed reporting ownership.
- **实际行为**: PR introduces parallel `expiry_structure=diagonal`, different leg roles/group fingerprint semantics, a second manual-open handoff, and edits the pre-extraction reporting implementation. A direct merge has conflicts in 25+ production/test files and would risk replacing released safety fixes or creating two representations of the same strategy.
- **直接证据**: PR metadata base is `0af7adac`, head `ad6f4627`, `mergeable=false`; `git merge-tree` reports conflicts in candidate engine/config, resolver, reporting, Close Advice, CLI and tests. Current main owns staggered pairing in `src/application/positions/combo_pairing.py` and assigned-stock lifecycle in `domain/domain/assigned_stock.py`, neither existed at the branch base.
- **影响**: PR cannot merge; naive conflict resolution can regress released production behavior, split identity/state semantics, or duplicate configuration/public contracts.
- **建议改法和验证点**: merge current `origin/main` into the branch; resolve overlapping files in favor of current main contracts; re-port only the missing lifecycle/group Close Advice/single-cycle safeguards at current owner boundaries; adapt tests to `funding_put` / `participation_call`, `structure_mode`, `pair_intent_id`, and extracted assigned-stock projection; rerun current-main regression suite plus feature tests and CI.
- **修复风险（低/中/高）**: 高
- **严重程度（低/中/高/严重）**: 严重

## Open Questions

- 无。Current main provides authoritative architecture and the confirmed product behavior remains valid; this is an integration repair, not a product-direction decision.

## Residual Risk

- CodeQL checks are still in progress at initial PR review time.
- The branch must be re-reviewed after current-main integration because the effective diff and ownership boundaries will materially change.
