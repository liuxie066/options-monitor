# Code Review

## Scope

- Mode: current changes
- Branch or PR: `feature/option-notification-experience`
- Base: accepted plan commit `d62c2289`
- Output file: `docs/reviews/code-review-20260722-230946.md`
- Included scope: S1 shared Futu option-fee formulas and dispatch; Close Advice broker/currency/economic projection; pure fee safety gate; existing action mapping and combo handoff; public read/analysis fields; contract docs; direct-consumer and failure-path tests
- Excluded scope: S2 lifecycle/date changes; notification/Daily Brief; config/state/broker writes; release/deploy; unrelated untracked `docs/reviews/plan-review-20260722-225255.md`
- Parallel review coverage: 无；scope is one bounded fee/economic call chain and was reviewed end-to-end from shared calculator through runner, domain gate, action mapping, read models, and tests

## Findings

未发现实质性问题。

The adversarial pass checked:

- buy-to-close versus sell-to-close regulatory-fee direction;
- US commission threshold, ORF/OCC/settlement/CAT/SEC/TAF components and OCC cap;
- HK exact-0.01 tariff waiver and Tier-1 upper-bound labeling;
- missing broker, unsupported broker/currency, invalid contracts/multiplier, and calculator exception paths;
- otherwise-actionable versus already-hold behavior;
- short/take-profit lifetime net P&L versus long-salvage net-proceeds ownership;
- `tier`, `exit_state`, `evaluation_status`, `close_action`, combo aggregation, and notification selection consistency;
- public numeric normalization and analysis snapshot propagation;
- shared consumer regressions without changes to scanner threshold/rank code.

## Open Questions

- 无。

## Residual Risk

- USD account fixed/tiered platform-package selection is not present in the input contract. The implementation correctly exposes the result as `schedule_estimate`; actual package integration remains assigned to a later work unit.
- HK instrument tariff classification is unavailable. The implementation correctly exposes Tier 1 as `conservative_estimate`; instrument metadata remains assigned to a later work unit.
- Focused validation passed (`167 passed, 1 existing deprecation warning`), but the full repository suite is reserved for aggregate validation after S2.
- S2 lifecycle behavior was intentionally not reviewed in this artifact.
