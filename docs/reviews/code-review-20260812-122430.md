# Code Review

## Scope

- Mode: current changes re-review
- Work unit: `retire-ai-decision-advice`
- Gate: Slice 1 re-review
- Branch: `refactor/retire-ai-decision-advice`
- Base: accepted Gateflow plan commit `ea6eb4a9`
- Initial review: `docs/reviews/code-review-20260812-120834.md`
- Fix artifact: `docs/gateflow/retire-ai-decision-advice/slice-1-review-fix.md`
- Output file: `docs/reviews/code-review-20260812-122430.md`
- Included scope: complete Slice 1 diff and all accepted finding fixes
- Excluded scope: Slice 2 physical subsystem/config/service deletion; release; deployment; runtime-state mutation
- Parallel review coverage: none; re-reviewed as one persistence-to-delivery compatibility boundary

## Findings

No actionable findings.

## Re-review disposition

### 1-已修复-中-旧 AI 重试被拦截后 Tick 仍以成功跳过结束，永久阻塞被误报为无需投递

- `DailyBriefNotificationPreparation.blocked_error_code` now reaches terminal flow control.
- A blocker-only run returns 2 and records terminal failure; a mixed-account run sends clean accounts but keeps a
  nonzero result, local failure fact, and terminal idempotency failure.
- The guard freezes delivery selection/state while deterministic current Brief persistence still advances; pending
  and ambiguous delivery bytes remain identical in delivery-only and post-scan runs.
- Quiet hours cannot convert the blocker into a successful skip.
- Final status: `已修复`.

### 2-已修复-中-历史摘要兼容仅覆盖 v2 source validator，overlay-era v1 pointer 仍会被当前代码判为损坏

- Existing v1 pointer reads, confirmation, migration preview, and delta confirmation now accept only digests in
  `daily_brief_compatible_digests(raw)`.
- New persisted data and migrated target-state metadata continue to use the stripped current digest.
- Overlay-era prepare-next, migrate-preview, unconfirmed-confirm, and delta-confirm regressions pass; unrelated wrong
  digests remain fail-closed.
- Final status: `已修复`.

### 3-已修复-低-classifier 检查的 raw revision 不是其随后完成摘要校验的同一次读取

- `_read_validated_successful_revision_source()` now returns normalized and raw values from one strict read.
- `_normalize_delivery_envelope()` may pass that exact validated raw mapping to the classifier without changing the
  normalized/persisted envelope contract.
- Counted-read coverage proves one revision read for classification.
- Final status: `已修复`.

## Open Questions

- 无。

## Residual Risk

- Known-marker matching intentionally remains narrow to the retired renderer headings; it is not a generalized AI
  content classifier.
- Historical runtime artifact deletion and installed service reconciliation remain outside this work unit's local
  source authority.

## Conclusion

`pass`. Slice 1 is ready for its Gateflow commit.
