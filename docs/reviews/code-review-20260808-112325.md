# Code Review

## Scope

- Mode: current changes（第二轮，针对第一轮 Finding 1 的修复再审查）
- Branch: `main` (工作区未提交改动)
- Base: `HEAD` (cc6f9e04, v1.10.17)
- Output file: `docs/reviews/code-review-20260808-112325.md`
- Included scope: 第一轮修复之上的增量——`_audit_paths` 对 `output_runs`/run 目录加 `is_symlink()` 拒绝；两个符号链接回归测试
- 前一轮 artifact: `docs/reviews/code-review-20260808-111256.md`

## Findings

### 1-已修复-低-containment 检查不防符号链接逃逸（第一轮 Finding 1）

- 修复方式：`_audit_paths` run 分支在解析前对 `base/"output_runs"` 与 run 目录做 `is_symlink()` 拒绝，错误消息与 containment 一致（`audit path must stay under output_runs`）。与 `position_advice_runner._validated_run_roots`、`opend_symbol_outputs`、`close_advice_required_data` 等 6+ 处既有先例的模式一致。
- 验证：新增 `test_notification_perception_run_id_rejects_symlinked_output_runs`（output_runs 整体为符号链接）与 `test_notification_perception_run_id_rejects_symlinked_run_dir`（单个 run 目录为符号链接）两个回归，planted 外部文件均不被读取。全部 142 个相关测试绿。

### 2-未修复-低-output_shared 默认分支存在同型符号链接逃逸

- **入口/函数**: `read_notification_perception_events()` -> `_audit_paths()` 无参默认分支
- **文件(行号)**: `src/application/notification_perception_read.py:101`
- **输入场景**: 攻击者可控环境中 `output_shared` 为指向任意外部目录的符号链接，工具在不带 run_id/audit_path 的默认调用下读取 `output_shared/state/audit_events.jsonl`。
- **实际分支**: 该分支既无 containment 也无符号链接校验，`.resolve()` 直接跟随符号链接。已实测：symlink 的 output_shared 下外部文件被成功读取（status=ok）。
- **预期行为**: 与 run 分支修复后的姿态一致，符号链接的 output_shared 应被拒绝。
- **直接证据**: 第二轮实测脚本输出 `shared symlink: ok 0`（放行）；该行为表达式全程无校验。
- **影响**: 低。同第一轮 Finding 1 的部署前提。
- **建议改法和验证点**: 对 `base/"output_shared"` 加同样的 `is_symlink()` 拒绝 + 回归测试。runtime_root 本身为符号链接是 `resolve_runtime_root` 设计内形态（已实测兼容），不受影响。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## 已验证无问题项

- 嵌套符号链接（run 目录内 `state` 为符号链接）被 containment 的 `relative_to` 正确拒绝，错误消息一致。
- runtime_root 本身为符号链接（`OM_RUNTIME_ROOT` 设计内形态）不受影响，读取正常。
- `audit_path` 分支行为与消息不变。
- 无 run_id 误杀：真实 run_id 格式与现存 13 个历史目录名均通过校验（第一轮已验证，本轮校验逻辑未变）。

## Open Questions

1. 无新增。第一轮的部署形态问题已由代码证据回答：仓库 6+ 处先例均拒绝符号链接目录，本仓姿态为 fail-closed。

## Residual Risk

- `audit_path` 显式路径分支仍保持既有 repo_root 范围校验，未加符号链接检查；该分支面向显式运维传参，且本次未改动其行为。如需与 run/shared 分支对齐，可作为后续低优先级收紧项。
- 路径组件在符号链接检查与文件读取之间存在理论 TOCTOU 窗口；攻击者需已有运行目录写权限，与既有先例接受的风险水平一致，不在本次范围。
