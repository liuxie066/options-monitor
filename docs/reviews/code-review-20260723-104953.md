# Code Review

## Scope

- Mode: current changes
- Branch or PR: `fix/daily-brief-close-tier-wording`
- Base: accepted plan commit `f7d5fcf4`
- Output file: `docs/reviews/code-review-20260723-104953.md`
- Included scope: Daily Brief position status projection, actionable filtering/counting, close-detail metric naming,
  renderer regressions, implementation artifact
- Excluded scope: close-advice policy correctness, P1/P2/P3 promotion, notification selection/scheduling,
  runtime config, delivery, deployment
- Parallel review coverage: 无；单个 renderer 切片由主 reviewer 沿完整投影链审查

## Findings

未发现实质性问题。

Adversarial path checked:

- `daily_decision_brief_service._position_view()` preserves `tier` and `close_action`.
- `_position_views()` filters through `_position_has_advice()` before rendering status and details.
- `_position_status_label()` gives data-quality failures priority, then applies the tier mapping only to the
  standard `close` action; special combo/call actions retain their existing mapping.
- Known tier labels and the generic fallback are all present in `_POSITION_ACTIONABLE_LABELS`, so the wording
  change cannot silently remove a close row or lower the actionable count.
- Missing/unknown tier values take the previous generic `建议平仓` path.
- `_position_close_details()` changes only the label; it still reads the same
  `metrics.remaining_annualized_return` value.
- Full-message tests prove all four known tiers, two version-skew fallbacks, the unchanged actionable count,
  degraded input behavior, special action wording, and both HK/US metric rendering.

## Open Questions

- 无。

## Residual Risk

- P0 still owns the formal `close_action=close` decision for weak/optional tiers. This slice accurately exposes
  urgency but intentionally does not resolve the strategy-level action/state mismatch.
- The existing renderer still couples actionable filtering to presentation labels. This change updates and tests
  the complete known label set; a predicate-based refactor would be broader than the confirmed bug-only scope.
- Validation is local. No real notification was sent, and no production runtime or config was mutated.

