# Plan Re-review — combo-yield-remove-staggered（第二轮，final）

## Reviewed Target and Scope

- Reviewed artifact: `docs/gateflow/combo-yield-remove-staggered/plan-20260810.md`（修复版 v2）
- 前序 review: `docs/reviews/plan-review-20260810-231011.md`（6 findings）
- 前序 re-review: `docs/reviews/plan-re-review-20260810-231440.md`（6 已修复 + Finding 7/8 新增）
- Work unit: `combo-yield-remove-staggered`
- 本轮目的: 裁决 Finding 7/8 修复，确认 plan 达到 code-generation-ready
- Review date: 2026-08-10

## Finding 7/8 裁决

| # | finding | 修复证据 | 裁决 |
|---|---|---|---|
| 7 | validate_yield_enhancement_pair gap 参数与 helper policy gap 行字段未写死 | plan §7 Domain 接口与 Slice 1 均写死：删除 validate_yield_enhancement_pair 的 min_expiry_gap_days / max_expiry_gap_days 参数；删除 sell_put_call_helper.py 的 policy_min_expiry_gap_days / policy_max_expiry_gap_days 行字段与对应参数；expiry_gap_days 行字段保留（canonical_schema identity，同期值恒 0）。代码证据确认 gap 参数仅 staggered 分支使用（yield_enhancement.py:138-141）、policy 行字段无下游消费者 | 已修复 |
| 8 | ComboYieldResearchPolicy gap 字段去留未定死 | plan §7 Domain 接口与 Slice 3 均写死：保留 structure_mode 字段（仅允许 same_expiry_pair），删除 min_expiry_gap_days / target_expiry_gap_days / max_expiry_gap_days 三个字段及对应 staggered gate/rank 分支。代码证据：gap 字段仅在 staggered gate/rank 使用（yield_enhancement.py:606-641），structure_mode 字段参与 gate-reason 比对（:533-535） | 已修复 |

## 全量复检（8 findings 终态）

| finding | 终态 |
|---|---|
| 1 strategy_attribution diagonal 保留 | 已修复 |
| 2 config validator 显式 removed-fields 检查 | 已修复 |
| 3 domain structure_mode 参数删除 | 已修复 |
| 4 Slice 1 structure_mode 表述矛盾 | 已修复 |
| 5 docs/plans/、docs/reviews/ 不改 | 已修复 |
| 6 shadow_replay 5 文件 | 已修复 |
| 7 domain gap 参数与 policy gap 行字段删除 | 已修复 |
| 8 research policy gap 字段删除 | 已修复 |

## Code-Generation-Readiness 检查

- 每个 slice 的 allowed files / exact allowed changes / validation / completion signal 均可直接执行，
  无"实现时决定"类留白。
- Slice 边界为可验证行为增量：S1 开仓路径单一化 → S2 配置/校验面 → S3 通知/research/文档；无机械文件拆分。
- Goal alignment：所有删除对象均可映射到 goal confirmation 的"删除错期开仓路径 + 消除冗余"；ledger/
  生命周期（Q2）与 shadow 排序（同期机制）明确保留。
- Residual risks 已分类：提交隔离（重叠文件）、历史运行产物只读确认、文档改写边界、既有测试基线。

## Open Questions

- 无 blocking。

## Residual Risks and Suggested Tracking

- 历史 output_runs/ staggered rows 不重写；S3 完成后只读确认，不新增兼容代码（assigned to later
  verification step）。
- STRATEGY_ARCHITECTURE.md 错期指标退化理由删除时保留同期指标语义（S3 文档改写边界）。

## Final Plan Review Conclusion

**pass** — 前序 8 个 findings 全部已修复并复核；plan 已 code-generation-ready，可以进入 accepted plan commit
与 implementation slice。
