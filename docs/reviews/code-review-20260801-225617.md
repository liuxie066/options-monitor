# Code Review

## Scope

- Mode: current changes, aggregate deepreview
- Branch: `codex/fix-shadow-replay-integrity-selection`
- Base: `origin/main@8d467282`
- Output file: `docs/reviews/code-review-20260801-225617.md`
- Included scope: complete work unit from integrity derivation through status
  projection, write eligibility, OpenD circuit/quota ordering, direct collection
  precondition, locked downstream writer validation, receipts, tests, docs, and
  dependency direction
- Excluded scope: legacy manifest migration, OpenD provider correctness,
  unrelated research-policy evaluation
- Parallel review coverage: none; the change is one narrow application-layer
  call chain

## Findings

### DR-AGG-01-已修复-中-旧写模式测试夹具未建立新 integrity 前置条件

- **入口/函数**: `test_data_plan_keeps_receipt_time_separate_from_actual_mark_time()`
  -> `run_shadow_replay_data_plan(write=True)`
- **文件(行号)**: `tests/test_close_advice_shadow_readiness.py:338-383`
- **输入场景**: 全量套件运行旧的 receipt-time 测试，其 dataset 只有
  candidate/filter 文件，但以 `write=True` 执行。
- **实际分支**: 新 planner 正确把该数据集识别为
  `legacy_unverified` 并跳过，因此 monkeypatched collector 没有被调用。
- **预期行为**: 该测试要验证 receipt time 与采样 time 分离，它的
  write-mode 数据集应是 verified，不应意外测试 legacy 资格。
- **实际行为**: 测试在 `seen and seen[0]["as_of"] is None` 失败，阻断全量门禁。
- **直接证据**: 首轮非权限失败显示 `seen=[]`；生产分支在
  `data_plan.py:192-200` 按契约跳过。
- **影响**: 测试基线失败，但生产 fail-closed 行为正确。
- **建议改法和验证点**: 补齐 `DATASET_FILES` 并调用现有
  `refresh_dataset_manifest()`；重跑该测试、聚焦套件和依赖图检查。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

#### Re-review

- Final status: `已修复`.
- The fixture now creates every required dataset file and refreshes the canonical
  manifest before write-mode execution.
- Target plus all focused Shadow Replay/Strategy Lab/Research tests passed:
  `126 passed`.
- Dependency graph was regenerated and its dedicated tests passed: `3 passed`.

No other material findings.

The aggregate trace confirmed:

- integrity remains owned by the existing common validator and status read
  model; no duplicate hashing or fallback truth source was added;
- mutation eligibility runs before both the provider circuit and execution
  counter, so legacy rows cannot spend quota or provider calls;
- exact `verified` is required, so missing/malformed projections fail closed;
- direct write collection validates before dataset reads, OpenD fetch, runtime
  limiter/cache writes, or required-data writes, while the existing locked
  writer still validates again before dataset mutation;
- dry-run behavior and public CLI parameters are unchanged;
- action/summary receipt fields make skip behavior auditable;
- tests cover real legacy/verified projections, quota ordering, provider
  non-invocation, existing rate-limit behavior, and the pre-existing receipt-time
  contract under a verified fixture.

## Open Questions

- None.

## Residual Risk

- There is an existing narrow concurrent-change window between the direct
  pre-fetch validation and the later locked writer validation. The later check
  fails closed, limiting impact to potentially wasted provider capacity. Owner:
  a later collection-locking work unit if concurrent dataset writers become a
  supported production mode.
- Unit tests do not prove live OpenD availability or dataset population. Owner:
  the authorized post-upgrade production canary.
