# Code Review

## Scope

- Mode: current changes（aggregate deepreview，跨全部 approved slices）
- Branch: `design/combo-yield-remove-staggered`
- Base: `main`（merge-base `afd670b8`）
- Output file: `docs/reviews/code-review-20260811-082229.md`
- Included scope: 工作单元 main...HEAD 全部改动（41 个文件，+1569/-1246）——plan +
  三个 approved slices（S1 扫描/配对/配置、S2 通知/展示、S3 research/shadow/文档/测试），
  以及聚合 deepreview 修复（combo_settlement 死代码、Daily Brief 测试 fixture、
  SHADOW_REPLAY_RUNBOOK 文档漂移）。
- Excluded scope: ledger/生命周期保留面（`combo_yield_lifecycle.py`、`trades/resolver.py`、
  `positions/combo_pairing.py`、`combo_reconciliation.py` 对历史
  `structure_mode=staggered_expiry_pair` 的兼容，Q2 用户决策）；`docs/plans/` 与
  `docs/reviews/` 历史归档引用；`CHANGELOG.md`。
- Parallel review coverage: 4 个分区子代理按模块并行走读：
  1. domain 引擎 + 配置/校验/数据规划（yield_enhancement、config_validator、
     required_data_planning、required_data_prefetch_planning、sell_put_call_helper、
     yield_enhancement_config + 4 测试）——未发现实质问题；
  2. 输出/通知/报告（alert_engine、notify_symbols、render_yield_enhancement_alerts、
     report_summaries + 4 测试，另核 daily_decision_brief_renderer、multi_tick/notify_format）
     ——发现 DR-C；
  3. research/shadow/strategy-lab + performance attribution（capture、combo_capture、
     combo_evaluation、combo_settlement、combo_variants、combo_evaluator、
     strategy_attribution + 2 测试，另核 performance engine/attribution 边界）
     ——发现 DR-B；
  4. 文档/示例/配置一致性 + 全仓残留扫描（4 文档/示例文件 + runbook 未改动检查）
     ——发现 DR-A。
  主 reviewer 复核全部证据链并裁决。

## Findings

### 1-已修复-中-`docs/SHADOW_REPLAY_RUNBOOK.md` 仍宣称错期研究变体能力，与源码和示例矛盾

- **入口/函数**: 研究操作手册章节「Combo Yield 同期/跨期研究变体」
- **文件(行号)**: `docs/SHADOW_REPLAY_RUNBOOK.md:370`（章节标题）、`:445-448`
  （跨期结果/同期与跨期 scorecard 表述）
- **输入场景**: 用户按 runbook 配置 shadow 研究变体
- **实际分支**: runbook 未随 S3 改动更新；源码已 fail-closed 拒绝错期变体
- **预期行为**: 手册只描述 `same_expiry_pair`；错期能力无宣称
- **实际行为**: 手册仍宣称「同期/跨期研究变体」与「跨期结果分别报告……」，与
  `combo_variants.py:98-110` 的 removed-gap ValueError 和
  `yield_enhancement.py:406-408` 的 same-only 校验直接矛盾
- **直接证据**: `git diff main...HEAD --stat -- docs/SHADOW_REPLAY_RUNBOOK.md` 为空
  （文件未被工作单元触碰）；plan §11 文档清单未列 runbook，§10 扫描只覆盖
  `domain/ src/ tests/`
- **影响**: 活文档漂移；照手册操作会得到 fail-closed 报错，无运行时安全问题
- **建议改法和验证点**: 已实施——标题改「Combo Yield 同期研究变体」，:445-448 改为
  共享到期结算语义；验证：`rg -n "跨期|错期|staggered" docs/SHADOW_REPLAY_RUNBOOK.md`
  零命中（除意图说明外）
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中
- **裁决**: accepted → 已修复

### 2-已修复-低-`combo_settlement` 保留错期结算死代码（不可达分支、冗余 mark 查询、死 helper）

- **入口/函数**: `combo_settlement._settle_one`
- **文件(行号)**: `src/application/shadow_replay/combo_settlement.py`（修复前
  :241/:297-305/:349-353/:417-431/:433-441）
- **输入场景**: 任意同到期 combo pair 结算（改动后唯一合法模式）
- **实际分支**: `call_horizon` 恒为 `"put_expiry"`，`if call_horizon == "put_expiry"`
  else 分支静态不可达；`post_put_expiry_state` 错期残值分支不可达；
  `_leg_mark` / `_executable_long_exit_value` 仅被死分支引用
- **预期行为**: 同期化后结算路径只保留 put_expiry 单 horizon 语义
- **实际行为**: 输出语义正确但保留误导性死分支、每次结算的冗余 mark 查询与孤立 helper
- **直接证据**: `rg "_leg_mark|_executable_long_exit_value"` 仅命中死分支与定义；
  测试仅断言 `post_put_expiry_state == "terminal"`（test_combo_yield_research.py:582）
- **影响**: 纯可维护性/审计成本；未来改动 `call_horizon` 会误以为错期仍受支持
- **建议改法和验证点**: 已实施——删除 `call_put_expiry_mark`、不可达 if/else、
  `put_expiry_residual_call_bid_missing` 错误路径、错期 `post_put_expiry_state` 分支
  与两个死 helper；`funding_horizon_pnl = put_pnl + call_intrinsic - call_cost`；
  `post_put_expiry_state` 恒为 `"terminal"`；验证：test_combo_yield_research.py 全绿
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低
- **裁决**: accepted → 已修复

### 3-已修复-低-Daily Brief 测试仍以错期结构 fixture 固化「跨到期渲染」语义

- **入口/函数**: `assemble_daily_decision_brief` → `_load_combo_yield_snapshot_family`；
  测试 `test_combo_yield_event_projection_relates_to_both_expirations`
- **文件(行号)**: `tests/test_daily_decision_brief_service.py:1617-1651`（修复前）
- **输入场景**: 测试 fixture 使用 `put_expiration=2026-08-21`、`call_expiration=2026-09-18`
  错期结构，经 CSV fixture 封存后进入 Brief
- **实际分支**: 断言固化 put `after_expiration`、call `before_expiration` 的跨到期渲染
- **预期行为**: S3「输出只渲染同期组合」下 Brief 测试面应只剩同期结构
- **实际行为**: 错期 fixture 端到端跑通，测试把跨到期渲染固化为契约；生产当前
  sealed snapshot 只产同期对，故无生产回归
- **直接证据**: fixture 无 staggered 关键字（`rg -i "staggered|错期"` 扫不到），
  S3 测试清理未覆盖此类「错期形态」；`_load_combo_yield_snapshot_family` 仅按
  market 过滤，错期行直接透传渲染
- **影响**: 输出路径在旧/重放快照含错期行时仍会渲染跨到期组合，且测试固化该行为
- **建议改法和验证点**: 已实施——fixture 改为共享到期 `2026-08-21`，earnings 同日期，
  两腿断言均为 `on_expiration` / `days_before_expiration: 0`，
  `in_attention_window` 保持 True；测试改名
  `test_combo_yield_event_projection_relates_to_shared_expiration`
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低
- **裁决**: accepted → 已修复

### 4-已拒绝（deferred-with-owner）-低-兼容层错期文案与双到期格式分支是否清理

- **入口/函数**: `build_account_message_compact`（deprecated 兼容层）测试 fixture；
  `report_summaries._format_combo_contract`
- **文件(行号)**: `tests/test_multi_tick_notify_format.py:207`；
  `src/application/report_summaries.py:212-218`
- **输入场景**: deprecated notify_format 计数测试以「🧩 组合·跨期」字符串为输入样例；
  report_summaries 保留 `put_expiration != call_expiration` 双到期格式分支
- **实际分支**: 两者均为旧 artifact / 旧数据容错路径，当前生产数据不产生错期输入
- **预期行为**: 由 plan §8 字段契约保留决策与「兼容性容错」口径界定
- **实际行为**: 行为正确；错期文案/格式分支仅存在于兼容层，不属本工作单元改动面
- **直接证据**: `notify_format.py` 是 deprecated compatibility-only wrapper；
  plan §8 Decision 明确保留字段契约与旧 artifact 容错
- **影响**: 无；仅字面残留
- **建议改法和验证点**: 不修改；作为 residual risk 记入 implementation-s3.md 与本文档，
  owner = 后续兼容层清理 work unit
- **修复风险（低/中/高）**: 不适用
- **严重程度（低/中/高/严重）**: 低
- **裁决**: deferred-with-owner（兼容层清理 work unit）

## Open Questions

- 无

## Residual Risk

- 旧 shadow artifact / 旧 sealed combo snapshot 含错期行时：capture 分组与结算按同期
  语义统一处理（`_combo_pair_group_id` 合并、`call_horizon` 恒 put_expiry）；
  strategy-lab 对旧错期 trace 走 fail-closed（`combo_yield_structure_mode_invalid` +
  expiration mismatch）。目前无生产路径生成该数据，属研究侧兼容面。
  分类：assigned to later work unit。
- `strategy_attribution` 校验 set 收窄为 `{diagonal}`：全仓无 writer 产生
  `expiry_structure=staggered_expiry_pair`；ledger 将 `structure_mode=staggered_expiry_pair`
  归一化为 `diagonal`（combo_yield_lifecycle.py:34），生命周期顺序校验（:162）与 trades
  intake guard 仍生效。分类：fixed in current slice。
- `derive_yield_enhancement_policy` 不自行校验 structure_mode，fail-closed 依赖
  config_validator 与 research policy `__post_init__`；当前生产入口均在加载时校验。
  分类：assigned to later work unit（纵深防御缺口，可后续补）。
- removed-gap 负向测试同时携带 `structure_mode=staggered_expiry_pair` 与 gap 字段，
  未单独锁定「仅 structure_mode=staggered、无 gap 字段」路径（走 same-only 校验）。
  分类：assigned to later work unit。
- deprecated `multi_tick/notify_format` 测试 fixture 与 `report_summaries` 双到期格式
  分支保留（DR-4 deferred）。分类：assigned to later work unit（兼容层清理）。
- 验证范围：聚合修复后完整 S3 套件 303 passed（14 文件，含 sell_put 链路、
  required_data、render/notify/summary/brief、research/strategy-lab、perf attribution、
  lifecycle、positions context）；ruff 变更文件 clean；分区分组测试 88/87/137 passed。
