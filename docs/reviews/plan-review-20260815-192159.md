# Plan Re-Review — Sell Put Top1 W0R Exact-Expiration Close Boundary

## Reviewed target and scope

- Target: `docs/gateflow/sell-put-top1-w0r-exact-expiry-close/plan.md`
- Prior review: `docs/reviews/plan-review-20260815-192053.md`
- Fix artifact: `docs/gateflow/sell-put-top1-w0r-exact-expiry-close/plan-fix.md`
- Work unit: `sell-put-top1-w0r-exact-expiry-close`
- Base: `origin/main@813ec6f8021148ff6d152ff4ee4f5c39e36897fc`
- Artifact path: `docs/reviews/plan-review-20260815-192159.md`

## Assumptions re-tested

- Only the installed SDK's DataFrame-like success shape can become a close fact or successful absence.
- Required columns remain verifiable when the result has zero rows.
- Code/date/cardinality/pagination checks uniquely bind a positive close to the request.
- The method remains a provider primitive and does not absorb W5 timing, classification, persistence, or symbol orchestration.
- The generic QFQ history path remains behaviorally untouched.

## Finding re-review

### PR-01 — 已修复

The plan now requires callable DataFrame-like materialization plus `code`, `time_key`, and `close` columns. Plain list/tuple/dictionary payloads fail even when empty, while a correctly shaped zero-row frame alone returns `None`. The required tests cover missing columns and malformed materialization.

## Architecture, best-practice, optimality, and complexity result

- Ownership is correct: the strict SDK adaptation stays in `FutuGateway`; future domain identity and receipt semantics stay in W5.
- The trust boundary checks request identity, target date, price validity, row cardinality, and pagination completeness without adding storage or a new type hierarchy.
- A dedicated method avoids destabilizing the generic history path used by QFQ realized-volatility collection.
- Existing stdlib parsing, numbers abstractions, enum normalization, and gateway error mapping are sufficient; no dependency or configuration is needed.
- The fake-provider suite is the minimum runnable proof because live OpenD access is deliberately out of scope.

## New findings

None.

## Open questions

None.

## Residual risks

- Live provider shape, availability, quota headroom, and latency: separately authorized W0R live probe.
- Domain symbol conversion, timing/deadline, absence classification, retry/dedupe, receipt sealing/storage, and publication: remaining W5 runner.
- Overall account fee-plan, calendar, terms, observation, and readiness: remaining W0R/W7 work.

All residual risks are classified outside this exact source-boundary slice.

## Conclusion

`pass`

The plan is code-generation-ready for the confirmed exact-expiration close work unit.
