# Code Review

## Scope

- Mode: current changes, third DeepReview pass after all prior findings were fixed
- Branch: `fix/coherent-option-fx-snapshot` at `2ae7d59f`
- Base: local `main` at `2ae7d59f`; all reviewed changes are unstaged worktree changes
- Output file: `docs/reviews/code-review-20260805-101032.md`
- Included scope: the complete coherent multi-account option-position/FX worktree and S6-lite scoped-union/run-local Spot-observation slice; repository snapshot ownership, prepared publication and validation, pipeline/Position Advice consumers, close-advice planning, required-data dedupe/cache/execution, account error classification, tests, and generated dependency metadata
- Excluded scope: Gateflow, S6 subprocess exact-spec/multi-spec support, S7 event sharing, full S5 re-entry/plan-v2/global-risk hardening, release, deployment, production configuration or data writes, broker access, and real notifications
- Verification evidence reviewed: focused integrated suite (`223 passed`), final adversarial repair set (`6 passed`), full Ruff, `compileall`, `git diff --check`, and current dependency graph (`581 production modules, 0 cycles`)
- Parallel review coverage: none; the full change was re-reviewed as one end-to-end state/authority/provider chain

## Findings

No actionable findings.

## Prior finding disposition

- First pass, high: prepared option payload/manifest integrity failures in the pipeline child are now emitted as `ACCOUNT_CONFIG_PREPARED_OPTION_CONTEXT_INVALID`; the parent preserves the typed reason and forces `ran_scan=false`, `should_notify=false`. Fixed and covered through real child `main()` plus parent classification tests.
- First pass, medium: physical OpenD host identity now uses trim + lowercase consistently for candidate/position route comparison, prefetch deduplication, merged provider configuration, and expiration/Spot run-local cache identity. Fixed and covered by duplicate-plan, one-call cached-`None`, and close-advice route tests.
- Second pass, low: generated dependency metadata was stale after the new child-boundary import. Both generated artifacts were rebuilt; `generate_dependency_graph.py --check` now reports `581 production modules, 0 cycles`.

## Open Questions

- None within the approved S6-lite scope.

## Residual Risk

- S6 subprocess mode still does not support multi-spec execution and does not carry an observed-`None` Spot through a new CLI contract. This is the explicitly retained S6-lite boundary; canonical runtime configs currently default to in-process execution.
- Full S5 ledger-group all-or-none re-entry, stronger identity/provenance, promotion-time revalidation, and global-path risk support remain intentionally deferred rather than being implemented only to satisfy an older slice plan.
- `_resolve_spot_reference()` can still fall back to an existing CSV Spot without proving the CSV trading date. That pre-existing policy should be changed only as a separate stale-fallback decision.
- Review and tests are local and deterministic. No real OpenD connection, production tick, notification, production configuration mutation, or business-data write was performed.
