# Deepreview Re-review — Daily Decision Brief Aggregate

## Findings

### CR-AGG-1 — High — Crash after immutable revision write permanently wedges future preparation

- **Final status**: 已修复.
- **Code location**: `src/application/daily_decision_brief_repository.py` revision allocation and `_list_revision_numbers()`.
- **Fix evidence**: allocation occurs under the existing account+market lock and advances after every existing same-day immutable revision filename, rather than deriving only from stale current state. No immutable file is deleted or rewritten.
- **Adversarial verification**: injected interruption after revision 1 write with current still at revision 0 now recovers by publishing revision 2; revisions remain `[0, 1, 2]` and the latest full lifecycle remains deliverable. The no-current first-publication window is also covered.
- **Regression evidence**: `test_prepare_advances_past_orphan_revision_after_interrupted_publication[False/True]`.
- **Impact after fix**: the later eligible scheduler slot can recover automatically from this publication crash window.
- **Severity**: High.

### CR-AGG-2 — Medium — Existing high-priority action becoming active is silently dropped from the delta

- **Final status**: 已修复.
- **Code location**: `domain/domain/daily_decision_brief.py` existing-action transition evaluation.
- **Fix evidence**: an action entering active P0/P1 from outside the active high-priority set emits material `action_added`; an existing P0 upgrade remains represented once by `priority_upgraded_to_p0`.
- **Adversarial verification**: P0 `blocked`, `observe`, and `invalidated` each transition to active with `material=true`; P2 active to P1 active is material; unchanged active P1 remains non-material.
- **Regression evidence**: focused domain parameterization plus `test_stable_high_priority_action_recovery_is_material()` through persisted last-delivered state.
- **Impact after fix**: recovered Close Advice or candidate actions can no longer become executable without a delta notification.
- **Severity**: Medium.

## Open Questions

无。

## Residual Risk

- Provider idempotency behavior remains external production-canary evidence; local key and pointer state machines are deterministic and tested.
- Publication is not a multi-file transaction. The proven orphan-revision wedge is fixed; later individual write failures either leave a valid current revision or fail closed without advancing delivery.
- Exchange early-close calendars remain inherited scheduler scope and are assigned to a later work unit.
- No unclassified residual risk.

## Adversarial failure pass

- **Revision allocation**: normal same-day increments, new-day reset, orphan first revision, orphan later revision, current/revision mismatch and malformed state were rechecked.
- **Delivery pointer**: full/delta/none remain based on last confirmed revision; revision gaps do not allow wrong envelope confirmation because confirmation verifies the target revision's immutable run diff and base digest.
- **Action diff**: new action, P0 upgrade, active-set entry, downgrade, invalidation, disappearance, capacity change and non-material price/rank noise were rechecked for duplicate or missing events.
- **Multi-market/account**: helper scanning is scoped by normalized market/date/account filename prefix under the existing per-market lock.
- **Architecture/ownership**: fixes stay in the domain diff owner and application repository owner; no cross-layer import or new abstraction was introduced.

## Validation

```text
python3 -m pytest -q tests/test_daily_decision_brief_domain.py tests/test_daily_decision_brief_repository.py tests/test_daily_decision_brief_scenarios.py
46 passed

focused aggregate
260 passed

python3 -m pytest -q
2800 passed, 10 skipped

python3 scripts/generate_dependency_graph.py --check
475 production modules, 0 cycles

python3 scripts/guardrails_check.py --check-runtime-config-tracking
OK

python3 scripts/release_check.py
release metadata valid for 1.2.420

ruff / compileall / git diff --check
passed
```

## Review scope

- **Mode**: Current Changes Mode / aggregate re-review.
- **Selected base**: `5aecee73b3e4ace39b0c38ce9a98d18180020d1b` (`v1.2.420`).
- **Branch**: `codex/v1.3.0-daily-decision-brief`.
- **Reviewed target**: CR-AGG-1 and CR-AGG-2 fixes, focused regressions, generated dependency graph, and their interaction with the full S1-S5 lifecycle.
- **Review timestamp**: `20260719-193119`, generated from the local system clock with `TZ=UTC` when re-review began.
- **Artifact path**: `docs/reviews/code-review-20260719-193119.md`.

## Gate decision

- **Decision**: pass.
- **Accepted findings**: CR-AGG-1 and CR-AGG-2, both fixed and re-reviewed.
- **Current gate**: accepted aggregate deepreview commit.
- **Next entry point**: verify branch/status, stage only aggregate review/fix/re-review artifacts plus the accepted fixes/generated graph, and commit `gateflow: accept deepreview for daily-decision-brief`.
