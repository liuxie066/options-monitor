# Plan Review Fix — CC+LP（同到期）

- 关联 plan review：docs/reviews/plan-review-20260808-123655.md
- 日期：2026-08-08
- 用户裁决：方案 A（只加 `combo_yield.variant`，不扩展 `objective`）
- 状态：plan 已修复

## Finding 处置

| # | 严重程度 | 裁决 | 处置 |
|---|---|---|---|
| 1 | 高 | accepted | 修复：domain 层角色参数化 `strike_order`，Slice 1 实施 |
| 2 | 中 | accepted（用户裁决 A） | 修复：只加 `variant`，`objective` 保持 `premium_funded_long_call` |
| 3 | 中 | accepted | 随 Finding 2 收敛：不改 `YIELD_ENHANCEMENT_OBJECTIVES` / `mode` 推导 / validator 的 objective 枚举 |
| 4 | 低 | accepted | 修复：Sell Call 独立扫描 `stock` 从 `portfolio_ctx` 注入，无持仓 → not_applicable |
| 5 | 低 | accepted | 修复：Daily Brief 消费 = 加载快照，不改 renderer / 通知格式 |

## Plan 修改

- §7：配置契约改为只加 `variant`，`objective` 不变；
- §8.2：Sell Call 独立扫描输入来源写明 `portfolio_ctx.stock`，无持仓 → not_applicable；
- §8.6：snapshot 消费边界写明不改 renderer；
- §9 Slice 2：config 默认值/validator 只加 `variant`，不改 objective 相关；
- §9 Slice 1：`strike_order` 参数化（Finding 1 修复点）。

## 结论

plan review findings 全部裁决，blocking open question 已由用户收敛（方案 A），plan 已修复。进入 re-review。
