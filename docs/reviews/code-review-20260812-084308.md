# Code Re-review

## Scope

- Mode: current changes re-review
- Branch: `fix/candidate-brief-evidence-integrity`
- Base: `8a3520f0` (accepted plan commit)
- Prior reviews: `docs/reviews/code-review-20260812-083924.md`,
  `docs/reviews/code-review-20260812-084107.md`
- Fix artifact: `docs/gateflow/candidate-brief-evidence-integrity/slice-1-review-fix.md`
- Output file: `docs/reviews/code-review-20260812-084308.md`
- Included scope: complete Slice 1 diff, both accepted finding fixes, implementation/review artifacts, and focused tests
- Excluded scope: approved Slice 2; unrelated primary-worktree changes; runtime/release/deploy
- Parallel review coverage: 无

## Finding dispositions

### CR-S1-01 — accepted — 已修复

Concrete nested reject diagnostics are collected in tiers across all rejects in one contract scope. A generic
top-level sibling no longer competes lexicographically with a nested reason. Both reject orders are covered.

Final status: `已修复`.

### CR-S1-02 — accepted — 已修复

All unresolved-only contract reasons are now aggregated by `(symbol, mode)` before the reason is selected. The
lexicographically first code from the full per-scope set replaces only a generic strategy reason. Both contract orders
are covered, and a specific strategy-owned reason still cannot be overwritten.

Final status: `已修复`.

## Findings

未发现新的实质性问题。

## Validation

```text
./.venv/bin/python -m pytest -q tests/test_scan_volume_gate_min_zero.py tests/test_candidate_scanning_evidence.py tests/test_opening_candidate_snapshot.py
```

Result: `39 passed`.

`git diff --check`: passed.

Reviewed the full source/test diff against Candidate Engine's closed reject vocabulary, the calculation error path,
scope sealing, universe projection, mixed unavailable/definitive outcomes, and strategy-specific reason preservation.

## Open Questions

- 无。

## Residual Risk

- Future definitive calculation reasons remain fail closed and are `assigned to later work unit`.
- Daily Brief consumption is `covered by later approved Slice 2`.
- Full related suite, compile, and analyzer checks are `covered by aggregate DeepReview`.

## Conclusion

`pass`

Both accepted findings are fixed with deterministic regressions. Slice 1 is ready for its protected commit.
