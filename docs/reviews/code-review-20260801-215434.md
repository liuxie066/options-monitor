# Code Re-review

## Scope

- Prior review: `docs/reviews/code-review-20260801-215326.md`
- Base: `main@c3853c08`
- Branch: `codex/fix-opend-runtime-boundary`
- Work unit: `om-opend-runtime-boundary`
- Result: pass

## Finding Resolution

### DR-01 - 已修复 - 高 - Persistent profile-backed sampling now requires a runtime root

For `source=opend` plus `write=True`, a supplied profile that cannot resolve `runtime_root` now raises `CONFIG_ERROR` before the application service is called. Profile-free local/developer invocations retain the historical repo-root fallback. A regression test confirms the fail-closed exception and zero service invocation.

## Verification

- Focused suite: `233 passed`.
- Ruff changed-file check with `--no-cache`: pass.
- `git diff --check`: pass.

## Residual Risk

- Provider-side OpenD throttling remains possible and is intentionally represented as a bounded deferred run.
- Direct non-CLI callers retain opt-in responsibility for passing an external runtime root.

## Decision

Implementation review passes with no unresolved high, critical, or accepted medium findings.
