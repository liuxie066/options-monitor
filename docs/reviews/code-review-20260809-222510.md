# Code Review

## Scope

- Mode: current changes aggregate re-review
- Branch or PR: `feat/ai-decision-advice`
- Base: `8320cabe` (`feat(ai-advice): accept drift remediation S3`)
- Prior review: `docs/reviews/code-review-20260809-222006.md`
- Output file: `docs/reviews/code-review-20260809-222510.md`
- Included scope: complete S4 Advice schema/input, fact registry and exact-scope
  validator, quality ceilings, shared repair budget, anonymous reference and
  reuse behavior, prompt changes, review fixes and deterministic tests
- Excluded scope: S5 collector/evidence authority, S6 typed orchestration and
  Daily Brief integration, provider network calls, release/deployment
- Parallel review coverage: 无

## Findings

No material findings remain in S4.

Both prior findings are fixed and covered by deterministic regressions:

- legacy-only PM context hashes cannot masquerade as new portfolio-distribution
  bindings at the Advice entrypoint;
- `repair_attempted` now reports an actual second model call, including at the
  sub-second shared-budget boundary.

## Open Questions

- 无。The legacy dataclass fields and broken production caller are explicitly
  assigned to S6 rather than hidden behind an S4 compatibility adapter.

## Residual Risk

- The expanded AI Decision Advice suite reports `166 passed, 3 failed`. All
  three failures are the unchanged S6 handoff: `orchestration.py` still invokes
  `build_frozen_inputs()` with removed `portfolio_context` / `position_lots`
  arguments and still passes the retired `context_complete` boolean.
- S5 has not yet completed the collector/evidence authority cutover, and S6 has
  not yet integrated the accepted Advice result into the production tick and
  receipt path. The branch remains non-mergeable and non-releasable.
