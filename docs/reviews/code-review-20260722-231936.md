# Code Review

## Scope

- Mode: current changes
- Branch or PR: `feature/option-notification-experience`
- Base: accepted S1 commit `66f1296f`
- Output file: `docs/reviews/code-review-20260722-231936.md`
- Included scope: S2 lifecycle classification, quote/fetch/event routing, single business-date propagation, diagnostic row construction, public read/analysis projection, contract documentation, lifecycle regressions, and the mechanically regenerated dependency graph
- Excluded scope: accepted S1 fee implementation; strategy thresholds/actions/ranking/notification selection; config/state/broker writes; release/deploy; unrelated untracked `docs/reviews/plan-review-20260722-225255.md`
- Parallel review coverage: 无；the slice is one bounded lifecycle-to-I/O/evaluation call chain and was reviewed end-to-end

## Findings

未发现实质性问题。

The adversarial pass checked:

- canonical-expiration precedence and malformed/missing expiration behavior;
- calendar-day DTE boundaries for `active`, `expiry_day`, and `expired_open`;
- quote-provided DTE cannot promote an `unknown` position;
- required-data planning, OpenD fallback, and event enrichment receive active positions only;
- lifecycle rows do not consume loaded quote artifacts or enter normal strategy evaluation;
- lifecycle rows remain `not_evaluable` through action mapping, combo synthesis, rendering, and notification selection;
- active rows retain the existing strategy evaluator, fee gate, action mapping, and quote diagnostics;
- the run reads `expiration_business_today()` exactly once and passes that date into both DTE and event enrichment;
- CSV, `close_advice_read`, and analysis snapshot surfaces preserve `position_lifecycle_state`;
- tests with historical 2026 fixtures now freeze their intended business date instead of depending on wall-clock time.
- the generated dependency graph changes only test import-edge counts; production/script edge count, boundary status, and cycle count are unchanged.

## Open Questions

- 无。

## Residual Risk

- Lifecycle classification intentionally does not infer exercise, assignment, called-away, or settlement. An `expired_open` row remains a diagnostic data inconsistency for the existing reconciliation/ledger path.
- Malformed canonical expiration is preserved in the diagnostic row for evidence while DTE remains null; no repair or fallback source is inferred.
- Focused validation passed (`179 passed, 2 existing deprecation warnings`). The full offline suite also passed after regenerating the stale dependency graph (`3002 passed, 10 skipped, 6 existing deprecation warnings`).
