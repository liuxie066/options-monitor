# Code Review

## Scope

- Mode: current changes re-review
- Branch or PR: `feat/ai-decision-advice`
- Base: `fa5f09aa` (accepted plan checkpoint; workspace S1 diff only)
- Output file: `docs/reviews/code-review-20260809-202458.md`
- Included scope: complete S1 diff after fixes, with focused re-review of mapped-account identity, exact PM query/response scope, freshness enum handling, provider config ownership, YAML passthrough, and regression tests
- Excluded scope: S2 prepared artifacts and row normalization; live PM calls; Candidate Engine behavior; release/deployment
- Parallel review coverage: 无。

## Findings

未发现实质性问题。

Initial review findings are closed:

- DR-S1-01: 已修复。Mapped PM account 保留配置的大小写和内部空格，response
  scope 改为 exact single-account binding。
- DR-S1-02: 已修复。Freshness/trust 只接受 vendored contract 的 canonical enum，
  distribution 和 valuation 都有非法大小写回归覆盖。

## Open Questions

- 无。

## Residual Risk

- Vendored PM OpenAPI 仍未声明 `by_asset` 行级结构和 `value` 的 CNY 单位；
  当前 consumer contract 已显式标记，producer schema 强化由独立跨仓 work unit 负责。
- 未连接真实 PM 服务；本切片通过固定协议 fixture 和 vendored OpenAPI 完成本地验证。
- Repo `.venv` 缺少 pytest；聚焦套件使用系统 Python pytest 运行。
