# Plan Review

## Reviewed target and scope

- Work unit: `data-storage-runtime-projection-p3a`
- Target: S4 amendment in
  `docs/gateflow/data-storage-runtime-projection-p3a/plan.md`
- Confirmation artifact:
  `docs/gateflow/data-storage-runtime-projection-p3a/s4-scope-amendment.md`
- Scope: the additional permission to modify
  `position_projection_runtime.py` for caller-owned transactions, input-order
  idempotency flags, and exact diagnostic passthrough
- Excluded: S4 implementation code, S5 migration/activation, schema changes,
  live data, configuration, deployment, and release
- Artifact path: `docs/reviews/plan-review-20260814-074509.md`

## Assumptions tested

1. The S3 runtime already owns fast/full selection and has one in-transaction
   core; the amendment does not require a second projector or publisher.
2. Public writer facades with adjunct Combo/lifecycle/outbox writes must keep
   one caller-owned transaction, so calling the self-committing S3 entrypoints
   is not safe.
3. Existing writer results expose projection diagnostics. Every current
   diagnostic producer emits severity `error`; therefore successful results
   carry the exact empty list and diagnostic-bearing projections fail before
   publication. Replaying the prefix to reconstruct the empty result would
   restore the O(E) duplication that S4 exists to remove.
4. Fast and checkpoint-backed projection is already eligible only with an
   empty diagnostic list, matching the full oracle's successful contract.
5. A mixed existing/new batch needs one created flag per submitted event, in
   input order; a single aggregate count is insufficient for current facade
   idempotency and outbox behavior.
6. Transaction ownership stays unambiguous if the narrow entrypoint requires
   an active SQLite transaction and performs no commit/rollback itself.

## Findings

未发现实质性问题。

The amendment resolves the concrete file-ownership and contract gap without
adding a new layer. Its required assertions cover the strongest failure modes:
partial commit, idempotent retry drift, diagnostic loss, and hidden prefix
replay.

## Open Questions

- 无。

## Residual Risk

- Full fallback remains O(E) by design and must be reported separately in S5;
  this is an accepted safety path, not an S4 regression.
- Checkpoint mode remains disabled until S5 evidence and explicit operator
  activation; S4 tests must enable it only on temporary stores.
- Facade integration remains subject to S4 DeepReview and inventory closure.

## Lens conclusions

- Goal-bound minimal design: pass; every added contract is required by the
  accepted atomicity, compatibility, or O(tail) success signal.
- Architecture boundary: pass; projection semantics remain in domain/runtime,
  and facade transactions remain application-owned.
- Best practice and optimal solution: pass; reuse of the existing runtime core
  is smaller and safer than callbacks, nested transactions, or facade-local
  replay.
- Overengineering and overcoupling: pass; one narrow entrypoint replaces
  cross-facade private calls and duplicate replay without new configuration or
  persistence entities.

## Conclusion

`pass`
