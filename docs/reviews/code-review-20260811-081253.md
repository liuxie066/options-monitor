# Code Review

## Scope

- Mode: current changes
- Branch: `design/combo-yield-remove-staggered`
- Base: `c9238062`（accepted S2 commit + main `afd670b8` clean merge）
- Output file: `docs/reviews/code-review-20260811-081253.md`
- Included scope: S3 slice 全部未提交改动（21 个文件，+108/-641）——通知/渲染/summary 清理、
  research/shadow/strategy-lab 同期化、performance attribution set 调整、文档对齐、测试重写；
  以及本轮 code review 已应用的 removed-gap fail-closed 修复与其回归测试。
- Excluded scope: 已接受的 plan/S1/S2 commits（`5461d566`、`fe967db9`、`e8fcd036`、`e7d18b12`）；
  ledger/生命周期保留面（Q2 用户决策：`combo_yield_lifecycle.py`、`trades/resolver.py`、
  `positions/combo_pairing.py`、`combo_reconciliation.py` 对 `structure_mode=staggered_expiry_pair`
  历史输入的兼容面）；`docs/plans/`、`docs/reviews/` 历史引用（不改）。
- Parallel review coverage: 无

## Findings

### 1-已修复-低-`combo_research_policy_from_dict` 静默忽略已移除的错期 gap 字段

- **入口/函数**: `combo_research_policy_from_dict`（由 `parse_combo_variant_spec` /
  `normalize_combo_variant_spec` 调用，research/shadow variant 配置入口）
- **文件(行号)**: `src/application/shadow_replay/combo_variants.py:99-109`（修复后）；
  修改前同一函数直接构造 `ComboYieldResearchPolicy`，无 removed-fields 检查
- **输入场景**: research/shadow 配置的 variant 仍携带 `min_expiry_gap_days` /
  `target_expiry_gap_days` / `max_expiry_gap_days` 残余字段
- **实际分支**: 修复前删除 gap 字段解析后，这三个字段被静默丢弃、构造成功（silent no-op）；
  修复后 `removed_gap_keys` 非空即 `raise ValueError`
- **预期行为**: 与 `docs/STRATEGY_ARCHITECTURE.md:186`「配置校验会显式报错（fail closed），
  而不是静默忽略或改变语义」及 plan §8 Decision 3 的 removed-fields 先例一致
- **实际行为**: 修复前静默忽略，语义与文档矛盾；修复后显式 `ValueError`
- **直接证据**: 修改前 `combo_research_policy_from_dict` 不读取/不消费三个 gap 字段
  （`yield_enhancement.py` 中三个字段已随 S2/S3 删除，无任何消费者）；修复后
  `combo_variants.py:100-109` 先收集 `removed_gap_keys` 再抛错
- **影响**: 配置漂移被隐藏，旧 shadow 配置可能带着失效语义继续跑；仅局部行为错误，低严重度
- **建议改法和验证点**: 已实施——显式 `ValueError`；
  `tests/test_combo_yield_research.py:225-237` 新增
  `test_combo_research_policy_rejects_removed_expiry_gap_fields`，对三个字段逐一断言 `ValueError`；
  验证：`test_combo_yield_research.py` 17 passed
- **修复风险（低/中/高）**: 低（仅残余/非法配置触发；仓库示例配置已不含这些字段）
- **严重程度（低/中/高/严重）**: 低
- **裁决**: accepted → 已修复（fix 已应用并 re-review 通过）

### 2-已拒绝-中-`strategy_attribution` 移除 `staggered_expiry_pair` 后历史错期持仓失去顺序校验

- **入口/函数**: `strategy_attribution._build_topology`
- **文件(行号)**: `domain/domain/performance/strategy_attribution.py:142-146`
- **输入场景**: 审查者担忧历史持仓中可能存在 `expiry_structure=staggered_expiry_pair` 的
  attribution bucket，使 `invalid_diagonal_expiry_order` 校验失效
- **实际分支**: 修改前 `{diagonal, staggered_expiry_pair}` 共享顺序校验；修改后仅 `{diagonal}`
- **预期行为**: 无 writer 产生 `expiry_structure=staggered_expiry_pair`，删除不构成回归
- **实际行为**: 拒绝该 finding（rejected-with-reason）
- **直接证据**:
  - 全仓 `rg "staggered_expiry_pair"`：所有写入点均写 `structure_mode`（
    `src/application/trades/resolver.py:44,719,747,766,786,957,972`；
    `domain/domain/combo_reconciliation.py:447`），无任何代码写
    `expiry_structure=staggered_expiry_pair`
  - ledger 归一化：`domain/domain/combo_yield_lifecycle.py:27-35` `_expiry_structure`
    将 `structure_mode=staggered_expiry_pair` 归一化为 `diagonal`
  - 生命周期顺序校验仍生效：`combo_yield_lifecycle.py:162` 对 `diagonal` 执行
    `call_expiration <= put_expiration → invalid_diagonal_expiry_order`
  - trades intake 自身 guard：`resolver.py:836-838` 对冲突 `structure_mode` 拒绝受理
- **影响**: 无实际回归；该删除符合 plan §8 Decision 5 与 S3 验收
- **建议改法和验证点**: 不修改
- **修复风险（低/中/高）**: 不适用
- **严重程度（低/中/高/严重）**: 中（担忧项，已被证据驳回）
- **裁决**: rejected-with-reason（证据链完整，无回归）

## Open Questions

- 无

## Residual Risk

- 历史 shadow artifact 兼容：`capture._combo_pair_group_id`（S3 改动）统一以
  `expiration/exp` 作为组合分组字段，旧错期 shadow trace 若重新 capture/settle 会按同期
  分组与结算语义处理（`combo_settlement` call horizon 恒为 `put_expiry`）；目前无生产路径
  生成该数据，属研究侧兼容面。分类：assigned to later work unit（不阻塞本 slice）。
- 验证范围：修复后已跑 `test_combo_yield_research.py`（17 passed）与 10 文件组合
  （含 performance attribution / lifecycle / positions context，215 passed）；
  完整 S3 套件在修复前为 238 passed，提交前将复跑全量确认。
- 测试环境：`.venv/bin/python` 无 pytest，使用
  `PYTHONPYCACHEPREFIX=/tmp/om_s3 python3.12 -m pytest`。
