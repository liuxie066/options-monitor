# Code Review

## Scope

- Mode: current changes，完成全部修复后的独立 adversarial re-review
- Branch: `main`
- Base: `HEAD@b07b4aad`
- Output file: `docs/reviews/code-review-20260811-032005.md`
- Included scope: `strict_profit_capture.v1` domain policy；Close Advice 持仓、冻结行情、费用和 report manifest 证据链；scheduled Daily Brief 消费；Agent read/materialization/Assistant renderer；Shadow Replay strict decision/mark/outcome/readiness；Candidate Trace 隔离；Position Advice v2 控制面、策略和服务入口删除；配置拒绝/弃用边界；活动文档与依赖图
- Excluded scope: 生产 runtime 数据、真实通知发送、交易/ledger 写入、发布、部署、远端升级、历史 release artifact，以及 `docs/plans/`、`docs/gateflow/` 和既有 review artifact
- Preserved unrelated files: `docs/plans/controlled-production-upgrade-design-20260810.md`、`docs/reviews/code-review-20260810-231115.md`
- Parallel review coverage: 无；本次按用户要求在同一工作树上逐轮 review、修复并重新 review

## Findings

未发现实质性问题。

## Verification

- 全量测试：`4687 passed, 10 skipped, 7 warnings in 75.40s`
- Close Shadow / Strategy Lab 聚焦回归：`65 passed`
- Ruff：`./.venv/bin/ruff check domain src tests scripts` 通过
- Compile：`python -m compileall -q domain src tests scripts` 通过
- Dependency graph：`production_modules=586`、`cycles=0`，生成结果与当前源码一致
- Config：US/HK `validate` 通过；US/HK `build --dry-run` 通过且 `write_applied=false`
- Patch hygiene：`git diff --check` 通过
- Residue audit：活动 source/docs 中未发现旧 v2 action、Position Advice runner/authority、Close reallocation、多策略 Close policy 或 `function=close_advice` Candidate Trace producer
- Compatibility audit：`position_advice` 字样仅在两个必须稳定的 wire schema、旧配置拒绝校验和明确的“已移除”文档说明中保留
- Operations audit：未修改 `VERSION`、`CHANGELOG.md`、`config.yaml`、`config.us.json`、`config.hk.json` 或 runtime output；未发送通知，未写交易/ledger，未发布、部署或升级

## Open Questions

无。

## Residual Risk

- Close 提醒仍只随现有 scheduled Daily Brief 发送；没有新增 Close-only 即时通知状态机。
- 两个兼容 wire identifier 保持原值，只用于 portfolio scope fingerprint 和 sealed source receipt；不对应可执行 v2 策略或控制面。
- 缺少成功 Close report manifest 或冻结父证据哈希的历史报告不会进入正式 Shadow Replay；需要由新严格版本生成完整 run evidence。
- 本轮只完成源码和本地验证，没有 commit、push、release、deploy、远端升级或生产写入。
