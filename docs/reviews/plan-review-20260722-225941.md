# Plan Re-review — Close Advice Bug-Only Safety Fixes Revision 2

## Reviewed target and scope

- **Target**: `docs/gateflow/close-advice-safety-boundaries-plan-20260722.md`, revision 2 after plan-review fixes
- **Prior review**: `docs/reviews/plan-review-20260722-225836.md`
- **Repository baseline**: `feature/option-notification-experience@e47a4aa1`
- **Review mode**: `$planreview` re-review
- **User boundary**: fix demonstrable bugs only; no strategy, threshold, ranking, priority, or notification change

## Assumptions retested

1. Fee reliability labels do not claim facts absent from the current input contract.
2. Canonical expiration and one business date are the only lifecycle/DTE authority.
3. Unknown, expiry-day, and expired-open rows cannot enter ordinary action evaluation.
4. Fee estimates may gate safety without introducing a new fee config/provider abstraction.
5. S1/S2 remain independently reviewable and strategy/product work remains deferred.

## Finding re-review

### PR-20260722-R2-01-已修复-高-美国期权费率缺少账户平台套餐事实却被标记为 exact
- **位置**: 计划 4.2 fee status/basis rules、risk table
- **问题类型**: 契约缺失 / 最佳实践偏离 / 不可直接实施
- **当前写法**: USD 结果已改为 `schedule_estimate`，basis 明示 `futu_us_fixed_package_2026-07-22`；计划明确当前输入没有 account platform-package fact，因此不得标为 exact。
- **反例/失败场景**: tiered package 账户不再被错误描述为 exact；本 work unit 仍使用 fixed-package assumption，但 public evidence 可识别该假设。
- **为什么有问题**: 原问题已通过真实性标签和 residual-risk destination 收敛，无需在 bug fix 中新增账户 fee-package 配置。
- **直接证据**: 计划 `4.2` 将状态枚举改为 `schedule_estimate` / `conservative_estimate`，并将 actual account-package integration 分配到后续 work unit。
- **影响**: 已消除 public diagnostic 夸大精确性的 blocker。
- **建议改法和验证点**: 实现测试必须断言 fixed-package USD 不会标为 exact。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 高

### PR-20260722-R2-02-已修复-高-unknown lifecycle 仍可能通过 quote dte 回退进入普通策略评估
- **位置**: 计划 4.4 lifecycle table、4.5 single-date invariant、S2 steps/tests
- **问题类型**: 状态机漏洞 / 契约缺失 / 测试缺口
- **当前写法**: `_calc_dte(expiration, business_date)` 明确不接收 quote、不读时钟、不回退 quote DTE；unknown lifecycle 稳定投影为 existing `not_evaluable` contract。
- **反例/失败场景**: missing/malformed canonical expiration 即使 quote 带正 DTE，也不能进入 active evaluation；计划已要求对应回归测试。
- **为什么有问题**: 原绕过路径已在函数签名、状态表和测试三处关闭。
- **直接证据**: 计划 `4.4` unknown row、`4.5` 第一条 invariant、S2 exact change 4 和 test 6。
- **影响**: canonical lifecycle authority 已可直接实施和验收。
- **建议改法和验证点**: 实现时保留反例测试，并验证 active row 行为不变。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 高

## New findings

无新的 material findings。

## Open questions

无。

## Residual risks and tracking destination

- USD actual account platform package unknown: correctly exposed as `schedule_estimate`; account-package integration belongs to a later fee-authority work unit.
- HK instrument tariff class unknown: correctly exposed as Tier-1 `conservative_estimate`; instrument metadata belongs to a later work unit.
- Expiry-day/expired reconciliation is not notified or automated: explicitly assigned to later strategy/product and operations work units.
- Midpoint execution risk remains unchanged and belongs to later replay/strategy evaluation.

All residual risks are classified and none blocks the bug-only implementation.

## Final plan review conclusion

**pass-with-risks**

The plan is code-generation-ready for the two approved bug slices. Its remaining risks are explicit estimate/lifecycle limitations with named later destinations, not hidden strategy changes.
