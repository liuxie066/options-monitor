# Code Review

## Scope

- Mode: current changes after latest-main propagation
- Branch or PR: `refactor/retire-ai-decision-advice` / Draft PR #150
- Base: `origin/main@beb0836562c269e78a5f540659d20f76ee71e3d0`
- Output file: `docs/reviews/code-review-20260812-234102.md`
- Included scope: the complete retirement diff after rebasing over candidate evidence-integrity PR #149; Daily Brief normalization, digest compatibility, persistence, rendering, Agent reads and frozen-delivery retry behavior; Tick input and notification plumbing; config and service contracts; provider and portfolio boundaries; tests and current operator docs
- Excluded scope: merge, release, deployment, runtime config/service/secret/data mutation, historical artifact deletion, live notification sending, and unknown external private importers
- Parallel review coverage: none; the review followed the owning source, persistence, retry, service and retained-provider paths directly

## Findings

No actionable findings.

## Latest-main conflict audit

- Preserved PR #149's sealed candidate evidence validation and the specific `term_matched_rv_unavailable` hard-evidence reminder in both renderer behavior and regression coverage.
- Removed PR #149's new Advice-only unavailable-copy branches and tests because the accepted work-unit boundary retires that product surface completely.
- Kept `docs/AI_DECISION_ADVICE_DESIGN.md` as the current retirement tombstone instead of restoring the former live design. Its production cutover warning remains explicit.
- Deleted the main-updated Advice renderer and its exclusive test rather than retaining an unreachable compatibility implementation.
- Range comparison against the pre-rebase branch showed the intended five-commit series retained; differences are confined to the conflict-resolved latest-main intersections.

## Adversarial checks

- Current Brief normalization strips both retired fields before persistence, diffing, Agent reads and rendering. Raw overlay values are reattached only to calculate an exact historical digest candidate; they cannot enter the current normalized object.
- Frozen pending or ambiguous deliveries are classified from the same raw revision used for digest validation and from the exact frozen text/card payload. A legacy Advice payload is neither sent nor advanced; clean accounts remain independently eligible.
- Root YAML, US/HK market YAML and runtime JSON reject the exact retired key. Nearby unknown keys preserve the generic validation contract.
- Current service rendering emits no Collector service/timer and no Tick-side DeepSeek credential binding. Read-only drift treats previously installed Collector units as extras without generating an apply operation.
- Candidate Engine, the sealed opening-candidate snapshot, Close Advice, SQLite option-position context, generic portfolio distribution reads and Assistant-owned LLM credentials remain at their existing owners.
- Active-reference search found only the deliberate historical normalizer/retry marker, exact config rejection, negative regressions, current retirement docs and immutable release history.

## Validation

```text
latest-main focused integration suite: 485 passed
sandbox-safe full suite: 4459 passed, 10 skipped, 5 existing deprecation warnings
localhost-only HTTP quality suite: 4 passed
ruff check .: passed
compileall domain src tests: passed
dependency graph: current; production_modules=568; cycles=0
US/HK example config validate and build --dry-run: passed
git diff --check: passed
origin/main is an ancestor of HEAD; five intended commits precede this review artifact
```

The dependency graph was regenerated after PR #149 added test imports. The production-module count and zero-cycle result are unchanged.

## Open Questions

- None.

## Residual Risk

- **requires separate explicit authorization**: deployed configs and installed Collector service/timer units remain until a controlled production reconcile.
- **requires separate destructive-data authorization**: historical Advice/evidence artifacts remain on disk.
- **outside repository visibility**: unknown private consumers importing deleted internal Python modules receive no compatibility shim, by design.
- **preserved current behavior**: candidate evidence gaps introduced by PR #149 remain visible and continue to fail closed where required.

## Conclusion

`pass`. The latest-main source diff is ready for the Draft PR update gate.
