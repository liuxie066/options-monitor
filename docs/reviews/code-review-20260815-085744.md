# Code Review

## Scope

- Mode: current changes（W1B 未提交实现，严格只读；未改生产/测试、未 stage/commit/push）
- Branch or PR: `feat/sell-put-top1-w1b`，worktree `/private/tmp/options-monitor-sell-put-top1-w1b-20260815`
- Base: `f39ede44`（HEAD，`chore(strategy-lab): update w1b baseline from main`，已含 PR #156/#157 merge `9b29e05b`）；review target = 相对 HEAD 的全部 working-tree changes（M constraints×2、requirements/runtime.txt、DEPENDENCY_GRAPH.md、dependency_graph.mmd、product plan doc、test_strategy_lab_top1_architecture.py + untracked contracts.py/economics.py/statistics.py/test_strategy_lab_top1_w1b.py）
- Output file: `docs/reviews/code-review-20260815-085744.md`
- Included scope: 严格 ExperimentSpec/hash 投影、behavior binding 12 字段、费用 owner 调用绑定与 fail-closed、point→day 聚合、20/40 日动态 Student-t 与 gate precedence、输入不变性、类型/边界、过度设计/纯逻辑范围、测试同源自证检查；规范来源 `docs/gateflow/sell-put-top1-w1b/plan.md` + `plan-fix.md`
- Excluded scope: 未跟踪 `.venv` symlink（他人/既有，指向根仓 venv，仅为本机跑只读检查使用，不纳入任何 artifact/commit）；clean-install venv 验证（plan 授权但需临时环境安装，属开发环境变更）；full-suite 全量重跑
- Parallel review coverage: 无；主 reviewer 逐行读取全部 4 个新文件真实内容与 tracked diff，并对 26 个 adversarial 探针实跑

## 验证事实（实跑，非符号名猜测）

- Focused（plan Validation 命令原样）: `test_strategy_lab_top1.py + test_strategy_lab_top1_w1b.py + test_strategy_lab_top1_architecture.py + test_candidate_engine_contract.py + test_candidate_engine_parity.py + test_fee_calc.py` → **148 passed**
- Ruff（3 模块 + 2 测试文件）→ `All checks passed!`
- `scripts/generate_dependency_graph.py --check` → `[OK] dependency graph current; production_modules=582 cycles=0`
- `pip check` → `No broken requirements found.`；`import scipy` → `1.18.0`（与 constraints pin 一致）
- `git diff --check f39ede44` → 通过
- Adversarial 探针（26 项实跑，全部符合预期）: spec 12 项（空格 statement、大写 profile、大写 SHA、`data//x.json`/`./x.json` 路径、非 Z 时间戳、非 canonical 日期、bool timer、尾部空格 account、`72.0` float、第二个 baseline id）全部 fail-closed；behavior binding 4 项（空值/padded/缺键拒绝，custom Mapping 接受符合 generic builder 合同）；economics 4 项（None plan facts → not_evaluable、no-fill 额外键/错 stage 结构性拒绝、spy 负 fee 拒绝）；statistics SciPy block 在新解释器（scipy 未预导入）实跑确认 `statistics_backend_unavailable` 路径真实可达
- Economics 手算独立复核: `_standard_fixed_hk_stock_fee_components(100.0, 100)` = 29.27；commission-free + platform_fee=0 → 29.27-3-15 = **11.27**，与测试断言一致（非镜像，真实 calculator 复算）
- basedpyright: 根仓 venv 无该模块（`No module named basedpyright`），plan 的类型检查命令在本机不可执行——记 Residual Risk

## Findings

未发现实质性问题。

关键路径走读记录（证据，非 finding）：

1. **ExperimentSpec 严格性**（contracts.py:417-441）: 顶层键集精确匹配 research/validation 两 shape，partial validation group 因键集不等 fail-closed；`deepcopy` 在验证前执行，输入不变性由 `test_experiment_spec_golden_shapes_are_detached_and_accept_all_profiles` 断言（含嵌套 mutation 隔离）。`_fixed()` 用 `type(value) is not type(expected)`，bool/int 与 int/float 混型均被拒（`40.0`、`72.0` 探针确认）。`_sha256`/`_text`/`_iso_date`/`_utc_timestamp`/`_relative_posix_path` 全部 canonical 检查（大小写、空格、`..`/`\`/空段/dot 段、非 Z 时间探针确认）。
2. **Behavior binding 12 字段**（contracts.py:154-161, 176-198）: 哈希载荷 = schema_version + 12 字段，与 plan JSON 一致；`build_behavior_binding` 为 generic builder（接受任何非空 canonical 版本值，允许 drift 测试改值），`validate_experiment_spec` 经 `_validate_baseline`（contracts.py:223-225）重算并比对 supplied hash，伪造 hash 测试（`forged_behavior`）确认拒绝；12 字段逐一改变均改变 digest（测试 `test_behavior_binding_has_exact_domain_and_every_field_changes_digest`）。
3. **Hash 投影**: `build_research_spec_sha256` 重验证任一 shape 后仅投影 9 个 research 键（contracts.py:443-446），identity（topic/experiment/account）与 validation-only 字段改变不影响 digest（测试确认）；`build_validation_spec_sha256` 要求 validation-ready、lowercase SHA、非 baseline 且在 variants 中的 challenger（contracts.py:449-485），哈希 10 个键与 plan 一致；两函数均不写回输入、不授权 phase。
4. **费用 owner 绑定与 fail-closed**（economics.py:170-176）: W1B 自身调用 `calc_futu_hk_terminal_fee("assignment"|"expired_worthless", order_price=strike, shares=multiplier, contracts=1, account_fee_plan=fee_plan)`；spy 测试证明绑定参数精确且无 caller 注入金额；plan 载荷仅做子集键检查（extra 键结构性 ValueError），None/partial/错类型 allowed facts 原样传 owner，由 owner fail-closed → `not_evaluable/required_outcome_missing/expiry_fee_unavailable`，`estimated_amount` 永不替代（economics.py:201-215）；owner 结果契约校验（HKD、schedule_version 等于当前常量、complete bool、reason canonical、complete→basis=estimated、amount finite 非负）齐备，schedule drift spy 测试确认 ValueError。
5. **point→day 聚合与 gate precedence**（statistics.py）: hard_risk violated → missing → 逐点分类（同候选 0.0 不需 outcome；单侧/缺效率 `official_decision_incomplete` 立即退；concentration 证据缺失 `risk_evidence_missing`；concentration_failed 打标不立即退）→ 日内 fmean 聚合 → `concentration_non_increase_failed`（完整证据后、统计门前，与 plan-fix 澄清一致）→ `effective_days_below_required` → 统计（stdev n-1、se、worst_k=ceil(n·fraction)、scipy lazy ppf、backend 失败无 fallback）→ `non_positive_mean` → `negative_worst_tail` → `positive_mean_lcb_not_above_zero` → pass 三码。决策顺序与 plan "Decision order" 逐条一致；20/40 日手算 fixture（mean/std/se/t 分位 1.729132811521367/1.6848751217112248、worst tail 0.025/0.045）与第二 valid `required_days=3`（t=2.919985580353725）证明无隐藏常数。
6. **输入不变性**: statistics 构造 normalized 新 dict 不触碰输入行（测试 `rows == before`）；economics `dict(fee_plan_mapping)` 复制；contracts deepcopy。
7. **过度设计/纯逻辑范围**: 三模块无 I/O/SQLite/时钟/注册表/外观层；`__init__.py` 保持空（0 字节实测）；import 边界由 `test_top1_core_imports_only_approved_pure_owners` 固化且与 plan Import boundary 逐行一致；scipy 仅 statistics.py lazy import ppf；依赖改动精确 3 行（requirements/runtime.txt + 两个 constraints pin `scipy==1.18.0`），与 plan Dependency decision 一致。
8. **测试同源自证检查**: economics 期望值 11.27 经独立 calculator 复算验证；statistics 期望 t 分位/worst tail 为硬编码已知值；behavior/research/validation hash 测试为差分测试（改字段→digest 变）而非自洽镜像；golden spec 行为绑定 hash 在 fixture 构建期用被测函数生成属必要自引用（validator 重算比对同一公式），但 drift 场景由独立 forged/drift 测试覆盖。未发现同源自证削弱断言。

## Open Questions

- 无。

## Residual Risk

- **Type check gap**: plan Validation 要求 `basedpyright` 三模块检查；根仓 venv 未安装 basedpyright（`No module named basedpyright`），本机未执行该项；安装属开发环境变更需单独授权。Ruff 与 148 focused tests 通过，但不能替代类型检查证据。
- **Clean-install 未验证**: plan 要求临时 Python 3.12 venv 走 `requirements.txt -c constraints.txt` 证明 scipy 1.18.0 可安装；本轮只读 review 未创建临时环境（需网络与本地环境变更）。`pip check` 与 scipy 1.18.0 import 已在现有 venv 确认。
- **Full-suite 未重跑**: 本轮执行了 plan 的 focused 命令（148 passed）；全仓套件未跑，留待 implementation 侧/后续 gate。
- **Runtime / W0R**: 真实 `lx` fee-plan receipt、provider 证据仍归 W0R/W5/W6；W1B 以合成 fixture 覆盖 fail-closed 路径，但不声明 runtime readiness。
- **Serial correlation**: v1 合同有意不调整且始终披露（`serial_correlation_unadjusted=true`），属已接受设计。
