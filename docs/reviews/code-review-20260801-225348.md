# Code Review

## Scope

- Mode: current changes
- Branch: `codex/fix-shadow-replay-integrity-selection`
- Base: `origin/main@8d467282`
- Output file: `docs/reviews/code-review-20260801-225348.md`
- Included scope: status-to-plan integrity projection, write-mode branch
  ordering, quota/circuit semantics, direct collection validation, receipts,
  tests, operator documentation, dependency direction
- Excluded scope: historical manifest migration, provider implementation,
  unrelated Strategy Lab analysis/recommendation behavior
- Parallel review coverage: none

## Findings

### DR-S1-01-已修复-中-关键 integrity 事实生产链没有回归证明

- **入口/函数**: `shadow_replay_dataset_status()` -> `_data_plan_rows()` ->
  `run_shadow_replay_data_plan()` -> `_run_plan_rows()`
- **文件(行号)**: `src/application/shadow_replay/status.py:393-480`;
  `tests/test_shadow_replay.py:1219-1297`
- **输入场景**: 真实 dataset 的 manifest 缺失或有合法 integrity receipt，
  data-plan 从 status 真实入口生成执行行。
- **实际分支**: 新 quota 测试 monkeypatch 了
  `shadow_replay_dataset_status()`，直接注入手写
  `dataset_integrity` 行；它只证明 consumer，没有证明
  `_data_plan_rows()` 的 producer 投影。
- **预期行为**: 至少一个真实 status/data-plan 测试应断言 legacy
  和 verified dataset 的 integrity 状态确实进入 plan/action，使
  consumer 测试的前提受回归保护。
- **实际行为**: 若后续重构遗漏 candidate 或 close 投影，手写
  row 测试仍会通过，而生产执行会把缺失状态视为
  `unknown` 并跳过原本可执行的 verified dataset。
- **直接证据**: 生产投影是 `status.py:397,413,460`；核心新测试在
  `tests/test_shadow_replay.py:1251-1259` 替换了该 producer，并在
  `1225-1249` 手写所需字段。
- **影响**: 核心修复的跨函数契约可在单元测试全绿时退化，
  再次造成样本任务零执行。
- **建议改法和验证点**: 在现有真实 dry-run 状态测试断言
  `legacy_unverified/manifest_missing`；在已 sealed 的真实 write-plan 测试断言
  plan/action 状态是 `verified`。保留 monkeypatch 测试专注配额顺序。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

#### Re-review

- Final status: `已修复`.
- `test_shadow_replay_data_plan_dry_run_is_read_only` now proves the real
  status/data-plan/action chain carries `legacy_unverified/manifest_missing`.
- `test_shadow_replay_data_plan_collects_local_marks_and_receipt` now proves a
  sealed dataset carries `verified` through the same chain.
- Targeted re-review validation: `4 passed`.

## Open Questions

- None.

## Residual Risk

- Direct pre-fetch validation and the later locked writer validation leave a
  narrow existing concurrency window in which another legitimate dataset writer
  could change evidence after the first check. The later locked check still
  fails closed, so the remaining consequence is wasted provider capacity rather
  than an invalid dataset write. This is outside the deterministic legacy
  selection failure and is assigned to a later collection-locking work unit if
  concurrent writers become supported.
- Live OpenD success remains unverified until the authorized production canary.
