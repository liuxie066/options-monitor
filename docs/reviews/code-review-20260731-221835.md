# Code Review

## Scope

- Mode: current changes, aggregate re-review after fixing `docs/reviews/code-review-20260731-215256.md`
- Branch: `post-trade-combo-reconciliation-v1`
- Base: `main@d579683d45f976f38e3ba22b03748fcd1d5a9bff`
- Output file: `docs/reviews/code-review-20260731-221835.md`
- Included scope: the aggregate position-fact remediation and post-trade Combo reconciliation work unit, with focused re-review of lifecycle-backed trade-intake delegation, exact pending-deal identity propagation, runtime status, and quality gates.
- Excluded scope: real OpenD/Futu traffic, real notification delivery, production ledger/config/runtime writes, release, deployment, and remote upgrade.
- Parallel review coverage: none; the final stable workspace was reviewed as one end-to-end work unit.

## Validation

- Focused trade-intake state reconciliation and quality suite: `36 passed`.
- Full suite: `3870 passed, 10 skipped, 6 warnings`.
- `python3.12 -m ruff check domain src tests`: passed.
- `python3.12 scripts/generate_dependency_graph.py --check`: passed, `production_modules=576 cycles=0`.
- `git diff --check`: passed.
- US/HK example configuration validate and build dry-run: all passed with `write_applied=false`.

## Fixed Finding Verification

The high finding in `docs/reviews/code-review-20260731-215256.md` is fixed:

1. Pending trade-intake delegation now consumes `lifecycle_account_coherent_facts()` and accepts only canonical account-resolution cases whose status is `direct` or `bridged`; it no longer maintains a weaker lifecycle/bridge resolver in the trade-intake module.
2. A repository without the coherent lifecycle reader cannot prove delegation. Invalid coherent facts make the read-only preview unavailable and therefore keep quality consumers fail closed.
3. The preview and runtime status now publish the exact `delegated_lifecycle_pending_deal_ids`; the published count is derived from that normalized set.
4. Quality computes an exact set intersection between current pending deal IDs and current preview-proven delegated IDs. Historical intake-state diagnostics and count-only preview fields remain diagnostic and cannot reduce actionable rows.
5. Regression coverage proves that a valid direct anchor and a valid migration bridge delegate, while a missing target manifest, invalid bridge, ambiguous bare deal ID, or state-only historical acceptance does not.

## Findings

No substantive findings.

## Open Questions

None.

## Residual Risk

- This review did not connect to live OpenD/Futu, send a real notification, or mutate production ledger/config/runtime state; provider behavior and live trade-arrival ordering remain observe-canary concerns under separate authorization.
- A malformed coherent lifecycle generation intentionally makes reconciliation preview unavailable and blocks consumers; operators must repair the canonical lifecycle facts instead of relying on stale intake-state diagnostics.
- Matcher correctness is covered by deterministic tests, but production-scale high-density unmatched-pool performance remains an offline replay concern before broad rollout.
