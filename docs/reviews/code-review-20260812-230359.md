# Code Review

## Scope

- Mode: current changes
- Branch or PR: `refactor/retire-ai-decision-advice`
- Base: `main@b85607be9872ba8b3cc7d21db82dafeda416dce9`
- Output file: `docs/reviews/code-review-20260812-230359.md`
- Included scope: post-rebase full work-unit diff; Daily Brief normalization, persistence, rendering, Agent read and delivery retry state machine; Tick preparation and execution; AI-only provider, collector, config and service deletion; portfolio-query preservation; config, service, secret and documentation contracts; related tests
- Excluded scope: release, deployment, runtime config/service/secret/data mutation, historical artifact cleanup, notification sending, and external private importers
- Parallel review coverage: 无；review 沿当前 Brief 写入/读取/重试链、Tick 输入链、配置与 service bundle 链逐项复核

## Findings

未发现实质性问题。

## Adversarial failure and ownership checks

- Current normalization strips both retired Advice fields before persistence, diffing, Agent reads, and rendering; the legacy values are reattached only as an exact immutable digest candidate.
- Pending or ambiguous frozen envelopes are classified from the same raw revision used for source-digest validation plus the frozen text/card payload. A retired payload is never sent or mutated; clean accounts remain independently deliverable.
- Candidate Engine, Close Advice, option-position preparation, generic portfolio distribution queries, and Assistant provider credentials remain at their existing owners. Removed portfolio-distribution preparation and the dedicated DeepSeek Responses adapter have no current production caller.
- Runtime YAML and JSON reject the exact retired key, while nearby unknown keys keep the generic error contract. Service rendering no longer emits collector units or binds DeepSeek credentials to Tick, and read-only drift reports already installed retired units as extras.
- Rebase conflict resolution keeps the v1.13.13 compact fixed-report headings, event copy, and confirmed-source reminder behavior without restoring any Advice-visible family, candidate expansion, or AI status line.

## Validation

```text
post-rebase focused retirement suite: 526 passed
sandbox full suite excluding localhost-only HTTP file: 4445 passed, 10 skipped, 5 existing deprecation warnings
localhost-only quality HTTP file: 4 passed
ruff check .: passed
compileall domain src tests: passed
dependency graph check: current; production_modules=568; cycles=0
US/HK example config validate and build --dry-run: passed
git diff --check: passed
main is an ancestor of HEAD; branch is four intended commits ahead before this review artifact
```

The first post-rebase full run correctly exposed a stale generated dependency graph and the sandbox's localhost bind restriction. The graph was regenerated and rechecked; the only localhost test file passed under bounded local-port permission, followed by the clean split full-suite result above.

## Docs decision

Current operator documentation and the existing design path now describe retirement and preserve the production-cutover boundary. Historical release and review artifacts remain unchanged.

## Open Questions

- 无。

## Residual Risk

- **requiring new issue or explicit user decision**: deployed configs may still contain the retired key, and installed Collector service/timer units may still exist until a separately authorized production reconcile.
- **requiring new issue or explicit user decision**: historical Advice/evidence artifacts remain on disk and are not deleted by this source work unit.
- **requiring new issue or explicit user decision**: unknown external private consumers of deleted internal Python modules cannot be discovered from this repository; no compatibility shim is provided by design.
- **fixed in current slice**: current source, config generation, service rendering, Tick execution, Brief rendering/Agent reads, and notification retry paths cannot generate or expose AI Decision Advice.

## Conclusion

`pass`. The rebased work unit is ready for the accepted main-chain review commit and the Draft PR gate.
