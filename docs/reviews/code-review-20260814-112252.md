# Code Review

## Scope

- Mode: current changes, final S5 aggregate re-review
- Branch or PR: `plan/data-storage-runtime-projection-p3a`
- Base: `75d5dcc1`
- Output file: `docs/reviews/code-review-20260814-112252.md`
- Included scope: complete S5 cumulative diff, DR-S5-01 through DR-S5-03,
  full-suite call-inventory correction, migration/activation/status contracts,
  benchmark time/space gates, CLI safety, docs, and all S5 tests
- Excluded scope: live migration/activation, production stores, release/deploy,
  service/notification/broker changes, and unrelated `.venv`
- Parallel review coverage: none; final review followed the exact source/store/
  benchmark binding and every declared full-replay/write call

## Findings

未发现剩余实质性问题。

The three initial trust-boundary findings are fixed. Ordinary append execution
remains tail- and changed-account-bounded; full O(E + L) work is confined to
explicit migration, verification, rebuild, recovery, research, and special
force-full paths. The exact AST inventory names every such production call.
Runtime telemetry has a fixed 256-sample memory ceiling and adds no per-event
SQLite row. Activation remains impossible without a clean source commit,
matching host/store/schema/implementation/generation evidence, and passing
benchmark plus shadow parity.

## Open Questions

- None.

## Residual Risk

- The permanent event/research authorities still grow by retention policy;
  Phase 3A deliberately does not delete canonical history.
- `retained_lots_10x` is diagnostic rather than guaranteed; its capacity
  warning is the explicit signal to revisit the current-row fingerprint model.
- Process-local telemetry is empty after a fresh CLI process by design.
- Formal exact-host 5-warmup/30-repetition evidence must be generated only
  after the final clean source commit, otherwise source binding must fail.

## Verification Evidence

- Focused S5 migration/benchmark/CLI set: `102 passed`.
- Ledger/research/CLI aggregate set: `297 passed`.
- Migration plus exact call inventory: `20 passed`.
- Final repository suite excluding the sandbox-only localhost bind:
  `4734 passed`, `10 skipped`, `1 deselected`.
- The deselected localhost HTTP test passed outside the sandbox: `1 passed`.
- Ruff, compileall, dependency graph (577 modules, zero cycles), and
  `git diff --check`: passed.
