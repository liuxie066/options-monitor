# Code Review

## Scope

- Mode: current changes re-review
- Branch or PR: `codex/diagonal-combo-yield-lifecycle`
- Base: `main` (`34e56cac`)
- Output file: this file
- Included scope: aggregate `main...HEAD` plus the F1 fix in the working tree; focused re-review of explicit diagonal intake cycle identity/capacity, and regression validation across candidate, intake, lifecycle, assignment reporting, Close Advice, CLI and agent contracts.
- Excluded scope: unchanged production subsystems; generated/runtime outputs; no live broker/notification/config mutation.
- Parallel review coverage: 无。

## Findings

### F1-已修复-高-已关闭的 Put 容量可被同一 residual Call 组再次融资
- **入口/函数**: `resolve_trade_deal()` → `_resolve_combo_yield_companion()`
- **文件(行号)**: `src/application/trades/resolver.py:804-861`, `tests/test_trades_resolver_open.py`
- **修复**: resolver 现在在跳过 closed lots 前审计 group 内全部历史合约数量；`contracts_closed > 0` 或 `contracts_open < contracts` 会标记 group cycle capacity 已消费，任何新 open 都以 `diagonal_combo_yield_cycle_reuse` fail closed。无效 opened/open/closed 数量同样 fail closed。
- **验证**: 新增 residual Call 后第二轮 Sell Put 回归测试；正常 put-first、call-first、restart、aggregate partial-fill、quantity conflict tests 均通过。
- **裁决**: accepted → fixed → re-reviewed；无未修复 finding。
- **修复风险（低/中/高）**: 中；采取 conservative fail-closed，无法证明仍属同一 fill cycle 的“平仓后迟到增量 open”会进入人工复核，而不会污染 immutable group lineage。
- **严重程度（低/中/高/严重）**: 高

## Open Questions

- 无。

## Residual Risk

- Explicit diagonal intent remains required for broker/structured intake; broker-only fills lacking group metadata remain unresolved by design.
- PR CI 尚未运行；本地 aggregate suite、config dry-run 和 import-boundary validation 已通过。
- No future Call residual value, assigned-stock sale thesis, automatic exercise, or production execution behavior was introduced.
