# Code Review

## Scope

- Mode: current changes；修复第二轮 6 项 finding 后的最终 DeepReview。
- Branch: `fix/atomic-run-scoped-account-config`。
- Selected base / frozen HEAD: `cc37b8022e9d13f40ecec32c1c5e2aec267f1b86`。
- Frozen tracked diff SHA-256: `2b23b58a904185f4e8cc9fc22c1d40786a88f9016454aa182e074540499ce03d`。
- Included scope: 29 个 tracked source/test/doc changes；显式账户范围、canonical account config publication、parent/worker/scan-child authority、prepared portfolio generation、双账户 shared planning 与 provider barrier、typed failure projection、YAML authoring、worker lifecycle、路径安全、测试、文档和依赖图。
- End-to-end paths reviewed: CLI/runtime account scope -> Tick workspace -> per-account config publish/load -> prepared workers -> prepared manifest/payload promotion -> cross-account union / Close Advice plan / required-data prefetch -> AccountRun -> event prefetch -> scan-pipeline child -> prepared-context final load；同时覆盖 same-run re-entry、coherent replacement、symlink replacement、partial spawn、absolute deadline 和 unhealthy-account isolation。
- Excluded scope: S1/S2 已交付行为、S4-S7、真实 OpenD/provider、通知、broker、ledger、生产配置或业务数据写入、release/deploy/merge/push，以及 selected base 之后的其他分支改动。
- Review execution: 主 reviewer 对冻结快照执行端到端只读复核；审查阶段源码保持不变，仅新增本报告。

## Findings

未发现实质性问题。

第二轮报告的 6 项 finding 均已闭环：显式空账户不再 fallback；config generation 在 shared/parent/child 间保持唯一 authority；prepared payload 绑定 immutable manifest generation 与 source account；config failure 不再沿 symlink 写出 workspace；YAML init 在 canonical write 前完成统一校验；部分 worker spawn/timeout 不再泄漏或降级健康账户。

最终冻结前额外关闭了 4 个边界反例：timeout/terminate 间退出竞态、child compatibility authority 对末端 symlink 的错误解析、绝对 deadline 后完成结果的误晋升、same-run config failure 对健康 existing generation 的跨账户扩散。

## Open Questions

- 无。

## Residual Risk

- 两份 config artifact 仍是两个独立 filesystem commit point；首份成功、第二份失败时依靠 write-once/same-bytes retry 收敛，而不是跨文件事务。失败账户不会启动 prepared worker 或 pipeline，原字节不会被覆盖。
- Descriptor-anchored reads/writes会拒绝不安全 ancestor；config 在最终 barrier 后使用 parent-retained bytes，prepared generation 若随后漂移则 final consumer fail closed。该实现不提供跨进程 writer coordination，外部 writer 仍可能令对应账户终止。
- 验证未连接真实 OpenD、通知、broker、ledger或生产运行目录；provider 与并发反例均使用临时目录和 fake boundaries。
- 本结论只适用于上述 selected base 与冻结 diff；未对之后进入 `main` 的其他改动做 aggregate review。

## Verification Evidence

- S3 直接回归：`196 passed`。
- 相邻 pipeline / multi-tick / portfolio 回归：`192 passed`。
- 全库 sandbox：`4324 passed, 10 skipped`；唯一失败为 sandbox 禁止 `127.0.0.1` 临时端口 bind。
- 同一 loopback 测试在 sandbox 外单独重跑：`1 passed`。
- Ruff：`All checks passed!`。
- `python3 -m compileall -q domain src`：pass。
- Dependency graph：current，`577` production modules，`0` cycles。
- `git diff --check`：pass。
