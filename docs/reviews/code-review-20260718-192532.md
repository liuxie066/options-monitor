# Aggregate Deepreview Re-review — Python 3.12 Runtime Contract

## Scope

- Mode: current branch aggregate re-review
- Branch: `codex/python312-runtime-contract`
- Base: `origin/main@e128b412`
- Prior review: `docs/reviews/code-review-20260718-192227.md`
- Output file: `docs/reviews/code-review-20260718-192532.md`
- Included scope: all runtime-contract implementation, current operational docs/generator outputs, platform setup hints, CI, and tests
- Excluded scope: historical design documents retained as snapshots
- Parallel review coverage: none

## Finding decisions

| Finding | Decision | Re-review evidence |
|---|---|---|
| `AGG-001` current docs still recommend bare `python3` | accepted/fixed | current setup, deployment, release, agent, README, AGENTS, and dependency-graph guidance now uses `python3.12` only for bootstrap and `./.venv/bin/python` for repo scripts/tests; static runtime-contract test added |
| `AGG-002` upgrade docs/hints still describe `uv --python python3` | accepted/fixed | deployment, release, and tool reference now state that current supported Python 3.12+ `sys.executable` is passed to uv; Linux/macOS platform hints name Python 3.12+ and tests assert it |

## Findings

未发现新的实质性问题。

## Open Questions

None.

## Residual Risk

- Historical design documents may retain old commands as snapshots; current runbooks no longer direct operators to them.
- Arbitrary direct shebang execution remains outside the supported normal path.
- Exact Python 3.12 patch versions remain intentionally unpinned.

## Validation observed

- Updated runtime/platform focused tests: 12 passed.
- Dependency graph regenerated and `--check` passes with 468 production modules and zero cycles.
- Focused Ruff and `git diff --check` pass.
- Repository-wide current-runbook scan finds no remaining `python3 -m venv` or `uv venv --python python3` guidance outside intentional tests/history.

## Conclusion

**pass-with-risks**

Both aggregate findings are fixed. Implementation, current operational guidance, generated documentation, and setup diagnostics now express the same Python 3.12 minimum and interpreter-selection ownership.
