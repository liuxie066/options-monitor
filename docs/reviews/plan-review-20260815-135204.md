# Plan Re-review — Sell Put Top1 W4

- Reviewed target: `docs/gateflow/sell-put-top1-w4/plan.md`
- Prior review: `docs/reviews/plan-review-20260815-134811.md`
- Fix artifact: `docs/gateflow/sell-put-top1-w4/plan-fix.md`
- Review artifact: `docs/reviews/plan-review-20260815-135204.md`

## Finding status

- PR-W4-01: `已修复` — capture now requires a date, validates the exact day expectation artifact, and proves point-ID membership before any point persistence.
- PR-W4-02: `已修复` — one exact-key window-facts object now binds dates, cutoff, calendar/maturity refs and hashes, selector, and recomputed content hash.
- PR-W4-03: `已修复` — public freeze rejects non-40 values and tests use the real 40-date contract.
- PR-W4-04: `部分修复` — command-result keys/status/reasons are exact, but freeze/status output field names are still described as nouns rather than a literal key set.

## Remaining finding

### PR-W4-04R-未修复-中-freeze 与 corpus status 仍没有 literal exact-key contract
- **位置**: §10 Compact status
- **问题类型**: 契约缺失 / 不可直接实施
- **当前写法**: freeze 写成“schema, ready|blocked, reason, identity...”且 status 写成“market/account, total/on-time...”，没有像 command result 一样给出 exact field identifiers。
- **反例/失败场景**: implementation 可以合理地产生 `reason` 或 `reason_code`、`days_total` 或 `total_days`、`dataset_sha256` 或 `dataset_content_sha256` 等互不兼容 payload，测试再跟随任一实现仍会通过文字计划。
- **为什么有问题**: 这是 W5/W7 将直接消费的模块 seam；前次 finding 要求冻结 exact keys，当前修复只完成了一半。
- **直接证据**: 同一 §10 对 command result 明确列出 literal keys，却未对另外两个 schema 使用相同精度。
- **影响**: 调用方返工、测试迎合实现、PR review 无法判断 schema drift。
- **建议改法和验证点**: 列出 freeze result 与 corpus status 的 literal exact-key set，并要求 exact-key tests；不增加新字段或 schema。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

## Open questions

None.

## Residual risks

- Provider truth remains W0R/W5 ownership.
- W7 owns timer ordering and source-retention cadence.
- Artifact-first orphan cleanup remains deferred until measured growth.

## Review conclusion

`fail`

The architecture, scope, two-slice sequencing, recovery, and fixed-window semantics now pass. Fix the one remaining literal schema ambiguity, then perform final re-review.
