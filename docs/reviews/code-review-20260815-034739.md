# Code Review

## Scope

- Mode: fix re-review
- Branch or PR: `feat/sell-put-top1-hk-terminal-fee-contract`
- Base: accepted plan `8b879390`
- Output file: `docs/reviews/code-review-20260815-034739.md`
- Included scope: fix diff in `domain/domain/fee_calc.py`, `domain/domain/assigned_stock.py`, `domain/domain/portfolio_assignment_scenario.py`, and matching regression tests; fix decisions recorded in `docs/gateflow/sell-put-top1-hk-terminal-fee-contract/code-review-fix.md`; re-review of `docs/reviews/code-review-20260815-034135.md` findings plus fix-introduced regressions/overdesign.
- Excluded scope: release/deploy; production write; HK live broker fee re-verification; whole-repo full-suite re-run.

## Findings

### 1-已关闭-中-原始 `amount=2e16` 成分求和不一致不可复现，当前实现满足返回成分求和不变量
- **入口/函数**: `domain/domain/fee_calc.py::f("exercise", order_price=450.0, shares=100, contracts=10**16, account_fee_plan=commission_free_plan)`
- **文件(行号)**: `domain/domain/fee_calc.py:244-291`; `tests/test_fee_calc.py:497-529`
- **输入场景**: 与 `code-review-20260815-034135.md` 相同的超大 `contracts=10**16` 输入。
- **实际分支**: 当前 `amount = round(sum(effective_components.values()), 6)` 后直接返回同一 `effective_components`；结果 `amount=2.0000000000000064e+16`，且 `amount == round(sum(components.values()), 6)` 为 `True`。
- **预期行为**: 在“不增加任意 contracts 上限”边界下，保持返回金额与返回成分求和一致即可。
- **实际行为**: 复现输入下不变量成立，原始报告中的 `amount=2e16` 与实际返回不符。
- **直接证据**: 本机按原始输入重跑结果为 `2.0000000000000064e+16`；`test_hk_terminal_fee_component_sum_matches_amount_and_remaining_quote_gates` 已覆盖成分求和与 `_round_amount` 保留语义。
- **影响**: 该发现按当前证据关闭为不成立；不要求 contracts 上限修复。
- **建议改法和验证点**: 保持当前单一路径求和实现；后续如另设 contracts 上限，应作为独立产品决策而不是本次 defect 修复。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 2-已修复-低-字符串数字 `platform_fee` 现在 fail-closed
- **入口/函数**: `src/application/account_capital_service.py::evaluate_account_fee_profile()` -> `FeeAccountPlan.from_config()` -> `domain/domain/fee_calc.py::_finite_float()`
- **文件(行号)**: `domain/domain/fee_calc.py:139-186, 295-302`; `tests/test_fee_calc.py:113-126`
- **输入场景**: HK fee plan 将 `platform_fee` 配置成字符串数字（如 `"15"`）。
- **实际分支**: `_finite_float()` 先拒绝 `bool/str`，使字符串数字与负数、NaN、Inf 一样进入缺项路径。
- **预期行为**: 非数值型平台费不得被静默当成有效 `0` 参与 terminal fee 计算。
- **实际行为**: `platform_fee="15"` 返回 `complete=False`、`reason=hk_account_fee_plan_missing`、`missing_plan_facts=["platform_fee"]`。
- **直接证据**: invalid-plan 参数已覆盖 `"15"`、`True`、负数、NaN、Inf；`test_hk_terminal_fee_*` 中的缺项回归全部通过。
- **影响**: 修复成立，原 `existing_platform_fee = float(_finite_float(...)) or 0.0` 的静默降级已移除。
- **建议改法和验证点**: 保持 `_finite_float()` 作为共享输入边界；无需再改调用方。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 3-已修复-中-聚合生命周期 summary 在任一成员缺净额时不再发布伪造 0
- **入口/函数**: `domain/domain/assigned_stock.py::summarize_assigned_stock_receipts()` -> `_lifecycle_efficiency_summary()`
- **文件(行号)**: `domain/domain/assigned_stock.py:680-833`; `tests/test_assigned_stock_projection.py:1447-1520`; `tests/test_portfolio_assignment_scenario.py:1686-1740`
- **输入场景**: 聚合口径下部分成员 receipt 能算净额、另一部分因缺 fee fact 只能得到 `lifecycle_pnl_net=None`。
- **实际分支**: 成员行仍逐项保留；新增 `has_unavailable_lifecycle_pnl_net`，任一成员净额缺失时聚合 `lifecycle_pnl_net` 和 `annualized_capital_efficiency` 一并置 `None`。
- **预期行为**: 聚合行不能把缺失净额的成员静默当作 `0` 参与合计。
- **实际行为**: `summary["lifecycle_pnl_net"] is None` 且 `summary["annualized_capital_efficiency"] is None`；成员行仍明确给出 `None`，不会误伤整行可见性。
- **直接证据**: 原直接返回 `None` 的 `summary` 断言已更新；显式 net-zero 场景仍验证 `0.0` 是合法值；focused 45 tests passed。
- **影响**: ROOT-HKF-01 修复成立，聚合误导性 0 已消除。
- **建议改法和验证点**: 无新增问题；维持“行级展示缺失、聚合 fail-closed”这一边界。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

## Open Questions

- 无。

## Residual Risk

- 4754 passed / 1 skipped 的 full-suite 证据来自实施侧 gateflow artifact，本轮未在 review worktree 重跑完整套件。
- W0R 遗留 runtime gap（OpenD FSM 与真实 HK broker fee 再验证）不在本次 fix re-review 范围内。
- 若未来决定限制极端 `contracts` 输入，应单独评审产品边界；当前不构成阻塞。
