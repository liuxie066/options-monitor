# Code Review

## Scope

- Mode: current changes, aggregate DeepReview re-review
- Branch or PR: `refactor/candidate-csv-retirement`
- Base: `origin/main@ded8f882`
- Output file: `docs/reviews/code-review-20260813-023618.md`
- Included scope: fixes for AGG-CR-01 through AGG-CR-03; terminal manifest/latest resolution; Agent filter/rank entry paths; opening decision scope and current-contract validation; Combo Yield owner identity; migrated Daily Brief fixtures; focused tests and documentation
- Excluded scope: production deployment; live OpenD, notification, broker, or runtime writes; integration with remote `main` commits published after the confirmed work-unit base
- Parallel review coverage: 无；the fixes were re-reviewed end to end along their manifest, owner, consumer, and test paths

## Findings

未发现新的实质性问题。

### AGG-CR-01-已修复-高-Agent 候选解释已统一经过 terminal manifest gate

- **复审证据**: `candidate_filter_explain_tool()` and `candidate_rank_explain_tool()` now call only `load_candidate_snapshot_bundle()` or `load_latest_candidate_snapshot_bundle()` before selecting the opening owner. The bundle loader validates the terminal manifest first, the bound v2 status index, exact owner files, file/content/config/policy hashes, exact scopes, terminal status/reason/count, and current opening contract.
- **失败路径**: explicit run without a manifest raises `DEPENDENCY_MISSING`; a newest account run without a manifest fails closed and is not skipped in favor of an older complete run; a bad last-run pointer is not bypassed.
- **验证点**: explicit filter/rank missing-manifest tests, valid terminal filter/rank tests with manifest hash metadata, latest valid bundle test, and newest-incomplete/no-stale-fallback test all pass.
- **最终状态**: `已修复`.

### AGG-CR-02-已修复-高-Opening accepted/rejected decisions 均受精确扫描 scope 约束

- **复审证据**: the sealer derives an allowlist from normalized scan statuses and checks every decision's canonical `(symbol, strategy_mode)` before publication. Full-current validation independently reconstructs the strategy-scope allowlist and applies the same membership check before recomputing scopes, results, and aggregate status.
- **兼容边界**: terminal manifest publication/loading explicitly requires the full current opening contract. The direct loader's minimal v1 allowance remains confined to the pre-manifest historical classification path and cannot be consumed by current Agent, Daily Brief, Tick, or Advice paths.
- **验证点**: cross-symbol rejected decision fails at seal time; a rehashed cross-scope snapshot fails validation; a minimal legacy decision record cannot be committed by the terminal manifest; all 52 Daily Brief scenarios now build current evidence through the production sealer and manifest publisher.
- **最终状态**: `已修复`.

### AGG-CR-03-已修复-中-Combo v2 顶层 owner 身份已严格绑定

- **复审证据**: `validate_combo_yield_candidate_snapshot()` requires `candidate_owner == "sp_lc"` before accepting the content-hashed payload, matching its file/schema owner and nested scope normalization.
- **验证点**: a payload changed to `cc_lp` and rehashed is rejected with owner mismatch.
- **最终状态**: `已修复`.

## Open Questions

- 无。

## Residual Risk

- The source-only aggregate remains unverified against live OpenD and production runtime artifacts; those operations are outside the confirmed work unit and require a separately authorized deployment/verification flow.
- Remote `main` advanced after the work unit's fixed review base. This does not invalidate the finding fixes, but current-base integration must be performed and revalidated before the draft PR passes its own review gate.
- Validation evidence: focused final re-review suite `227 passed`; sandbox-compatible repository suite `4789 passed, 10 skipped, 1 deselected`; the one loopback-bind test passed separately (`1 passed`); Ruff, compileall, dependency graph (585 production modules, zero cycles), US/HK config validate/build dry-run, and `git diff --check` passed.
