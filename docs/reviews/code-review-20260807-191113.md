# Code Review

## Scope

- Mode: current changes（第二轮，复核第一轮修复）
- Branch or PR: `main`
- Base: `main`
- Output file: `docs/reviews/code-review-20260807-191113.md`
- Included scope: 第一轮 findings #1（决策时刻 snapshot 300 秒重验）、#2（scan-status JSON 保留 completed reason）的修复，及修复引发的测试与调用链变更
- Excluded scope: 现场 shadow replay、远端升级、二期 order-book shortlist 复核
- Parallel review coverage: 无

## Findings

未发现实质性问题。

第一轮 findings 复核结论：

- **#1（决策时刻重验）已正确闭环**：`calculate_opening_candidate_metrics` 现在以真实决策时钟对 `snapshot_received_at_utc` 重算 `snapshot_age`，缺失/未来/超 300 秒均 fail closed 为 `evidence_unavailable`；生产链路（`scan_sell_put.compute_metrics`/`scan_sell_call.compute_metrics`）已把决策 `now_utc` 透传到 engine（`scan_sell_put.py` 此前的 `del now_utc` 已移除，`scan_sell_call.py` 新增 `quote_freshness_now_utc` 透传）。`evaluate_opening_candidate_policy`（underwriting）虽不独立重验，但其输入一律先经 `compute_metrics` 决策时刻校验，不存在绕过路径。回归测试 `test_snapshot_stale_at_decision_moment_fails_closed`/`test_snapshot_missing_receipt_fails_closed_at_decision_moment`/`test_fresh_snapshot_at_decision_moment_passes` 直接覆盖。
- **#2（scan-status reason 保留）已正确闭环**：`publish_strategy_scan_status` 对 `completed` 状态持久化受限 reason（`no_candidate`/`partial_data`/`market_closed`），Daily Brief `_append_strategy_status_gaps` 对 `completed`+`partial_data` 补 warning gap；`_validate_status` 与 success-empty 校验不回归。

第二轮 adversarial pass 检查的点（均未构成 defect）：

- snapshot receipt 校验在 engine 内的顺序（在策略硬门槛前）只影响 fetch 时刻已 `ready` 的合约，非 ready 合约在更前面的 `opening_contract_status` 分支返回，不会掩盖更具体的合约级 reason。
- `evaluate_opening_candidate_policy` 不独立校验 snapshot 决策时刻，但其调用方（underwriting）只接收已通过 `compute_metrics` 决策时刻校验的行，无绕过。
- 测试时钟确定性：`scan_sell_call` 新增 `quote_freshness_now_utc` 后，相关测试注入与 receipt 对齐的固定时钟；conftest `phase2_opening_row` 提供默认 receipt，可 override。

## Open Questions

- 设计 §4.2“决策时刻”的精确口径（scan 入口 vs 逐合约决策）在实现上以 engine 决策入口为准；如后续要求逐合约时刻，可在此处收紧，属可演进点而非当前缺陷。
- 现场 shadow replay（15:00/15:30/15:50）需生产 OpenD 数据，仍为离线验证后续步骤。

## Residual Risk

- 全量测试 4541 passed（跳过仅网络沙箱的 `tests/quality/test_om_quality_gate_http.py`）。
- 现场 replay 未执行，恢复的合约数量与最终候选排序变化尚无生产数据佐证。
- `snapshot_received_at_utc` 现在成为候选决策的必要证据；任何缺失该列的旧 required-data CSV 会在决策时 fail closed（`evidence_unavailable`）。这是有意行为，但与历史 artifact 不向后兼容，部署后首轮 tick 需重建 required-data。
