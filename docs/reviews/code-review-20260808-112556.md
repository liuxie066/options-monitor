# Code Review

## Scope

- Mode: current changes（第三轮，验证第二轮 Finding 2 修复并终扫）
- Branch: `main` (工作区未提交改动)
- Base: `HEAD` (cc6f9e04, v1.10.17)
- Output file: `docs/reviews/code-review-20260808-112556.md`
- Included scope: `_audit_paths` shared 分支符号链接拒绝 + 两分支 `state` 中间目录符号链接显式拒绝；新增 1 个回归测试
- 前序 artifact: `docs/reviews/code-review-20260808-111256.md`（第一轮）、`docs/reviews/code-review-20260808-112325.md`（第二轮）

## Findings

未发现实质性问题。

### 1-已修复-低-output_shared 默认分支符号链接逃逸（第二轮 Finding 2）

- 修复方式：shared 分支对 `output_shared` 加 `is_symlink()` 拒绝；同时两个分支对中间 `state` 目录也加显式拒绝，把原先 containment 兜底前的"静默 missing"对齐为先例的显式报错。
- 实测验证（临时目录构造真实符号链接）：
  - shared 嵌套 `state` 符号链接 → 显式拒绝 `must stay under output_shared`
  - run 嵌套 `state` 符号链接 → 显式拒绝 `must stay under output_runs`
  - 正常 run/shared 路径 → `ok`，正常读取
  - state 目录真实缺失 → `missing`（语义不被新校验干扰）
- 回归：`test_notification_perception_rejects_symlinked_state_dir` 覆盖两分支中间目录。全部 143 个相关测试 + 43 个 tick/notify 测试绿。

### 第三轮终扫确认无残留

- 空 run_id 走 shared 默认分支属设计内行为（非安全问题，空组件无法构造路径）。
- `audit_path` 显式分支：仓外拒绝、仓内放行，行为与消息均未被本系列改动影响。
- run_id 单组件校验五类恶意输入（`../x`、`a/b`、`a\b`、`..`、前后空白变体）全部拒绝。
- 三层防御闭环：`_safe_run_id` 组件校验 → `is_symlink()` 目录拒绝 → `_resolve_path` containment `relative_to` 兜底。

## Open Questions

无。

## Residual Risk

- `audit_path` 显式路径分支仍未加符号链接检查（保持既有行为）；该分支面向显式运维传参且限于 repo_root 内，如需与 run/shared 分支对齐可作后续低优先级收紧项，本轮不改动。
- 符号链接检查与文件读取间存在理论 TOCTOU 窗口；需攻击者已有运行目录写权限，与仓库既有先例接受的风险水平一致。
