# Code Review

## Scope

- Mode: current changes, adversarial completion re-review
- Branch or PR: `main`
- Base: `HEAD@b07b4aad`
- Output file: `docs/reviews/code-review-20260811-014150.md`
- Included scope: strict Close Advice domain policy, report publication state machine, scheduled Account Run and Daily Brief consumption, read tools, Shadow Replay evidence, Position Advice v2 removal, generic receipt/scope compatibility, and service retirement source changes
- Excluded scope: runtime output data, historical release records, `docs/plans/**`, `docs/gateflow/**`, release/deploy/remote upgrade, and unrelated user files
- Parallel review coverage: none; review performed directly against the current working tree

## Findings

### 1-已修复-高-禁用 Close Advice 时旧的空成功报告在输出改写窗口仍可被读取

- **入口/函数**: `run_close_advice()` disabled branch -> `_write_csv()` -> `validate_close_advice_report_manifest()`
- **文件(行号)**: `src/application/close_advice_runner.py:1611-1629`; `tests/test_close_advice_runner.py:169-199`
- **输入场景**: 上一轮成功报告恰好为空行集，新一轮禁用 Close Advice；进程在空 CSV/文本写入后、非成功 manifest 写入前中断，或并发读取者恰在该窗口校验。
- **实际分支**: 旧 success manifest 的 CSV/文本 hash 仍与新写入的空输出相同，因而校验可继续成功。
- **预期行为**: disabled 必须首先成为持久化的非成功状态，然后才能改写报告字节。
- **实际行为**: 崩溃/并发窗口内可将已禁用的旧报告误视为有效。
- **直接证据**: 原顺序是先 `_write_csv()` 和 `atomic_write_text()`，再调用 `publish_close_advice_report_status()`；旧空成功报告与新空输出字节一致。
- **影响**: 读取面短暂暴露已失效的建议，破坏 report manifest 作为状态转换权威的契约。
- **修复**: disabled 分支首先原子发布 `status=failed, reason=close_advice_disabled`，再写空 CSV/文本；回归测试在 `_write_csv()` 入口内立即验证 manifest 已非 success。
- **验证点**: `test_disabled_close_advice_invalidates_manifest_before_writing_outputs` 与 Close Advice 聚焦测试通过。
- **修复风险**: 低
- **严重程度**: 高

### 2-已修复-中-布尔值可伪装成严格策略的数值证据

- **入口/函数**: `safe_float()` -> `safe_int()` -> `evaluate_close_advice()`
- **文件(行号)**: `domain/domain/close_advice.py:29-43`; `tests/test_strict_close_advice.py`
- **输入场景**: 持仓或行情 JSON/CSV 中的 premium、bid/ask、DTE、strike、spot、contracts、multiplier 或 fee 被错误序列化为 `true/false`。
- **实际分支**: Python 的 `bool` 是 `int` 子类，`float(True)` 返回 `1.0`，原实现可将非数值证据当作合法数值继续评估。
- **预期行为**: 严格策略对非法数值类型必须 fail closed 为 `not_evaluable`。
- **实际行为**: 错误的布尔证据可进入经济性公式，并产生 `hold` 甚至理论上的 `close`。
- **直接证据**: 修复前 `safe_float()` 直接执行 `float(value)`，没有在数值转换前排除 `bool`。
- **影响**: 损害“证据不完整或非法则不建议”的核心安全性，并使领域函数过度信任上游。
- **修复**: `safe_float()` 在转换前显式拒绝 `bool`；逐字段回归确认所有数值证据的布尔输入均返回 `not_evaluable` 和对应缺失/fee flag。
- **验证点**: `test_boolean_numeric_evidence_is_not_evaluable` 与领域/runner 聚焦测试通过。
- **修复风险**: 低
- **严重程度**: 中

### 3-已修复-中-Shadow Replay 状态汇总仍按已删除的多策略状态计数并丢失分析命令

- **入口/函数**: `shadow_replay_dataset_status()` -> `_close_readiness_commands()`
- **文件(行号)**: `src/application/shadow_replay/status.py:95-110,238-272`; `tests/test_close_advice_shadow_evidence.py:258-323`
- **输入场景**: 一个 Close Replay dataset 已满足单一严格策略的样本、结果和证据覆盖要求。
- **实际分支**: readiness 返回 `ready_for_strict_policy_evaluation` / `evaluate_strict_close_policy`，但状态汇总仍查找已删除的 `ready_for_paired_policy_analysis` / `run_paired_policy_analysis`。
- **预期行为**: 公开状态应准确计数可评估的严格策略 dataset，并给出只读 analyze 命令。
- **实际行为**: ready count 恒为 0，且 `suggested_command` 为 `null`，使真实 CLI/研究入口无法引导后续分析。
- **直接证据**: `status.py` 中两个字符串分支与 `readiness.py` 的新状态合同不一致；公共 `shadow_replay_dataset_status()` 会经过该分支。
- **影响**: 不改变生产交易或通知，但会让研究状态错误、隐藏已经齐备的严格策略历史证据。
- **修复**: 汇总字段改为 `close_decision_ready_for_strict_evaluation_count` 并匹配 `ready_for_strict_policy_evaluation`；命令路由匹配 `evaluate_strict_close_policy`。集成回归构造 30 个完整 episode，验证公开状态计数和 analyze 命令。
- **验证点**: `tests/test_close_advice_shadow_evidence.py` 通过（6 passed）。
- **修复风险**: 低
- **严重程度**: 中

### 4-已修复-高-报告消费者校验 hash 后重新打开文件，并发改写时可消费未提交字节

- **入口/函数**: `validate_close_advice_report_manifest()` -> Daily Brief `_load_close_advice()` / `close_advice_read` / `close_advice_tool` summary
- **文件(行号)**: `src/application/close_advice_report_manifest.py:127-264`; `src/application/daily_decision_brief_service.py:916-976`; `src/application/agent_tools/close_advice_read_impl.py:450-539`; `src/application/agent_tools/materialization_impl.py:307-388,1249-1274`
- **输入场景**: 读取者已用旧 success manifest 校验旧 CSV/text，同一 run 随后开始重入并原子切换 pending marker 与新 CSV；读取者在两个动作之间被调度。
- **实际分支**: manifest validator 内部打开并 hash 文件后返回；三个消费入口随后再由 pandas/csv/text reader 打开相同路径。
- **预期行为**: 解析的必须正是与一个 success manifest hash 匹配的字节；并发发布只能让读取成功地看到上一个已提交快照、新的已提交快照，或 fail closed。
- **实际行为**: 校验可针对旧字节成功，实际解析却可读到 pending 阶段刚改写的新 CSV/text。
- **直接证据**: 修复前 Daily Brief 在 validator 后执行 `pd.read_csv(path)`，read tool 在 source discovery 后执行 `source.path.open()`，materialization 在 validator 后调用路径型 summary。
- **影响**: 一个尚未由 success manifest 提交的平仓建议行可进入 Daily Brief 或人工读取面，破坏报告完整性和提交边界。
- **修复**: 新增单一 snapshot reader，按 CSV/text 先、manifest commit marker 后的顺序读取，对这两份确切 bytes 验证 hash 并一并返回；三个消费入口全部改为解析返回 bytes，不再重开路径。同时删除 read tool 从未校验 context 回填 `side` 的兼容分支，严格报告必须自包含持仓方向。
- **验证点**: snapshot 单测在获取后改写磁盘文件，仍只返回已校验的旧 bytes；Daily Brief、read tool 和 materialization summary 分别有“校验后改写路径”回归。聚焦套件通过（`166 passed`）。
- **修复风险**: 中
- **严重程度**: 高

### 5-已修复-高-已校验的持仓、行情 manifest 和计划被按路径二次读取，可混用不同代输入

- **入口/函数**: `run_one_account()` -> `load_prepared_option_positions_context()` -> `run_close_advice()` -> required-data manifest/plan/receipt resolution
- **文件(行号)**: `src/application/account_run.py:542-587,655-659`; `src/application/close_advice_runner.py:1604-1730,2092-2142`; `src/application/required_data_snapshot.py:270-357,560-634`; `src/application/close_advice_required_data.py:376-532`; `src/application/source_receipts.py:242-265`
- **输入场景**: Account Run 已校验 prepared option context A，或 Close runner 已校验 frozen manifest/plan/receipt A；在下游再次打开同一路径前，该路径被并发替换为 B。
- **实际分支**: Account Run 丢弃 loader 返回的持仓对象，runner 重读 context path；manifest 和 plan 先解析验证再重读计算文件 hash；各 symbol 独立重读 manifest；receipt validator 校验 payload hash 后只返回路径，下游再次打开 payload。
- **预期行为**: 一次决策只能消费同一代的已校验对象/字节，并与父级 Account Run 冻结的 manifest SHA-256 一致。
- **实际行为**: 可以出现“持仓 A 通过授权校验、但评估 B”，或“解析 manifest A、记录 hash B、某些 symbol 消费另一代 manifest/payload”的混合证据链。
- **直接证据**: 修复前 `account_run.py` 仅调用 prepared loader 不保存返回值；`close_advice_runner.py` 对 manifest/plan 都存在“load 后再 `read_bytes()`”，且逐 symbol resolver 各自 load manifest；`validate_source_receipt()` 校验 payload 后仅供下游重读 `payload_path`。
- **影响**: 严格 Close 可在并发换代窗口中基于未通过父级授权的持仓或混合行情证据生成建议，破坏 run/account/config/hash 隔离和可审计性。
- **修复**: Account Run 把 prepared loader 返回对象直接传给 runner，并传递父 manifest hash；runner 对 manifest/plan 均单次读取并用同一份 bytes 解析、校验和计算 hash，逐 symbol 校验所用 manifest hash 必须一致；receipt validator 返回已校验 payload bytes，所有下游直接解析该 bytes。Account Run 同时直接消费 runner 返回的 notification text，不再重读报告路径。
- **验证点**: 新增 prepared context 转交、父 manifest generation mismatch、manifest/plan exact-byte snapshot、receipt payload exact bytes 与 result-text 转交回归；相关聚焦套件通过（`99 passed`）。
- **修复风险**: 中
- **严重程度**: 高

### 6-已修复-中-只读摘要把缺少严格策略证据的旧格式行展示为有效建议

- **入口/函数**: `close_advice_tool()` -> `close_advice_rows_summary()`
- **文件(行号)**: `src/application/agent_tools/materialization_impl.py:309-370`; `tests/test_agent_plugin_smoke.py`
- **输入场景**: 一个 hash 完整、但内容采用旧字段或被错误生产的 Close Advice 报告，其行缺少 `policy_version`、缺少完整证据状态，或包含已删除的 `reallocate` 建议。
- **实际分支**: 摘要函数把缺失 `evaluation_status` 默认成 `priced`，也不校验策略版本、证据完整性和建议状态枚举，因此可把非严格策略行计入人工读取摘要。
- **预期行为**: 所有建议读取面都必须按 `strict_profit_capture.v1 + priced + complete + close/hold` 同一契约失败关闭。
- **实际行为**: 定时通知不会选中这些行，但 Agent 工具的推荐计数和 top rows 会把它们展示为有效结果。
- **直接证据**: 修复前唯一过滤条件是 `evaluation_status`，且使用 `or "priced"` 兼容默认；`recommendation_state` 为空时还会被转换成 `not_evaluable` 并计数。
- **影响**: 不会触发自动消息或交易，但会误导人工判断，并形成已删除 v2 语义的只读残留入口。
- **修复**: 摘要只接受严格策略版本、`priced`、`decision_evidence_status=complete` 且建议为 `close` 或 `hold` 的行；其他行不进入推荐计数、账户计数或 top rows。
- **验证点**: 新增 legacy、incomplete 和 `reallocate` 三类行均失败关闭的回归；摘要聚焦测试通过（`3 passed`）。
- **修复风险**: 低
- **严重程度**: 中

### 7-已修复-中-运行适配层会先把布尔持仓证据转换成合法数值，绕过领域层拒绝

- **入口/函数**: `_position_to_input()` / `_strict_fee_estimates()` / `_original_dte()` -> `evaluate_close_advice()`
- **文件(行号)**: `src/application/close_advice_runner.py:961-1078`; `tests/test_close_advice_runner.py`
- **输入场景**: ledger/context 中的 `multiplier` 或 `opened_at` 因序列化错误成为 JSON 布尔值。
- **实际分支**: 通用 `effective_multiplier()` 可把 `true` 转成 `1.0`；通用时间戳解析可把 `true` 转成 Unix timestamp 1。领域函数接收到的已不再是布尔值，因此此前的 `safe_float(bool)` 拒绝无法生效。
- **预期行为**: 严格策略应在适配边界保留非法证据语义，分别输出 `missing_multiplier` 或 `missing_original_dte`，不得继续形成 priced `hold/close`。
- **实际行为**: 非法字段可能被当作 multiplier=1 或 1970 年开仓日进入经济性门槛；其最终结果取决于其他数值，而不是固定 fail closed。
- **直接证据**: 修复前 `_strict_fee_estimates()` 与 `_position_to_input()` 直接使用 `effective_multiplier(pos)`，`_original_dte()` 直接调用接受数值型布尔的时间戳 helper。
- **影响**: 上游类型损坏可绕过“布尔不是数值证据”的严格规则，并产生看似完整的错误评估。
- **修复**: 增加严格持仓 multiplier 适配，先拒绝布尔再做有限数值转换，且不再以 quote multiplier 覆盖持仓证据；`opened_at` 在时间戳解析前显式拒绝布尔。
- **验证点**: 新增两个 runner 回归，均明确得到 `not_evaluable`、对应缺失 flag 且 `notify_rows=0`；相关聚焦测试通过（`3 passed`）。
- **修复风险**: 低
- **严重程度**: 中

### 8-已修复-中-Agent 读取面未校验严格建议必须已经完成定价

- **入口/函数**: `close_advice_read_tool()` -> `_public_row()` -> `_decision_fields_for_read()`
- **文件(行号)**: `src/application/agent_tools/close_advice_read_impl.py:826-880`; `tests/test_agent_plugin_smoke.py`
- **输入场景**: 一个 hash 有效的报告行同时声明严格策略 `close/hold`、完整证据，但 `evaluation_status` 缺失或为 `not_evaluable/coverage_missing`。
- **实际分支**: read tool 只检查策略版本、建议枚举、decision basis 和证据状态，仍把矛盾行投影为 `close/hold`。
- **预期行为**: `close/hold` 必须与通知选择器一致，仅在 `evaluation_status=priced` 时可被读取为有效决策。
- **实际行为**: 定时提醒会拒绝该行，但 Agent 读取面会展示 `close`，同一报告在两个消费入口产生相反结论。
- **直接证据**: `_decision_fields_for_read()` 的有效条件原本不包含 `evaluation_status`，而领域通知 selector 明确要求 `priced`。
- **影响**: 不会自动发消息或写交易，但会向人工查询暴露未经定价的平仓建议。
- **修复**: 对 `close/hold` 增加 `evaluation_status=priced` 条件；矛盾或缺失时保守投影为 `not_evaluable`，basis 为 `strict_decision_not_priced`。
- **验证点**: 新增矛盾行回归，并保留旧策略、缺 basis 和证据不一致的失败关闭覆盖；聚焦测试通过（`2 passed`）。
- **修复风险**: 低
- **严重程度**: 中

### 9-已修复-低-Shadow Replay 手册仍宣称存在已删除的多策略分析产物

- **入口/函数**: 操作方阅读 `SHADOW_REPLAY_RUNBOOK.md` 后运行 `shadow-replay analyze`
- **文件(行号)**: `docs/SHADOW_REPLAY_RUNBOOK.md:455-456`
- **输入场景**: 操作方根据手册核对 Close facet 分析输出。
- **实际分支**: 文档仍声称会生成 `close_policy_analysis` 并比较 P0/P1/P2/P3，但对应模块和所有多策略投影已删除。
- **预期行为**: 文档应准确说明只保留 `strict_profit_capture.v1` 的 episode、mark、outcome 和机械就绪度。
- **实际行为**: 操作方会等待不存在的字段，或误以为系统仍保留替代策略与自动 policy 比较能力。
- **直接证据**: 全仓搜索仅此手册仍包含 `close_policy_analysis` 和 P0/P1/P2/P3；运行代码只写入 `close_decision_readiness`。
- **影响**: 不影响运行时决策，但破坏删除边界和研究操作合同。
- **修复**: 删除过时描述，明确 Close facet 不再重建多策略、不输出 policy winner 或自动晋级结论。
- **验证点**: 对 active docs/source/config 做旧策略标识残留搜索。
- **修复风险**: 低
- **严重程度**: 低

### 10-已修复-中-严格建议的评估状态关系在 Shadow Replay 和人工读取面仍可自相矛盾

- **入口/函数**: Shadow Replay `_formal_policy_result()`；Agent read `_decision_fields_for_read()`
- **文件(行号)**: `src/application/shadow_replay/capture.py:525-592`; `src/application/agent_tools/close_advice_read_impl.py:826-892`; `tests/test_close_advice_shadow_evidence.py:199-249`; `tests/test_agent_plugin_smoke.py:3699-3762`
- **输入场景**: 一个 hash 有效的严格报告行声明 `close/hold + complete`，但 `evaluation_status` 不是 `priced`；或声明 `not_evaluable`，却把 `evaluation_status` 标成 `priced`。
- **实际分支**: Shadow Replay 只验证策略版本、建议枚举和 evidence status，仍会把矛盾行写入正式 episode；人工读取面此前虽拒绝未定价的 `close/hold`，却仍接受 `not_evaluable + priced`。
- **预期行为**: 严格决策状态必须保持双向一致：`close/hold` 只能是 `priced`，`not_evaluable` 不能是 `priced`；任何矛盾均失败关闭。
- **实际行为**: 研究数据可把未经定价的动作当作正式策略结果，人工读取也可能保留一个被标成已定价的不可评估结论。
- **直接证据**: 两个消费入口分别缺少上述关系的全部或反向检查；通知 selector 因只接受 `close + priced + complete` 不受影响。
- **影响**: 不会触发自动通知或交易，但会污染严格策略的历史评估证据，并让人工读取对报告状态给出误导性投影。
- **修复**: Shadow Replay 将 `evaluation_status` 纳入正式策略必填字段并校验双向关系；read tool 对 `not_evaluable + priced` 保守投影为 `not_evaluable`，basis 为 `strict_not_evaluable_marked_priced`。
- **验证点**: 两类矛盾状态的 Shadow Replay 参数化回归和 read tool 回归通过；相关聚焦测试为 `8 passed` 与 `27 passed, 74 deselected`。
- **修复风险**: 低
- **严重程度**: 中

### 11-已修复-中-Shadow Replay 绕过 Close report manifest 并重读可变持仓 context

- **入口/函数**: `build_shadow_replay_dataset()` -> `capture_close_decision_episodes()`
- **文件(行号)**: `src/application/shadow_replay/capture.py:306-482`; `src/application/close_advice_report_manifest.py:243-270`; `tests/test_close_advice_shadow_evidence.py:127-335`; `docs/SHADOW_REPLAY_RUNBOOK.md:180-188`
- **输入场景**: 历史 run 的 `close_advice.csv` 或 `state/option_positions_context.json` 在报告成功发布后被改写、部分复制，或 Close facet 指向一个没有成功提交 marker 的报告。
- **实际分支**: Shadow Replay 直接通过路径读取 CSV 和 context，只要求有一条 `close_advice status=ok` 审计事件；它没有校验相邻 report manifest，也没有确认 context 是生成报告时绑定的同一份内容。
- **预期行为**: 正式严格策略 episode 只能来自 `success + frozen_snapshot` 的完整报告快照，并保持 run、账户、行数、required-data/plan hash、CSV bytes 和 context hash 一致。
- **实际行为**: 被改写或混代的历史输入仍可能进入 dataset，污染后续严格策略收益测算和 readiness。
- **直接证据**: 修复前 `capture_close_decision_episodes()` 调用通用 `read_csv_rows(close_path)`，context 也由 `_read_json_object(path)` 独立加载；整个 Shadow Replay 包没有调用 report manifest validator。
- **影响**: 不影响生产通知或交易，但会破坏历史测算的证据可信度，使错误样本看起来像正式严格策略结果。
- **修复**: 使用单次 report snapshot 读取并校验 exact CSV/text bytes；强制 `run_id`、账户和 `quote_mode=frozen_snapshot`；校验 manifest 行数及三个 SHA-256 绑定；逐行核对 required-data/plan hash，并将加载后的 context canonical hash 与 report manifest 对齐。任一不一致立即终止 dataset build。
- **验证点**: 新增成功捕获、CSV 篡改和 context 换代回归；`tests/test_close_advice_shadow_evidence.py` 为 `9 passed`，相关 manifest/读取聚焦测试为 `49 passed, 121 deselected`。
- **修复风险**: 低
- **严重程度**: 中

### 12-已修复-中-Assistant 持仓处置仍按已删除的 v2/组合腿字段渲染

- **入口/函数**: `render_canonical_tool_result(renderer_key="position_exit_analysis")` -> `_render_position_exit_analysis()`
- **文件(行号)**: `src/application/assistant/renderer.py:1091-1195`; `tests/test_assistant_runtime.py:196-246`
- **输入场景**: `close_advice_read` 返回严格策略行，其结论仅由 `recommendation_state=close/hold/not_evaluable` 表达。
- **实际分支**: Assistant renderer 仍优先查找旧 `close_action` / `tier_label` / `exit_state`，并渲染 `optional_combo_action`、Put/Call 组合腿、IV/RV、delta 和路径压力等已删策略字段。
- **预期行为**: 公开对话入口应只展示严格策略的三态结论、原因和同一套经济性门槛证据。
- **实际行为**: 有效严格行可以没有“结论”；若输入混入旧字段，界面还会显示已删除的组合处置语义。
- **直接证据**: 修复前 `_close_advice_conclusion()` 不读取 `recommendation_state`；`_close_action_label()` 保留 `close_put_keep_call`、`close_both_optional`、long-call salvage 等分支；指标文本不读取 `net_capture_ratio`、`all_in_close_cost`、`close_cost_ratio` 和 `remaining_term_ratio`。
- **影响**: 不会触发交易或通知，但会让人工查询看不到严格策略结论，并在已删 v2 后继续暴露其决策语义。
- **修复**: 结论只映射 `recommendation_state`；原因只读取 `reason`；删除可选组合动作渲染和旧 action mapper；指标收敛为净捕获、开仓净权利金、全成本买回、成本/行权本金、剩余期限、价差、DTE 和价外状态。
- **验证点**: 新增混入旧 v2 字段的 renderer 回归，验证严格结论和指标正确显示，且旧可选动作、Put 腿、IV/RV 和 delta 均不泄露；聚焦测试为 `7 passed` 及相关路由 `98 passed`。
- **修复风险**: 低
- **严重程度**: 中

### 13-已修复-低-活动架构文档仍将 Close Advice 定义为已删除的多腿退出策略

- **入口/函数**: 开发者按 `docs/ARCHITECTURE.md` 定位 Close Advice 责任边界
- **文件(行号)**: `docs/ARCHITECTURE.md:333-346`
- **输入场景**: 开发者依据当前架构文档修改 Close Advice runner 或 domain policy。
- **实际分支**: 文档仍宣称 runner 会配对 yield-enhancement legs，domain 会处理 short-vol 退出和 long-call 凸性，并通过 action registry 生成 `close_put_keep_call`、`hold_call_as_convexity` 和 `close_both_optional`。
- **预期行为**: 活动架构文档应与唯一 `strict_profit_capture.v1` 策略和三态合同一致。
- **实际行为**: 已删代码仍被描述为当前系统权威，会引导后续实现重新引入被明确删除的边界。
- **直接证据**: 终审的活动 docs/source 残留搜索仅在该架构段落命中三个已删 action wire value。
- **影响**: 不影响当前运行时，但会破坏代码所有权和策略边界的可维护性。
- **修复**: 架构段落改为固定严格策略、short Put/Call、三态输出、Futu 费用和 report manifest；明确不配对组合腿，不生成 roll/replacement/reallocation/short-vol/long-option 退出动作。
- **验证点**: 重跑活动 docs/source 旧 action 标识搜索，不再有活动实现性引用。
- **修复风险**: 低
- **严重程度**: 低

### 14-已修复-中-Close Advice 仍写入 Candidate Filter Trace 并污染开仓候选回放

- **入口/函数**: `run_close_advice()` -> `_append_close_advice_filter_trace()` -> Shadow Replay `filter_decision_rows()` / `candidate_snapshots_from_filter_decisions()`
- **文件(行号)**: `src/application/close_advice_runner.py:45-78,2050-2112`; `src/application/candidate_filter_trace.py:8-23`; `tests/test_candidate_reject_summary.py`
- **输入场景**: 一次 run 同时生成开仓候选 trace 和持仓 Close Advice，其中存在 `not_evaluable` 或超出消息展示上限的 close 行。
- **实际分支**: Close runner 把行追加到 `candidate_filter_trace.jsonl`，分类为 `rejected`、`ranked_below`、`accepted` 或 `notified`；Shadow Replay 的通用候选捕获会把其中 `rejected/ranked_below` 再投影成 candidate snapshots。
- **预期行为**: 开仓候选 trace 只描述 Candidate Engine 及其容量过滤；严格 Close 的历史证据只走独立 report manifest、运行审计和 Close Replay facet。
- **实际行为**: 持仓处置的未定价/提醒截断行可以增加候选样本数、拒绝数和 evidence level，将两个已明确分离的策略重新耦合。
- **直接证据**: 修复前 runner 在 success manifest 发布前 best-effort 调用 `_append_close_advice_filter_trace()`；`candidate_snapshots_from_filter_decisions()` 只按 status 过滤，不排除 `function=close_advice`。
- **影响**: 不影响生产交易与通知选择，但会污染开仓策略的离线样本、拒绝摘要与就绪度证据。
- **修复**: 删除 Close runner 的 candidate trace 适配器和调用，并从新 trace builder 允许的 function 集合中移除 `close_advice`。严格 Close 审计仍由 report manifest、Account Run audit 和独立 Shadow Replay facet 完整保留。
- **验证点**: runner、candidate reject summary 与 strict Close Shadow Replay 聚焦套件为 `31 passed`；全仓活动 Python 已无 `function=close_advice` trace producer。
- **修复风险**: 低
- **严重程度**: 中

### 15-已修复-中-公开工具示例和活动架构文档仍宣称 Close Advice 处理 short-vol/long-call/组合退出

- **入口/函数**: Assistant capability catalog；Close Advice lifecycle 文案；开发者架构文档
- **文件(行号)**: `src/application/assistant/tool_bindings.py:180-188`; `src/application/close_advice_runner.py:1098-1107`; `domain/domain/short_vol_assessment.py:46-53`; `docs/STRATEGY_ARCHITECTURE.md:15-25,232-243`; `docs/PRODUCT_ARCHITECTURE.md:263-289`
- **输入场景**: 用户或模型根据 Assistant 工具目录询问 long Call 止盈，或开发者根据当前架构文档扩展 Close Advice。
- **实际分支**: 工具仍以“long call 是否应该平仓”为主示例；到期日原因仍描述“剩余年化或凸性平仓评估”；策略/产品架构仍声称 Close 消费 `short_vol` thesis、Event Risk 和 legacy `yield_enhancement` 退出适配。
- **预期行为**: 所有活动公开面都只宣告 short Put/Call 的 `strict_profit_capture.v1`，不暗示 long option、方向、事件、delta、集中度或组合腿退出能力。
- **实际行为**: 用户会被引导询问策略明确不处理的问题，开发者也可能按过时文档恢复已删逻辑。
- **直接证据**: 以上文字均存在于活动 source/docs，且与 `domain/domain/close_advice.py` 的三态固定合同直接冲突。
- **影响**: 不会自动交易，但会破坏产品边界、人工预期和后续代码所有权。
- **修复**: Assistant 改为 short Put/Call 严格平仓示例；到期日原因改为离开提前止盈窗口；开仓历史 short-vol 兼容明确限于离线研究；产品架构将 Candidate Trace 与 Close Report Evidence 分开，并移除 Event Risk/组合退出声明。
- **验证点**: Assistant catalog/route 和 Close runner 聚焦套件为 `111 passed`；活动 source/docs 已无上述过时声明。
- **修复风险**: 低
- **严重程度**: 中

### 16-已修复-低-Strategy Lab 回归夹具没有生成严格报告 manifest，导致全量测试无法验证新证据边界

- **入口/函数**: `tests/test_strategy_lab.py::_write_close_run()` -> `run_strategy_lab_update()` -> `capture_close_decision_episodes()`
- **文件(行号)**: `tests/test_strategy_lab.py:304-410`; `src/application/shadow_replay/capture.py:306-430`
- **输入场景**: Strategy Lab 测试构造一个包含 Close Advice 的历史 run，再要求 Shadow Replay 构建 Close decision facet。
- **实际分支**: 测试夹具仍只写 `close_advice.csv` 和持仓 context，没有 `close_advice.txt`、成功 report manifest、冻结快照标识及 required-data/plan 父证据哈希。
- **预期行为**: 测试输入应遵守与生产相同的严格报告提交合同；Shadow Replay 对缺 manifest 的旧报告继续失败关闭。
- **实际行为**: 最终全量测试有 6 项因 `close_advice_manifest_missing` 失败，无法证明 Strategy Lab 在新合同下正常工作。
- **直接证据**: 失败均发生在 `read_close_advice_report_snapshot()` 的 manifest 校验处，而非业务计算；相关测试原先未调用 report manifest publisher。
- **影响**: 生产 fail-closed 行为正确，但测试基线中断，并可能诱使实现者错误地为旧裸 CSV 增加兼容旁路。
- **修复**: 升级唯一 `_write_close_run()` 夹具，生成空文本报告和成功 manifest，并在行与 manifest 中绑定 `quote_mode=frozen_snapshot`、required-data snapshot hash、required-data plan hash、run、账户和 context hash；不修改 Shadow Replay 的拒绝规则。
- **验证点**: `tests/test_strategy_lab.py` 全部 `55 passed`，Ruff 通过；随后重跑全量测试。
- **修复风险**: 低
- **严重程度**: 低

### 17-已修复-中-最新 Close run 选择器会把未提交的空报告静默当成无数据

- **入口/函数**: `run_strategy_lab_update()` -> `latest_close_decision_run_dir()`
- **文件(行号)**: `src/application/shadow_replay/capture.py:306-505,1735-1799`; `tests/test_strategy_lab.py:1854-1915`
- **输入场景**: 最新 run 存在一个缺失 report manifest 的 `close_advice.csv`，且 CSV 只有表头；这也覆盖成功报告被意外清空并丢失 commit marker 的场景。
- **实际分支**: 选择器在正式 capture 之前直接调用通用 `read_csv_rows()` 计数；零行时记为 `skipped_empty_count` 并继续，因此永远不会进入严格 report snapshot 校验。
- **预期行为**: 合法、完整提交的零行严格报告可以作为“本次无 episode”跳过；缺失、失败或哈希不一致的报告必须失败关闭，不能伪装成无数据。
- **实际行为**: 未提交或被清空的报告被静默忽略，Strategy Lab 仍可继续构建候选 dataset，与手册“任一 report manifest 不一致都会终止 build”的合同冲突。
- **直接证据**: `capture_close_decision_episodes()` 已使用 report snapshot validator，但 `latest_close_decision_run_dir()` 修复前独立读取 CSV 并在零行分支提前返回。
- **影响**: 不会把错误 Close 行纳入研究样本，但会隐藏证据损坏并让自动研究更新表现为正常无 Close 数据，削弱审计可信度。
- **修复**: 抽取单一 `_validated_close_report_rows()`，在选择和正式 capture 两条路径都校验 success manifest、CSV/text exact bytes、run、账户、`frozen_snapshot`、row count 及三个 SHA-256 绑定；只有通过这些校验的零行报告才计入 `skipped_empty_count`。
- **验证点**: 新增“完整零行报告可跳过”和“缺 manifest 的零行报告报错且候选证据仍被保留”回归；Strategy Lab + Close Shadow 聚焦套件 `65 passed`，Ruff 通过。
- **修复风险**: 低
- **严重程度**: 中

## Open Questions

无。

## Residual Risk

- Close 提醒仍使用现有 scheduled Daily Brief；本次没有新增 Close-only 即时通知状态机。
- 兼容 wire identifier 仍包含 `position_advice` 字样，仅为旧 fingerprint 和 sealed quote receipt 可读性，不对应可执行 v2 策略或控制面。
- 此 artifact 记录本轮已修复 finding；修复后的最终零 finding 结论和全量验证将写入后续 re-review artifact。
