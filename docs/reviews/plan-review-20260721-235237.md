# Plan Re-Review — Option Notification Experience after final integration fixes

- Reviewed target: `docs/plans/option-notification-experience-implementation-plan-20260721.md`
- Review timestamp: `2026-07-21T23:52:37+08:00`
- Scope: adversarial re-review after renderer-authority rollback revision and multi-market terminal-boundary correction
- Supersedes: `docs/reviews/plan-review-20260721-234559.md`

## Assumptions tested

1. Daily Brief is the only scheduled ordinary-notification renderer; Compact/Legacy has no fallback authority.
2. Deprecated `notifications.daily_brief.enabled` is warning-only and cannot disable Daily Brief or select another renderer.
3. Reliable manual/force single-market scans may update successful current but cannot create an ordinary envelope, resolve provider delivery, or advance delivery confirmation.
4. Fixed and half-hour scan points share one scheduler and one canonical pipeline.
5. Delivery v2 persists exact payload/key/hash before scheduler watermark commit and provider work.
6. Delivery-only no-scan retry cannot assemble, persist revisions/current, refresh broker data, or re-render.
7. Combined multi-market execution terminates before Daily Brief assemble, revision/current persistence, envelope creation, or provider work.
8. Rollback requires stopped scheduling plus a compatible code/version/state rollback; no config value restores Compact as a scheduled sender.

## Review lenses

- **Architecture boundary**: scheduler target identity, application orchestration, repository state, renderer projection, and provider adapter remain separately owned with one outbound authority.
- **Best practice**: fail-closed multi-market handling, durable-before-send sequencing, exact replay, explicit ambiguity, conservative migration, and read-only query behavior are all specified.
- **Optimal solution**: the plan reuses the existing timer, pipeline, repository root, and provider adapter rather than adding a second scanner, sender, config switch, or outbox service.
- **Overengineering**: retention, combined-market dispatch, and manual-send commands remain deferred until independently justified.
- **Overcoupling**: the plan does not encode delivery state in Markdown, scheduler reason text, candidate ranking, or compatibility renderer output.

## Findings

未发现实质性 plan finding。The two prior integration blockers are closed:

- renderer rollback no longer depends on `daily_brief.enabled=false` and cannot restore Compact/Legacy scheduled authority;
- `--market-config all` now consistently terminates before Daily Brief snapshot/envelope/provider work in the plan, docs, source, and tests.

## Open Questions

无。

## Residual Risks

1. Production remains single-market; multi-market snapshot or dispatch support requires a separate work unit.
2. Immutable revision growth remains intentionally unoptimized until runtime evidence justifies retention work.
3. Provider outages can leave exact pending/ambiguous envelopes requiring bounded retry and operator audit.
4. Historical v1 delivery facts that were never persisted cannot be reconstructed during migration.
5. Release, remote upgrade, pointer migration, real-send canary, and scheduler observation remain separate approval gates.

## Conclusion

**pass** — the corrected plan is code-generation-ready, matches the latest mainline notification authority and terminal safety boundaries, and is accepted for final integration.
