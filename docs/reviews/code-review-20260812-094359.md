# PR Code Re-review

## Scope

- Mode: GitHub Draft PR re-review
- PR: `https://github.com/liuxie066/options-monitor/pull/149`
- Branch: `fix/candidate-brief-evidence-integrity`
- Base: `github/main@b85607be` (`1.13.13`)
- Prior review: `docs/reviews/code-review-20260812-093929.md`
- Fix artifact: `docs/gateflow/candidate-brief-evidence-integrity/pr-review-fix.md`
- Output file: `docs/reviews/code-review-20260812-094359.md`
- Included scope: complete work-unit diff after latest-main integration, all prior finding dispositions, compact fixed
  report, candidate evidence classification, sealed reason propagation, AI candidate-presence semantics, CC+LP
  configuration gating, tests, docs, and mergeability
- Excluded scope: runtime replay, notification delivery, release/deploy/upgrade, merge, and unrelated primary
  worktree changes
- Parallel review coverage: none

## Finding dispositions

### CR-PR-01 — accepted — 已修复

The compact fixed-report reminder path now displays only recognized stable hard-evidence causes from an
`opening_candidate_strategy_partial_data` row. `term_matched_rv_unavailable` retains its specific Chinese RV text;
generic partial gaps remain hidden. The fixed-card regression proves both sides in one input.

### CR-PR-02 — accepted — 已修复

The compact projection keeps one global AI-unavailable notice without collapsing per-family facts. Sell Put and
Covered Call modules remain independently visible, canonical pre-budget candidate presence controls their wording,
and a family with no raw candidates says `本轮无可展示的策略排序。`. A candidate-bearing family does not repeat
the global boilerplate. The pre-existing render-budget regression remains green.

### CR-PR-03 — accepted — 已修复

Only explicit `opening_contract_status=ready` can use the definitive calculation-reason allowlist. Missing status is
canonical `input_invalid`; other non-ready statuses retain the Candidate Engine reason. The new regression proves a
missing-status net-premium outcome remains unresolved.

### Prior findings

- CR-S1-01: `已修复`.
- CR-S1-02: `已修复`.
- CR-S2-01: `已修复`.
- CR-AGG-01: `已修复`.

## Findings

No new material finding.

## Validation

Core candidate/Brief/render suite:

```text
/Volumes/liuxie的硬盘/workspace/options-monitor/.venv/bin/python -m pytest -q \
  tests/test_candidate_scanning_evidence.py \
  tests/test_scan_volume_gate_min_zero.py \
  tests/test_opening_candidate_snapshot.py \
  tests/test_daily_decision_brief_service.py \
  tests/test_daily_decision_brief_renderer.py \
  tests/test_ai_decision_advice_render.py
```

Result: `160 passed`.

Related AI Advice, Daily Brief agent/CLI/domain/notification/repository/scenarios, CC+LP, and Combo Yield suite:
`400 passed`.

- `python -m compileall -q domain src tests`: passed.
- Ruff on all changed source/tests: `All checks passed!`.
- `git diff --check`: passed.
- Latest-main merge conflict markers: none.
- PR scope against `github/main`: 28 functional/audit files before these PR-review artifacts; no generated config,
  secrets, runtime artifact, notification write, or broker/ledger state included.

The re-review covered HK/US and Sell Put/Covered Call symmetry; explicit ready-vs-missing opening status; genuine
quote/RV/binding failures; lawful zero candidate; nested sealed reason precedence; fetched prefetch success;
template-expanded current-market CC+LP requirements; candidate existence before render budgets; compact and legacy
fixed-report behavior; and latest-main release-context integration.

## Documentation decision

`docs/AI_DECISION_ADVICE_DESIGN.md` and `docs/OPTION_NOTIFICATION_EXPERIENCE_PRD.md` now explicitly distinguish
generic partial-data suppression from recognized sealed hard-evidence alerts, and document that the single global AI
notice cannot replace per-family candidate-presence facts. No public schema or command changed.

## Open Questions

- None.

## Residual Risk

- Runtime replay and scheduled delivery remain assigned to separately authorized release/upgrade verification.
- Manual symbol-subset CC+LP config propagation remains assigned to a later work unit.
- Future definitive calculation reasons require separate proof and remain assigned to later work.

All residual risks are classified and do not block this source-only Draft PR.

## Conclusion

`pass`

All accepted PR-review findings are fixed with deterministic regressions. The work unit is ready for its accepted PR
review commit and final push.
