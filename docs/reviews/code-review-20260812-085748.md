# Code Review

## Scope

- Mode: current changes
- Branch: `fix/candidate-brief-evidence-integrity`
- Base: `0893f5e0` (accepted Slice 1 commit)
- Output file: `docs/reviews/code-review-20260812-085748.md`
- Included scope: complete Slice 2 source/test diff, implementation artifact, configuration authority, Brief projection,
  AI fallback behavior, and focused validation
- Excluded scope: accepted Slice 1 internals; unrelated primary-worktree user changes; runtime/release/deploy
- Parallel review coverage: 无

## Findings

### CR-S2-01-未修复-高-模板启用的 CC+LP 被 Brief 谓词漏判并绕过必需快照校验
- **入口/函数**: `assemble_daily_decision_brief()` -> `_cc_lp_snapshot_required()`
- **文件(行号)**: `src/application/daily_decision_brief_service.py:944-965`
- **输入场景**: 当前市场 symbol 通过 `use` 引用 template；template 中
  `combo_yield.enabled=true, variant=cc_lp`，symbol 本身不重复写 `combo_yield`。
- **实际分支**: `_cc_lp_snapshot_required()` 直接把原始 symbol row 交给
  `resolve_yield_enhancement_cfg()`；该 helper 只解析 row 内现有的 `combo_yield`，不会展开 `use`。谓词返回
  `False`，因此不调用 `_load_cc_lp_snapshot_family()`。
- **预期行为**: 与扫描/预取路径使用同一份有效 symbol policy。只要当前市场的有效配置启用 CC+LP，缺失或无效的
  sealed CC+LP snapshot 就必须 fail closed 为 Brief data gap。
- **实际行为**: 预取路径先通过 `apply_profiles()` 展开 template 后运行 CC+LP，而 Brief 收到原始
  `request.base_cfg` 并跳过同一快照要求；报告可在必需证据缺失时错误地呈现为无该缺口。
- **直接证据**: `tick_notification_flow.py:740-749` 将 `request.base_cfg` 原样交给 Brief；
  `multi_tick/required_data_prefetch.py:1234-1237` 明确先以 `resolve_templates_config(cfg)` 和
  `apply_profiles()` 展开 symbol；新谓词 953-960 行没有对应展开。现有测试只覆盖 inline
  `combo_yield`，未覆盖 template 引用。
- **影响**: CC+LP 证据完整性在受支持的模板配置下 fail open，违反本切片“required CC+LP failures still do”
  的完成信号，也会让扫描事实与决策简报状态分裂。
- **建议改法和验证点**: 在谓词中使用现有 `resolve_templates_config()` + `apply_profiles()` 得到有效 symbol
  config，再调用现有 yield policy helper；新增 template-enabled CC+LP 缺快照回归，并增加 symbol inline
  override 禁用 template CC+LP 的反例以证明 merge precedence 正确。配置解析错误保持 fail closed/显式失败，
  不应静默降级为“不需要”。
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 高

## Validation

```text
/Volumes/liuxie的硬盘/workspace/options-monitor/.venv/bin/python -m pytest -q tests/test_daily_decision_brief_service.py tests/test_daily_decision_brief_renderer.py tests/test_ai_decision_advice_render.py
```

Result: `114 passed`.

```text
/Volumes/liuxie的硬盘/workspace/options-monitor/.venv/bin/python -m ruff check src/application/ai_decision_advice/render.py src/application/daily_decision_brief_renderer.py src/application/daily_decision_brief_service.py tests/test_ai_decision_advice_render.py tests/test_daily_decision_brief_renderer.py tests/test_daily_decision_brief_service.py
```

Result: `All checks passed!`.

`python -m compileall` for the three changed source modules: passed. `git diff --check`: passed.

## Open Questions

- 无。

## Residual Risk

- Slice 1 plus Slice 2 end-to-end behavior remains `covered by aggregate DeepReview and clean-clone validation`.
- Runtime replay and delivery remain `assigned to separately authorized release/upgrade verification`.

## Conclusion

`fail`

CR-S2-01 must be fixed and re-reviewed before the Slice 2 protected commit.
