# Code Review

## Scope

- Mode: PR
- Branch or PR: `liuxie066/options-monitor#164` — feat(strategy-lab): add W0R history K-line quota boundary（OPEN / DRAFT / MERGEABLE）
- Base: `main@0da901b30cd26242636b9ec967b8aa281f61937c`（GitHub PR base 与本地 merge-base 一致）
- Head: `feat/sell-put-top1-w0r-history-quota@7c0ff70bd83ef04a23734d28ff8fc957c0f4cb2d`（与本地 HEAD 一致）
- Output file: `docs/reviews/pr-review-164-20260815-175313.md`
- Included scope: PR 相对 base 的完整 diff（3 commits，18 files，+783/-26）——`FutuGateway.get_history_kl_quota()` + `_normalize_history_kline_quota_response()`、`OpenDFetchLimits.history_kline` 配置解析、focused tests、capability preflight 更新、dependency graph 再生、全部 Gateflow/review artifacts；并对照已安装 SDK（futu `open_quote_context.py:1883-1901`、`quote_query.py:2184-2222`、`common/utils.py:182-192`）核对协议形状
- Excluded scope: live OpenD probe、W5 runner、quota sufficiency policy、receipt persistence（plan §2/§8 明确分配后续 work unit）
- Parallel review coverage: 无；范围窄，主 reviewer 沿 `get_history_kl_quota -> _normalize_history_kline_quota_response -> _raise_mapped/_map_error -> FutuGatewayError` 与 `runtime.opend_rate_limits.history_kline -> _raw_opend_endpoint_rate_limit -> resolve_opend_fetch_limits -> as_config / fetch_kwargs / discovery_kwargs / config_validator` 两条真实链路逐行走读，并逐一核对 5 个 `resolve_opend_fetch_limits` 生产消费点（`required_data_prefetch.py:129-136`、`positions/maintenance.py:276`、`positions/assigned_stock_quotes.py:120`、`performance/evidence_collection.py:263-317`）
- CI/check 状态（父任务经 `gh` 核实并提供）：agent-plugin pass、guardrails pass、Analyze(actions) pass、Analyze(python) pass、CodeQL pass，全部 COMPLETED/SUCCESS

## Findings

未发现实质性问题。

PR-level 走读记录（支撑无 finding 结论的直接证据，超出前序 aggregate review 的增量检查）：

- PR facts 一致性：GitHub PR base/head/draft/mergeable 与本地 `origin/main...HEAD` 范围一致；HEAD `7c0ff70b` 相对实现提交 `e107db50` 仅多两个 Gateflow 记录提交（`16a4f0b1`、`7c0ff70b`），无未审查的实现增量。
- 协议边界（对照已安装 SDK 源码）：SDK 成功路径恒返回 `(RET_OK, (used_quota, remain_quota, detail_list))`；`detail_list` 恒为 list 且每项含 `code/name/request_time`，`code` 经 `merge_qot_mkt_stock_str` 生成（`Market.to_string2` + 大写市场前缀），`request_time` 经 `str(item.requestTime)`。实现的形状校验（三元组 payload、非 bool `Integral` 非负计数、detail list/dict、code 非空、时间 canonical round-trip）与 SDK 成功路径兼容；任何畸形/错误路径经 `_raise_mapped(action="get_history_kl_quota")` -> `_map_error`（`futu_gateway.py:612-616, 583-602`）映射为带 reason_code 的 `FutuGatewayError` 子类，与既有 16 个 gateway 方法的错误边界模式一致，无新增错误分类分支。
- 严格校验合法性：fail-closed 方向全部有测试固化（14 case failure 矩阵 + success case）；`ret != 0` 时 `RuntimeError(payload)` 携带 provider 错误串并经 `_map_error` 分类，2FA/auth/rate-limit 语义不丢失。`len(details) != used_quota` 与 duplicate-code 拒绝均为 plan §5 rule 5 明确授权的严格审计边界，方向是拒绝可疑事实而非放行。
- 配置兼容性与参数生效链：canonical key `history_kline` 无别名（plan §6 明确）；`_endpoint_rate_limit` 默认 `60/30s/30s wait`，override round-trip 有测试；`OPEND_RATE_LIMIT_ENDPOINT_KEYS` 自动纳入，config_validator 无需改动即接受且 acceptance fixture 已扩展。`fetch_kwargs()`/`discovery_kwargs()`/`OPEND_FETCH_KWARG_KEYS` 明确不含 history 字段（有断言 `not any(key.startswith("history") ...)`），既有 candidate fetch/discovery 调用签名零变化。
- 现有消费点影响（PR-level 增量检查）：`resolve_opend_fetch_limits` 的全部 5 个生产调用点均按属性名读取既有 endpoint（`.option_chain`/`.market_snapshot`），新增 `history_kline` 字段为纯增量；`required_data_prefetch._resolve_opend_fetch_cfg` 按 key 名显式挑选三个既有 endpoint，不受 `as_config()` 新 key 影响。frozen dataclass 新字段无默认值会破坏位置构造，但仓内无位置构造调用点（`from_flat_kwargs` 与两个 resolver 均按关键字构造）。
- 文档准确性：preflight 只把窄缝标为 `SDK/project source green；live unknown`，W0R overall 保持 `runtime_no_go`；"官方首页请求上限 60 calls / 30s" 与 Futu v10.9 文档一致；implementation-s1 如实记录 9 个全量测试失败均为环境原因及复验路径（临时 venv symlink + 沙箱外 HTTP 复跑）。Gateflow 证据链完整（goal-confirmation → plan/plan-fix → plan-review ×2 → implementation-s1 → code-review → aggregate-review → ready-to-open-draft-pr），各 artifact 状态一致。
- 依赖图：diff 为 generator 对 origin/main stale 状态的机械再生（939 files / 590 production modules / 0 cycles），本 slice 新增零 import；`--check` 通过。
- 过度设计/目标偏离检查：无 provider registry、factory、schema、state machine、CLI、Agent tool、timer、生产 config 或 caller；diff 与 plan §4 允许清单一一对应，符合 PONYTAIL 最小实现与 W0R source-only 边界。
- CI 与测试有效性：5 项 check 全部 green；focused（58）+ adjacent（114）+ 全量（4889 passed, 10 skipped）均已在 Gateflow 证据中记录并由前序 review 独立复跑确认。

## Open Questions

- 无。

## Residual Risk

- Live provider 的 `request_time` 实际格式（时区语义、是否恒为 canonical `%Y-%m-%d %H:%M:%S`）未由真实 receipt 证明；若非 canonical，gateway fail closed——这是 plan 明确选择的行为，live 形状归属已授权的 W0R live probe。
- Duplicate-code 拒绝假设 live 不会返回同一标的多行明细；若真实返回同 code 多行则 fail closed。该语义由 plan §5 rule 5 授权，live 证据待 probe。
- Test gap（非阻断）：failure 矩阵未单独断言 code 首尾空白 trim 后接受的分支，以及 `from_flat_kwargs` 消费 history 默认值的显式断言；前者符合 plan trim-normalize 规则，后者行为已由 success/default resolver 测试隐式覆盖。
- 全量测试的 9 个环境失败（8 个 worktree 缺 `.venv` 入口测试 + 1 个沙箱 loopback HTTP 测试）已分别复跑 green，但该复验发生在提交前工作区，非 CI 环境；CI 5 项 check green 已覆盖 PR 合并门禁。
