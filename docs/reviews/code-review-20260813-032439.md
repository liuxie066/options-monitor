# PR Code Review

## Scope

- Mode: GitHub Draft PR review
- PR: `https://github.com/liuxie066/options-monitor/pull/151`
- Branch: `refactor/candidate-csv-retirement`
- Reviewed base/head: `main@6940a96cc6e546578720bf6af0f7ae70d5545037` -> `53a68f295783ace26119dd1a0e4e3ef3000d2fe6`
- Output file: `docs/reviews/code-review-20260813-032439.md`
- Included scope: all 117 changed files, 12,701 additions and 5,918 deletions; accepted plan and prior finding dispositions; latest-main conflict resolution; candidate production, terminal publication, current consumers, historical classification, research/archive, Shadow Replay, Strategy Lab, Combo Funding Put projection, tests, docs, mergeability and GitHub checks
- Excluded scope: PR merge, Ready transition, release, deployment, remote service/config/data mutation, live OpenD or notification execution, historical artifact deletion, and unknown external private consumers
- Parallel review coverage: none; GitHub's diff endpoint rejected the more-than-20,000-line patch with HTTP 406, so code review used the exact fetched `origin/main...HEAD` Git object range while GitHub supplied PR identity, counts, mergeability, description and checks

## Prior finding dispositions

- PlanReview findings were accepted into the plan and fixed before implementation.
- S1 findings S1-CR-01 through S1-CR-04, S2 findings S2-CR-01 and S2-CR-02, and S3 findings S3-CR-01 and S3-CR-02 are fixed and re-reviewed in committed Gateflow artifacts.
- Aggregate findings AGG-CR-01 through AGG-CR-03 are fixed and passed aggregate re-review.
- Latest-main integration review `docs/reviews/code-review-20260813-031626.md` passed with no actionable finding after integrating `main@6940a96c`.

## Findings

No actionable findings.

## PR-level evidence

- GitHub reports PR #151 open, Draft, mergeable and `CLEAN`, with base `6940a96c` and reviewed head `53a68f29`.
- Agent Plugin, Guardrails, CodeQL Actions, CodeQL Python and aggregate CodeQL checks all passed on the reviewed head.
- The PR body matches the implemented source-only boundary, validation counts, retained CSV contracts and separate operational/historical-cleanup authority.
- The local range and GitHub metadata agree on 117 files, 12,701 additions and 5,918 deletions. The GitHub diff endpoint limit is a presentation/API constraint; no path is omitted from the local Git range.
- Production source contains none of the retired candidate CSV names, readers or writers outside the two explicit historical-metadata classifiers. Those classifiers inspect names only and contain neither `read_csv()` nor `to_csv()`.
- Retired scanner output/reject-log parameters, CLI flags, Combo `output_mode`, CSV-only adapters and v1 status-index publication are absent. `output_mode` remains only as a targeted config-validation error, and the v1 index name remains only in bounded historical classification/inventory.

## Adversarial ownership review

- Current candidate completeness is owned by the write-once `candidate_snapshot_manifest.v1` after it binds the v2 status index, exact owners, paths, hashes, scopes, terminal statuses, reasons and counts. Interrupted or inconsistent modern runs fail closed.
- Opening, SP+LC and CC+LP validators bind run, account, market, config/policy identity, owner and exact strategy scopes. The opening owner additionally requires the complete current decision contract at manifest publication and current-consumer load.
- Daily Brief and Agent filter/rank explanations enter through the manifest-bound bundle. They do not salvage a partial owner or scan past a newer incomplete run to expose stale evidence.
- Historical classification has deterministic precedence. Valid pre-manifest v1 snapshot bundles are explicitly limited; modern evidence missing its manifest, malformed evidence and CSV-only runs are unsupported. Legacy candidate CSV names can influence classification but their bytes never contribute facts.
- Research, archive, Shadow Replay and Strategy Lab derive candidate observations only from validated owner snapshots. Trace JSONL can enrich decisions but cannot manufacture a terminal candidate universe; scoped coverage is recomputed and unknown-market unsupported evidence remains fail-closed.
- Combo Funding Put projection is derived from a supported manifest-bound SP+LC v2 owner and written as JSONL with a receipt/facet that binds source and dataset manifests, hashes, identities and row counts. Consumption revalidates the dataset, canonical path, receipt, facet, rows and hashes under the read boundary rather than recomputing underwriting or reading CSV.
- Required-data, Close Advice, symbols summary, mark/outcome and unrelated explicit CSV contracts remain outside the retirement set.

## Validation

```text
latest-main focused integration suite: 370 passed
sandbox-compatible full suite: 4540 passed, 10 skipped, 5 existing warnings
localhost-only HTTP suite: 4 passed
ruff check src domain scripts tests: passed
compileall src domain scripts tests: passed
dependency graph: current; production_modules=568; cycles=0
US/HK example config validate and build --dry-run: passed
git diff --check: passed
GitHub Agent Plugin: passed
GitHub Guardrails: passed
GitHub CodeQL actions/python and aggregate status: passed
```

## Open Questions

- None.

## Residual Risk

- Historical candidate CSV files remain immutable cold artifacts. They are inventoried/classified but not parsed or deleted; any retention cleanup requires a separately authorized, recoverable work unit.
- Bounded pre-manifest v1 snapshot support intentionally cannot prove modern completeness or strict promotion readiness. It remains a limited historical evidence class, not current authority.
- No live OpenD request, notification delivery, production runtime rewrite, release, deployment or remote upgrade was performed. Runtime cutover and production evidence require separate authorization.
- Unknown external private consumers of removed internal adapters are outside repository visibility and receive no hidden compatibility shim by design.

## Conclusion

`pass`. The PR is ready for the accepted PR-review commit while remaining Draft.
