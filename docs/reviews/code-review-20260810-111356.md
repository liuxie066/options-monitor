# Code Review

## Scope

- Mode: current changes
- Branch or PR: `fix/ai-decision-advice-account-context`
- Base: accepted plan commit `aca09b53`
- Output file: `docs/reviews/code-review-20260810-111356.md`
- Included scope: S1 final PM/option source authority, verified evidence-symbol derivation, orchestration handoff, source-failure semantics, evidence-index identity, and changed tests/artifact
- Excluded scope: S2 preparation-to-projection multi-account regression; Candidate Engine; projection formulas; external collector/provider; production config/runtime; release/deployment
- Parallel review coverage: 无；scope is one narrow contexts-to-orchestration data path

## Findings

未发现实质性问题。

The review traced the real entry from `run_or_reuse_ai_decision_advice()` through pre-freeze symbol derivation,
evidence-index freezing, final `build_frozen_inputs()`, portfolio/option freeze, projection inputs, and external evidence
hashing. The new helper reuses the same freeze owners instead of reproducing their source predicates. Ready/degraded PM
facts require the canonical prepared schema, PM provider, mapped account marker, and passed validation; schema-valid
formal unavailable PM artifacts retain soft-dependency behavior. Option facts and FX require the canonical prepared
schema and source marker in addition to existing account/trust validation. Rejected sources cannot contribute evidence
symbols or model facts.

Adversarial checks covered missing/wrong schema, wrong provider, missing mapped PM account, failed PM validation,
legacy option source, malformed option rows behind a rejected source, cross-account bindings, formal unavailable PM,
zero positions, and invalid-source versus unavailable evidence hash equivalence. The change preserves import boundaries
and removes the weaker orchestration-owned source parser.

## Open Questions

- 无。

## Residual Risk

- S1 proves the final consumer and pre-freeze evidence boundary. The accepted S2 plan still needs to prove nonzero
  `lx`/`sy` positions from one shared SQLite ledger through preparation, strict load, and per-account projection.
- `verified_relevant_symbols()` deterministically performs the existing in-memory freeze once before evidence loading,
  and final assembly performs it again afterward. There is no I/O in those helpers and no semantic divergence found;
  a future optimization must not reintroduce a second source-rule owner or mutable intermediate authority.
- No live PM, OpenD, notification, DeepSeek, production runtime, or GitHub CI was exercised in this slice review.
