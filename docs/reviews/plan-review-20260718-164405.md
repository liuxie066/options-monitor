# Plan Review — release upgrade optimization Slice 0 + Slice 1

## Reviewed target and scope

- **Target**: `docs/plans/release-upgrade-optimization-plan.md`
- **Approved implementation scope**: Slice 0（基线冻结）与 Slice 1（测试/preflight 提速）
- **Excluded from this review gate**: Slice 2–8 的实现；它们仍需在各自进入执行前重新确认事实和独立 gate。
- **Base facts**: `origin/main` `b5ba7693`, VERSION `1.2.413`

## Assumptions tested

1. **慢测试的真实 sleep 不是被测核心行为**：成立。两个测试分别关注保护/错误聚合，cooldown 数值已有 dedicated test 覆盖。
2. **monkeypatch sleep 不会删除 cooldown assertion**：成立，计划明确保留 dedicated test 的 `sleeps == [30.0]`。
3. **full pytest 已包含 focused agent/plugin tests**：成立于当前默认 pytest collection；计划要求用 `tests/test_release_test_plan.py` 固化 command-plan 契约，避免未来静默遗漏。
4. **目标可只改 tests/preflight**：成立；当前证据没有要求修改 production scheduling/cooldown implementation。
5. **45 秒目标可信**：成立但属于性能 acceptance，不是 correctness contract。去掉约 60 秒真实等待后，从约 101 秒降到约 41 秒仍有余量。

## Special lenses

### Architecture boundary

当前 slice 保持在 test/release-preflight 边界，不穿透 domain、runtime config 或 production service ownership。

### Best practice

用 deterministic sleeper monkeypatch 替代真实 wall-clock wait，提高测试速度和稳定性；full mode 避免重复执行同一测试集合，同时保留 fast-only 模式。

### Optimal solution

局部 monkeypatch 是最小、最直接方案；无需新增 clock service、scheduler abstraction 或生产 feature flag。

### Overengineering

未发现需要新增 abstraction、dependency 或配置。计划已明确禁止为两个测试建立通用时间框架。

### Overcoupling

Slice 0 + 1 与后续 timeout/dependency/venv/CI workflow 变更分离，可独立验证和回滚。

## Material findings

没有 material finding。当前批准的 Slice 0 + Slice 1 已足够 code-generation-ready。

## Open questions

无 blocking open question。

## Residual risks and tracking

| Risk | Classification / destination |
|---|---|
| hosted/local machine timing 存在波动，45 秒可能受瞬时负载影响 | 当前 slice：使用 3 次 median 验收，不以单次超时判失败 |
| 未来 pytest 配置可能排除 focused files | 当前 slice：由 release test-plan contract 固化；未来变更需同步更新测试 |
| timeout、dependency、venv 和 uv 风险仍存在 | covered by later approved slices；不属于本 work unit |

## Conclusion

**pass**

Slice 0 + Slice 1 可以进入 implementation。实施必须保持当前窄边界，不提前做 Slice 2–8。
