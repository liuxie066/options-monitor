# Code Review

## Scope

- Mode: PR
- Branch or PR: #165 `feat(strategy-lab): add exact-expiration close boundary` (https://github.com/liuxie066/options-monitor/pull/165)
- Base: `main` (`origin/main@813ec6f8021148ff6d152ff4ee4f5c39e36897fc`)
- Head: `feat/sell-put-top1-w0r-exact-expiry-close@2ddc111d93a58e4eee4dbd8112bd032f79e849f5`
- Output file: `docs/reviews/pr-165-review-20260815-195345.md`
- Included scope: complete PR diff（`src/infrastructure/futu_gateway.py`、`tests/test_futu_gateway_minimal.py`、capability preflight、DEPENDENCY_GRAPH、Gateflow artifacts），installed Futu SDK v10.9 `request_history_kline()` contract，以及现有 generic history/QFQ consumer 回归面
- Excluded scope: live OpenD probe、W5 runner、receipt schema、生产配置、release/deploy（本 work unit 明确 non-goal）
- Parallel review coverage: 无；单 reviewer 沿真实 SDK 请求/响应/分页路径与全部新增测试走读

PR facts（本机 `gh` 独立复核）：

- author: `liuxie066`；base `main`；head `2ddc111d93a58e4eee4dbd8112bd032f79e849f5`；state OPEN；DRAFT；MERGEABLE。
- CI：Analyze (actions) pass、Analyze (python) pass、CodeQL pass、agent-plugin pass、guardrails pass。
- PR/base/head drift：本地 worktree HEAD 与远端 head SHA 一致；`git merge-base origin/main HEAD` = `origin/main` = `813ec6f8`，无 drift。

## Findings

未发现实质性问题。

重点核查面与证据：

- External SDK protocol：新方法绕过 permissive `request_history_kline()` wrapper，直接调用 `quote.request_history_kline()`；`fields=["time_key","close"]` 经 `_normalize_history_kline_kwargs()` 映射为 `KL_FIELD.DATE_TIME/CLOSE`，SDK 组装列 `[code, name, time_key, close]`，与 response validator 要求的 `code/time_key/close` 列一致。`max_count=2` 的语义已对照 SDK 源码核实：单次响应最多 2 行且 `has_next` 时才返回非空 `page_req_key`，因此“非空 continuation 或多于一行 fail closed”完整覆盖了不完整/歧义结果。
- Empty vs malformed：只有带完整必需列的零行 DataFrame 返回 `None`；空 list、缺列、畸形 `to_dict`、非 dict 行均 fail closed，区分清晰。
- Pagination：实现先调用 `_normalize_history_kline_kwargs()`，随后显式执行 `params["page_req_key"] = None`，因此最终 SDK kwargs 确实携带显式 null 初始 continuation key；响应侧 `page_req_key not in (None, "", b"")` 拒绝任何真实 continuation。
- Code/date/cardinality/price：请求侧 code trim+upper、expiration 要求 canonical `YYYY-MM-DD` 且 `date.fromisoformat` round-trip；响应侧 row code trim+upper 与请求绑定，`time_key` 只接受 `YYYY-MM-DD` 或 `YYYY-MM-DD HH:MM:SS` 两种 canonical 形式且日期必须等于 expiration，close 要求非 bool Real、finite、>0。code 大小写归一化与既有 `_normalize_history_kline_quota_response()`（1119 行同模式）一致，属 gateway 既定 wire-text 约定。
- 错误分类：所有 input/provider-shape/value 违规经 `_raise_mapped(action="get_exact_expiration_close")` 映射为 `FutuGatewayError`；SDK 抛出的 2FA/auth/rate-limit/transient 异常保留既有 mapped class（测试覆盖 rate-limit）。
- QFQ 回归：generic `request_history_kline()` 与 `_fetch_qfq_history_rows()` 未修改；邻接回归 `tests/test_short_vol_metrics.py tests/test_strategy_lab_top1_research.py` 26 passed。
- 过度设计/readiness 夸大：无新增 abstraction、无 runner/receipt/storage；preflight 只把 project source capability 标 green，live receipt 与 W0R overall 保持 `runtime_no_go`，无 readiness 夸大。
- 本机复验：focused `test_futu_gateway_minimal.py` 25 passed；邻接 26 passed；`ruff` 通过；`git diff --check` 通过；worktree clean。

## Open Questions

- 无。

## Residual Risk

- Live `time_key` 真实形态、exact-expiration 当日可用性、响应耗时与 quota 占用仍未证明，已明确分配给单独授权的 W0R live probe；源码合同不能替代 live evidence。
- 若未来真实 provider 返回的 `code` 大小写与请求不一致（当前无证据），大小写归一化绑定会放行；与既有 quota 校验同模式，暂不视为缺陷。
- 未跑 full repository suite（4900 tests）；aggregate review 已记录其通过，CI Analyze (python)/guardrails 均 green，残余风险低。
