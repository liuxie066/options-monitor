# Code Review

## Scope

- Mode: current changes, clean S1 slice re-review after all accepted fixes
- Branch: `feat/earnings-near-expiry-window`
- Base: accepted plan commit `fa5076f2`
- Output file: `docs/reviews/code-review-20260811-222733.md`
- Included scope: OpenD earnings-calendar v2 fetch/projection/validation, shared N=6 natural-day Candidate Engine
  policy, Sell Put/Covered Call/Combo Funding Put parity, cash/capacity and evidence-status ownership, opening/Combo
  JSON snapshot validation, AI Advice/Daily Brief completeness disclosure, non-authoritative CSV audit boundaries,
  docs, generated dependency artifacts, and all direct tests
- Excluded scope: live OpenD/provider behavior, production configuration/runtime artifacts, release/deploy/notification,
  historical provider completeness guarantees, and unrelated strategy formulas
- Parallel review coverage: 无；按 OpenD interval -> per-expiry projection -> Candidate Engine -> strategy status ->
  sealed JSON -> Advice/Brief 完整链路复审

## Prior finding resolution

- `CR-S1-01`: fixed and verified. Combo captures calculation/underwriting decisions and preserves partial/unavailable
  status through the sealed Combo snapshot and Daily Brief.
- `CR-S1-RR-01`: fixed and verified. Combo cash processing is enrichment-only before Candidate Engine; capacity is
  decided once, while diagnostic earnings gaps remain auditable.
- `CR-S1-RR-02`: fixed and verified. Malformed JSON collection items fail with
  `OpeningCandidateSnapshotError` before field access.
- `CR-S1-RR-03`: fixed and verified. Candidate-universe completeness uses the existing clean/benign reason taxonomy
  and outcome-unresolved contract scopes, so non-benign sibling gaps are disclosed to Advice.

## Findings

无。

## Verification evidence

- Final affected policy/strategy/snapshot/Advice/Brief set: `265 passed`.
- Final snapshot/Advice/Brief focus: `106 passed`.
- Full repository run: `4717 passed, 10 skipped`; the sole failure was sandbox denial of binding `127.0.0.1`.
- Exact read-only localhost HTTP test outside the sandbox: `1 passed`.
- Ruff over `domain`, `src`, and `tests`: pass.
- `python -m compileall -q domain src`: pass.
- Dependency graph: current, `585` production modules, `0` cycles.
- `git diff --check`: pass.

## Open Questions

- 无。已确认的策略仍是 N=6 自然日、0/6 日包含、7 日及更远不硬过滤、同日全天视为
  未发生，并统一适用于 Sell Put、Covered Call 和 Combo Yield Funding Put。

## Residual Risk

- OpenD does not expose a per-symbol completeness guarantee. Successful hard-window intervals are therefore the
  strongest accepted absence evidence under the confirmed policy; stronger provider assurance requires a later
  work unit or explicit additional source decision.
- Live OpenD was not queried in this local source gate. Runtime verification belongs to a separately authorized
  release/upgrade gate.
- Parsed-market-data, audit/report/history, Close Advice, research, and Shadow Replay CSVs still exist. They are not
  formal opening-candidate authority; full CSV retirement is a separate work unit.
- No PR/CI exists yet; draft PR and PR review remain later Gateflow gates.

No live OpenD request, notification, config/runtime write, broker/ledger write, release, deployment, or upgrade was
performed during this review.
