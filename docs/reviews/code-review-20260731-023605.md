# Code Review

## Scope

- Mode: current changes
- Branch or PR: `main` workspace
- Base: `main@dcd0a7b04ed56ba51df08ed6c0432409624eceea`
- Output file: `docs/reviews/code-review-20260731-023605.md`
- Included scope: all staged, unstaged, and untracked implementation changes relative to `main`, with end-to-end review of option-close evidence intake, Push/Poll identity, lifecycle case/state, stock-settlement matching, close-reason resolution, canonical ledger writes/projection, notification Outbox, migration, Daily Brief degraded delivery, tests, and contract documentation
- Excluded scope: live Futu/OpenD queries, live Feishu delivery, production ledger/runtime artifacts, migration apply, service changes, release, and deployment
- Change size: 66 tracked files, 9130 insertions, 898 deletions, plus untracked lifecycle implementation/tests/docs
- Validation: `git diff --check`; Ruff over `domain src tests scripts`; generated dependency graph check (`production_modules=571`, `cycles=0`); 301 focused lifecycle/Daily Brief tests; full suite `3761 passed, 10 skipped, 6 warnings`
- Parallel review coverage: 无；本轮按单一端到端生命周期与账本状态机完成聚合审查

## Findings

### 1-未修复-严重-股票交割匹配会选择首个宽松候选并把无关股票成交写成 assignment 或 exercise

- **入口/函数**: `resolve_trade_deal()` -> `_resolve_stock_settlement_leg()` -> `_find_matching_option_case()` / `_find_matching_stock_evidences()` -> `_stock_matches_lifecycle_close()` -> `_write_v2_lifecycle_close_from_case()` -> `reconcile_lifecycle_evidence()`
- **文件(行号)**: `src/application/trades/lifecycle.py:288-360,829-886,1193-1253`; `src/application/trades/lifecycle_reconciliation.py:819-892`; `src/application/ledger/repository.py:847-878`; `src/application/ledger/writer.py:740-798`
- **输入场景**: 同一 internal account 和 underlying 下存在两个到期日或两个可兼容 case；随后出现方向、数量近似匹配的普通股票成交。成交来自另一 Futu account，成交价与 strike 有小幅偏差，或者成交时间已晚于冻结 settlement deadline。
- **实际分支**: `_find_matching_option_case()` 按 repository 的 `updated_at_ms DESC` 顺序遍历并在首个命中项立即返回，不计算全局候选集合。匹配不比较 Futu account id；到期日后的成交只有时间下界、没有 deadline 上界；价格通过 `float` 和最高 `strike * 0.001` 容差比较；数量允许小于 case 总目标。writer 的二次校验只覆盖方向、数量、symbol 和时间下界，也不验证 Futu account、Decimal 精确 strike 或 settlement deadline。
- **预期行为**: 自动匹配必须同时满足 internal account 与 Futu account id、canonical underlying、方向、Decimal 精确 strike、冻结 observation window、可验证数量和未消费 broker source，并在全部 pending cases 中得到唯一候选；writer 必须在同一 ledger transaction 中重新验证。
- **实际行为**: 任意命中的第一条 case 可被选为 assignment/exercise owner，随后写 terminal event、allocation、projection 和 source claim；case 的更新时间会改变赢家，使同一经济事实的解释依赖可变查询顺序。
- **直接证据**: `lifecycle.py:869-885` 首个匹配即返回；`repository.py:864-870` 使用可变更新时间排序；`lifecycle.py:1200-1204` 对到期日后的时间直接接受；`lifecycle.py:1233-1253` 使用浮点容差且允许 partial quantity；`lifecycle_reconciliation.py:834-892` 没有账户、价格和 deadline 校验。冻结方案 `docs/plans/option-expiry-close-reason-receipt-redesign-plan-20260730.md:691-717` 明确要求这些条件以及全局唯一候选和事务内重验。
- **影响**: 无关股票成交可永久改变 `trade_events -> position_lots` 权威投影，造成持仓数量、收益、风险提示和通知错误；写入后的唯一 source claim 还会让错误解释取得消费权。
- **建议改法和验证点**: 收集全部候选后要求 `len(candidates) == 1`；以 canonical Futu identity、Decimal、冻结 timing policy 和精确/聚合数量匹配；在 writer transaction 内重查 source claim、reservation、remaining、deadline、price 和唯一性。新增跨 Futu account、相同 strike 不同 expiry、`strike ± 0.01`、deadline 后、多个候选、partial/multi-deal 的公开 intake 回归，所有非唯一场景必须 fail closed 且无 terminal event。
- **修复风险（低/中/高）**: 高
- **严重程度（低/中/高/严重）**: 严重

### 2-未修复-严重-close-reason 可在没有 terminal event 或 allocation 时把未平仓案件写成 ledger_written 并创建最终通知

- **入口/函数**: `reconcile_lifecycle_close_reason()` -> `resolve_close_reason()` -> `advance_lifecycle_case_state()` -> `advance_lifecycle_case_state_atomically()`
- **文件(行号)**: `src/application/trades/close_reason_reconciliation.py:104-139,254-318,319-387`; `domain/domain/option_close_reason.py:327-346`; `src/application/ledger/writer.py:1948-2180`; `tests/test_option_close_reason.py:120-143`
- **输入场景**: case 的冻结目标仍有 2 张未结、没有任何 terminal allocation；observation 传入 `stock_match_status="full"` 和 `stock_contracts=1`。
- **实际分支**: application 优先接受 observation 自报的 `stock_match_status`，不会根据 target total 重新推导；domain 看到 `full` 就返回 `resolved`，但 `contracts_resolved` 仍为 1。application 只有 `expiration_no_settlement` 进入 terminal evidence writer；assignment/exercise 的普通 `resolved` 分支直接把 case status 映射为 `ledger_written`，再由 state writer 创建 `resolution_confirmed` Outbox，而 state writer不写 terminal event，也不要求 remaining 为零。
- **预期行为**: `full` 只能由 canonical evidence、effective allocation 和 frozen remaining 推导；assignment/exercise 的最终状态、terminal event、allocation、projection 和最终 Outbox 必须在同一事务中一致写入。
- **实际行为**: case 可以变成 `ledger_written` 并产生“结果已确认”消息，但 `terminal_event_ids=[]`、`allocation_ids=[]`，position lot 仍完全开放。
- **直接证据**: `close_reason_reconciliation.py:123-139` 只有 observation 未提供状态时才按数量推导；`option_close_reason.py:327-346` 对 `full` 不校验目标数量；`close_reason_reconciliation.py:374-387` 把普通 `resolved` 直接映射为 `ledger_written`；`writer.py:1994-2034,2085-2163` 只读取现有 allocation 并可创建 Outbox。现有测试 `test_option_close_reason.py:120-143` 还固定了“目标 2 张、stock 1 张、状态 full 即 resolved”的错误契约。
- **影响**: lifecycle read model、通知和权威账本永久分叉；后续系统或人工会把仍开放的期权当作已完成，且固定 transition key 会增加修正难度。
- **建议改法和验证点**: 禁止 observation 自证 `full`；按有效 stock evidence/allocations 和 frozen target 推导 none/partial/full/conflict；assignment/exercise 的 resolved 分支统一进入 canonical terminal writer，并在事务内断言 projection 与 remaining。新增 0/partial/full/over-allocation 用例，数量不足必须保持 partial，只有 event/allocation 写入且 projection 正确后才允许 `ledger_written` 和最终 Outbox。
- **修复风险（低/中/高）**: 高
- **严重程度（低/中/高/严重）**: 严重

### 3-未修复-高-lifecycle case 身份没有 Futu account id，不同经纪账户的 option anchors 会合并

- **入口/函数**: option deal intake -> `accept_option_close_evidence()` -> `accept_option_close_evidence_atomically()` -> `build_lifecycle_case()` / `lifecycle_case_key()` -> due reconciliation
- **文件(行号)**: `domain/domain/option_lifecycle.py:67-132`; `src/application/ledger/writer.py:1132-1163,1257-1300`; `src/application/trades/lifecycle_runtime.py:155-185`
- **输入场景**: internal account `lx` 映射多个 Futu account；两个经纪账户对同一 option contract 各产生一笔零价 close evidence。
- **实际分支**: evidence/source claim 保存 Futu account id，但 case key 只包含 internal account、broker、contract、side、expiration 和 target lots；既有 v2 case 查找也只按 internal account 与 contract key 复用。case payload 本身没有 `futu_account_id`。
- **预期行为**: case 必须绑定唯一的 internal account + Futu account id；不同经纪账户的 evidence、reservation、observation 和最终原因不得共享 case。
- **实际行为**: 两个 Futu account 的 anchors 会复用同一 case；due reconciliation 又从 source 配置选择一个 Futu account 查询，无法证明它与 case 内所有 evidence 一致。
- **直接证据**: `option_lifecycle.py:67-87` 的 key 参数不含 Futu account；`option_lifecycle.py:118-132` 的 case payload也不含该字段；`writer.py:1257-1270` 仅按 account/contract 复用；`lifecycle_runtime.py:163-177` 要求 runtime source 恰有一个 Futu account，却无法从 case 校验绑定。
- **影响**: 不同经纪账户的 evidence、source ownership、reservation、settlement observation 和原因可能交叉污染；即使单独修复 stock matcher，case 级身份仍不能证明账户归属。
- **建议改法和验证点**: 将 canonical Futu account id 纳入 case immutable identity、case key、case lookup、collector 和 writer 校验；旧 case 仅在一对一可证明时迁移，否则 review-only。新增同 internal account/同 contract/不同 Futu account 的隔离测试，以及单一/歧义旧 case 映射测试。
- **修复风险（低/中/高）**: 高
- **严重程度（低/中/高/严重）**: 高

### 4-未修复-高-expire_close 后到达的股票交割被静默跳过，设计好的 conflict 分支不可达

- **入口/函数**: `resolve_trade_deal()` -> `_resolve_stock_settlement_leg()` -> `_write_lifecycle_close_from_case()` -> `reconcile_lifecycle_evidence()`
- **文件(行号)**: `src/application/trades/lifecycle.py:288-320,353-360`; `src/application/trades/lifecycle_reconciliation.py:519-534`; `src/application/trades/close_reason_reconciliation.py:414-459`
- **输入场景**: complete observation 已为 v2 case 写入 `expire_close`，随后 broker 的 assignment/exercise 股票交割 Push 或 Poll 才到达。
- **实际分支**: stock intake 先找到 `FINAL_STATUSES={"ledger_written"}` case，在调用 canonical reconciliation writer 之前直接返回 `skipped/lifecycle_already_written`。writer 内已有的 `late_settlement_conflicts_with_expire_close` 检查因此无法执行；due reconciliation 又只扫描 `reason_state == cause_pending` 的 case。
- **预期行为**: 保留原 terminal result，但将 case 原子转为 `conflict`，记录固定 reason code并生成一次 operator alert；只能由受控 correction 修正。
- **实际行为**: 迟到股票证据只被 upsert 后静默跳过，case 继续显示已完成，无 conflict、无告警，也不会被后续 due tick自动再处理。
- **直接证据**: `lifecycle.py:311-320` 在 final case 上提前返回；`lifecycle_reconciliation.py:519-534` 才包含正确的冲突分支；`close_reason_reconciliation.py:425-430` 排除非 pending case。冻结方案 `docs/plans/option-expiry-close-reason-receipt-redesign-plan-20260730.md:532-542` 明确要求 conflict、operator alert 和 correction-only。
- **影响**: 已确认的到期未交割结果可与后到的真实 broker settlement 相矛盾，而系统仍向操作员展示旧结果；这是金融事实纠错链路的静默失败。
- **建议改法和验证点**: final case 不能在 transport adapter 提前返回；将 late evidence 交给 canonical writer，由 writer 判定 idempotent 或 conflict。新增 v2 `expire_close -> late Push`、`expire_close -> late Poll` 和重复迟到 evidence 测试，断言无第二 terminal、case conflict、固定 reason code、恰好一个 alert intent。
- **修复风险（低/中/高）**: 高
- **严重程度（低/中/高/严重）**: 高

### 5-未修复-高-到期轮询能看到股票交割却只保存布尔值，Poll-only 链路不能自动判定 assignment 或 exercise

- **入口/函数**: listener due tick -> `reconcile_due_lifecycle_cases_for_source()` -> `collect_broker_settlement_observation()` -> `reconcile_lifecycle_close_reason()`
- **文件(行号)**: `src/application/trades/auto_intake.py:1224-1270`; `src/application/trades/settlement_observation.py:360-373,430-461,820-855`; `src/application/trades/close_reason_evidence.py:265-318`; `src/application/trades/close_reason_reconciliation.py:115-139,414-459`; `domain/domain/option_close_reason.py:327-346`
- **输入场景**: Push 丢失或 listener 重启；settlement deadline 到达时，`history_deals` 已返回有效股票交割，系统只依靠内置 due polling 链路收敛原因。
- **实际分支**: collector 找到 `stock_rows` 后只写 `stock_settlement_present=True`，没有保留已验证的 deal identity、方向、数量、价格、时间、match status 或 allocation proposal；`reconcile_lifecycle_close_reason()` 得不到 `stock_contracts`，推导为 `stock_status="none"`。由于 observation 又明确不是“无股票交割”，domain 无法走到 assignment/exercise 或 expiration_no_settlement，只能 review/pending。listener 每 60 秒先跑 due reconciliation，独立的 history backfill 每 300 秒才运行且可以被配置禁用。
- **预期行为**: Push 和 Poll 应对同一 canonical broker deal形成相同不可变经济证据，并由同一 matcher/writer 得到一致结果；顺序只影响 provenance。
- **实际行为**: Poll collector 证明“有股票交割”却不能证明是哪一笔、多少张、匹配哪个 case，因此其正向结果不能自动收敛；系统必须寄希望于另一条 backfill intake 稍后再次取得相同成交。
- **直接证据**: `settlement_observation.py:366-373` 收集 rows，但 `:445-461` 只传 boolean；`close_reason_evidence.py:265-318` observation schema 不包含 stock allocation事实；`close_reason_reconciliation.py:115-139` 只有已有 allocation 或外部 `stock_contracts` 才能推导 full/partial。冻结方案要求 Push/Poll 同 identity 且经济字段漂移 fail closed（`docs/plans/...:208-304`），验收也明确包含 poll-first。
- **影响**: 用户要求的第二条权威获取链路并未闭环；Push 缺失或 backfill disabled/延迟时，真实 assignment/exercise 会进入人工复核或长期不收敛。
- **建议改法和验证点**: 将 history stock rows 通过与 Push 相同的 canonical deal normalizer、source claim、全局唯一 matcher 和 terminal writer处理；observation 只负责否定性完整快照，不应以 boolean 代替正向经济证据。新增 Poll-only、Push-first/Poll-second、Poll-first/Push-second、backfill disabled 和同 deal provenance 差异测试。
- **修复风险（低/中/高）**: 高
- **严重程度（低/中/高/严重）**: 高

### 6-未修复-高-同 broker key 的 Push/Poll 重放可以改写尚未分配的经济证据

- **入口/函数**: `resolve_trade_deal()` -> `_resolve_stock_settlement_leg()` -> `_upsert_evidence()` -> `SQLiteOptionPositionsRepository.upsert_trade_lifecycle_evidence()`
- **文件(行号)**: `src/application/trades/lifecycle.py:294-328,743-775,1407-1411`; `src/application/ledger/repository.py:86-96,880-916`; `src/application/trades/resolver.py:304-319`; `src/application/trades/inbox.py:18-50`; `src/application/trades/intake.py:632-699`
- **输入场景**: stock-first Push 首次到达时尚无 option anchor，evidence 被保存为 retryable unresolved；稍后 Poll 以相同 canonical broker key 返回，但数量、价格、时间或 order id 发生经济字段漂移。
- **实际分支**: evidence id 只由 evidence type + broker key 决定；repository 的 same-source 校验只比较 `source_type/source_event_id/evidence_type`，随后直接 `UPDATE raw_json`。unresolved state 允许重试；Inbox 在 handled 前也允许以同 identity 覆盖 payload。没有 immutable economic hash 在覆盖前 fail closed。
- **预期行为**: Push/Poll provenance 可以不同，但 canonical economic payload 必须 append-once；同 key 的经济字段漂移必须 conflict，不能更新原 evidence。
- **实际行为**: option anchor 到达前，Poll 可把 Push 已落库的 stock quantity/price/time/order 改写；后续 matcher 和 source claim会消费改写后的事实。已 processed 的同 key 重放则只返回 duplicate，不审计经济漂移。
- **直接证据**: `repository.py:86-96` 只比三个 identity 字段；`:897-916` 对 raw JSON差异执行 UPDATE；`lifecycle.py:768-774` 表明被更新字段包括 qty/price 和完整 raw；`resolver.py:315-319` 允许 retryable unresolved 重放。冻结方案 `docs/plans/...:294-304` 明确规定同 key 下经济字段漂移 fail closed。
- **影响**: broker evidence 不再可审计地不可变；最终 assignment/exercise 可基于后来的改写值，而非首次接受的经济事实，且 Push/Poll 顺序会影响结果。
- **建议改法和验证点**: 在 Inbox、state 和 ledger evidence acceptance 边界使用同一个版本化 economic canonicalizer/hash；insert-once 后只允许独立的 case binding 字段按 CAS 绑定，禁止覆盖经济 payload。新增 unresolved Push -> Poll drift、processed replay drift、仅 provenance 变化和 JSON 顺序变化测试。
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 高

### 7-未修复-高-迁移工具既不能把 legacy case 转成 v2，也会把缺 timing policy 的 v2 case记为迁移成功

- **入口/函数**: `build_lifecycle_migration_inventory()` -> `select_lifecycle_migration_targets()` -> `apply_lifecycle_migration_manifest()` -> `_apply_manifest_row()`
- **文件(行号)**: `src/application/ledger/lifecycle_migration.py:116-127,263-324,354-381,500-596`; `tests/test_lifecycle_redesign_contracts.py:90-135,328-373`
- **输入场景**: 一类是可一对一证明的 legacy-only case；另一类是已有 `lifecycle_case.v2`、option anchor 与 source claim，但尚未绑定 immutable timing policy 的 case。
- **实际分支**: 所有非 v2 case 无条件标记 `legacy_case_requires_explicit_mapping`，apply 又拒绝全部非 exact row；代码没有可消费 explicit mapping 并创建 v2/bridge/supersede 的路径。对 v2 case，inventory 即使 `timing_policy=None` 仍标记 exact；apply 只有 row 已携带 policy dict 才插入 binding，但 inventory 不生成缺失 policy，于是仍写 suppression 和 migration receipt并报告成功。
- **预期行为**: 唯一可映射 legacy case 应创建 canonical v2 case、非 allocating `migration_bridge_evidence.v1`，并在 legacy+v2 共存时标记 superseded；已有 v2 case 应在成功 cutover 前获得 immutable timing binding，无法生成时进入 review。
- **实际行为**: legacy 永远无法通过所提供的 manifest/apply 流程进入新状态机；缺 timing 的 v2 case却可收到“迁移完成”receipt，之后因 `lifecycle_timing_policy_unavailable` 无法自动形成到期未交割结果。
- **直接证据**: `lifecycle_migration.py:124-127` 无条件阻断 legacy；`:375-381` 禁止选择 review row；`:530-596` apply 只写 claims、可选现成 timing、Outbox 和 receipt，不创建 v2/bridge/supersede；`:316-317` 缺 timing 不是 review reason。现有 `test_lifecycle_redesign_contracts.py:328-373` 使用未绑定 timing 的 helper case并断言 apply 成功。冻结方案 `docs/plans/...:982-995` 要求 v2 timing binding、legacy bridge 和 supersede。
- **影响**: 存量 cutover 无法按设计完成，同时成功 receipt会掩盖未具备自动收敛能力的 v2 case；上线后会留下永久 review-only 或无法到期关闭的案件。
- **建议改法和验证点**: 定义显式 source/target mapping artifact；对唯一 legacy 在单事务中创建 v2、bridge、source claims、supersede 和 suppression；inventory 为缺 timing 的 v2 生成可验证 policy或标记 needs_review，apply 后 readback 断言 binding 存在。新增 legacy-only、legacy+v2、歧义、缺 timing、重复 apply 和 source drift 测试。
- **修复风险（低/中/高）**: 高
- **严重程度（低/中/高/严重）**: 高

## Open Questions

- 无。上述问题均可从当前实现与冻结方案之间的可执行分支差异直接确认，不依赖 live Futu 或生产数据。

## Residual Risk

- 全量测试、Ruff、依赖图和 whitespace 门禁均通过，但现有测试没有覆盖跨 Futu account、全局候选歧义、deadline 上界、未分配却 resolved、Poll-only 正向交割、经济 payload 漂移、v2 late settlement 和真实 legacy cutover，因此绿灯不能证明生命周期账本安全。
- 项目 `.venv` 当前没有 pytest，本轮测试使用 `python3.12` 和隔离的 `PYTHONPYCACHEPREFIX`；这是本地环境差异，不作为产品缺陷。
- 本轮未调用 live Futu/OpenD、未发送 Feishu、未执行迁移、未修改生产账本/配置/服务，也未发布或部署；生产是否已经存在同类错误不在本 review 证据范围内。
- Daily Brief 本地缺口降级链路的 180 个聚焦测试通过，本轮未发现新的可证明阻断项；本报告 findings 均位于新增期权生命周期/原因/迁移链路。
