# Code Re-review

## Scope

- Mode: current changes, focused S4 re-review
- Branch: `excited-rhino`
- Base: `ecd7c306` (accepted S3 commit)
- Original review: `docs/reviews/code-review-20260717-164622.md`
- Fix artifact: `docs/gateflow/option-performance-refactor-s4-fix-20260717-170209.md`
- Output file: `docs/reviews/code-review-20260717-170210.md`
- Included scope: accepted findings `S4-CR-01` through `S4-CR-05`, their direct call paths, regression tests, and S4 residual-risk classification
- Excluded scope: unrelated Feishu/version/changelog changes and approved later slices S5-S10

## Review Method and Evidence

Re-review followed the real current-report path:

`build_option_period_performance()` -> evidence repository read -> boundary position projection -> optional current collector -> persisted/live validation and merge -> deterministic selectors -> performance engine -> report provenance/quality.

Evidence inspected:

- recursive raw-value normalization and canonical JSON generation;
- source-identity validation against existing persisted facts;
- schema-state diagnostics entering top-level quality;
- account-independent instrument dedupe and stored-code promotion;
- separation of compact report provenance from the full capture envelope;
- focused regression and S1-S4/ledger compatibility results.

## Finding Status

### S4-CR-01-已修复-高-外部 snapshot raw 值可使当前报告在 fact ID 生成阶段崩溃

- External adapter values are recursively normalized before fact construction.
- Datetime/Timestamp becomes ISO text, Decimal becomes canonical text, NumPy-like scalar becomes a native scalar, and non-finite float becomes null.
- Domain/repository canonical JSON uses `allow_nan=False`.
- Regression coverage verifies JSON-safe quote/FX raw data and explicit rejection of non-finite canonical evidence.

### S4-CR-02-已修复-中-persisted 与 live evidence 未做 identity conflict 验证

- Live marks/rates are validated against persisted evidence before merge.
- A conflict discards the whole live batch, preserves persisted facts, and emits a valuation-scoped diagnostic.
- The selector no longer arbitrates invalid duplicate source identities.

### S4-CR-03-已修复-中-unsupported evidence schema 不会降级 report quality

- `unsupported_schema` now contributes a scoped valuation diagnostic with repository detail.
- Top-level quality is partial even when no open position otherwise demands a mark.

### S4-CR-04-已修复-中-cross-account dedupe 会丢失后出现的 stored exact market code

- Dedupe replaces an earlier no-code representative with a later stored-code representative for the same instrument.
- Different non-empty stored codes still produce a conflict and skip collection for that identity.
- Regression coverage verifies no-code -> stored-code input order.

### S4-CR-05-已修复-中-report collection provenance 内嵌完整 raw evidence

- Report provenance contains only collection status, counts, diagnostics, and selected fact IDs.
- Explicit capture still receives the complete `.envelope` for deterministic replay/import.
- Regression coverage confirms report payload does not contain raw evidence.

## Validation

`39` focused S4 tests passed, `174` S1-S4 plus ledger compatibility tests passed, Ruff passed, and `git diff --check` passed after the final test database-path correction.

## Adversarial Re-review Result

- No remaining accepted finding is partially fixed.
- No new blocker was found in the reviewed S4 paths.
- Error paths fail closed: invalid live evidence cannot replace persisted facts; unsupported schema degrades quality; missing/invalid market data becomes diagnostics rather than fabricated valuation.
- S4 preserves the domain/application/infrastructure import boundaries and does not add a second lot-matching or evidence source of truth.

## Residual Risks and Uncovered Areas

| Risk / uncovered area | Classification |
|---|---|
| Assigned-stock lifecycle/valuation | covered by later approved S5 |
| Capital exposure and efficiency | covered by later approved S6 |
| Agent/CLI capture/import/report public contracts | covered by later approved S7 |
| Analysis consumer migration | covered by later approved S8 |
| Portfolio PnL and cash bridges | covered by later approved S9 |
| Historical evidence population and operational shadow validation | covered by later approved S7/S10 |
| Real OpenD field variability beyond injected adapter tests | assigned to S10 operational validation; current adapter fails closed |
| General non-assignment stock inventory | assigned to a later work unit |

No unclassified residual risk remains.

## Decision

- S4 code review loop: PASS
- Accepted findings: 5
- Final finding state: all `已修复`
- Blocking open questions: none
- Next entry point: accepted S4 slice commit
- Artifact path: `docs/reviews/code-review-20260717-170210.md`
