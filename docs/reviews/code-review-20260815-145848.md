# Code Review — Sell Put Top1 W4 Aggregate Kimi DeepReview

## Scope

- Mode: read-only aggregate review
- Branch: `feat/sell-put-top1-w4`
- Reviewed range: `origin/main@baa68162..e41fe477`
- Output file: `docs/reviews/code-review-20260815-145848.md`
- Reviewer: Kimi K3, maximum reasoning
- Included scope: W4 S1+S2 day denominator seal, official point capture, v2 Corpus indexes, accepted-only ranking projection, status, hash-bound window facts, fixed latest-mature 40-day selection, and reference-only dataset
- Excluded scope: W5 40-day research, W6 hidden validation, W7 timer/CLI/provider integration, release, deployment, and live writes

## Verification performed by reviewer

- Corpus/store focused suite: `16 passed`.
- Producer/lifecycle/architecture/scheduler adjacent suite: `104 passed`.
- Re-read the accepted plan plus all S1/S2 implementation and fix artifacts, then traced seal -> capture -> index -> artifact -> status -> freeze.
- Probed feature-off ordering, wrong/cross-date capture, capture before expectation, conflict-day status, and mixed-offset cutoff comparisons.

## Findings

No substantive issues found.

The reviewer independently confirmed that all five earlier slice findings remain closed: HK-only first release, null artifact fields on conflict results, normalized invalid timezone/date failures, strict cutoff checks for seal/decision/capture timestamps, and lawful coverage classification for older projection schemas.

It also confirmed:

- schedule targets reuse the production calculation rather than a second parser;
- feature-off commands create no Corpus artifact or row;
- immutable artifact bytes, semantic/file hashes, and SQLite bindings are revalidated;
- conflict is absorbing and wins over ordinary coverage gaps;
- source `output_runs` deletion does not affect baseline reranking or deterministic re-freeze;
- only the latest mature 40-date window is inspected, with no older-window fallback;
- multiple recommendation points per day remain ordered by the sealed denominator;
- v1-to-v2 migration is fail-closed and preserves valid lifecycle data;
- stored projections contain accepted ranking facts only, while datasets contain refs/hashes and no candidate rows;
- no Candidate Engine or production tick reverse dependency was introduced.

## Open questions

- None.

## Residual risks and observations

- Calendar and maturity truth remain caller-owned evidence; W4 validates and binds it but does not query a provider.
- A mistakenly sealed non-trading empty day is immutable and not evaluable; W7 must call sealing from the authoritative calendar workflow.
- Compact status counts a conflict day's denominator in both expected/missing totals while also reporting the day conflict; freeze does not consume this summary field.
- Content-addressed artifact orphans after a crash remain inert and retry-adoptable; cleanup is deferred until growth is measured.
- Multi-process race tests are deferred until W7 introduces the production caller; current SQLite transactions and uniqueness constraints cover the intended single-host boundary.
