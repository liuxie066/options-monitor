# Code Review

## Scope

- Mode: current changes re-review
- Branch or PR: `feat/sell-put-top1-w4`, W4 S1 workspace changes after accepted fixes
- Base: `de7932bc` (`docs(gateflow): accept plan for sell-put-top1-w4`)
- Output file: `docs/reviews/code-review-20260815-142002.md`
- Included scope: schema v1-to-v2 migration, HK-only Corpus boundary, scheduler target reuse, day expectation seal/conflict semantics, official point capture, projection publication, feature gate, retries/failure paths, tests, and architecture guard
- Excluded scope: W4 S2 status/window freeze; W5 research; W6 hidden validation; CLI/service/provider/runtime changes
- Parallel review coverage: 无；主 reviewer 复核三项 finding 的触发路径，并重新走读 seal/capture -> artifact -> SQLite 及 migration/retry/failure paths

## Findings

未发现实质性问题。

三项初审 finding 均已关闭：首发市场范围统一为 HK；任何 day conflict 回执不再声明 artifact ref/hash；market 和 gate 非法 timezone 均规范化进入 `CorpusError(corpus_input_invalid)`。新增失败回归还覆盖 feature-off capture、projection byte conflict、opening snapshot missing/content conflict。

## Open Questions

- 无。

## Residual Risk

- W4 S2 status、外部 window-facts 校验和固定 40 日 dataset freeze 尚未实现，不在本次 S1 re-review 范围。
- S1 当前只建立行为 seam，尚无 W7 timer/CLI 接线；因此测试证明的是确定性合同，不是生产采集已启动。
