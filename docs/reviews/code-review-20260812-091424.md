# Aggregate Code Re-review

## Scope

- Mode: current branch aggregate re-review
- Branch: `fix/candidate-brief-evidence-integrity`
- Base: `8a3520f0` (accepted plan commit)
- Prior review: `docs/reviews/code-review-20260812-090935.md`
- Fix artifact: `docs/gateflow/candidate-brief-evidence-integrity/aggregate-review-fix.md`
- Output file: `docs/reviews/code-review-20260812-091424.md`
- Included scope: complete Slice 1 + Slice 2 diff plus CR-AGG-01 fix, all prior finding dispositions, candidate/Brief
  authority boundaries, rendering limits, notification handoff, and aggregate validation
- Excluded scope: unrelated primary-worktree user changes; production artifacts; release/deploy/notification writes
- Parallel review coverage: 无

## Finding dispositions

### CR-AGG-01 — accepted — 已修复

Canonical candidate existence is now captured before `_candidate_views()` applies its shared display budget. The
transient user view passes only per-family booleans to AI copy selection; candidate details remain budget-limited.
The new regression exhausts the budget before Covered Call and proves that its omitted canonical candidate still
selects the candidate-present unavailable copy. Existing empty-family coverage remains green.

Final status: `已修复`.

### Prior findings

- CR-S1-01: `已修复`.
- CR-S1-02: `已修复`.
- CR-S2-01: `已修复`.

## Findings

未发现新的实质性问题。

## Validation

Core aggregate suite:

```text
/Volumes/liuxie的硬盘/workspace/options-monitor/.venv/bin/python -m pytest -q tests/test_candidate_scanning_evidence.py tests/test_scan_volume_gate_min_zero.py tests/test_opening_candidate_snapshot.py tests/test_daily_decision_brief_service.py tests/test_daily_decision_brief_renderer.py tests/test_ai_decision_advice_render.py
```

Result: `156 passed`.

Related call-path suite covering AI Advice, Daily Brief agent/CLI/domain/notification/repository/scenarios, CC+LP, and
Combo Yield: `387 passed`.

`python -m compileall -q domain src tests`: passed. Ruff on all changed source/tests: `All checks passed!`.
`git diff --check`: passed.

Reviewed the full branch for HK/US and Sell Put/Covered Call symmetry, deterministic-vs-unavailable classification,
mixed contract reasons, sealed snapshot-only facts, lawful zero candidate, template-expanded CC+LP requirements,
prefetch success vocabulary, raw-candidate existence before/after render budgeting, card/legacy/alert projections,
and absence of schema/config/ranking/capacity mutations.

## Open Questions

- 无。

## Residual Risk

- Runtime replay and scheduled delivery remain `assigned to separately authorized release/upgrade verification`.
- Manual symbol-subset config propagation remains `assigned to later work unit` per the accepted plan.
- Future deterministic calculation reasons remain `assigned to later work unit`.

## Conclusion

`pass`

All accepted findings are fixed with deterministic regressions. The work unit is ready for its accepted aggregate
review commit and clean-commit validation.
