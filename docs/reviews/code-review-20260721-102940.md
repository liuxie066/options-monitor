# Aggregate Deep Review Re-review: Daily Brief Close-Position Details

## Findings

未发现新的实质性问题。

### DR-001-已修复-中-分支修改后依赖图生成物未同步导致全量质量门失败

- **原位置**: `docs/DEPENDENCY_GRAPH.md`; `tests/test_dependency_graph_generator.py::test_dependency_graph_generator_check_passes`
- **修复证据**: `docs/DEPENDENCY_GRAPH.md` now records `4194` total internal import edges and `1574` `tests -> application` edges, matching the current branch tree.
- **验证证据**: `python3 scripts/generate_dependency_graph.py --check` passes with `production_modules=476 cycles=0`; the focused renderer/service/dependency-graph suite reports `39 passed`.
- **最终状态**: `已修复`

## Review Target and Scope

- Mode: Current Changes / aggregate branch re-review
- Selected base: `origin/main` at `502c0332`
- Reviewed head before accepted-deepreview commit: `a3a76e70` plus the aggregate review fix workspace delta
- Reviewed: the complete application/test/docs branch diff, the corrected initial aggregate review artifact, the generated dependency graph update, and the fix artifact.

## Intent and Real Path

The branch keeps close-advice policy unchanged and closes only the Daily Decision Brief projection gap:

```text
close_advice.csv
-> daily_decision_brief_service._position_view
-> positions[].metrics allowlist
-> daily_decision_brief_renderer._position_close_details
-> compact Markdown detail line
```

The renderer emits details only for explicit close actions with usable status, formats the existing advisory mid and signed net P&L, and suppresses malformed or stale metrics. No duplicate financial calculation, threshold, trigger, delivery, configuration, ledger, or broker-side behavior was added.

## Adversarial Failure Pass

Rechecked positive and negative P&L, missing/partial/non-finite metrics, unavailable quote status, hold actions carrying stale metrics, unknown actions, HK/US currency, old persisted briefs without `metrics`, long-Call exit actions, output bounds, and allowlist leakage. No additional failure path was found.

## Project Instruction Check

Pass. The original dirty worktree remains untouched; only the independent feature worktree is modified. The fix does not write config, runtime state, notifications, ledger data, or broker-facing data. `paseo.json` remains unrelated and excluded from staging.

## Overcoupling, Ownership, and Parsimony

- Overcoupling: pass; the service transports source facts and the renderer owns presentation.
- Semantic ownership drift: pass; no policy or financial values are recomputed downstream.
- Overengineering: pass; the implementation adds no new entity, configuration key, dependency, workflow, or compatibility layer.

## Validation Evidence

- Focused compile: pass.
- Focused implementation tests: `68 passed`.
- Renderer/service tests from the implementation loop: `37 passed`.
- Current renderer/service/dependency-graph regression set: `39 passed`.
- Daily Brief tests excluding the expired baseline fixture: `107 passed, 1 deselected`.
- Ruff on changed Python files: pass.
- `git diff --check`: pass.
- Dependency graph check: pass.
- Full suite: `1 failed, 2858 passed, 10 skipped in 42.58s`.
  - Only failure: the fixture valid until `2026-07-20T20:00:00+00:00` expects `live_actionable` on July 21, 2026; runtime correctly reports `planning_only`.
  - Classification: unrelated date-sensitive baseline; assigned to a separate maintenance work unit.

## Open Questions

无。

## Residual Risk

- Expired date fixture: assigned to a separate maintenance work unit.
- Production notification canary: assigned to later operator-authorized release/deployment work.
- No unclassified residual risk remains.

## Conclusion

`pass-with-risks`. `DR-001` is fixed and the branch is ready for the accepted deepreview commit. The only suite failure is unrelated, date-sensitive, and explicitly owned outside this work unit.

## Artifact

`docs/reviews/code-review-20260721-102940.md`
