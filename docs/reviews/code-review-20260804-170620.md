# Code Review

## Scope

- Mode: current changes
- Branch or PR: `fix/retire-scheduler-shadow-execution`
- Base: `main@3ba1ffc27c5216baaf4deb096c627c4e7fdacf0f`
- Output file: `docs/reviews/code-review-20260804-170620.md`
- Included scope: S2 scheduler execution-authority changes; the real `./om -> main -> scheduler_ops -> run_scheduler` path; structured error contract; decision/mark compatibility; scheduler callers; focused tests; operator documentation; generated dependency graph.
- Excluded scope: S1 and S3-S7 implementation; production cron configuration and unmanaged external callers; release, deployment, runtime mutation, notifications, and the unrelated concurrent changes in the original worktree.
- Parallel review coverage: 无；the change is a narrow single call chain and was reviewed end to end by the primary reviewer.

## Findings

未发现实质性问题。

The CLI boundary rejects `--run-if-due` before repository/runtime-root resolution or adapter invocation, and the Python boundary rejects it before `_resolve_base`, config resolution, state-path creation, state reads, or writes. The legacy subprocess branch and imports are absent. Existing decision and account-scoped mark behavior remains on the unchanged `run_if_due=False` path. Repository-wide search found no remaining production caller of `run_if_due`.

## Open Questions

- 无。

## Residual Risk

- Unmanaged callers outside the repository will now receive the intentional exit-code-2 `UNSUPPORTED_OPERATION` response. Production caller migration and deployment observation were outside this review.
- The isolated worktree does not contain `.venv/bin/python`, so four pre-existing CLI test files that hard-code that path were excluded from the broad suite. The changed CLI boundary was instead covered by direct tests and a real `./om scheduler --run-if-due` invocation, which returned exit code 2 and created neither config nor state files.
- Full non-environment suite evidence: `4238 passed, 10 skipped`; the sole sandbox-only localhost-bind failure was rerun with local socket permission and passed. Focused scheduler/CLI/architecture coverage: `52 passed`. Ruff, compileall, diff-check, and dependency-graph check passed; the graph reports 577 production modules and zero cycles.
