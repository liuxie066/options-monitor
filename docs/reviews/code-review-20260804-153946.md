# Code Review

## Scope

- Mode: current changes
- Branch or PR: `fix/required-data-multi-account-integrity`
- Base: accepted-plan `194ce5c5083be25022e122611bf26b73ab26d56a` (tree based on `origin/main@ed2531e956f7ff818b3198a6dc3d918bb5328b3f`)
- Current upstream note: live `origin/main` independently advanced by 11 commits to `6bc86f81361882b4175eb456a0ae31e36af7bc6c`; this review intentionally does not merge or rebase those commits into S1.
- Output file: `docs/reviews/code-review-20260804-153946.md`
- Included scope: the complete S1 workspace diff, including all 14 approved production owners, directly corresponding changed/new tests, generated dependency-graph artifacts, the accepted plan and four S1 scope amendments, implementation/fix evidence, and both prior S1 DeepReview artifacts.
- End-to-end paths reviewed: frozen expected contract -> ordered exact in-process provider children -> typed aggregate -> raw/CSV exact projection -> receipt-last publication/adoption -> terminal seal/re-entry -> frozen consumer bytes; fresh, cached, subprocess, manual-unplanned, and success-empty error boundaries; snapshot duplicates/subsets/unexpected rows; RV 20/60/120/estimate reconciliation across aggregate, rows, and every ordered child; trading-date/DTE, timezone, strict port, gateway-health, and multi-account shared-market-plan behavior.
- Excluded scope: S2-S7 implementation, live OpenD/provider correctness, notification/broker/ledger/config/runtime mutations, release/deploy/merge/push, the accepted candidate-CSV reopen TOCTOU, distributed publication locking, and integration with the 11 newer upstream commits.
- Parallel review coverage: a dedicated provider/finalizer audit covered actual multi-child merge, snapshot completeness, RV evidence, raw/CSV persistence, receipt-last and gateway health; it surfaced `DR-S1-FR-07`, then post-fix re-reviewed the child-RV closure with zero residual finding. A separate scope/constructor audit checked the S1 allowlist, plan/amendment consistency, generated graph, raw-fetch guard, and Shadow Replay zero-diff boundary. The main reviewer independently re-read and validated the final bytes and owns this aggregate conclusion.

## Findings

未发现实质性问题。

## Open Questions

- 无。

## Residual Risk

- Candidate CSV verification followed by a later mutable-path reopen remains an explicit later-work-unit TOCTOU; S1's run-scoped receipt/seal and exact frozen-byte consumer path do not broaden that authority.
- Subprocess exact multi-spec execution, scoped-union correction, and run-local spot tri-state reuse remain assigned to S6. S1 fails subprocess multi-spec before subprocess/output/receipt effects and executes all exact children only in process.
- Cross-process/distributed publication coordination remains out of scope. Current behavior is payload-first/receipt-last with write-once-or-verify conflicts; a commit-validator rejection may intentionally leave an unreferenced immutable payload without a completion receipt.
- Shadow Replay remains byte-for-byte unchanged relative to the accepted base. Its offline bundle still uses the compatibility RV default and ambient trading date and therefore cannot be treated as strict scheduled publication evidence until its later coordinated migration.
- The broad non-environment suite result (`4199 passed, 10 skipped`) is closure evidence produced after `DR-S1-FR-07`; this aggregate reviewer reran the complete changed-file S1 suite, not that full repository suite. Four CLI-only files still depend on a worktree-local `.venv/bin/python`, absent from this isolated worktree.
- No live OpenD call or production/runtime mutation was performed, so provider protocol behavior outside deterministic adapters and fixtures remains operational verification risk.
- The accepted S1 lineage is 11 commits behind current `origin/main`; integration/rebase conflict and behavior against those newer upstream changes require a separate gate before delivery.

## Verification Evidence

- Complete changed-file S1 regression set: `508 passed in 5.16s`.
- Child-RV/provider post-fix independent evidence: focused `128 passed`; RV negative/positive cases `9 passed`; real multi-spec merge/finalize `1 passed`; zero material residual.
- `python3 scripts/generate_dependency_graph.py --check`: current, `577` production modules, `0` cycles.
- Dependency graph and raw-fetch architecture guards: `3 passed`.
- Shadow Replay production package plus direct Shadow/Combo tests: zero diff against `194ce5c5`.
- Ruff over S1 production owners and strict new tests: pass.
- Compileall over all 14 changed production owners: pass.
- `git diff --check`: pass.
