# Code Review

> Supersedes `docs/reviews/code-review-20260815-034739.md`：上一份 fix 复审 artifact 引用了本 worktree 中不存在的符号（`src/application/account_capital_service.py::evaluate_account_fee_profile`、`FeeAccountPlan.from_config`、`summarize_assigned_stock_receipts`、`has_unavailable_lifecycle_pnl_net`、`test_hk_terminal_fee_component_sum_matches_amount_and_remaining_quote_gates`），并误报 full-suite 为 "1 skipped"。因 factual reference errors 作废，以本 artifact 为准。

## Scope

- Mode: fix re-review（严格只读，全部符号经 `rg` 在当前 worktree 验证存在）
- Branch or PR: 隔离 worktree `/private/tmp/options-monitor-hk-terminal-fee-contract-20260815`（HEAD = accepted plan `8b879390`，分支 `feat/sell-put-top1-hk-terminal-fee-contract`）
- Base: `8b879390`（accepted plan）
- Output file: `docs/reviews/code-review-20260815-035048.md`
- Included scope: `git diff 8b879390` 下的 `domain/domain/fee_calc.py`、`domain/domain/assigned_stock.py`、`domain/domain/portfolio_assignment_scenario.py`、`tests/test_fee_calc.py`、`tests/test_assigned_stock_projection.py`、`tests/test_portfolio_assignment_scenario.py`；fix 决策 `docs/gateflow/sell-put-top1-hk-terminal-fee-contract/code-review-fix.md`；对 `code-review-20260815-034135.md` 的 DR-HKF-01 / DR-HKF-02 及 fix 引入的 ROOT-HKF-01 修复做独立复核
- Excluded scope: release/deploy；生产写入；HK live broker fee 再验证；全仓套件在本 worktree 的重跑（采信 implementation artifact 记录）

## 符号存在性验证（rg 实测）

- `domain/domain/fee_calc.py:295` `_finite_float`；`fee_calc.py:305` `calc_futu_hk_terminal_fee`
- `domain/domain/assigned_stock.py:796` `_lifecycle_efficiency_summary`（调用点 `assigned_stock.py:1459`）
- `tests/test_fee_calc.py:435` `test_hk_terminal_rejects_invalid_plan_fact_types`
- `tests/test_assigned_stock_projection.py:371` `test_hk_assignment_stock_fee_fails_closed_and_suppresses_net_outputs`

## Findings

### 1-已关闭（不成立）-中-DR-HKF-01 成分求和不变量在极大 contracts 下不成立——本轮复跑确认不成立
- **入口/函数**: `calc_futu_hk_terminal_fee()` 完整分支 `amount = round(sum(effective_components.values()), 6)`
- **文件(行号)**: `domain/domain/fee_calc.py:419-424`（fix 后完整分支 `effective_components` / `amount`）；`tests/test_fee_calc.py:435-452`
- **输入场景**: 原报告同一输入 `f("exercise", order_price=450.0, shares=100, contracts=10**16, account_fee_plan=commission_free_plan)`
- **实际分支**: `exercise_fee = round(2.0 * 10**16, 6) = 2e16` 已进入 `effective_components`，`amount = round(sum(effective_components.values()), 6)` 与 components 同源同值
- **预期行为**: complete 结果满足 `amount == round(sum(components.values()), 6)`
- **实际行为**: 本轮实跑输出 `amount = 2.0000000000000064e+16`，`round(sum(components.values()), 6) = 2.0000000000000064e+16`，不变量成立（`True`）；原报告声称的 `amount=2e16` 不可复现
- **直接证据**: 本 worktree `.venv` 实跑复现不变量成立；`code-review-fix.md` 中 DR-HKF-01 "rejected — current source already satisfies the invariant" 的决策与复跑一致
- **影响**: 该 finding 关闭为不成立；按任务边界不引入任意 contracts 上限
- **建议改法和验证点**: 维持现状；若未来要将 contracts 上限作为产品边界，应作为独立 work unit 评审
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中（原评级；关闭后无遗留）

### 2-已修复-低-DR-HKF-02 字符串 `platform_fee` 现 fail-closed
- **入口/函数**: `calc_futu_hk_terminal_fee()` -> `_finite_float(plan.get("platform_fee"))`
- **文件(行号)**: `domain/domain/fee_calc.py:295-302`（`_finite_float`）、`fee_calc.py:360-373`（plan 校验）；`tests/test_fee_calc.py:435-452`
- **输入场景**: `account_fee_plan={"commission_free": False, "platform_fee": "15", "fee_plan_ref": "x"}`
- **实际分支**: `_finite_float` 先拒绝 `isinstance(value, (bool, str))`，字符串数值进入 `missing_plan_facts`
- **预期行为**: 字符串 `"15"` 不得解锁 complete 结果
- **实际行为**: 实跑输出 `complete=False, reason=hk_account_fee_plan_missing, missing_plan_facts=['platform_fee']`
- **直接证据**: `test_hk_terminal_rejects_invalid_plan_fact_types` 覆盖 `platform_fee="15"`、`commission_free=1`（非 bool）、`platform_fee=inf`、`fee_plan_ref=1`（非 str）四类，且断言 `estimated_amount == 79.215`（估计仍保留用于 audit）；focused 45 passed 本轮复跑确认
- **影响**: 修复成立，类型严格度与 `commission_free` 的 `isinstance(..., bool)` 对称
- **建议改法和验证点**: 无新增；`_finite_float` 作为共享输入边界保持不变
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 3-已修复-中-ROOT-HKF-01 聚合生命周期 summary 不再把缺失净额静默计为 0
- **入口/函数**: `domain/domain/assigned_stock.py::_lifecycle_efficiency_summary()`（由 `project_assigned_stock_lifecycle` 在 `assigned_stock.py:1459` 调用）
- **文件(行号)**: `domain/domain/assigned_stock.py:796-833`；`tests/test_assigned_stock_projection.py:371-398`
- **输入场景**: 聚合桶内任一成员行 `lifecycle_pnl_net=None`（如 HK assignment 缺 fee-plan fact 时）
- **实际分支**: diff 实测——`row["lifecycle_pnl_net"] is None` 时 `bucket["lifecycle_pnl_net"] = None`；输出阶段 `net = _round_money(...) if bucket[...] is not None else None`，`annualized_capital_efficiency` 仅在 `net is not None and capital_days > 0` 时计算。行级另有 `fees_complete = not fee_summary["fee_missing_components"]` 门控（`assigned_stock.py:1346-1351`），成员行净额缺失时即为 None
- **预期行为**: 聚合净额不得在成员净额缺失时发布 0 或部分合计
- **实际行为**: `test_hk_assignment_stock_fee_fails_closed_and_suppresses_net_outputs` 同时断言成员行 `lifecycle_pnl_net is None`、`annualized_capital_efficiency is None` 与 summary 行两项同为 None
- **直接证据**: 上述测试本轮复跑通过；fix diff 中 `_lifecycle_efficiency_summary` 的 None 传播路径逐行确认，无第二求和路径
- **影响**: 修复成立；聚合误导性 0 消除，成员行可见性保留
- **建议改法和验证点**: 无新增问题
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

## 已排除的候选 finding（fix diff 走读后不成立）

- **HK assignment 无 plan 时 `_stock_fee_fact` 直接返回 `basis=missing`**: 两个 consumer（`assigned_stock.py` HK 分支、`portfolio_assignment_scenario.py::_fee_fact` HK 分支）均不以 payload 中的 `account_fee_plan` 调用 calculator；`test_hk_assignment_projection_rejects_unvalidated_fee_plan_payload` 证明注入无效。合同成立。
- **`portfolio_assignment_scenario._fee_fact` HK 分支返回 `status=missing` 但带 `estimated_stock_fee_native`**: 估计值明确以 `estimated_*` 命名、`fee_native/fee_cny` 为 None、`estimated_basis="standard_fixed_non_commission_free"`，与 fail-closed 语义一致，不构成误导性 actual。
- **`calc_futu_hk_stock_fee` 重构为 components 求和**: 纯等价重构（同一七项、同一 round），且 `_standard_fixed_hk_stock_fee_components` 被 terminal calculator 复用，消除重复算术；无行为偏移。
- **fix 过度设计检查**: 未新增 contracts 上限、未新增第二算术路径、未引入新模块/配置键；diff 与 finding 一一对应。

## Open Questions

- 无。

## Residual Risk

- Full-suite 证据采信 implementation artifact（`docs/gateflow/sell-put-top1-hk-terminal-fee-contract/implementation.md`）：全仓 sandbox 内 `4754 passed, 10 skipped, 1 failed`；该 1 个失败为 sandbox-only HTTP 限制，沙箱外重跑 `1 passed`。本轮只读复审未重跑全仓套件。
- 本轮独立复跑：focused 45 passed、Ruff `All checks passed!`（6 个改动文件）、`git diff --check` 无输出（通过）。
- W0R 遗留 runtime gap（OpenD FSM、真实 HK broker fee、真实 `lx` fee-plan receipt）不在本 work unit 范围，继续阻断 provider-dependent research 与试点。
