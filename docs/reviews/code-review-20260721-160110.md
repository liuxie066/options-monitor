# Final PR Re-Review — PR #109 after latest-main integration

- Mode: PR Review / final local integration state
- PR: `#109` (`feature/option-notification-experience` -> `main`)
- Selected base: `origin/main@ba6efbe9df943db62c19880f2733918d39f5f3c1`
- Review time: `2026-07-21T16:01:10+00:00`
- Prior findings: `MR109-INTEGRATION-03`, `MR109-INTEGRATION-04` in `docs/reviews/code-review-20260721-234931.md`
- Fix re-review: `docs/reviews/code-review-20260721-235359.md`

## Findings

未发现实质性问题。

## Final contract verification

- Scheduled ordinary notifications have one authority: Daily Brief.
- Deprecated `daily_brief.enabled` values are warning-only and do not select Compact/Legacy.
- The 10-minute timer is only a wake-up; canonical scans occur at 09:40, valid whole hours, valid half-hours, and 15:50.
- Fixed points send one complete report; half-hours send only for newly pending candidate identities; fixed wins if both apply.
- Manual/force single-market reliable scans update successful current but cannot create/send ordinary delivery envelopes.
- Pipeline failure cannot advance current or masquerade as normal no-candidate.
- Delivery v2 persists and retries exact message/key/hash; definite failure, ambiguity, and confirmation remain distinct.
- Delivery-only `--no-send` records skipped/would-send and leaves the envelope pending.
- Combined multi-market execution terminates before Daily Brief assemble/persistence/envelope/provider work.
- Funds show cash total, option-opening funds, and candidate capacity, not total assets/NAV/securities value.
- Query reads latest successful current and does not mutate delivery state.

## Adversarial and ownership checks

- No second scanner, timer, sender, renderer switch, outbox, or config frequency was added.
- Scheduler owns target identity; repository owns current/revision/delivery; renderer owns user projection; provider adapter owns transport.
- Legacy Compact/Legacy code remains compatibility/preview only and is absent from scheduled flow.
- No unresolved conflict markers, semantic fallback branch, or stale generated dependency artifact remains.
- Four unrelated untracked historical plan-review files are outside the change and remain untouched.

## Validation

- Full repository tests: `2985 passed, 10 skipped`.
- Ruff: passed.
- `compileall -q src domain scripts`: passed.
- Dependency graph: `production_modules=479`, `cycles=0`, current.
- US example YAML validation: `ok=true`.
- HK example YAML validation: `ok=true`.
- Focused final integration suite: `99 passed` before full-suite stale-assertion reconciliation; affected tests then passed and the full suite was rerun.
- Conflict-marker scan and `git diff --check`: passed.

## Open Questions

无。

## Residual Risk

- GitHub checks must pass on the final pushed merge commit before PR merge.
- Real provider behavior, pointer migration, production release/upgrade, scheduler observation, and notification send remain separately approval-gated and were not exercised.

## Conclusion

`pass` — the final local PR state is merge-ready once the merge commit is created, pushed, and remote checks confirm green.
