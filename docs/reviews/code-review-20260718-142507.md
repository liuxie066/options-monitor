# Code Re-review

## Scope

- Mode: current changes re-review (Gateflow S4 workspace diff)
- Branch: `codex/diagonal-combo-yield-lifecycle`
- Base: `HEAD` (`d7768416`)
- Output file: `docs/reviews/code-review-20260718-142507.md`
- Prior review: `docs/reviews/code-review-20260718-142206.md`
- Included scope: fixes for F1-F3 plus regression impact across S4 group synthesis and output contracts
- Parallel review coverage: 无

## Finding Status

### F1-已修复-中-Combo inventory adapter can discard snapshot-only identity and top-level expiry structure
- `_combo_inventory_input` now resolves `strategy`, `leg_role`, `strategy_group_id`, `yield_enhancement_mode`, and `expiry_structure` from top-level fields with `strategy_snapshot` fallback.
- A snapshot-only diagonal fixture verifies the group remains `active_combo` rather than becoming `missing_group_inventory` or `same_expiry_mismatch`.

### F2-已修复-中-Active group action ignores the already-authoritative Call action
- Domain synthesis now consumes both actions and maps the Put close/hold × Call sell/hold matrix to `close_both`, `close_put_keep_call`, `sell_call_keep_put`, or `hold_active_combo`.
- `close_both_optional` is now emitted only when the group action is `close_put_keep_call`; it is not emitted when both leg evaluators already recommend closing.
- The new fixture verifies both leg actions remain unchanged while group action becomes `close_both`.

### F3-已修复-中-Combo economics silently treats unavailable transaction fees as zero
- Active-group economics now detects `fee_calc_unavailable` on either participating leg, sets `combo_cost_basis_status=fee_calc_unavailable`, and leaves group net fields plus optional close-both unset.
- Group inventory/action classification remains available because fee availability is not used to rewrite the leg thesis.
- A dedicated fixture verifies unknown fees remain unknown rather than zero.

## Validation

```text
python3 -m pytest \
  tests/test_close_advice_runner.py \
  tests/test_close_advice_action_policy.py \
  tests/test_close_advice_reallocation_shadow.py \
  tests/test_notify_symbols_markdown.py

92 passed
```

```text
python3 -m pytest \
  tests/test_combo_yield_steps.py \
  tests/test_strategy_policy.py \
  tests/test_strategy_lab.py \
  tests/test_agent_plugin_contract.py \
  tests/test_agent_plugin_smoke.py

152 passed
```

```text
git diff --check
passed

python3 -m py_compile \
  domain/domain/close_advice.py \
  domain/domain/combo_yield_lifecycle.py \
  src/application/close_advice_runner.py \
  src/application/agent_tools/close_advice_read_impl.py
passed
```

## Findings

未发现新的实质性问题。

## Open Questions

- 无。

## Residual Risk

- Historical rows without group identity remain fail-closed and are not guessed.
- Closed group state and assigned-stock semantics remain in S3 full-lifecycle reporting, not option-row Close Advice.
- These residuals are approved scope boundaries, not unclassified risks.

## Decision

- S4 code review: pass.
- All accepted findings: 已修复.
- Next gate: accepted S4 slice commit.
