# Code Review Fix — Slice 3

- 关联 review: docs/reviews/code-review-20260808-125915.md
- 日期: 2026-08-08
- 状态: fix 完成

## Findings 处置

| # | 裁决 | 处置 |
|---|---|---|
| 1 | accepted | 修复：`run_cc_lp_variant` 加 `combo_pairs_sink_fn` 参数并转发候选（转 dict 列表） |
| 2 | accepted | 修复：仅当 `capture_statuses` 含 `variant=="cc_lp"` 的 status 时封存 CC+LP snapshot |
| 3 | accepted | 修复：status 判断基于 CC+LP 自身 status（`cc_lp_statuses`），不再混用 combo_statuses |

## Changed Files

- `src/application/combo_yield_steps.py`：run_cc_lp_variant 加 sink 参数、转发候选；run_combo_yield_for_symbol_and_summarize 分派处传 sink
- `src/application/cc_lp_steps.py`：summarize_cc_lp_result 加 variant 字段
- `src/application/pipeline_watchlist.py`：封存逻辑加 `if cc_lp_statuses:` 条件；status 基于 CC+LP 自身
- `tests/test_cc_lp_steps.py`：新增 sink 转发测试

## Validation

- `pytest tests/test_cc_lp_steps.py tests/test_cc_lp_candidate_snapshot.py`：12 passed
- 全量相关回归（domain + steps + snapshot + combo + pipeline watchlist）：63 passed
- `ruff check`：All checks passed
