# Code Review

## Scope

- Mode: current changes
- Branch or PR: `combo-yield-remove-staggered-ledger`
- Base: `main@abe6ca0e`
- Output file: `docs/reviews/code-review-20260811-091411.md`
- Included scope: 工作树 17 文件改动（slices 1-3 合并 review）：resolver 错期分支删除、`combo_pairing.py` 与 CLI `pair-combo-yield` 删除、lifecycle inventory 收窄、归组 `_build_edge` 只接受同到期、归因 fail-closed 校验、receipt/CLI/context/performance 测试改写、策略架构文档更新。slices 1-3 在单次实现 pass 中完成，本 review 一次性覆盖。
- Excluded scope: `config_validator` / `shadow_replay` 中 removed 配置文案（plan non-goal）；`docs/SHADOW_REPLAY_RUNBOOK.md`（上一 work unit 已修复，本 diff 不含该文件）；未跟踪文档；release/upgrade/生产写入。
- Parallel review coverage: 无；主 reviewer 逐文件走读 diff、生产写入方枚举与归因消费链。

## Findings

### 1-已修复-严重-归因 fail-closed 检查把“生产未写 expiry_structure”误判为不支持结构，所有生产同期组合将落入 partial
- **入口/函数**: `build_strategy_attribution` -> `_build_topology`；归因输入解析 `resolve_event_attribution`
- **文件(行号)**: `domain/domain/performance/strategy_attribution.py:144-146`；`domain/domain/performance/attribution.py:27-40,62`；对照 `domain/domain/combo_yield_lifecycle.py:27-38`
- **输入场景**: 生产 same-expiry 开仓事件（resolver / ledger 手动开仓落账）不携带 `expiry_structure` 字段；旧错期数据携带的是 `strategy_snapshot.structure_mode=staggered_expiry_pair`（见已删除 `combo_pairing.py` 的 `_strategy_snapshot`），同样不携带 `expiry_structure`。
- **实际分支**: `resolve_event_attribution` 只从 `_KEYS = (strategy, leg_role, strategy_group_id, expiry_structure)` 取值，无 `structure_mode` fallback，`values["expiry_structure"]` 为空 -> `StrategyAttribution.expiry_structure=None`。`_build_topology` 中 `structure = str(bucket.get("expiry_structure") or "").strip().lower()` 得到 `""`，`"" != "same_expiry"` 成立 -> 追加 `unsupported_expiry_structure` -> bucket `status="partial"`，并写入 `strategy_group_invalid:<group>:unsupported_expiry_structure`。
- **预期行为**: 生产同期组合应正常 ready；仅显式 `expiry_structure=diagonal` 或旧错期 `structure_mode=staggered_expiry_pair` 数据 fail-closed（plan R1：`不阻塞同期待处理数据`）。
- **实际行为**: 所有生产组合（字段缺失形态）都会落入 partial + strategy_group_invalid，绩效归因面整体失效。当前测试夹具全部显式携带 `expiry_structure`（如 `tests/test_performance_strategy_attribution.py:31,60`），掩盖了生产形态。
- **直接证据**:
  - `rg -n 'expiry_structure' src/ --glob '*.py'` 零命中：应用层无任何写入方。
  - resolver 同期写入面 `src/application/trades/resolver.py:744-798`（`_with_combo_yield_long_call_payload` / `_with_combo_yield_sell_put_payload`）只写 strategy/leg_role/yield_enhancement_mode/strategy_group_id/strategy_snapshot，不含 expiry_structure。
  - ledger 手动开仓 `src/application/ledger/commands.py:597-621` 只透传 `strategy_snapshot`，不注入 expiry_structure。
  - lifecycle 消费端 `combo_yield_lifecycle.py:27-38` 已实现正确 fallback（expiry_structure 优先，其次 `structure_mode` staggered->diagonal，缺省 same_expiry），归因解析器缺失同一逻辑。
  - 旧错期数据形态：`git show HEAD:src/application/positions/combo_pairing.py` 中 `_strategy_snapshot` 写 `structure_mode=STAGGERED_EXPIRY_PAIR`，无 expiry_structure。
  - 计划原文 Slice 3「归因: 删 diagonal 到期序校验，替换为 fail-closed：非 `same_expiry` 结构追加 `unsupported_expiry_structure`」未区分“缺失”与“显式非 same_expiry”，实现按字面执行导致回归。
- **影响**: 生产绩效归因静默失效（所有组合组 invalid），核心成功信号「不阻塞同期待处理数据」被破坏；属高影响回归。
- **建议改法和验证点**: 在归因解析器补 `structure_mode` fallback（与 lifecycle 一致）：显式 `expiry_structure` 优先；否则 `structure_mode=staggered_expiry_pair -> diagonal`、`same_expiry_pair -> same_expiry`；两者都缺省时保持 `None`（序列化契约不变）。`_build_topology` 改为 `structure and structure != "same_expiry"`（空值按同期默认）。新增两条回归测试：(a) 生产形态（无 expiry_structure/structure_mode）组合必须 ready、无 unsupported issue；(b) 旧错期形态（snapshot 带 structure_mode=staggered_expiry_pair）必须 partial + unsupported。`_assigned_stock_attribution`（engine.py:1336-1376）同步使用同一 fallback，避免 assigned-stock 语义分叉。
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 严重

## Open Questions

- 无。

## Re-review

- Finding 1 状态：`已修复`。
- 修复记录：`docs/gateflow/combo-yield-remove-staggered-ledger/fix-code-review-20260811.md`
- 修复内容：归因解析器新增 `resolve_expiry_structure_from_fields`（expiry_structure 优先，structure_mode
  staggered->diagonal / same_expiry_pair->same_expiry，缺省 None）；`_build_topology` 空值按同期放行；
  `_assigned_stock_attribution` 复用同一 fallback。
- 回归测试：生产形态（无 expiry_structure/structure_mode）组合必须 observed + 无 unsupported；
  旧错期形态（snapshot 带 structure_mode=staggered_expiry_pair）必须 partial + unsupported；
  same_expiry_pair 形态放行；`resolve_event_attribution` 对缺省形态保持 `expiry_structure is None`。
- 验证：8 文件 173 passed；focused 4 文件 49 passed；`git diff --check` clean；compileall ok。

## Residual Risk

- 未运行生产 performance 报告入口做端到端验证（本 gate 只走 domain 测试与静态证据）；修复后应补一条「resolver 落账 -> projection -> build_period_performance」形态的集成回归。
- 旧 diagonal v1（显式 expiry_structure=diagonal 的历史事件）与旧错期（structure_mode=staggered_expiry_pair）两种历史形态均已覆盖；混合单腿带结构字段的边界未单独测试。
