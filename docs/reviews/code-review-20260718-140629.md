# Code Review

## Scope

- Mode: current changes
- Branch or PR: `codex/diagonal-combo-yield-lifecycle`
- Base: `HEAD` (Slice S3 uncommitted implementation only)
- Output file: this artifact
- Included scope: lifecycle event construction, assignment/expiry metadata lineage, assigned-stock lot/sale read rows, full-group domain classification, monthly reporting integration, focused tests
- Excluded scope: accepted S1/S2; S4 Close Advice synthesis
- Parallel review coverage: 无；沿 option lot → lifecycle close event → projected trade-event report → assigned-stock lot/sale → full-group classifier 主链路走读

## Findings

未发现实质性问题。

## Evidence Review

- Lifecycle close event builder reads target-lot facts at the write boundary and copies only the existing canonical strategy metadata keys; assignment settlement validation remains unchanged.
- Reporting derives option quantities from active canonical events and assigned-stock quantities from validated stock lots/sales; no `last_close_type` or advice-side inference is used.
- Partial assignment reconciliation is quantity-based: only the explained mismatch `put_open + assigned_contracts == call_open` is cleared; all other inventory issues remain fail-closed.
- Residual Call classification uses inventory quantity only and does not estimate future value or trigger an action.
- Stock sales mutate remaining-share reporting but retain assignment IDs, stock-lot IDs, and group metadata.

## Open Questions

- 无。

## Residual Risk

- `full_group_lifecycle` intentionally reports still-active or mixed non-terminal groups as `review_required`; S4 remains responsible for option-only Close Advice synthesis.
- Historical assignment events created before this metadata contract may lack a group ID and therefore cannot be retroactively linked without explicit repair; no speculative backfill was added.

## Validation

```text
107 passed:
- positions reporting
- option-position CLI/lifecycle integration
- option-position domain
- Combo lifecycle domain

git diff --check: passed
```
