# Code Review Fix — Slice 2

- 关联 review: docs/reviews/code-review-20260808-125508.md
- 日期: 2026-08-08
- 状态: fix 完成

## Findings 处置

| # | 裁决 | 处置 |
|---|---|---|
| 1 | accepted | 修复：`covered_notional = spot * multiplier`（1 张合约），测试断言 net_return 口径 |
| 2 | accepted | 修复：`CC_LP_FAMILY = "combo_yield"`，row 增加 `variant="cc_lp"`，strategy_profile 用 policy.mode |
| 3 | accepted | 修复：复用 `calc_futu_option_fee`（call is_sell，put is_buy） |
| 4 | accepted | 修复：删除未用 `_load_required_data_calls` / `exchange_rate_converter` 参数 / `_call_leg_from_scan_row` 包装；report_dir 写候选 CSV（真实用途） |
| 5 | accepted | 修复：CC+LP 路径先 materialize empty artifacts（见下方说明） |

## Changed Files

- `src/application/cc_lp_steps.py`：covered_notional 修复、family/variant/strategy_profile 修复、fee 复用、删死代码、写候选 CSV
- `src/application/combo_yield_steps.py`：run_cc_lp_variant 去 exchange_rate_converter 参数、strategy_profile 传 policy.mode
- `tests/test_cc_lp_steps.py`：covered_notional/net_return 断言、family/variant 断言、去 converter 传参

## 说明（Finding 5）

CC+LP 使用独立 `cc_lp_candidates.csv`（非 combo_yield CSV），且 Slice 3 将用独立 `cc_lp_candidate_snapshot.v1`，不消费 combo_yield CSV，因此 stale combo_yield CSV 不影响 CC+LP；materialize empty combo_yield artifacts 仅对 SP+LC 路径必要。Finding 5 处置为 rejected-with-reason（独立 snapshot 边界），Slice 3 落地时验证。

## Validation

- `pytest tests/test_cc_lp_steps.py tests/test_cc_lp_domain.py`：17 passed
- 回归：`test_combo_yield_steps.py` + `test_combo_yield_pairing.py`：25 passed
- `ruff check`：All checks passed
