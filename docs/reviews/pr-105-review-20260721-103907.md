# PR Review: #105 Daily Brief Close-Position Details

## Findings

未发现实质性问题。

## Review Target and Scope

- Mode: PR Review
- Pull request: `#105` — `fix: include close details in daily decision brief`
- URL: `https://github.com/liuxie066/options-monitor/pull/105`
- Base: `main` at `502c0332`
- Reviewed head: `517ba536b2fadf0b9f08c9cef7dd33599587eb0e`
- PR state: open draft; mergeable
- Reviewed: the full 15-file PR diff, PR description, gate artifacts, application projection/renderer changes, tests, documentation, dependency graph, and repository instructions.

## Change Intent and Real Path

The PR closes a user-facing projection gap without changing close policy:

```text
close_advice.csv
-> daily_decision_brief_service._position_view
-> positions[].metrics allowlist
-> daily_decision_brief_renderer._position_close_details
-> Markdown detail line under the actionable position
```

The requested values already exist in close advice. The service now transports only the three allowlisted values, and the renderer formats them only for explicit close actions with usable status.

## Contract and Correctness Review

- `close_mid` remains an advisory mid and is explicitly marked `(mid)`; no fill guarantee is implied.
- `realized_if_close` remains signed net P&L. Non-negative values use `预计锁定收益`; negative values use `预计平仓损益` and retain the sign.
- `remaining_annualized_return` is formatted as a percentage without recomputation.
- Hold, unknown-action, not-evaluable, and unavailable rows do not display close details even if stale metrics are present.
- Missing, malformed, and non-finite metrics degrade field-by-field; no value is invented.
- Currency rendering follows the brief market (`HK$`/`$`).
- Existing persisted briefs without the optional metrics retain the former compact rendering.
- The brief schema version, action IDs, material-diff semantics, delivery routing, and notification state are unchanged.

## Adversarial Failure Pass

Reviewed positive/negative/zero P&L, partial metrics, `nan`/invalid values, stale metrics on hold and unavailable rows, unknown status/action values, HK/US formatting, long-Call exit actions, message limits, allowlist leakage, and backward compatibility. No credible unhandled failure scenario was identified in the PR diff.

## Project Instruction Check

Pass.

- Domain policy remains outside the renderer/service presentation path.
- No config, runtime state, Feishu, ledger, broker, release, deployment, or production canary change is included.
- The original dirty worktree is untouched.
- PR commits and files are limited to this work unit; unrelated `paseo.json` is absent from the PR.

## Overcoupling, Ownership, and Overengineering

- Overcoupling: none found. The renderer consumes a small structured mapping and does not import close-advice internals.
- Semantic ownership drift: none found. Source facts remain owned upstream; the renderer only labels/formats them.
- Structural clarity: the new helper is narrow and action-gated.
- Overengineering: none found. No new schema, config key, abstraction, dependency, or workflow was introduced.

## Test and Validation Evidence

Local evidence recorded in the PR and gate artifacts:

- Focused implementation tests: `68 passed`.
- Renderer/service/dependency-graph regression set: `39 passed`.
- Daily Brief suite excluding the expired fixture: `107 passed, 1 deselected`.
- Ruff on changed Python files: pass.
- Dependency graph check: pass.
- `git diff --check`: pass.
- Full suite: `1 failed, 2858 passed, 10 skipped in 42.58s`.
  - The only failure uses `valid_until=2026-07-20T20:00:00+00:00`; on July 21, 2026 the runtime correctly reports `planning_only`, while the fixture expects `live_actionable`.
  - Classification: unrelated date-sensitive baseline; assigned to a separate maintenance work unit.

GitHub checks at review time:

- `agent-plugin`: pass.
- `Analyze (actions)`: pending.
- `Analyze (python)`: pending.
- `guardrails`: pending.
- `CodeQL`: queued/skipping status reported by GitHub.

Pending CI is an external validation state, not a code finding; final closeout must re-check it after the PR-review commit is pushed.

## Open Questions

无。

## Residual Risk

- Expired date-sensitive fixture: assigned to a separate maintenance work unit.
- Pending GitHub checks: must be re-queried before final closeout.
- Production notification canary: assigned to later operator-authorized release/deployment work.
- No unclassified residual risk remains.

## Conclusion

`pass-with-risks`. No PR-specific finding requires a code fix. Create the accepted PR review commit, push it, then re-check GitHub validation before `draft-PR-pass` and final closeout.

## Artifact

`docs/reviews/pr-105-review-20260721-103907.md`
