# Plan Re-review — Retire AI Decision Advice

- Review type: adversarial PlanReview re-review
- Work unit: `retire-ai-decision-advice`
- Gate: `re-review` (plan review)
- Reviewed target: revised `docs/gateflow/retire-ai-decision-advice/plan.md`
- Binding scope: `docs/gateflow/retire-ai-decision-advice/goal-confirmation.md`
- Initial review: `docs/reviews/plan-review-20260812-112642.md`
- Fix artifact: `docs/gateflow/retire-ai-decision-advice/plan-review-fix.md`
- Base: `origin/main@ded8f882`
- Artifact path: `docs/reviews/plan-review-20260812-113353.md`
- Conclusion: `pass-with-risks`

## Reviewed scope

The re-review retested all four initial findings against the revised call sequencing, canonical normalization/digest
contract, repository-owned frozen-retry classifier, and root/market/runtime configuration errors. It also repeated the
goal-minimality, architecture, optimal-solution, overengineering, and overcoupling lenses.

## Assumptions retested

1. Slice 1 leaves every currently called Python signature runnable until Slice 2 removes caller and callee plumbing in
   one accepted change set.
2. Current Brief normalization can guarantee absence of retired fields while raw immutable source values remain
   available only to strict digest compatibility.
3. The retry guard can inspect raw revisions, fallback text, and card transport without leaking repository paths or
   storage parsing into notification orchestration.
4. Every supported config entry surface gives the intended retired-key behavior.
5. The compatibility logic remains bounded and does not recreate an Advice schema or migration.

## Finding status

### PR-01-已修复-高-Slice 1 会在 Slice 2 前移除被现有 Tick 调用仍传入的 Brief 参数

- Slice 1 now retains and ignores all private handoff parameters used by the current Tick caller.
- Slice 2 explicitly owns `_advice_handoff_for_account()`, request-field, caller, and assembler-signature deletion in
  the same slice and includes `daily_decision_brief_service.py` in its allowed set.
- Required regression proves Slice 1 accepts existing handoff arguments without model invocation or retired output.
- Final status: `已修复`.

### PR-02-已修复-高-公共 normalizer 保留 retired keys，和新写入及公共读取契约冲突

- The revised current normalizer unconditionally strips both retired keys.
- Historical digest candidates use only the raw persisted key/value pairs attached to a normalized deterministic core;
  no Advice parser/defaulting or per-consumer strip list survives.
- Required tamper tests cover both core fields and retired legacy values, preventing a widened integrity allowance.
- Final status: `已修复`.

### PR-03-已修复-中-冻结重试检查没有锁定 repository-owned 原始 revision 契约

- The plan now names the repository as the sole storage/path/digest owner and limits its inspection result to
  `clean | legacy_ai_payload_retired`.
- It explicitly covers raw source fields, fallback text, and normalized card transport before any outbound map write.
- Missing/corrupt sources retain the existing state-error path; the guard does not mutate delivery state.
- Final status: `已修复`.

### PR-04-已修复-中-YAML 作者入口会先报 generic unknown key，达不到 targeted retired-key contract

- Root YAML, market YAML, and runtime JSON now each have an exact targeted regression.
- A nearby misspelling remains generic, preventing the tombstone from becoming a broad fuzzy branch.
- Final status: `已修复`.

## New findings

No new material findings.

## Lens conclusions

- **Goal-bound minimal design**: every active deletion, digest compatibility rule, retry blocker, test, and doc change
  maps to confirmed success signals 1–8. No candidate-policy or unrelated provider work was added.
- **Architecture boundary**: deterministic Brief normalization owns the current contract; repository owns persisted raw
  integrity; notification flow owns outbound selection. Shared Candidate/Close Advice/Assistant/provider boundaries are
  explicitly preserved.
- **Best practice / optimal solution**: canonical stripping plus exact raw digest candidates is the smallest robust
  alternative. The transitional ignored signature is intentionally short-lived and removed in the next slice.
- **Overengineering**: no schema v2, migration, compatibility renderer, feature tombstone service, replacement model,
  or generalized inspection framework is planned.
- **Overcoupling**: the revised slices have independently testable behavior and no known transient broken call path.

## Open questions

None.

## Residual risks and suggested destination

- Installed collector units continue until an authorized environment reconciliation — `assigned to later work unit`.
- Historical Advice/Evidence/Brief disk usage remains — `assigned to later work unit` requiring destructive approval.
- Third-party importers of repository-private Python signatures are not discoverable — `assigned to later work unit`;
  public CLI/Agent contracts and all repository callers are covered here.
- Already accepted provider deliveries cannot be retracted by source retirement — `assigned to later work unit`; no
  provider-history mutation is authorized.
- Exact overlay-era digest fixtures may reveal an undocumented historical shape — `fixed in current slice` if compatible
  using exact raw values; the approved stop condition applies if a schema migration would be required.

## Final conclusion

`pass-with-risks`. All accepted initial findings are fixed, no blocking open question remains, and every residual risk
has an explicit destination. The plan is code-generation-ready and may proceed to the accepted plan commit.
