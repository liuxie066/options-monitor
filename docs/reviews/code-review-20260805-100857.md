# Code Review

## Scope

- Mode: current changes, second DeepReview pass after fixing the first pass
- Branch: `fix/coherent-option-fx-snapshot` at `2ae7d59f`
- Base: local `main` at `2ae7d59f`; all reviewed changes are unstaged worktree changes
- Output file: `docs/reviews/code-review-20260805-100857.md`
- Included scope: the complete coherent multi-account option-position/FX worktree plus S6-lite scoped-union and run-local Spot-observation changes; special focus on the two first-pass fixes and their downstream error/identity contracts
- Excluded scope: Gateflow, S6 subprocess exact-spec/multi-spec support, S7 event sharing, full S5 re-entry/plan-v2/global-risk hardening, release, deployment, production configuration or data writes, broker access, and real notifications
- Verification evidence reviewed: first-pass red/green reproductions (`4 passed` after fixes), parent fail-closed cases (`2 passed`), focused integrated regression (`223 passed`), full Ruff, `compileall`, `git diff --check`, and dependency-graph conformance check
- Parallel review coverage: none; this pass re-reviewed the integrated worktree and both repaired call chains directly

## Findings

### 1-未修复-低-新增的 pipeline prepared-context 依赖边未同步到生成依赖图

- **入口/函数**: `pipeline_runtime.main()` -> `PreparedOptionPositionsContextError`
- **文件(行号)**: `src/application/pipeline_runtime.py:20-22`; `docs/DEPENDENCY_GRAPH.md`; `docs/dependency_graph.mmd`
- **输入场景**: 第一轮修复在 `pipeline_runtime` 子进程消费边界新增对 `PreparedOptionPositionsContextError` 的直接导入和类型化转换。
- **实际分支**: 源码依赖已改变，但依赖图仍是修复前的生成结果。
- **预期行为**: 生成的 Markdown/Mermaid 依赖视图应与当前源码导入图一致，`generate_dependency_graph.py --check` 应通过。
- **实际行为**: 检查返回 `[DEPENDENCY_GRAPH_ERROR] generated file is stale: docs/DEPENDENCY_GRAPH.md`。
- **直接证据**: `pipeline_runtime.py:20-22` 存在新 import；`python3 scripts/generate_dependency_graph.py --check` 退出码为 1，指向过期生成文件。
- **影响**: 不影响当前运行时功能，但会使架构依赖基线与源码失真，并使项目的依赖图质量门失败。
- **建议改法和验证点**: 用项目生成器重建两份依赖图，然后重跑 `--check`，并确认仍为 `0 cycles`。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## First-pass finding disposition

- `PreparedOptionPositionsContextError` 现在于子进程 consumer 边界转换为 `ACCOUNT_CONFIG_PREPARED_OPTION_CONTEXT_INVALID`；真实 `main()` 路径回归通过，父进程对该代码保留 typed reason 且强制 `ran_scan=false` / `should_notify=false`。第一轮高严重度问题已修复。
- physical OpenD host 现在在 candidate/position 路由比较、prefetch 去重、合并配置以及 expiration/Spot run-local cache key 上统一为 trim + lowercase；大小写反例已证明只有一个计划和一次 Spot 观测。第一轮中严重度问题已修复。

## Open Questions

- 无。

## Residual Risk

- S6 subprocess mode 仍不支持 multi-spec，且 observed-`None` Spot 不会通过新的 CLI contract 传递；这是用户明确排除的 S6-lite 边界，当前 canonical runtime configs 默认 in-process。
- 完整 S5 的 ledger-group all-or-none re-entry、更强 identity/provenance 与 promotion-time revalidation 仍未实施；本轮不为补齐旧计划而扩大范围。
- `_resolve_spot_reference()` 的旧 CSV Spot 回退仍未校验交易日；这是既有策略，不是本轮缓存修复引入的问题。
- 当前仍是本地确定性验证，未连接真实 OpenD，未执行生产 tick，也未发送通知。
