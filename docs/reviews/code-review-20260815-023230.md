# Code Review

## Scope

- Mode: current changes（Gateflow final fix re-review）
- Branch or PR: `feat/sell-put-top1-w1a`
- Base: accepted-plan commit `f5f9ea06`
- Output file: `docs/reviews/code-review-20260815-023230.md`
- Included scope: 最新 `src/application/strategy_lab/top1/ranking.py`、`tests/test_strategy_lab_top1.py`、`docs/gateflow/sell-put-top1-w1a/code-review-fix.md`、上轮报告 `docs/reviews/code-review-20260815-022735.md`；聚焦核验 DR-W1A-02 修复与 DR-W1A-01 关闭状态
- Excluded scope: AGENTS.md、data-storage plan、assigned_stock/fee_calc/portfolio_assignment_scenario 及其测试、docs/performance、旧 review artifacts、其余既有 dirty/untracked 文件
- Parallel review coverage: 无；主 reviewer 逐行走读新增 strategy-status 校验段与 snapshot `_strategy_results` 语义对照，并运行聚焦测试

## Verdict

- 结论: **pass — 无任何未关闭 finding。**
- 验证证据: 聚焦测试 136 passed（含新增 `test_empty_projection_rejects_unavailable_strategy_evidence` 两个参数化场景）；DR-W1A-01 回归仍通过；DR-W1A-02 修复直接消费已验证 snapshot 的 `strategy_results`，无重复计算、无越界、无错误拒绝。

## Findings

### 1-已关闭-误报-DR-W1A-01（null-return 行排在有回报行之前）

- **Final status**: 已关闭（误报）。
- **本轮复核**: 上轮已确认 `_rank_return_bands()`（`domain/domain/engine/candidate_engine.py:1165-1196`）的 band 构造条件含 `period_return_fn(row) is not None`，null-return 行只在 `usable` 为空后才参与 tie-key 排序；回归 `test_current_tie_break_ranks_known_return_before_null_return` 本轮仍在通过状态（包含在 136 passed 内）。

### 2-已修复-DR-W1A-02（空 projection 不区分 lawful `no_candidate` 与 `partial_data`/`data_unavailable`）

- **Final status**: 已修复。
- **修复位置**: `src/application/strategy_lab/top1/ranking.py:235-242`
- **直接证据**: 修复在 `validate_opening_candidate_snapshot(..., require_current_contract=True)` 通过之后，读取 `snapshot["strategy_results"]` 中 `strategy_mode == "put"` 的行，要求恰好一行且 `strategy_status` 严格等于期望值——candidates 非空时为 `"candidates_found"`，空集时为 `"no_candidate"`；其他任何状态（`partial_data`、`data_unavailable`、`not_applicable`）、缺失 put 行或多行都 fail closed 为 `Top1RankingError`（reason_code 默认 `ranking_projection_incomplete`）。
- **语义对照（无错误拒绝）**: snapshot 密封器 `_strategy_results`（`src/application/opening_candidate_snapshot.py:951-1019`）保证有 ranked put 候选时 status 只会是 `candidates_found`/`partial_data`；`partial_data` + 有候选在 snapshot 语义中确实是合法的不完整证据，projection 拒绝它是**有意的收紧**（Top1 实验要求完整证据），不属于错误拒绝——fix 文档与计划目标一致。空集 + `no_candidate` 仍合法通过（`test_empty_accepted_set_is_a_valid_projection` 保持绿色）。
- **无越界**: `strategy_results` 的读取出现在 snapshot 全量校验（含 `require_current_contract` 强制的 results 形状与重建一致性检查，`opening_candidate_snapshot.py:369-376, 560-567`）之后，validated snapshot 保证该字段是 list 且每个 mode 恰好一行，不存在 KeyError/歧义路径；校验只读、无 schema 变化、无 Candidate Engine 变化。
- **回归验证**: `test_empty_projection_rejects_unavailable_strategy_evidence`（`tests/test_strategy_lab_top1.py:311-329`）参数化覆盖 sealed 空 snapshot 的 `data_unavailable`（scan failed）与 `partial_data`（completed + partial reason）两个真实语义路径，均断言 `Top1RankingError` 且消息匹配 "strategy status"；非通过篡改 projection 字段的弱断言。本 reviewer 运行通过。

## Open Questions

- 无。

## Residual Risk

- 未运行 BasedPyright（仓库环境无此模块，沿用前序评审记录）。
- 未执行 `scripts/generate_dependency_graph.py --check`（写操作，只读 review 不允许）；本轮 ranking.py 未新增 import，architecture guard 测试仍通过。
- W1A Gateflow review loop 可以收敛：finding 1 关闭（误报），finding 2 已修复并有真实回归覆盖，无未关闭 finding。
