# Code Review — Aggregate Deepreview Re-review

## Scope

- **Gate**: aggregate deepreview re-review
- **Work unit**: `option-performance-refactor`
- **Mode**: current changes
- **Branch**: `excited-rhino`
- **Base**: `2d249ab6`
- **Target**: aggregate fixes over `a96c1844`
- **Output file**: `docs/reviews/code-review-20260718-094740.md`
- **Source review**: `docs/reviews/code-review-20260718-094014.md`
- **Fix artifact**: `docs/gateflow/option-performance-refactor-aggregate-fix-20260718-094614.md`
- **Included scope**: ADR-01 through ADR-04 fixes, their tests/docs, and interactions among assigned-stock quality, proven-zero semantics, public option reports, and portfolio bridge evidence gates.
- **Excluded scope**: unrelated release/Feishu dirty changes and external PM implementation.
- **Parallel review coverage**: none; subagents were not authorized.

## Findings

未发现新的实质性问题。

### ADR-01-已修复-[高]-per-account bridge ownership is exact

- PM fact contracts require the payload account to equal the requested row account.
- Option report scope must contain that exact account; missing/aggregate scope is rejected.
- PnL and cash regressions cover foreign PM facts and aggregate option reports.
- **最终状态**: 已修复。

### ADR-02-已修复-[中]-bridge cutoffs share an exact reporting timezone

- PM facts require `period.timezone=Asia/Shanghai`.
- Option reports require `period.reporting_timezone=Asia/Shanghai`.
- Both bridge suites prove UTC PM facts and UTC option reports fail closed with explicit reasons.
- **最终状态**: 已修复。

### ADR-03-已修复-[高]-assigned-stock review debt degrades top-level quality

- Assigned-stock diagnostics from both boundary projections are now scoped by account/broker like valuation diagnostics instead of being incorrectly filtered by a lot-assignment timestamp.
- Historical missing settlement blocks proven-zero normalization.
- An invalid current-period sale with complete stock marks and FX preserves the known subtotal for audit but makes report quality partial and exposes `source_conflict:sale-too-many`.
- **最终状态**: 已修复。

### ADR-04-已修复-[高]-partial report quality is binding at portfolio bridges

- Bridge evidence requires both nested metric status and report-level quality status to be observed.
- A known metric inside a partial report remains visible in evidence but is unusable; bridge option and derived-other steps remain null.
- PnL and cash regressions cover this exact cross-slice state.
- **最终状态**: 已修复。

## Adversarial Failure Pass

Re-checked the original failure modes against the fixed implementation:

1. `scope.account=null` aggregate option report under an `lx` bridge row -> account mismatch, no amount used.
2. PM `account=sy` under the `lx` dictionary key -> PM account mismatch, row unavailable.
3. matching calendar date with UTC PM or option timezone -> explicit timezone mismatch.
4. May assignment without stock settlement followed by June proven scope -> top-level partial, warning retained, no zero promotion.
5. June over-sale against a May assigned-stock lot with complete marks/FX -> top-level partial with source conflict; sale omitted visibly rather than silently.
6. observed nested PnL/cash metric inside a partial report -> evidence `usable=false`, bridge amount null.

The stricter assigned-stock diagnostic relevance does not double count economics: it only affects report quality/warnings. Option and stock facts remain owned by the existing canonical fact stream. The stricter bridge gates do not alter equations or known evidence payloads; they only prevent incomplete evidence from being used.

No new dependency cycle, import-boundary violation, public command churn, production write path, or notification/config side effect was introduced.

## Validation

Focused bridge/assignment/agent suite:

```text
53 passed
```

Expanded aggregate-risk suite:

```text
111 passed
```

Focused Ruff:

```text
All checks passed!
```

`git diff --check`: passed.

## Open Questions

无 blocking open question.

## Residual Risk

- **fixed in current aggregate loop**: ADR-01 through ADR-04.
- **assigned to operational follow-up**: repair lifecycle rows surfaced by the stricter fail-closed quality policy.
- **assigned to external PM owner**: runtime endpoint conformance; OM now rejects mismatched contracts.
- **assigned to later work unit**: legacy adapter removal after the migration window.
- **accepted v1 design limitation**: separate FX-attribution decomposition remains out of scope.

No unclassified residual risk remains.

## Accepted Deepreview Validation

Repository-wide adjusted suite (excluding only the four known missing-`.venv` entrypoint files and the unrelated dirty Feishu assertion):

```text
2588 passed, 10 skipped, 1 deselected
```

Repository quality gates:

```text
python3 -m ruff check .
All checks passed!

python3 scripts/generate_dependency_graph.py --check
[OK] dependency graph current; production_modules=465 cycles=0

git diff --check
passed
```

Exact legacy-reference inventory:

```text
status=pass
unowned=[]
stale_allowlist=[]
matches=25
```

## Completion Status

- **Gate result**: pass-with-classified-risks
- **Finding status**: 4 已修复, 0 未修复, 0 部分修复, 0 证据失效
- **Accepted validation**: pass
- **Current gate / next entry point**: create protected accepted-deepreview commit, then `ready-to-open-draft-PR`
