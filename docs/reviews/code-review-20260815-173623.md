# Code Review

## Scope

- Mode: current changes
- Branch or PR: `feat/sell-put-top1-w0r-history-quota`
- Base: `0fc8bd6d`（accepted plan commit，origin/main@0da901b3 之上）
- Output file: `docs/reviews/code-review-20260815-173623.md`
- Included scope: Gateflow S1 全部改动——`FutuGateway.get_history_kl_quota()` 与 `_normalize_history_kline_quota_response()`（SDK result normalization/error mapping）、`OpenDFetchLimits.history_kline` / `OPEND_RATE_LIMIT_ENDPOINT_ALIASES` / `resolve_opend_fetch_limits()`（config owner 与 validator 联动）、focused tests、capability preflight 文档更新、dependency graph 机械再生
- Excluded scope: live OpenD probe、W5 runner、quota sufficiency policy、receipt persistence（均属 plan §8 已分配 residual owners，不在本 slice）
- Parallel review coverage: 无；S1 改动面窄，由主 reviewer 沿 `get_history_kl_quota -> _normalize_history_kline_quota_response -> _raise_mapped/_map_error` 与 `resolve_opend_fetch_limits -> _endpoint_rate_limit -> _raw_opend_endpoint_rate_limit` 及 config_validator 消费链完整走读

## Findings

未发现实质性问题。

走读记录（支撑无 finding 结论的直接证据）：

- External protocol shape：已安装 SDK（`.venv/.../futu/quote/open_quote_context.py`）`get_history_kl_quota` 成功返回 `(RET_OK, (used_quota, remain_quota, detail_list))`，`HistoryKLQuota.unpack_rsp`（`quote_query.py:2205-2221`）确认 `detail_list` 恒为 list 且每项含 `code/name/request_time`；`usedQuota/remainQuota` proto 为 int32（`Qot_RequestHistoryKLQuota.proto`），非 bool 的 `Integral` 校验与真实形状一致。正常返回时 SDK 无 exception 路径，`except Exception + _raise_mapped(action="get_history_kl_quota")` 与现有 `request_history_kline` 等 16 个方法的 error boundary 模式一致。
- Fail-closed validation：ret bool/非整数、ret != 0（`RuntimeError(payload)`）、payload 非三元组、count bool/非整数/负数、detail 非 list、detail 非 dict、code 空/重复（规范化后）、request_time 非 canonical `%Y-%m-%d %H:%M:%S`（strptime + strftime round-trip，拒绝 `2026-02-30`）、detail count != used_quota，全部 raise 并经 `_raise_mapped` 变为 `FutuGatewayError`；无 partial/default fact 出口。
- Parameter effectiveness：`get_detail=True` 硬编码在 `futu_gateway.py:880`，caller 无法 weaken；`history_kline` 经 `runtime.opend_rate_limits.history_kline` -> `_raw_opend_endpoint_rate_limit` -> `OpenDEndpointRateLimit.from_values(defaults=60/30/30)`，canonical override round-trip 有测试（`test_opend_batch_config.py:92`）；`OPEND_RATE_LIMIT_ENDPOINT_KEYS` 自动包含新 key，`config_validator.py:986-992` 无需改动即接受，acceptance fixture 已扩展（`test_global_liquidity_filters.py:792`）。
- Existing call regression：`fetch_kwargs()`/`discovery_kwargs()`/`OPEND_FETCH_KWARG_KEYS` 未变；`from_flat_kwargs()` 三个既有 caller（`opend_symbol_chain_fetching.py:126`、`opend_symbol_fetching.py:381`、`opend_market_snapshot_fetching.py:106,175`）不传 history 参数时走 defaults，无行为变化；focused 58 tests + 邻接 114 tests 通过。
- Scope/goal drift：无 caller、receipt schema、CLI、生产 config 改动；preflight 文档只把 source capability 标 green、live 保持 unknown、W0R 保持 runtime_no_go；dependency graph diff 为 origin/main 既有 stale 的机械再生（plan 已声明该义务），数值自述自洽（939 files / 590 production modules / 0 cycles）。

## Open Questions

- 无。

## Residual Risk

- Live provider 的 `request_time` 实际格式（是否恒为 canonical `%Y-%m-%d %H:%M:%S`、时区语义）未由真实 receipt 证明；若 provider 返回带毫秒/时区或非零填充格式，严格校验会 fail closed——这是 plan 明确选择的行为，实际形状归属已授权的 W0R live probe。
- `detail_list` 非 dict/空 code 等畸形形状仅由 fake-provider 测试固化；SDK 当前 unpack 保证 dict，但 SDK 升级后形状变化只能依赖本 slice 的 fail-closed 拒绝，无兼容层（符合 plan 不预测 future shape 的决定）。
- Duplicate-code 拒绝（`code.strip().upper()` 后唯一）对“同一标的不同 request_time 多行”的真实 provider 行为假设未验证；若 live 返回同 code 多行，gateway 会 fail closed。该语义由 plan §5 rule 5 明确授权，live 证据仍待 probe。
- Test gap（非阻断）：failure 矩阵未单独覆盖 `code` 含首尾空白（如 `" HK.00700 "`，当前会被 trim 后接受）与 `request_time` 含首尾空白（已隐式覆盖于非 canonical 检查）的显式断言；行为本身符合 plan 的 trim-normalize 规则。
- CI gap：本 slice 验证均为本地执行，未观察 PR CI。
