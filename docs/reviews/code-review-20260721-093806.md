# Code Re-review — Candidate Event Risk Monitoring Slice A

## Scope

- Mode: current changes re-review
- Branch: `codex/candidate-event-risk-monitoring-v1.4.0`
- Base: accepted plan commit `aabaf4a1`
- Prior review: `docs/reviews/code-review-20260721-093657.md`
- Output: `docs/reviews/code-review-20260721-093806.md`
- Included scope: Slice A implementation plus SA-01 fix and regression

## Finding Re-review

### SA-01-已修复-高-Malformed structured events

`EventStore._normalize_fetch_payload()` now requires structured `events` to be a list whose every item is a mapping. Invalid structure raises before successful cache persistence and follows the existing EventStore source-error path. The regression proves snapshot coverage is empty and the cache contains neither successful events nor coverage.

The fix is at the owning trust boundary and does not alter legacy list fetcher compatibility, provider selection, stale semantics, or public fetch/probe response shape.

## Validation Evidence

- Focused event suite after fix: `25 passed`.
- Ruff on touched production/tests: passed.
- `git diff --check`: passed.
- Direct call chain rechecked: provider evidence -> EventStore strict normalization -> cache -> orchestrator snapshot.

## Findings

No remaining accepted finding.

## Open Questions

None.

## Residual Risk

- Object-shaped event rows with invalid type/date are not eligible for user semantics until Slice B strict normalization; classified as covered by approved Slice B.
- Candidate association/materiality/renderer remain covered by later approved slices.
- Live provider drift remains assigned to later authorized canary.

All residual risks are classified.

## Conclusion

**pass** — SA-01 is fixed. Slice A is ready for accepted-slice commit.
