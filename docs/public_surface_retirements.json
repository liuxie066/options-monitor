{
  "description": "Declared public names that were deliberately retired from src/, domain/ and scripts/. Append-only: an entry is never deleted, because the gate reads this file to explain every name that disappeared between two revisions. See docs/GUARDRAILS.md section B.",
  "retirements": []
}
