{
  "description": "Declared public names that were deliberately retired from src/, domain/ and scripts/. Append-only: an entry is never deleted, because the gate reads this file to explain every name that disappeared between two revisions. The one exception is a \"*\" entry whose module has come back -- it is then false, and the gate only lets it be dropped, never rewritten. See docs/GUARDRAILS.md section B.",
  "retirements": [
    {
      "module": "domain/domain/ledger/position_fields.py",
      "name": "upsert_note_kv",
      "reason": "Note-key/value upsert retired with the note-as-fact path: exp/multiplier are no longer written into note, because facts live in structured keys; merge_note is the surviving note writer."
    },
    {
      "module": "domain/domain/close_advice.py",
      "name": "STRICT_MIN_DTE",
      "reason": "The fixed 14-day minimum was retired when Close Advice switched to remaining annualized return with a positive DTE requirement."
    },
    {
      "module": "domain/domain/close_advice.py",
      "name": "STRICT_MIN_REMAINING_TERM_RATIO",
      "reason": "The first-half-of-contract gate was retired because opening term no longer determines the remaining yield decision."
    },
    {
      "module": "domain/domain/close_advice.py",
      "name": "STRICT_MAX_CLOSE_COST_TO_STRIKE_RATIO",
      "reason": "The strike-notional close-cost ceiling was replaced by the Put or Call capital-basis remaining annualized return calculation."
    },
    {
      "module": "domain/domain/close_advice.py",
      "name": "STRICT_MAX_SPREAD_RATIO",
      "reason": "The spread percentage ceiling was retired; a valid two-sided quote is now evaluated at its ask price with fees."
    },
    {
      "module": "src/application/runtime_logs_cli.py",
      "name": "MAX_LOG_FILE_BYTES",
      "reason": "Whole-file rejection was retired because runtime logs now read only a bounded tail regardless of file size."
    },
    {
      "module": "src/application/ledger/lot_identity_migration.py",
      "name": "APPLY_SCHEMA",
      "reason": "R2 retired the one-off D1-D4 production window's destructive half (table rebuild, payload rewrite, wheel_events rename, apply/preview) once the window closed on 2026-09-22; only the read-only inventory/verify surfaces remain."
    },
    {
      "module": "src/application/ledger/lot_identity_migration.py",
      "name": "REBUILD_RECIPE",
      "reason": "R2 retired the one-off D1-D4 production window's destructive half (table rebuild, payload rewrite, wheel_events rename, apply/preview) once the window closed on 2026-09-22; only the read-only inventory/verify surfaces remain."
    },
    {
      "module": "src/application/ledger/lot_identity_migration.py",
      "name": "RETIRED_LOT_COLUMNS",
      "reason": "R2 retired the one-off D1-D4 production window's destructive half (table rebuild, payload rewrite, wheel_events rename, apply/preview) once the window closed on 2026-09-22; only the read-only inventory/verify surfaces remain."
    },
    {
      "module": "src/application/ledger/lot_identity_migration.py",
      "name": "apply_lot_identity_migration",
      "reason": "R2 retired the one-off D1-D4 production window's destructive half (table rebuild, payload rewrite, wheel_events rename, apply/preview) once the window closed on 2026-09-22; only the read-only inventory/verify surfaces remain."
    },
    {
      "module": "src/application/ledger/lot_identity_migration.py",
      "name": "preview_lot_identity_migration_apply",
      "reason": "R2 retired the one-off D1-D4 production window's destructive half (table rebuild, payload rewrite, wheel_events rename, apply/preview) once the window closed on 2026-09-22; only the read-only inventory/verify surfaces remain."
    },
    {
      "module": "src/application/ledger/api.py",
      "name": "apply_lot_identity_migration",
      "reason": "R2 retired the one-off D1-D4 production window's destructive half (table rebuild, payload rewrite, wheel_events rename, apply/preview) once the window closed on 2026-09-22; only the read-only inventory/verify surfaces remain."
    },
    {
      "module": "src/application/ledger/api.py",
      "name": "preview_lot_identity_migration_apply",
      "reason": "R2 retired the one-off D1-D4 production window's destructive half (table rebuild, payload rewrite, wheel_events rename, apply/preview) once the window closed on 2026-09-22; only the read-only inventory/verify surfaces remain."
    }
  ]
}
